# Pinned dependencies of the peer-network contract suite (FLEET-PEER-NETWORK-001).
# Installed by the always-run `peer-contract` CI job; every import in the suite is hard.
pytest==8.4.1
pyyaml==6.0.2
jsonschema==4.25.1
rfc3339-validator==0.1.4
