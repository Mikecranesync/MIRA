# Crawl Verification Scorecard — 2026-04-14

Generated by: `POST /ingest/crawl-classify-historical`
Version: v2.5.0

## Summary

| Run ID | Manufacturer | Model | Outcome | Pages | Shell% | Density | Model Hit% | Avg Len |
|--------|-------------|-------|---------|-------|--------|---------|-----------|---------|
| Brgo1xN4QLjhr0Pgc | Yaskawa | V1000 | **LOW_QUALITY** | 103 | 32% | 12% | 9% | 1,736 |
| 9ELqsnRqp384TeoxJ | Pilz | safety gate input | **SHELL_ONLY** | ~1 | 100% | 0% | 0% | <500 |

## Yaskawa V1000 — `Brgo1xN4QLjhr0Pgc`

**Apify status:** SUCCEEDED  
**Duration:** 1.9 min  
**Dataset:** `Uv6DPMIUzzNLQNoeu`

**Verifier outcome:** `LOW_QUALITY`

**Why LOW_QUALITY, not SUCCESS:**
- `avg_content_length = 1,736 chars` — below the 4,000 char threshold for real manual content
- `content_density = 0.12` — only 12 of 103 pages contain ≥2 technical manual signals (fault code, parameter table, installation guide, etc.). The other 91 are download directory listings.
- `model_keyword_hit = 0.09` — only 9 pages contain "v1000" as a model identifier
- All 103 URLs are from `yaskawa.com/downloads?productGroup=...` — download index pages, not actual manual pages

**What the technician saw:** "New knowledge added ✅" (prior to v2.5.0)  
**What they should have seen:** "Found listing pages, not manual content — send a PDF directly"

**Root cause:** Apify's Cheerio crawler with `maxCrawlDepth=1` starting from `yaskawa.com/downloads/` indexes the download portal's filter/navigation pages without reaching actual document content. The portal is a React SPA that requires JS rendering to navigate to specific product documents.

**Phase 2 fix:** Route to `apify_playwright` strategy (JS-rendered Chromium), which can execute the React navigation and reach actual V1000 manual pages.

## Classification Thresholds (v2.5.0)

| Metric | SUCCESS threshold | Meaning |
|--------|------------------|---------|
| `page_count` | ≥ 3 | Minimum pages |
| `shell_ratio` | < 0.5 | Less than 50% shell (<500 char) pages |
| `content_density` | ≥ 0.3 | 30%+ pages with technical manual signals |
| `model_keyword_hit` | > 0 | At least 1 page mentions the specific model |
| `avg_content_length` | ≥ 4,000 chars | Real manual vs listing page discriminator |

All thresholds are tunable via env vars (`VERIFIER_MIN_PAGES`, `VERIFIER_MAX_SHELL_RATIO`, etc.).
