# Campaign findings — one row per DEFECT (not per conversation).
# Keyed by fingerprint from findings.fingerprint(); see that module for why.
# status: OPEN | FIXED | FALSE_POSITIVE | NEEDS_RERUN
# applied: true only once the fix is merged to main.
t1:fault_code_pf525:
  applied: false
  convs:
  - t1_012_fault_code_pf525
  - t1_s42_012_fault_code_pf525
  first_seen: c1
  fix: 'PR #3155 (_alias_pattern separator tolerance + expand_abbreviations collapse-and-retry);
    PR #3153 fixes the resolver half only'
  last_seen: c2
  note: 'Neither PR is merged. Reproduced offline both directions; c1r4 passed this
    scenario once, which is one run and not proof. | c2: still FAILS on main, as expected
    — neither #3155 nor #3153 is merged. Evidence: MIRA asked for ''the manufacturer
    and model (e.g. Allen-Bradley PowerFlex 525)'' after the technician wrote pf-525.
    | c3 (branch qc/cold-start-baseline, 2026-08-08): PASSES live. c2 FAIL -> c3 PASS
    on the same seed and scenario, the only change being the deployed build. Consistent
    with the offline both-directions proof. Still applied=false: the branch is not
    merged.'
  status: FIXED
  summary: hyphenated model shorthand ("pf-525") did not resolve, so MIRA asked for
    the manufacturer and model the technician had just given
t1:reset_procedure:
  applied: false
  convs:
  - t1_013_reset_procedure
  - t1_s42_013_reset_procedure
  first_seen: c1
  fix: 'PARTIAL — CIT-005 (this branch) stops the footer landing on a question-only
    turn. The other half is untouched: MIRA still answers a procedural "how do I reset"
    with a bare clarifying question while holding a resolved PowerFlex 525 pack, so
    the scenario''s expect list ("reset", "PowerFlex") still fails.'
  issue: '3156'
  last_seen: c3
  note: 'Footer half is deterministic: running enforce_citation_or_gap_admission from
    current source reproduces the frozen reply byte-for-byte. Shares its root cause
    with t1:symptom_report. Failed on two different builds and two different phrasings
    ("AB PowerFlex 525" spelled out, and "PF-525"), so the hyphen fix does not explain
    it. | c2 (main@c0d3722e3, 2026-08-08): the seed-42 variant PASSED while t1:symptom_report
    — same root cause — still failed. CIT-005 is not merged, so this is mutation variance,
    not evidence the footer defect is gone. | c3: the FOOTER HALF IS PROVEN FIXED
    LIVE — the reply to the identical message is now ''What is the exact fault code
    displayed after the undervoltage fault?'' with NO KB-gap footer (compare the c1/c1r4
    transcripts). That is mechanism-level evidence, not a lucky pass. The scenario
    still FAILS because its expect list wants ''reset''/''PowerFlex'' and MIRA asks
    a clarifying question instead of answering the procedural question — exactly the
    half recorded as untouched. Fixing that is a separate change (answer the procedure
    from the resolved PF525 pack rather than gating it behind a fault code).'
  status: OPEN
  summary: the KB-gap footer is stapled onto a bare clarifying question — MIRA asks
    for the fault code AND tells the technician it has no documentation, while holding
    a resolved PowerFlex 525 drive pack
t1:symptom_report:
  applied: false
  convs:
  - t1_018_symptom_report
  - t1_s42_018_symptom_report
  first_seen: c1
  fix: 'CIT-005 (PR #3155) — a question-only reply no longer receives the KB-gap footer.
    Covers this finding''s whole root cause; the scenario''s expect list may still
    be over-strict independently of the fix.'
  issue: '3159'
  last_seen: c2
  note: 'Merge with t1:reset_procedure when fixing; one contract (CIT-005 / H4-NONANSWER-001)
    covers both. The scenario''s expect list is also arguably over-strict — the clarifying
    question itself is by design. | c3: PASSES live (c2 FAIL -> c3 PASS). CIT-005
    confirmed on the real bot.'
  status: FIXED
  summary: same footer-on-a-non-answer defect as t1:reset_procedure — a question-only
    turn receives the "no documentation indexed" admission
t2:continuation_is_kept:
  applied: true
  convs:
  - t2_002_continuation_is_kept
  - t2_007_continuation_is_kept
  first_seen: c1
  fix: 'PR #3150 (c0d3722e3) — shared subject-naming discriminator on both fresh-thread
    branches + IDLE equipment context in the retrieval query'
  last_seen: c1r1
  note: Confirmed fixed on main by two independent triage agents; c1r4 tier 2 passed
    10/10.
  status: FIXED
  summary: a pronoun follow-up ("is that fault serious?") after a drive-pack-answered
    turn lost its referent and asked the technician for the code MIRA had just explained
t2:pivot_after_fault:
  applied: false
  convs:
  - t2_000_pivot_after_fault
  - t2_005_pivot_after_fault
  - t2_s42_000_pivot_after_fault
  - t2_s42_005_pivot_after_fault
  first_seen: c1
  fix: 'partially addressed by PR #3153 (IDLE severance fault carry) — held, not merged'
  issue: '3160'
  last_seen: c3
  note: 'Root cause: the fresh-thread pivot fires only from ACTIVE_DIAGNOSTIC_STATES,
    and the low-groundedness self-critique clarifier parks the session in DIAGNOSIS_REVISION
    first. Suggested contract CTX-001d: a pivot fires from every state holding a pending
    diagnostic question. | c3: still fails, 1/2 variants this round (2/2 in c2). Unfixed
    on this branch as expected — the partial fix lives in held PR #3153.'
  status: OPEN
  summary: '"Actually forget that — my conveyor keeps stopping" is not recognised
    as a pivot when the session sits in DIAGNOSIS_REVISION, so MIRA answers the previous
    question one turn late and carries the dead fault forward'
t8:experienced:
  applied: false
  convs:
  - t8_41_001_experienced
  first_seen: c1
  issue: '3157'
  last_seen: c1
  note: 'The router intent check_equipment_history returns before the UNS confirmation
    gate. The judge''s own flag (conflicting F004 meanings) is likely a false positive;
    the real defect is the recall turn itself. Suggested contract MEM-002. | c2 (main@c0d3722e3,
    2026-08-08): did NOT reproduce — passed. The router-intent early return that bypasses
    the UNS gate is unchanged, so treat this as not-triggered rather than resolved.'
  status: OPEN
  summary: history questions ("did it throw F004 yesterday", "when was this last updated")
    are answered by dumping the last 5 raw chat lines under "Last 5 interactions for
    this equipment", with no work-order lookup and no asset scoping
t8:impatient:
  applied: false
  convs:
  - t8_41_003_impatient
  first_seen: c1
  issue: '3158'
  last_seen: c1
  note: 'The CTX-004 repeated-answer guard has _REPEAT_MIN_LEN = 40 and the reply
    normalises to 34 characters, so the guard returns before comparing. The judge
    filed this as GROUNDING, which is a false positive — the honest KB-gap admission
    was correct. The repetition is the real defect. Suggested contract CTX-004b. |
    c2 (main@c0d3722e3, 2026-08-08): did NOT reproduce — all 4 personas passed. This
    is non-reproduction, not a fix: _REPEAT_MIN_LEN is still 40 and the offending
    line still normalises to 34 chars, verified on the same commit. The persona simply
    did not drive MIRA into the repeat this round.'
  status: OPEN
  summary: MIRA repeats the byte-identical line "Check the display for a fault code"
    on three separate turns, including as its answer to a direct challenge about that
    very sentence
