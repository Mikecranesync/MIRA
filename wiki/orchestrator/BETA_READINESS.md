# MIRA Beta Readiness Scorecard

> ⚠️ Beta-readiness == production == origin/main. Latest run **B9 2026-06-16 (Lens B) — deploy-truth audit ABORTED: execution sandbox down all run (bash/git `useradd` I/O error ×4), so `origin/main` was UNREADABLE.** No freshness gate, no `git show`, no graphify, no `tsc`. **B8 GREEN (2026-06-15 @8c3a44c1) is carried forward, NOT re-confirmed; Lens B is retried next run.** Only file tools worked → one working-tree spot-check (canary SQL, invariant across the window) corroborates the standing item; two staleness markers proved the branch is pre-B7, so the tree was correctly NOT audited as deploy truth. Prior full audit: A9 2026-06-16 (Lens A, `origin/main` @ba79b4de).

North Star: a stranger signs up on app.factorylm.com, uses MIRA's grounded chat, and nothing breaks, leaks, or lies.
Legend: 🟢 GREEN ready · 🟡 YELLOW gaps · 🔴 RED blocker · ⬜ UNKNOWN never audited

> ⚠️ **2026-06-15 (F8): audited `origin/main` @ba79b4de — HEAD (`research/ignition-tag-client-readiness`) was 170 behind (ahead 11).** Lens F rotated (oldest — F7 ran 06-14; A8–E8 all ran 06-15). Freshness guard exit 3 (STALE); beta-readiness == production == origin/main. Verdict 🟢 GREEN held + strengthened — #1901 onboarding upload→ask **beta-gate close** merged; sharpest residual = that gate has no CI regression test.
>

> ⚠️ **2026-06-15 (E8): audited `origin/main` @64e156c9 — HEAD (`research/ignition-tag-client-readiness`) was 164 behind (ahead 11).** Lens E rotated (oldest; E7 ran 06-14). Freshness guard exited 3 (STALE; smoke-test.yml + docker-compose.saas.yml DIFFER) → audited deploy truth via `git show`/`git log`. Verdict **🟢 GREEN held + strengthened.** Only **2** E-scope commits since E7 baseline (`fa9bfde7..64e156c9`): #1970 (version counter — version-gate + version-tag auto-tag + rollback-on-every-merge) and #1981 (migration 053 dogfood-account cleanup). **Gap-1 (staging compose) + Gap-3 (staging bot) CLOSED** — `docker-compose.staging.yml` + `docker-compose.staging-vps.yml` + `@MiraStaging_bot` (qa-regression.yml) all exist; full staging suite live (deploy-staging, migration-verify, qa-regression, deploy-nginx-staging-passthrough). Migrations now **ledgered/idempotent** (`schema_migrations` keyed on FILENAME — migration 048; the dup-048 ordinal is SAFE by design, no renumbering). smoke-test.yml covers the **full hub money-path** (`/quickstart` grounded chat P1-1 + `/api/quickstart/ask` 429 rate-limit P0-1 on app.factorylm.com); deploy-vps **double-gated** (Staging Gate + Smoke; hotfix bypass audited). Open (non-blocking): doc-drift (patch `e8-staging-docdrift`) + `version-gate.yml` not yet a required branch-protection check (human rollout). KG: graphify not in sandbox + YAML poor AST fit → **+10 nodes/+6 edges** hand-extracted (`e8-findings.jsonl`); insight = CI/promotion subgraph sparse (~2 wf nodes @E7 vs ~15 staging/deploy workflows on main → deploy maturation invisible to code-AST KG).

> ⚠️ **2026-06-15 (D8): audited `origin/main` @64e156c9 — HEAD (`research/ignition-tag-client-readiness`) was 164 behind (ahead 11). Eval & test layer PROVABLY FROZEN since D7 (0 commits to `tests/eval`, `tests/`, eval workflows).**
>
> ⚠️ **2026-06-15 (C8): audited `origin/main` @79444d9 — HEAD (`feat/realtime-datapoint-clock`) was 160 behind (ahead 10).** Lens C rotated (oldest — C7 ran 06-14; A8/B8 ran 06-15). Freshness guard reported **STALE** (160 behind; `mira-hub`/`mira-web` DIFFER from origin/main) → audited deploy truth via `git show origin/main:<path>` / `git rev-list`. **Verdict: 🟢 GREEN held** — engine layer PROVABLY FROZEN since C7 (0 commits to `mira-bots/shared/` or `mira-pipeline/ignition_chat.py` across `9918cbf0..79444d9`); all 5 gate/citation/groundedness invariants re-verified intact on deploy truth; standing `ENFORCE_ASSET_AGENT_GATE` resolver gap unchanged, default-off, HMAC-gated, not stranger-reachable → not beta-blocking. KG: appended +10 nodes/edges to `kg/c8-findings.jsonl` (graphify still uninstallable in-sandbox — no LLM key — so nightly graph + `kg_query.py` + hand-extracted JSONL, the established pattern). Rotate → D next.
>
> ⚠️ **2026-06-15 (A8): audited `origin/main` @80f5da18 — HEAD (`feat/realtime-datapoint-clock`) was 154 behind (ahead 10).** Lens A rotated (oldest — A7 ran first on 06-14; round 7 since covered B7–F7). Freshness guard exited 3 (STALE; `mira-hub` + `mira-web` DIFFER from origin/main) → audited deploy truth via `git show origin/main:<path>`. **Verdict: 🟢 GREEN held + strengthened** — 15 hub/web commits since A7, zero weakened an auth surface; 2 new API routes both auth-gated + tenant-scoped (no IDOR); secrets scan clean.
>
> ⚠️ **2026-06-14 (F7): audited `origin/main` @a485bdf8 — HEAD (`feat/realtime-datapoint-clock`) was 153 behind (ahead 10).** Lens F rotated (oldest — F6 ran 06-13; A7–E7 all ran 06-14). Freshness guard exited 3 (STALE; `wiki/hot.md` + `docs/specs/` DIFFER from origin/main) → audited deploy truth via `git show origin/main:<path>`.
>

> ⚠️ **2026-06-14 (E7): audited `origin/main` @fa9bfde7 — HEAD (`feat/realtime-datapoint-clock`) was 150 behind (ahead 10).** Lens E rotated (D7 logged "Rotate E next"; E6 ran 06-13, oldest). Freshness guard exited 3 (STALE; `.github/workflows` + `docker-compose.saas.yml` + `mira-hub/db/migrations` DIFFER from origin/main) → audited deploy truth via `git show`/`git log`. Verdict **🟢 GREEN held + strengthened — the signup money-path is now a DEPLOY PRECONDITION.** Only **3 commits** touched E-scope since the E6 baseline (`afe9da64..fa9bfde7`): **(1)** `44fa7d97` "feat(ci): smoke gate for signup → upload → cite money-path" wires `playwright.signup.config.ts` into the **deploy-BLOCKING** smoke job (`smoke-test.yml:73-78`, factorylm.com→app.factorylm.com) — at E6 this config was a 0-workflow orphan; now a broken pricing→trial-CTA→checkout(303→stripe)→magic-link path **blocks the prod deploy** (the E-lens confirmation of B7's headline, from the gate's own side). **(2)** `0388b74c` adds `eval-replay-gate.yml` (the D4 keyless replay seam) — but it is **advisory, PR-only, and INERT**: it `hashFiles`-guards on `tests/eval/fixtures/llm_replay/cascade.json` and **skips with `::warning::`** because the store is `.gitignore`'d & unrecorded; it does **not** gate deploy. **(3)** `24a6c2ff` (#1966) plumbs `COMMAND_CENTER_DISPLAY_HOST_ALLOWLIST` into mira-hub (SaaS-only, benign). **Two-gate deploy intact & operable:** `deploy-vps.yml` fires on `workflow_run:["Smoke Test"]` `conclusion=='success'` AND verifies the same-commit **Staging Gate** for the PR head (refuses no-PR direct-push deploys; `skip_staging_gate` needs a reason → auto-opens an audit issue); the gate can fire because install is `bun install --frozen-lockfile` (#1855 held). **Staging REAL:** `docker-compose.staging.yml` + `staging-gate.yml`/`deploy-staging.yml`/`staging-namespace-create-e2e.yml`/`deploy-nginx-staging-passthrough.yml`. **Migration head 052** (filename-keyed ledger `048_schema_migrations_ledger.sql`). **Residuals (doc-hygiene only, none stranger-reachable ⇒ GREEN):** R1 `mira-hub/package-lock.json` STILL coexists with `bun.lock` — E5's top action, now a **3-cycle stall** (E5 06-12 → E6 06-13 → E7 06-14, moved 0); CI-inert but a local `npm ci` re-hits the #1855 graphology/unpdf drift → staged apply-ready `patches/2026-06-14-E7-drop-stale-package-lock.md`. R2 `hot.md` still claims migration `head=037` (L160) vs actual **052**. R3 `CLAUDE.md` Environments table still marks `docker-compose.staging.yml`/`@MiraStagingBot` *(TODO)* though staging is real. R4 migration numbering: **040–042 ABSENT** (gap) + **dup-048 prefix** (next free 053). **KG (USED nightly graph @308e7b55, 3888/5853, no-clobber; +11 nodes/+8 edges sidecar `kg/e7-findings.jsonl`; graphify uninstallable — all LLM keys unset; YAML gate→config edges hand-extracted):** the structural query "pipeline nodes *present but not operable*" = EXACTLY [`eval-replay-gate.yml` (new workflow that skips), `package-lock.json` (lockfile CI-bypassed yet a local footgun)] — the same present≠operable fingerprint E5 named and #1855 *fixed* for the deploy gate; the inverse query "deploy-gate workflow with a `runs` edge to a money-path test config" flipped EMPTY (E6) → EXACTLY [`smoke-test.yml`→`playwright.signup.config.ts`] this cycle. No code edited (audit-only; 1 patch staged). Rotate to **Lens F** next run (F6 06-13 now oldest).
>

> ⚠️ **2026-06-14 (C7): audited `origin/main` @9918cbf0 — HEAD (`feat/realtime-datapoint-clock`) was 137 behind (ahead 10).** Lens C rotated (B7 logged "Rotate→C next"; C6 ran 06-13, oldest of the 06-13 batch). Freshness guard exited 3 (STALE; `mira-bots/shared/` + `mira-pipeline/ignition_chat.py` both DIFFER from origin/main) → audited deploy truth via `git show`/`git log`. Verdict **🟢 GREEN held — engine layer PROVABLY FROZEN since C6: `git log --oneline b2d74f7c..origin/main -- mira-bots/shared/ mira-pipeline/ignition_chat.py` = empty (baseline confirmed ancestor of `9918cbf0`).** All 5 engine invariants re-verified byte-stable on deploy truth: **(1)** UNS chat-gate default-on (`_UNS_GATE_ENABLED` default true), single guarded call site `engine.py:2002`→`2005` via `_should_fire_uns_gate` (def @5434), fires on `_GATED_INTENTS` {diagnose_equipment, schedule_maintenance} when no `asset_identified` + state IDLE; **(2)** direct-connection carve-out returns False (skips gate) **ONLY** when `uns_ctx.source=='direct_connection'` (`engine.py:5469`); **(3)** Ignition reject-on-missing-identifier intact — `ignition_chat.py:384-394`: asset-specific turn with no `asset_id`/`asset_context` → `JSONResponse(status_code=422, {"error":"uns_required"})`, **no downgrade to chat-gate**; **(4)** citation_compliance wired (`engine.py:19-21` imports `check_citation_compliance`/`citation_enforce_enabled`/`enforce_citation_via_rewrite` + H4 `enforce_citation_or_gap_admission` L674); **(5)** groundedness 1-5 scoring + low-grounded clarify episode (L2445-2543) + `_is_grounded` rewrite-retry (L3415) intact. **NEW (sharpens why YELLOW sub-item isn't beta-blocking):** the Ignition classifier fail-open path (`ignition_chat.py:104`, `_is_asset_specific` except→`return False`) degrades to **plain chat** (no diagnosis), so a classifier/Neon blip can never produce an *ungrounded asset-specific diagnosis* — it just answers as general Q&A. **Standing YELLOW sub-item UNCHANGED (non-beta-blocking, Phase-6):** direct-connection certification is PRESENCE-based not RESOLUTION-based (`ignition_chat.py:254` "NON-FUNCTIONAL until `asset_context→(uns_path|equipment_id)` resolver"; L384 checks mere presence) — reachable ONLY via the HMAC-gated Ignition factory surface, never hub stranger-signup. **KG:** used nightly graph @308e7b55 (3888/5853, no-clobber); +10 nodes/+3 edges sidecar `kg/c7-findings.jsonl` (graphify uninstallable — GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset). Insight: a grep across engine.py/uns_resolver.py/ignition_chat.py/mira-bridge/mira-relay shows `source="direct_connection"` has **exactly ONE producer** (`ignition_chat.py:385`, presence-gated) and **exactly ONE consumer** (`engine.py:5469`, trusts unconditionally) → the entire P6 certification gap is **a single presence-gated edge behind HMAC**, and the lone producer is non-stranger-reachable, so the default-on chat-gate governs **every** beta path. No code edited (audit-only; C6's staged docstring patch still applies). Rotate to **Lens D** next run (D6 06-13 now oldest).
>
> ⚠️ **2026-06-14 (B7): audited `origin/main` @a0f79dc4 — HEAD (`feat/realtime-datapoint-clock`) was 135 behind (ahead 10).** Lens B rotated (oldest; B6 ran 06-13, earliest of the 06-13 batch). Freshness guard reported STALE (`mira-hub` DIFFERS from origin/main) → audited deploy truth via `git show`/`git log`/`git diff`. Verdict **🟢 GREEN held + strengthened.** Headline: the **signup money-path smoke gate LANDED** (`smoke-test.yml:73-78`, commit `44fa7d97` "feat(ci): smoke gate for signup → upload → cite money-path") — `playwright.signup.config.ts` was a 0-workflow orphan at the B6 baseline (`b48b8459`) and is now a real CI run-step against factorylm.com→app.factorylm.com, closing the B5/B6 "post-signup beta surface has no CI gate" concern for the money path. ADR-0017 helper CLEAN: `decide/route.ts` imports (L4) + calls `applyHubProposalTransition()` (L97); raw `kg_relationships` UPDATE (L138) = documented engine projection; the L94-96 comment now explicitly ties the lockstep to "the drift the proposal-state canary detects." 2 B-native fixes raise readiness, each shipping its own vitest unit test into the wired Hub Unit Tests job: **#1926** (`e9cdf5e7`) asset-validation citations scoped (`allowTenantFallback:false` → Validate/approve stops counting unrelated tenant manuals toward approval); **#1924** (`1a900171`) work-order start status persists (canonical `in_progress` PATCH before timer; legacy+canonical both active on reload). The ONE stalled item is now a 5-cycle stall (B3→B7): `proposal_state_drift.sql` still **2 forward-only `@check`s** → engine `proposal_transition.py` terminal transitions disown `ai_suggestions` (docstring L7-12: "Hub-side projections … owned by the TypeScript helper") while admin `proposals` GET renders `pending` = **stale-pending lie**, canary-blind; staged `2026-06-10-canary-reverse-drift-check.patch` apply-ready, unmerged — admin-only ⇒ not stranger-reachable ⇒ GREEN. Playwright coverage gap NARROWED 4→**3 ungated configs** (command-center/onboarding-validate/onboarding-walkthrough). **KG:** used nightly graph @308e7b55 (3888/5853); +13 nodes/+14 edges sidecar `kg/b7-findings.jsonl` (graphify uninstallable — no LLM key). Insight: `pw_config_signup` crossed orphan→wired this cycle, leaving EXACTLY [command-center, onboarding-validate, onboarding-walkthrough] as workflow-orphans; the canary carries 3 forward `checks` edges but 0 reverse edge from the engine = the one missing `@check` the staged patch adds. No code edited (audit-only). Rotate to **Lens C** next run (C6 06-13 now oldest).
>

> ⚠️ **2026-06-13 (F6): audited `origin/main` @2337a116 — HEAD (`feat/realtime-datapoint-clock`) was 102 behind (ahead 10).** Lens F rotated (E6 logged "Rotate→F next"; F5 ran 06-12, oldest after A6–E6 all ran 06-13). Freshness guard exited 3 (STALE; `docs/plans`,`docs/specs`,`wiki/hot.md` DIFFER) → audited deploy truth via `git show`/`git log`. Verdict **🟢 GREEN held + strengthened — North Star answers YES on deploy truth: zero stranger-reachable live break/leak/lie.** All 5 historically-tracked blockers re-verified closed; the 6 commits since F5 are all hardening or admin-only — **#1935 streaming ingest-v2** (the one NEW stranger-path upload/write surface) **preserves the #1903 `is_private=true` leak fix AND ships its own regression test** (`node-knowledge-ingest-is-private.test.ts`, runs in Hub Unit Tests); **#1875** now blanks citations on a refusal (extra anti-lie); **#1932** *strengthens* admin auth to capability-based RBAC (`requireCapability`); **#1945** was a label rename; **#1928** makes the scan refuse honestly when docs are missing. Master-plan Phases 4–13 are all industrial-connect (PLC/Ignition/tag-stream/anomaly) = post-beta, not stranger-reachable. **Sole residual = the same single unguarded invariant** (D's inert replay seam over the #1858 vendor-strip), plus B coverage/detective gaps + doc-hygiene staleness — none stranger-reachable today. **KG (USED nightly graph 3888/5853; +12 nodes/+10 edges sidecar `kg/f6-findings.jsonl`; graphify still uninstallable — no LLM key).** Insight: of every stranger-path invariant, EXACTLY ONE lacks an operable CI guard (the vendor-strip) — and the NEW code this cycle shipped WITH its guard, proving the gap is "one historical guard built inert," not "we don't write guards." No code edited (audit-only; ledger patches staged). Rotate to **Lens A** next run (round 7 begins).
>

> ⚠️ **2026-06-13 (E6): audited `origin/main` @afe9da64 — HEAD (`feat/realtime-datapoint-clock`) was 100 behind (ahead 10).** Lens E rotated (D6 logged "Rotate→E next"; E5 ran 06-12, the oldest formal audit after A6/B6/C6/D6 all ran 06-13). Freshness guard exited 3 (STALE; `.github/workflows` + `docker-compose*.yml` DIFFER from origin/main) → audited `origin/main` via `git show`/`git log`/`git diff`, the deploy/beta truth. Verdict **🟢 GREEN held — promotion pipeline is the most mature lens; the only gaps are doc-hygiene, none beta-blocking.** **#1855 deploy-gate fix is now PROVABLY LANDED on deploy truth:** `smoke-test.yml:60` runs `bun install --frozen-lockfile --ignore-scripts` (E5 saw it in-flight; it is merged & gating). **Two-gate deploy intact:** `deploy-vps.yml` fires on `workflow_run:["Smoke Test"]` AND refuses to deploy unless the same-commit **Staging Gate** succeeded (`staging-gate.yml` runs every PR to main vs NeonDB staging branch); `workflow_dispatch` hotfix bypass still REQUIRES `skip_reason` → auto-opens an audit issue. **Smoke gates the two beta-breakable P0 invariants on the LIVE hub:** `/api/quickstart/ask` flood→**429** (P0-1 rate-limit) + `/api/documents` unauth→**401** (P0-2 tenant gate), plus web routes (pricing CTA, /cmms magic-link, Stripe checkout 303). **Staging is REAL** (no longer TODO): `docker-compose.staging.yml` (4187B, engine/bot path only, `factorylm/stg`, `@MiraStaging_bot`, pipeline :9098) + **both** referenced docs present (`docs/specs/staging-environment-spec.md`, `docs/runbooks/staging-environment.md`). **E-scope FROZEN since E5:** `git rev-list --count c3f871e0..afe9da64` = 9 commits, **0** touch `.github/workflows`/`docker-compose*.yml`/`docs/migrations` (the 9 are hub UX + 1 `security(hub)` CSV-formula-injection fix #1919). **Residuals (carried, doc-hygiene only):** **R1 (YELLOW)** `mira-hub/package-lock.json` STILL coexists with `bun.lock` on deploy truth — E5's explicit top action (delete it) NOT done; CI is safe (uses bun) but a local `npm ci` still hits the #1855 drift + two lockfiles = ambiguous SoT. **R2** `wiki/hot.md` on origin/main still claims migration **head=037** while the actual `mira-hub/db/migrations` head is **052** (049–052 are the tenant-isolation fixes: 051 backfill_tenants #1899, 052 privatize_node_attachment_chunks #1903) — a 15-migration drift in the canonical "where to resume" doc that hides that the cross-tenant LEAK fix already landed; repo has 4 migration namespaces (`docs/migrations`=008, `mira-hub/db/migrations`=052, `mira-bridge/migrations`, `mira-core/mira-ingest/db/migrations`) and no single doc reconciles them. **R3** root `CLAUDE.md` Environments table still marks `docker-compose.staging.yml *(TODO)*` though it is fully real. **Why GREEN not YELLOW:** deploy gate fires, both gates intact, staging operable, P0 smokes live — nothing breaks/leaks/lies on the promotion path; residuals are docs lagging *good* reality. **KG (USED the nightly graph @308e7b55, 3888 nodes/5853 links; +8 nodes/+5 edges sidecar `kg/e6-findings.jsonl`; graphify still uninstallable in-sandbox — no LLM key, and E-scope is YAML which Tree-sitter code-extraction does not cover, so gate→route edges hand-extracted):** smoke P0-2 (`/api/documents` unauth→401) exercises **`sessionOr401()`** — the **#2 god node (degree 95)**, the hub's single most-depended-on auth chokepoint → the highest-leverage auth dependency in the graph is gated in production before every promote. No code patch (residuals are doc edits + a 1-file `git rm`, founder/owner-gated). Rotate to **Lens F** next run (F5 06-12 now oldest).
>
> ⚠️ **2026-06-13 (D6): audited `origin/main` @b2d74f7c — HEAD (`feat/realtime-datapoint-clock`) was 98 behind (ahead 10).** Lens D rotated (D5 was 06-12, the oldest formal audit after A6/B6/C6 all ran 06-13). Freshness guard exited 3 (STALE; `tests/eval` DIFFERS from origin/main) → audited `origin/main` via `git show`/`git log`, the deploy/beta truth. Verdict **🟡 YELLOW held — `tests/eval/` and all of `tests/` PROVABLY FROZEN since D5: `git rev-list --count 3fd194b2..b2d74f7c -- tests/` = 0.** All 4 standing findings re-verified UNCHANGED on deploy truth: **(1)** replay store still inert — `.gitignore:241` (`tests/eval/fixtures/llm_replay/*.json`) had 0 commits since D5 and `cascade.json`/`retrieval.json` are **absent** on origin/main, so strict replay raises on call #1 (`llm_replay.py:137`); **(2)** `offline_run --replay` is wired into **0 workflows** — the only 2 workflow commits since D5 (`2b29757f`, `e87fe563`) are hub folder-upload fixes (#1899), not eval; `ci-evals.yml`'s Tier-B runs `synthetic_eval.py --mode dry-run`, `deepeval-ci.yml` runs live on `GROQ_API_KEY`; **(3)** `fix_proposer_tasks.py` still clusters off a single scorecard (phantom-work path live); **(4, SHARPENED this run)** `test_offline_scorecard.py` is a **58-LOC scorecard-*writer* unit test** (`tmp_path`/`monkeypatch`), NOT the deterministic diagnostic eval — Tier-A `pytest tests/` being green does **not** mean the diagnostic suite is regression-guarded. Newest committed scorecard is still `2026-06-08T0229-offline-text.md` = **50/57 LIVE**; no replay-mode scorecard has *ever* been committed. **Both remediation patches remain STAGED & UNMERGED** (a frozen `tests/` is the direct cause — neither could land): D5 `fixproposer-median-quorum.patch` (keyless, mergeable-today) + D4 `replay-seam-activation.md` (founder-gated). **Why YELLOW not RED:** measurement-integrity gap, not a stranger-facing break/leak/lie — beta-critical checkpoints (No5xx/TurnBudget/RState + both safety fixtures) are bit-stable. **Why YELLOW not GREEN:** `cp_citation_vendor_relevance` (the only deterministic guard for the #1858 wrong-vendor citation strip) is structurally unreachable in CI. **KG (USED the existing nightly graph @308e7b55, 3879→3888 nodes / 5840→5853 links; +9 nodes/+13 edges sidecar `kg/d6-findings.jsonl`; graphify still uninstallable in-sandbox):** the structural query confirms the deterministic eval seam is a **graph ISLAND** — `eval_offline_run`/`llm_replay`/`eval_grader` exist as nodes but **no `.github/workflows/*` node has an inbound edge to them**, so `cp_citation_vendor_relevance` is an orphaned guard with no CI call path. No code patch (both fixes already staged + founder-gated). Rotate to **Lens E** next run (E5 06-12 now oldest).
>

> ⚠️ **2026-06-13 (C6): audited `origin/main` @b2d74f7c — HEAD (`feat/realtime-datapoint-clock`) was 98 commits behind.** Lens C rotated (oldest formal audit — C5 ran 06-12; A6/B6 already ran round 6). Freshness guard exited 3 (STALE) → audited `origin/main` via `git show`, the deploy/beta truth. Verdict **🟢 GREEN held — engine layer PROVABLY FROZEN since C5: 0 commits to `mira-bots/shared/` OR `mira-pipeline/ignition_chat.py` across `4344e99a..b2d74f7c` (all 50 drift files are mira-hub/peripheral).** Gate default-on, single guarded call site (engine.py:2002 `confidence>0 AND _should_fire_uns_gate`); direct-connection carve-out skips ONLY on `source=='direct_connection'` (engine.py:5469); Ignition reject-on-missing-identifier contract verified SHIPPED (422 `uns_required`, no downgrade — ignition_chat.py:384-394); citation_compliance + groundedness wiring intact. **One tracked YELLOW sub-item (NOT beta-blocking):** direct-connection certification is PRESENCE-based, not RESOLUTION-based (`direct_connection` stamped on mere presence of `asset_id`/`asset_context`, ignition_chat.py:384; the `asset_context→(uns_path|equipment_id)` resolver is NON-FUNCTIONAL, 254-259; `ENFORCE_ASSET_AGENT_GATE` default-OFF). Reachable ONLY via the HMAC-gated Ignition factory surface (not hub stranger-signup) + citations still apply → pre-scale hardening, not a beta gate. Also filed: stale engine.py docstring L1164 (contradicts L5469); patch staged `patches/c6-engine-docstring-direct-connection.patch` (apply-checked CLEAN vs origin/main).

> ⚠️ **2026-06-13 (B6): audited origin/main @b48b8459 — HEAD (`feat/realtime-datapoint-clock`) was 97 behind (ahead 10).** Lens B rotated (A6 ran 06-13 and logged "Rotate→B next"; B5 was 06-12, the oldest formal audit before C5–F5/A6). Freshness guard exited 3 (STALE, 97 behind; `mira-hub`+`mira-web` DIFFER from origin/main) → audited `origin/main` via `git show`/`git log`/`git ls-tree`, the deploy/beta truth. Verdict **🟢 GREEN held — and the route-coverage gap that has defined the B-series MATERIALLY NARROWED, even as the two named B items reached a 4-cycle stall.** Since B5 (`59790aa1`→`b48b8459`) **15 commits touched `mira-hub`**; most were already cross-checked by A6/E5/F5 (#1919 CSV-injection, #1903 is_private chunks, #1904 identity-lie, #1909 /magic, #1899 folder-upload 500s, #1912 NodeChat citations), but **2 are B-lens-native and had never been audited from B's seat:** **(1) #1890 (`11d308f2`) closes the admin Suggestions "Verify" 500** — migration **050** `kg_relationships_proposal_id.sql` adds the `relationship_proposal_id` column that "existed only in a COMMENT in 027_ai_suggestions.sql", so verifying a `kg_edge` AISuggestion no longer 500s; **code is merged** and the schema sits in the ledgered sequence (hub head now **052**: 050 proposal_id / 051 backfill_tenants / 052 privatize_chunks) applied by `apply-migrations.yml` — **caveat: the 500 is *fully* closed only once 050 is applied to prod NeonDB (code-merged ≠ schema-applied)**; admin-only ⇒ not stranger-reachable. **(2) #1888 (`c14449de`) route-surface drift guard** — `scripts/sitemap.mjs` records **48 pages + 116 API routes** as `docs/sitemap.snapshot.json`, and `src/lib/sitemap-drift.test.ts` is a vitest golden-file guard that **runs in the existing Hub Unit Tests job (no workflow edit) and fails when any page/API route is added or removed without re-running `bun run sitemap`** → the hub route surface went from **ungoverned to snapshot-gated**; a route can no longer appear or vanish silently (this directly answers the "routes vs spec" coverage angle). **#1889/#1886** additionally wired the namespace-builder E2E (`tests/e2e/namespace-inline-create.spec.ts`, **9/9 against prod**) into `staging-namespace-create-e2e.yml` (post-deploy). **ADR-0017 helper CLEAN on deploy truth:** `proposals/[id]/decide/route.ts` imports (L4) + calls `applyHubProposalTransition()` (L97); the raw `UPDATE kg_relationships` (L138) is the documented delegated engine-side projection — no raw-UPDATE violation. **Both standing B items UNCHANGED on deploy truth (0 of the 15 commits touched them) — now a 4-cycle stall (B3→B4→B5→B6):** (1) `tests/canary/proposal_state_drift.sql` still has **exactly 2 forward-only `@check`s** → an engine-side terminal transition (`proposal_transition.py`) updates `relationship_proposals` but disowns `ai_suggestions`, so the admin `proposals` GET can render a **stale `pending`** the canary is blind to; staged `patches/2026-06-10-canary-reverse-drift-check.patch` still apply-ready, unmerged. (2) the **4 named Playwright configs (command-center/signup/onboarding-validate/onboarding-walkthrough) still gate 0 workflows** (confirmed — each returns 0 references across `.github/workflows`); the post-signup command-center surface still has no *dedicated pre-merge* gate. **Why GREEN not YELLOW:** the canary gap is a detective-control over an **admin-only** queue (a stranger never sees `/proposals`); the Playwright gap is missing *suite* coverage, not a proven defect, and it **narrowed** this cycle (route surface now snapshot-gated via #1888; namespace builder now 9/9 post-deploy via #1889); the public signup→chat path is unaffected. Honest signal = **stalled remediation around a shrinking gap** — one zero-risk SQL patch + the four config-wirings, founder-gated, idle 4 cycles, while the surrounding coverage improved without them. **No code patch** (both stalled items already staged; #1890/#1888 landed on main). KG: USED the existing nightly-agent-owned graph (`@308e7b55`, 3879 nodes/5840 links); B6 triples recorded sidecar `kg/b6-findings.jsonl` (+11 nodes/+9 edges) per the F5/A6 no-clobber pattern; graphify still can't run in-sandbox (not installed + GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset). **Insight:** the structural query *"decide-route → status-table edges"* returns **EXACTLY `[decide_route_post → table_relationship_proposals]` and NO direct `→ ai_suggestions_table` edge** — `ai_suggestions` is reachable from the decision surface **only through `applyHubProposalTransition`** — which structurally MIRRORS the standing reverse-drift blind spot: any terminal transition that bypasses the hub helper (the engine's `proposal_transition.py`) reaches `relationship_proposals` but leaves `ai_suggestions` un-updated, which is exactly the stale-`pending` the 2-check canary cannot see.
>

> ⚠️ **2026-06-13 (A6): audited origin/main @51e3f5b0 — HEAD (`feat/realtime-datapoint-clock`) was 96 behind.** Lens A rotated (oldest formal audit — A5 ran 06-11; round 5 since covered B5/C5/D5/E5/F5). Hub NOT frozen since A5: 18 commits incl. 2 `security(hub)` fixes (#1919 CSV formula-injection, #1903 is_private chunks) + #1904 fresh-tenant identity-leak + #1909 pre-auth /magic nav-leak — all VERIFIED CLOSED on deploy truth. GREEN held + strengthened.
>

> ⚠️ **audited origin/main @51e3f5b0 (HEAD `feat/realtime-datapoint-clock` was 96 behind).**
>
> ⚠️ **2026-06-12 (F5): audited `origin/main` @51e3f5b0 — HEAD was 96 commits behind.** Lens F rotated (oldest formal audit — F4 ran 06-11; round 5 has since covered A5/B5/C5/D5/E5). Freshness guard → STALE (96 behind; `wiki/hot.md` + `docs/plans/` DIFFER from origin/main) → audited `origin/main` via `git show`/`git grep`/`git log`, the deploy/beta truth. Verdict **🟢 GREEN held — and materially STRENGTHENED.** Since F4 (`cb1be3fa`→`51e3f5b0`) `origin/main` advanced **27 commits, overwhelmingly stranger-path hardening that CLOSES the exact defect classes F tracks.** **All 5 historically-tracked blockers re-verified STILL closed on deploy truth (read the wired call):** **(BREAK)** `quickstart/ask` 429 before work (`route.ts:109`, per-IP-hash comment L17); **(LEAK)** `/api/documents` tenant-scoped **+ NEW** node-attachment chunks now written `is_private=true` at write time (#1903, `node-knowledge-ingest.ts:95-101`, defense-in-depth atop mig052); **(LIE)** `stripConflictingVendors` pre-`citations[]` (`route.ts:13→152`) **+ NEW** NodeChat `[n]` markers now 1:1 with rendered source chips (#1912, `manual-rag.ts:363-377` stable citation key; refusals suppress phantom chips); **(GATE)** money-path smoke wired (`smoke-test.yml:96`=/quickstart P1-1, `:97`=429 P0-1, `:94`=checkout 303) **+** the deploy gate itself restored (#1855 `bun install --frozen-lockfile`, `:60`); **(ENGINE)** citation enforce default-ON. **The literal North-Star flow — a stranger signs up, uploads a manual, MIRA cites it — was repaired across THREE distinct 500 BREAK causes this cycle** (#1899: unpdf standalone externalize #1907 + data-side `tenants` row at signup #1910 + `is_private` writer #1911), **plus** the magic-link pre-auth 401+nav leak (#1909), the empty-namespace New-Folder pre-hydration drop (#1917), the **hardcoded "Mike Harper · Admin" identity LIE shown to every tenant** (#1904 — verified gone: only a fix-comment at `feed/page.tsx:169` + a legit test fixture remain), a **CSV/spreadsheet formula-injection LEAK** neutralized + QA-repro-tested (#1919 `neutralizeFormula`, 5 export routes), and the Suggestions "Verify" 500 (#1890). Promo commit #1899 (`4aa96a83`) ships a **live proof** of a stranger doing exactly this. **Master-plan deploy truth:** Phase 0 baseline ✅; one F-relevant roadmap gap — **Phase 6: `ignition_chat.py` sets per-asset `chat_id` but not `source="direct_connection"`** yet (⚠️ partial, **conservative-safe** — direct surfaces currently get the chat-gate, not an unsafe bypass). **Residuals (no NEW stranger-reachable LIVE defect ⇒ GREEN):** (1) UNCHANGED top beta-risk = **D-owned** — the inert replay seam still can't regression-guard the #1858/#1877 vendor strip (a closed lie with no CI gate could silently re-open). (2) **SHARPENED** — the upload→cite North-Star flow **just repaired 3× is itself still NOT in the deploy smoke gate** (smoke covers /quickstart+checkout+429, not the authed folder-upload→NodeChat→cite path) → the same flow could silently re-500; fix now unblocked (namespace E2E green 9/9 #1889; staged `patches/2026-06-09-smoke-gate-signup-moneypath.patch`). **No code patch** (5 blockers closed on main; both residuals founder-/cross-lens-gated, already staged). KG: USED the existing graph (`@308e7b55`, 3879 nodes/5840 edges) — the repo's **own nightly graphify agent (#1854/#1888) now owns `graph.json`**, so F5 triples recorded sidecar `kg/f5-findings.jsonl` (+13 nodes/+9 edges) rather than clobber it; graphify still can't run in-sandbox (uninstalled + LLM keys unset). **Insight:** the two highest-degree HUB nodes are `sessionOr401()` (deg 95, `session.ts:L85`) + `withTenantContext()` (deg 80, `tenant-context.ts:L22`) — the auth + tenant-isolation choke points 95/80 nodes funnel through — which **structurally corroborates A5's "0 auth-orphans across 111 routes":** the money-path front door is a single well-guarded chokepoint, and any route bypassing it would surface as a missing inbound edge.
>
> ⚠️ **2026-06-12 (E5): audited `origin/main` @c3f871e0 — HEAD (`feat/realtime-datapoint-clock`) was 91 commits behind.** Lens E rotated (oldest formal audit — E4 ran 06-11; round 5 has since covered A5/B5/C5/D5). Freshness guard exited 3 (STALE, 91 behind; `.github/workflows` + `docker-compose*.yml` DIFFER from origin/main) → audited `origin/main` via `git show`/`git log`/`git diff`, the deploy/beta truth. Verdict **🟢 GREEN held — but now EARNED-operable, not merely structurally present; E4's GREEN was masking a silently-broken deploy gate.** **HEADLINE — #1855 (`cde28770`) revealed the E4-window deploy gate was OPERATIONALLY RED:** the Smoke Test job (which *gates* `deploy-vps`) ran `npm ci --ignore-scripts` for `mira-hub`, and `npm ci` **hard-fails** when `package-lock.json` drifts from `package.json` — which it did (`graphology` #1789 + `unpdf` were never written to the npm lock) → Smoke could never go green → **EVERY prod deploy silently blocked, including the merged #867/#1663/#962.** A present, well-architected gate that *cannot fire* is indistinguishable from GREEN in a structural audit. The fix switches the **three** mira-hub installs (`smoke-test.yml`=the deploy gate, `staging-namespace-create-e2e.yml`, `enforcement-audit.yml`) to `bun install --frozen-lockfile --ignore-scripts`; **verified on deploy truth:** `mira-hub/bun.lock` is committed and `package.json` declares `graphology ^0.26.0` + `unpdf ^1.6.2` (the exact drift deps) → the install now resolves and the gate can fire again. **(deploy gate structure intact)** `deploy-vps.yml` still fires on `workflow_run:["Smoke Test"]` + deploys only on `conclusion=='success'` (L46-50) + a "Verify Staging Gate passed" step that refuses a non-PR SHA (direct pushes can't deploy) + `skip_staging_gate=true` needs a `skip_reason` and auto-opens a 24h audit issue. **(new sign-in guard — #1897, `3fd194b2`)** `oauth-redirect-canary.yml` now runs **hourly** (`cron: "17 * * * *"`) and **pages a human** (dedup `oauth-incident` issue, `issues: write`) **only** on exit 1 = `redirect_uri_mismatch` — catches a stranger sign-in break within **~1h** instead of ~24h. **(migration head 049→052, ledger operable)** `apply-migrations.yml` MIG_DIR=`mira-hub/db/migrations`, filename-keyed `schema_migrations`; **two of the three new migs close stranger-reachable defects:** **051** `backfill_tenants_from_hub_tenants` fixes a fresh-signup folder/Knowledge upload **500 BREAK** (#1899 — signup made only the auth-side `hub_tenants`, never the data-side `tenants` row that `knowledge_entries.tenant_id` FKs to; fixed forward in #1910 + backfilled by 051); **052** `privatize_node_attachment_chunks` closes a **#1833-class cross-tenant LEAK** (#1903 — the node=brain chunk writer inserted per-tenant chunks **without** `is_private`, defaulting false → leaked via the hybrid filter `(is_private=false OR tenant_id=$caller)`; writer now pins `is_private=true`, 052 privatizes existing `v2`+`node_attachment` rows, shared OEM corpus untouched). **(staging real, unchanged)** `docker-compose.staging.yml` (CHARLIE bot-path, `factorylm/stg`, `@MiraStaging_bot` via `STAGING_TELEGRAM_BOT_TOKEN`) + `staging-vps.yml`. **Why GREEN not YELLOW:** all four pillars present **and now operable** (gate can fire — #1855 restored it; money-path smoke wired; staging built; ledger operable + head-consistent within the hub tree) and two stranger-reachable defects (signup-500, node-chunk leak) **CLOSED** via migs 051/052. **Residuals (E3/E4 carryovers, now multi-cycle, all founder-gated / cosmetic):** (1) **NEW** — the stale `mira-hub/package-lock.json` is **still in the tree** alongside `bun.lock`; inert in CI (now bun) but a local `npm ci` re-hits the #1855 drift → **delete candidate** so `bun.lock` is the unambiguous SoT; (2) `hot.md` head=037 claim now even staler vs actual hub head **052**; (3) dup `048` prefix in the hub tree (B5; ledger applies both, next free **053**); (4) 040–042 gap (abandoned #1677 source-preservation) still not in `known-issues.md`; (5) orphaned `signup-flow.spec.ts` (money-path moved to wired smoke spec) — delete candidate. **No code patch** (the #1855 fix already landed; residuals are cosmetic/doc-hygiene). KG +14 nodes/+17 links (3879/5840, hand-extracted `_lens:E5`; graphify clone at `/tmp/graphify` but GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset — same wall as every prior run). Insight: **present ≠ operable** — a structural audit sees a gate's **wiring** edges (`smoke→deploy`, `staging-gate→deploy`) but not its **operability** edge (`install resolves`); E4's GREEN had a broken install edge (`npm ci` couldn't resolve) that **#1855 filled**. Same fingerprint as the D-series inert replay seam (structurally wired, operationally inert) — except **E's got FIXED this cycle while D's is still inert**, which is why E is GREEN and D is YELLOW.
>
> ⚠️ **2026-06-12 (D5): audited `origin/main` @3fd194b2 — HEAD (`feat/realtime-datapoint-clock`) was 85 commits behind.** Lens D rotated (oldest formal audit — D4 ran 06-11; round 5 has since covered A5/B5/C5). Freshness guard exited 3 (STALE, 85 behind; `mira-bots/shared` + `mira-hub` DIFFER from origin/main) → audited `origin/main` via `git show`/`git grep`/`git log`, the deploy/beta truth. Verdict **🟡 YELLOW held — `tests/eval/` is PROVABLY frozen since D4 (0 of 16 commits cb1be3fa→3fd194b2 touched `tests/eval/` or the eval workflows)** so all 3 D4 findings are live **verbatim** on deploy truth (re-read, not carried): **(1)** replay store still `.gitignore:241` (`tests/eval/fixtures/llm_replay/*.json`, only `.yaml` committed) → strict replay **raises on call #1** (`llm_replay.py`, `by_scenario` empty); **(2)** `offline_run --replay` in **0 workflows** (`ci-evals.yml`=`synthetic_eval.py --mode dry-run`; `deepeval-ci.yml`=live `GROQ` judge); **(3)** the nightly fix-proposer (`tests/eval/fix_proposer_tasks.py` → `mira-bots/tools/fix_proposer.py`) clusters off **ONE** scorecard (`min_cluster=3`, no median) → phantom clusters #1773/#1788 (`vfd_abb_03` passes 7/9). Latest committed scorecard `2026-06-08T0229` = **50/57 LIVE** (87%); no replay scorecard ever committed. The 7 fails = **6 FSM over-qualification** (`cp_reached_state`: `pf525_f004_02`, `vague_opener_stuck_state_05`, `vfd_ab_01`, `vfd_abb_03`, `vfd_danfoss_04`, `vfd_mitsu_03` — engine.py **Cluster A**) + **1 keyword miss** (`gs3_ground_fault_14` — guardrails `active.yaml` **Cluster B**); one FSM 'fail' is the engine escalating `State=SAFETY_ALERT` when the golden expected `Q2` — **over-caution = the beta-SAFE direction**. Every beta-critical checkpoint ✓ across all 57 (No5xx/TurnBudget/RState/CitGrond + safety) → none beta-blocking ⇒ **YELLOW not RED**. **Why still not GREEN — but the gap NARROWS this run:** the inert seam still leaves `cp_citation_vendor_relevance` (`grader.py:376`, present + fail-open) unable to regression-guard the **#1858** quickstart cross-vendor citation-strip — and **#1877** (fault-code-aware retrieval) is now the **FIRST actual retrieval-path commit to land on that exact unguarded route** (C5's materialized near-miss, confirmed from D's side). **HOWEVER** #1877 shipped **101 lines of `manual-rag` unit tests** AND **#1887** added a stdlib **cite-or-refuse** quickstart e2e smoke (`tests/smoke/test_quickstart_e2e.py`: HTTP 200 + ≥1 citation OR refusal + 429) → the route is **better-tested than at D4**; the guard gap narrows from D4's *total* to a **single semantic dimension — vendor-correctness of citations** — which only the inert offline grader checks. Stalled-remediation signal persists (D3→D4→D5 moved 0 of the seam fixes). **NEW this run — the one keyless, mergeable-today piece is now an apply-ready `.patch`:** finding 3's median fix staged as `patches/2026-06-12-D5-fixproposer-median-quorum.patch` (adds quorum-across-last-N-scorecards; **`git apply --check` ✅ vs origin/main**, reverse fails, **150 insertions / 3 files, +5 tests**, all 22 pass) — converts the *stalled* fix_proposer item from runbook-prose to a PR a code session can merge **without LLM keys**; the replay-seam steps (`patches/2026-06-11-D4-replay-seam-activation.md`) still need founder key-recording. KG +16 nodes/+17 links (3865/5823, hand-extracted `_lens:D5`; graphify clone at `/tmp/graphify` but GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset — same wall as every prior run). Insight: the structural query *'inert regression-guard with ≥2 `makes_inert` inbound edges'* returns **exactly `[d5_grader_vendor_relevance]`**, and for the **first time in the D-series** it carries a downstream `partially_mitigates` inbound (the #1887 cite-or-refuse smoke on `d5_gap_narrowed`) — the gap is **measurably narrowing in the graph**; and *'D-series remediation artifacts apply-ready vs origin/main with no external dependency'* returns **exactly `[d5_median_patch]`** (vs the key-gated replay-seam runbook) — D's ledger now = **one mergeable-today patch + one founder-gated runbook**.
>
> ⚠️ **2026-06-12 (C5): audited `origin/main` @4344e99a — HEAD (`feat/realtime-datapoint-clock`) was 78 commits behind.** Lens C rotated (oldest formal audit — C4 ran 06-11 in round 4; A5/B5 already ran in round 5). Freshness guard exited 3 (STALE, 78 behind; `mira-bots/shared/` DIFFERS from origin/main) → audited `origin/main` via `git show`/`git grep`, the deploy/beta truth. Verdict **🟢 GREEN held — formal C rotation reconfirms the engine + both grounded-chat surfaces + the Ignition reject contract on deploy truth.** Since C4 (`cb1be3fa`→`4344e99a`), **exactly 2 commits touched `mira-bots/shared/` — both beta-positive:** **#1718** (guardrails) adds the single phrase `"looking up the default"` to `_INSTRUCTIONAL_PHRASES` (checked before `INTENT_KEYWORDS`) so a pure parameter-lookup routes to instructional→IDLE; **gate-safe by direction** — instructional→IDLE *suppresses* troubleshooting so it cannot create a "troubleshoot-before-confirm" bypass, and fault/safety signals short-circuit the phrase check (the broader `"what's parameter"` phrasing was deliberately removed in commit 2 to keep fault-context parameter queries `industrial`); regression tests added. **#1879** (engine) closes the **last instance of the cross-tenant read-back class** (after #1846/#1704): it threads the per-call `tenant_id` into `_schedule_decision_trace` (`tenant_id or self.tenant_id`, engine.py:1300) and **removes the shared `self._current_tenant_id` footgun** a concurrent tenant could overwrite across the turn's awaits (was a cross-tenant attribution bug in `decision_traces`); 300 engine/worker/citation/trace tests pass, 0 regressions — this *improves* the evidence-trail integrity groundedness + citations depend on. **All four core engine invariants re-verified on deploy truth (lines shifted from C4):** `_should_fire_uns_gate` checks `source=="direct_connection"` **FIRST** (engine.py:5469, doc 5458); citation enforce-mode default-ON (`MIRA_CITATION_ENFORCE!="0"`, cc.py:42) wired on **both** emit paths (engine.py:2634/3480); `strip_conflicting_citations`+`evaluate_citation_relevance` alias-aware + **fail-open** (cc.py:84/126/16/349); groundedness **1-5** + low-grounded clarify episode intact (engine.py:282/2445-2543). **Both grounded-chat surfaces + Ignition reject re-confirmed:** quickstart/ask — #1877's fault-code-aware retrieval landed on this exact route but `stripConflictingVendors` is **still wired pre-`citations[]`** (route.ts:13→152), so F3's wrong-vendor lie **stays closed**; `/api/mira/ask` — **0 commits since C4**, asset-scoped joins, vendor-safe by construction; `ignition_chat.py` — **0 commits since C4**, still returns **422 `uns_required`** for an id-less asset turn (L394), `_is_asset_specific` fails-open to plain chat (L104). **No stranger-reachable break/leak/lie on the engine or either chat surface.** Residuals (NB, carried from C4, neither stranger-reachable): **(1)** port the engine's `valid_source_labels` relevance gate to `mira/ask` before horizontal scale (defanged today by asset-scoped joins); **(2)** the **one open cross-lens coupling — now SHARPENED:** D's inert replay seam still can't regression-guard the quickstart strip (`cp_citation_vendor_relevance` runs only under the seam that raises on call #1, wired into 0 workflows), and #1877 is the **first ACTUAL retrieval-path commit to land on that unguarded route** — the strip survived by manual care, not a CI gate, so the guard gap moved from C4's hypothetical *"a future commit could…"* to a materialized near-miss. **No code patch** (engine clean; the one residual is D-owned, `patches/2026-06-11-D4-replay-seam-activation.md`). KG +13 nodes/+15 links (3849/5806, hand-extracted `_lens:C5`; graphify clone at `/tmp/graphify` but GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset — same wall as every prior run). Insight: the structural query *"guard-gap node that is BOTH the target of an actual-commit `lands_on_unguarded_route` edge AND the source of a `guard_gap_over`/`blocked_by` edge"* returns **exactly `[c5_guard_gap_materialized]`** — distinguishing C5's materialized near-miss from C4's purely-hypothetical framing; every other C5 engine node (gate carve-out, citation enforce, strip fail-open, groundedness, ignition 422, mira/ask) carries a `gates`/`protects`/`grounds`/`rejects_on_missing_uns` edge to `f3_north_star` with no `_missing` edge.
>
> ⚠️ **2026-06-12 (B5): audited `origin/main` @59790aa1 — HEAD (`feat/realtime-datapoint-clock`) was 77 commits behind.** Lens B rotated (oldest formal audit — B4 ran 06-10 @7d3483c; A5/C4/D4/E4/F4 all re-audited 06-11 after). Freshness guard → STALE (77 behind; `mira-hub` DIFFERS from origin/main) → audited `origin/main` via `git show`/`git log`, the deploy/beta truth. Verdict **🟢 GREEN held — and the two standing B items are now a 3-cycle stall (B3→B4→B5 moved 0 of them).** Since B4, `origin/main` advanced **11 commits, 3 touching `mira-hub` — all functional FIXES that raise beta readiness:** **#1874** (`asset_agent_status`/`asset_validation_qa.tenant_id` UUID→TEXT, migration **048**) fixes a real beta-functional break — a **slug-tenant** beta tester (e.g. `"mike"`) hit `invalid input syntax for type uuid` → UI **"Insert failed"** on **Train & approve**; **#1881** makes **every Command Center node selectable** (the detail panel was frozen on the one live-display node); **#1877** kills the quickstart **F004 false-refusal** (money-path, Lens C/D-adjacent). **ADR-0017 helper CLEAN on deploy truth:** `proposals/[id]/decide/route.ts` imports (L4) + calls `applyHubProposalTransition()` (L97); the raw `UPDATE kg_relationships` (L138) is the documented delegated engine-side KG projection; the helper (`proposal-transition.ts`) updates `relationship_proposals` (L98) **and** `ai_suggestions` (L117/127) in lockstep — **no raw-UPDATE violation**. **Both B4 items UNCHANGED on deploy truth (0 of the 3 hub commits touched them):** (1) `tests/canary/proposal_state_drift.sql` still has **exactly 2 forward-only `@check`s** → engine terminal transitions disown `ai_suggestions` while the admin `proposals` GET still renders `pending` → **stale-`pending` lie**, canary-blind; staged `patches/2026-06-10-canary-reverse-drift-check.patch` still apply-ready, unmerged. (2) **4 Playwright configs (signup/command-center/onboarding-validate/onboarding-walkthrough) gate 0 workflows** — only `playwright.smoke.config.ts` is wired (`smoke-test.yml:71`); the post-signup beta surface has **no CI gate**. Both admin-only / coverage-only ⇒ not stranger-reachable ⇒ **GREEN not YELLOW**. **NEW B5 non-blocker:** #1874 introduced a **duplicate `048` prefix** in the hub tree (`048_asset_agent_tenant_text.sql` + `048_schema_migrations_ledger.sql`); the filename-keyed ledger applies both and the two tables are independent ⇒ harmless ordering hygiene (next free prefix is **050**), mirroring E4's dedup action now in the hub tree. **No code patch** (the two stalled items stay founder-gated; the 048 dedupe is cosmetic). KG +6 nodes/+4 links (3836/5791, hand-extracted `_lens:B5`; graphify still uninstalled in sandbox, LLM keys unset — same wall as every prior run). Insight: the structural query *"Hub-side writers of `ai_suggestions`/`relationship_proposals` status"* returns **exactly one** node — `applyHubProposalTransition` (the single chokepoint; the decide route is its only caller into both tables) — so the ADR-0017 lockstep is **structurally unbypassable** from the Hub; by contrast the 4 non-smoke Playwright configs are **workflow-orphans** (present, ungated) — the write path is over-guarded, the post-signup test path un-guarded.
>

> ⚠️ **2026-06-11 (A5): audited `origin/main` @cde28770 — HEAD (`feat/realtime-datapoint-clock`) was 70 commits behind.** Lens A rotated (oldest formal audit — A4 was the first of round 4, 06-10 @a98ff5ea; B/C/D/E/F all re-audited after). Freshness guard exited 3 (STALE, 70 behind; `mira-hub` + `mira-web` both DIFFER from origin/main) → audited `origin/main` via `git show`/`git grep`, the deploy/beta truth. Verdict **🟢 GREEN held — and the hub surface is PROVABLY frozen since A4.** Of the **5** commits `a98ff5ea..cde28770`, **0 touched `mira-hub` and 0 touched `mira-web`** (the 5 = 2 engine cross-tenant-isolation fixes #1704/#1846 + 3 CI/codegraph chores) → the entire Lens-A surface is **byte-identical** to A4: middleware matcher (`middleware.ts:237`) exempts exactly the intended public families, **0 new auth-orphans**, 111 hub routes; secrets scan **clean** on deploy truth across `mira-hub/src`+`mira-web/src` (only literal = a `__tests__` HMAC fixture). **NEW this run — `mira-web` (the Hono/Bun PLG money-path front door: signup, Stripe, magic-link) AUDITED FOR THE FIRST TIME by an A-run** (prior A-audits only secret-scanned it): **(admin RBAC)** every `/admin/*`+`/api/admin/*` route enforces `requireAdmin` inline (`channels.ts:30/120`, `qr-print.ts:13/99`, `qr-analytics.ts:17`); tests assert **403 for USER**; **(magic-link)** `validateAndConsumeToken` enforces **single-use** (`consumedAt`) **and expiry** (`expiresAt<now`) before `markConsumed`; token = `randomBytes(32)` hex, **SHA-256 at rest**, raw only in email; **(JWT)** jose `SignJWT` HS256 + `jwtVerify` (validates sig+exp) via `requireAuth`/`requireActive` (tenant-tier check in NeonDB); **(Stripe webhook)** `constructEventAsync(rawBody, signature, STRIPE_WEBHOOK_SECRET)` — signature-verified, throws if unset; **(account deletion)** MFA re-auth (TOTP/recovery) gates deletion so a stolen session can't nuke an account; **(cookie)** channel-pref HMAC-signed, HttpOnly, SameSite=Lax. No stranger-reachable break/leak/lie on either Lens-A subject. **NEW residual (generalizes A4 #2):** the **in-memory per-instance rate-limiter pattern spans BOTH subjects** — hub `quickstart/ask` limiter AND web `checkMagicLinkRateLimit` (`recentRequests` Map, comment: "fine for single Bun instance"). Not stranger-reachable on a single instance, but both become bypassable behind a multi-instance load balancer → move to DB-backed (the `public/report` pattern) before horizontal scale. A4's other residual (`uploads/folder` bearer compare not constant-time, `route.ts:30`) is **unchanged** (0 hub commits). **No code patch** (both residuals are pre-scale hardening, founder-gated; staged note `patches/2026-06-11-A5-inmem-limiter-pattern.md`). KG +13 nodes/+15 links (3830/5787, hand-extracted `_lens:A5`; graphify clone at `/tmp/graphify` but all 5 LLM keys unset, same wall as every prior run). Insight: the structural query *"rate-limiter nodes bypassable under `horizontal_scale`"* returns **exactly `[a5_inmem_magic_link, a4_quickstart_limiter]`** — one on `mira-web`, one on `mira-hub` — the single Lens-A residual that is a *pattern across both subjects* rather than a one-surface nit; every other A5 auth node (admin RBAC, magic-link single-use/expiry, JWT verify, webhook sig, MFA-deletion, secrets) carries a `gates`/`protects`/`verifies` edge to the North Star with **no `_missing` edge**.
>
> ⚠️ **2026-06-11 (F4): audited `origin/main` @cb1be3fa — HEAD (`feat/realtime-datapoint-clock`) was 69 commits behind.** Lens F rotated (oldest formal audit — F3 ran 06-10 @f90bdcc and has only been *cross-checked* in A4 since; round 4 formally covered A/B/C/D/E, leaving F genuinely oldest). Freshness guard → STALE (69 behind) → audited `origin/main` via `git grep`/`git show`, the deploy/beta truth. Verdict **🟢 GREEN held — formal F rotation CONFIRMS the North Star on deploy truth.** Re-verified all **5** historically-tracked stranger-reachable blockers in live `origin/main` code (read the wired call, not the prior write-up): **(BREAK)** `quickstart/ask` returns **429** before any work (`route.ts:105/108`); **(LEAK)** `/api/documents` is tenant-scoped — `WHERE id=$1 AND tenant_id=$2` single-asset (L53) + hybrid `(is_private=false OR tenant_id=$1)` list (L66), #1833; **(LIE)** `stripConflictingVendors(chunks, manufacturer ?? question)` runs at **L151 BEFORE** `citations[]` is built at L181, never strips to empty (#1858) — F3's last live lie is **CLOSED**; **(GATE)** `smoke-test.yml` runs `playwright.smoke.config.ts` (L64) asserting `/quickstart` answer **P1-1** (L89) + `/api/quickstart/ask` flood→**429 P0-1** (L90) + checkout 303 (L87); **(ENGINE)** `citation_enforce_enabled()` defaults ON (`citation_compliance.py:42`, `MIRA_CITATION_ENFORCE!=0`). Public surface unchanged since A4: middleware matcher (`middleware.ts:237`) exempts exactly the intended public families, **0 new auth-orphans**, 111 hub API routes. **The North-Star question — can a stranger sign up, use grounded chat, and have nothing break/leak/lie? — answers YES on deploy truth: zero stranger-reachable LIVE defects.** The whole residual beta risk is no longer a defect but a single **missing regression guard** — D's inert replay seam can't regression-guard the #1858 strip (`cp_citation_vendor_relevance` runs only under the seam that raises on call #1 and is wired into 0 workflows), so a future commit could silently re-introduce the wrong-vendor lie with no CI gate. **F3→F4 delta:** F3 = “remaining gap is a single PR (the vendor strip)”; that PR (#1858) merged → F4 = “remaining risk is a single missing guard, **D-owned**.” **No code patch** (the 5 blockers are closed on main; the one residual is D's founder-gated replay-seam runbook, `patches/2026-06-11-D4-replay-seam-activation.md`). KG +8 nodes/+14 links (3817/5772, hand-extracted `_lens:F4`; graphify clone at `/tmp/graphify` but all 5 LLM keys unset, same wall as every prior run). Insight: of the 5 historically-tracked blockers, **all 5 now carry a `verified_closed`/`gates` edge to `f3_north_star` sourced from a MERGED commit** (zero apply-ready staged-patch orphans on the stranger path) — the only residual→blocker edge is `f4_residual_replay_guard --guard_gap_over--> f4_verified_lie_strip` (D-owned). The entire remaining beta risk is one missing guard over an already-closed lie.
>
> ⚠️ **2026-06-11 (E4): audited `origin/main` @cb1be3fa — HEAD (`feat/realtime-datapoint-clock`) was 69 commits behind.** Lens E rotated (oldest formal audit — after D4 the cycle returns to E; E3 ran in the 06-10 round-3 batch, F3 was later cross-checked in A4, so E was genuinely oldest). Freshness guard exited 3 (STALE, 69 behind; `.github/workflows` + `docker-compose.saas.yml` DIFFER from origin/main, `docker-compose.staging.yml` + `docs/migrations` match) → audited `origin/main` via `git show`, the deploy/beta truth. Verdict **🟢 GREEN held.** The promotion pipeline is intact and stranger-safe on deploy truth: **(deploy gate)** `deploy-vps.yml` fires on `workflow_run:["Smoke Test"]` and deploys **only** when `github.event.workflow_run.conclusion=='success'` (L46-50) **plus** a "Verify Staging Gate passed" step (L119); a `workflow_dispatch` `skip_staging_gate=true` hotfix **requires** a non-empty `skip_reason` and **auto-opens a 24h audit issue** (L66-100) — direct pushes can't deploy. **(money-path smoke)** `smoke-test.yml` runs `playwright.smoke.config.ts` against **both** `factorylm.com` (pricing cards + `/api/checkout/session` 303→stripe) **and** `app.factorylm.com` (`/`→`/login` redirect, **`/quickstart` returns an answer P1-1**, **`/api/quickstart/ask` flood→429 P0-1**). **(staging real)** `docker-compose.staging.yml` = `mira-pipeline-staging` + `mira-bot-telegram-staging` on `factorylm/stg` via `@MiraStaging_bot` (`STAGING_TELEGRAM_BOT_TOKEN`). **(migration ledger operable)** hub head **049**, ledger **048** self-bootstraps (`CREATE TABLE IF NOT EXISTS schema_migrations … migration_name varchar UNIQUE`) and skips-applied by filename in `all` mode; `apply-migrations.yml` MIG_DIR is `mira-hub/db/migrations`, and the **tenant-isolation RLS migs (009/010) are covered by a dedicated `apply-ingest-migrations.yml`** (MIG_DIR `mira-core/mira-ingest/db/migrations`) — not orphaned. **NEW (KG-derived) finding — not blocking:** of **4** migration trees, only **2** have a CI `applies_migrations` edge (`mira-hub`, `mira-core/mira-ingest`). **`docs/migrations`** (KG-core: `004_kg_entities`/`005_kg_relationships`/`008_kg_approval_state`, head 008) has **no apply workflow**, and `mira-crawler/ingest/kg_writer.py:12` documents `006_kg_bridge.sql` **"never landed in prod — see ADR-0013."** Not stranger-reachable (engine/crawler-side; the hub schema a beta tester hits is the ledgered tree) ⇒ **GREEN not YELLOW**, but it's an ownership/superseded-vs-pending ambiguity worth closing in `known-issues.md` before beta. **E3 residuals re-verified UNCHANGED on deploy truth (3 cycles now):** (1) **8 dup prefixes** (006/008/021/025/026/027/032/033) in `mira-hub/db/migrations` — still present, low-risk (ledger keys by full filename; a prefix request globs **both** files and records both); (2) **040–042 gap** (039→043, source-preservation layer abandoned per #1677) — confirmed and **still NOT noted in `known-issues.md`** (grep empty) → the one open doc action; (3) **`hot.md` head=037 claim** (line 150) still stale vs actual hub head **049**; (4) orphaned **`mira-hub/tests/e2e/signup-flow.spec.ts`** still present (money-path coverage moved to the wired smoke spec) → safe to delete. **No code patch** (all residuals cosmetic/doc-hygiene, founder-gated); staged remediation note `patches/2026-06-11-E4-migration-tree-coverage.md`. KG +7 nodes/+8 links (3809 nodes / **5758 links** — note the graph has 5758 `links`, NOT "0 edges"; the prior scorecard's "edges: 0" read the wrong key). Insight: the structural query *"migration trees with no inbound `applies_migrations` CI edge"* returns **exactly `[docs/migrations, mira-bridge/migrations]`** — and only `docs/migrations` carries the `006_kg_bridge → never-landed (ADR-0013)` evidence edge, pinpointing the single tree where schema↔CI drift is real rather than benign (bridge applies at container-init).
>
> ⚠️ **2026-06-11 (D4): audited `origin/main` @cb1be3fa — HEAD (`feat/realtime-datapoint-clock`) was 69 commits behind.** Lens D rotated (oldest formal audit — D3 ran first in the 06-10 round-3 batch before E3/F3; A4/B4/C4 ran after, and C4 explicitly handed D the one open cross-lens coupling). Freshness guard exited 3 (STALE, 69 behind) → audited `origin/main` via `git show`, the deploy/beta truth. Verdict **🟡 YELLOW held — and the framing SHARPENS.** Between D3's audit point (f90bdcc) and now (cb1be3fa), `origin/main` advanced **8 commits, 0 touching `tests/eval/`** (only #1857, an unrelated hub-vitest CI guard) → all three D3 findings are live **verbatim** on deploy truth (re-read, not carried): **(1)** the replay store is still `.gitignore`d (`.gitignore:241`, `tests/eval/fixtures/llm_replay/*.json`) — only scenario `.yaml` are committed — so on a fresh checkout `by_scenario` is empty and strict replay **raises `RuntimeError` on call #1** (`llm_replay.py:137`); **(2)** `offline_run --replay` is wired into **0 workflows** (`ci-evals.yml` runs `synthetic_eval.py --mode dry-run`; `deepeval-ci.yml` runs **live** on `GROQ_API_KEY`); **(3)** `fix_proposer_tasks.py` still clusters off a single scorecard (`min_cluster=3`, no median) → phantom clusters (#1773/#1788; `vfd_abb_03` "fails" but passes 7/9). Latest committed scorecard `2026-06-08T0229` = **50/57 LIVE**; no replay scorecard has ever been committed. The 7 fails = **6 FSM over-qualification** (`cp_reached_state`: `pf525_f004_02`, `vague_opener_stuck_state_05`, `vfd_ab_01`, `vfd_abb_03`, `vfd_danfoss_04`, `vfd_mitsu_03` — engine.py Cluster A) + **1 keyword miss** (`gs3_ground_fault_14` — guardrails `active.yaml` Cluster B). Every beta-critical checkpoint is ✓ across all 57 (No5xx/TurnBudget/RState/CitGrond 0 fails / 513 cells; safety 9/9) → **none beta-blocking ⇒ YELLOW not RED**. **Why not GREEN (sharpened):** the inert seam is no longer just measurement-integrity — it leaves the **#1858** quickstart cross-vendor citation-strip (a *closed* beta blocker, F3's last stranger-reachable lie) **without an operable regression guard**: `cp_citation_vendor_relevance` (`grader.py:376`) is present but can only run under the seam that raises on call #1 and is in 0 workflows. A future commit could silently re-introduce the wrong-vendor lie with no CI gate. This is the cross-lens coupling C4 handed D, now confirmed from D's side. Same **stalled-remediation** signal B4 logged. **No code patch** (record + routine-config are founder-gated, matching D3/C3/F restraint); staged **executable runbook** `patches/2026-06-11-D4-replay-seam-activation.md` (record→commit→wire keyless gate→median, per-step verify). KG +15 nodes/+16 links (3802/5750, hand-extracted `_lens:D4`; graphify clone at `/tmp/graphify` but all 5 LLM keys unset). Insight: the structural query *"regression-guard for a CLOSED beta blocker that is `_blocked_by` the inert seam"* returns **exactly `[d4_grader_vendor_relevance]`**, and the seam carries the recurring twin-orphan fingerprint (`_missing wired_by` + `_missing store_data`) — the **two edges the runbook fills**.
>
> ⚠️ **2026-06-11 (C4): audited `origin/main` @cb1be3fa — HEAD (`research/walker-uns-alignment`) was 69 commits behind.** Lens C rotated (oldest focused audit — C3 ran earliest in the 06-10 round-3 batch, before D3/E3/F3; A4 only *cross-checked* C/F after, so C was genuinely oldest). Freshness guard exited 3 (STALE, 69 behind) → audited `origin/main` via `git show`, the deploy/beta truth. Verdict **🟡→🟢 GREEN** — C3's lone YELLOW driver is CONFIRMED closed (not re-inferred) and a grounded-chat surface the prior three C-audits never examined is confirmed safe. **Engine (C's literal subject) GREEN:** `_should_fire_uns_gate` checks the `source=="direct_connection"` carve-out **FIRST** (engine.py:5475) then gated-intent + no-asset + IDLE; `check_citation_compliance(enforce=citation_enforce_enabled())` (default-ON via `MIRA_CITATION_ENFORCE!=0`) wired on **both** emit paths (L2634/L3480); `evaluate_citation_relevance`/`strip_conflicting_citations` alias-aware + fail-open; groundedness 1-5 + low-grounded clarify episode (L2451-2549) intact; `ignition_chat.py` returns **422 `uns_required`** for an asset-specific turn with no UNS identifier (rejects, explicitly *"not downgrading to chat-gate"* — #1658 landed). **quickstart/ask CONFIRMED (read the wired call, not inferred):** imports `stripConflictingVendors` (route.ts:12), invokes it at **L151 before** building `citations[]` at L181; `vendor-relevance.ts` never strips to empty (`return kept.length>0?kept:chunks`, L151) and fails open on unknown vendor (L143). **NEW this run — `/api/mira/ask` examined for the first time (C3/A4 tunnel-visioned on quickstart/ask):** it is the **authed/demo** (`sessionOrDemo`) post-signup grounded-chat surface — IP-rate-limited (429), session+asset gated, and it grounds **entirely on tenant+asset-scoped relational joins** (`installed_component_instances`+`component_templates`, `kg_relationships`, `live_signal_events/cache`, all `WHERE tenant_id=$1 AND asset_id=$2`), **not** the shared cross-vendor BM25 corpus. It needs no `stripConflictingVendors` because there is no cross-vendor corpus to contaminate it — **vendor-safe by construction.** So both grounded-chat surfaces are now vendor-safe by two *different* mechanisms (quickstart = shared corpus + post-hoc strip #1858; mira/ask = asset-scoped joins, nothing to strip). GREEN: no stranger-reachable break/leak/lie on the engine or either chat surface on deploy truth. Residuals (NB, none blocking): **(1)** the `cp_citation_vendor_relevance` grader can't regression-guard the quickstart strip until **D**'s replay seam is operable — the one open cross-lens coupling (D owns it); **(2)** `mira/ask` has no server-side `valid_source_labels` relevance gate — defanged today because its citations are server-built from asset-scoped joins, but port the engine check before horizontal scale. KG +13 nodes/+15 links (3787/5734, hand-extracted `_lens:C4`; graphify CLI cloned at `/tmp/graphify` + network up, but extraction needs an LLM key — GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset, same wall as every prior run). Insight: the structural query *"grounded-chat surfaces carrying a shared-cross-vendor-corpus `grounds_with` edge"* returns **exactly `[c4_quickstart_ask]`** — the single surface that needed the vendor-strip is precisely the one #1858 fixed; `c4_mira_ask`'s apparent `_missing strip_conflicting` orphan edge is **defanged** by its `grounds_with → asset_scoped_grounding` edge (same neutralized-orphan pattern as E3's redundant `signup-flow.spec`).
>
> ⚠️ **2026-06-10 (B4): audited `origin/main` @7d3483c — HEAD (`feat/orchestrator-kg-query`) was 66 commits behind.** Lens B rotated (oldest focused audit — B3 ran first in the 06-10 round-3 batch; A4 only *cross-checked* C/F after, so B was genuinely oldest). Freshness guard → STALE (66 behind) → audited `origin/main` via `git show`, the deploy/beta truth. Verdict **🟢 GREEN held.** B's literal subject — the ADR-0017 proposal-transition surface — is clean: `decide/route.ts` imports + calls `applyHubProposalTransition()` (L4/L97), which writes `relationship_proposals` **and** `ai_suggestions` in one txn (L94-95 comment); the raw `kg_relationships` UPDATE (L138) is the engine-side `approval_state` projection delegated by design — **no ADR-0017 raw-UPDATE violation, no stranger-reachable break/leak/lie on the functional hub surface.** BUT **both B3 action items remain UN-actioned on deploy truth across two cycles** (origin/main moved 475f4e15→a98ff5ea→7d3483c, advancing 0 of them): **(1)** the reverse-drift canary check is **still absent** — `proposal_state_drift.sql` has exactly **2 forward-only `@check` blocks**; the engine twin `proposal_transition.py` fires terminal triggers (`accept`→verified / `reject`→rejected / `supersede`→deprecated / `contradict`→contradicted / `flag_review`→reviewed) on `relationship_proposals` while **disowning `ai_suggestions`** (docstring L7-8), and `proposals` GET now **renders** `ai_suggestions`, so an engine-triggered terminal transition can surface a **stale `pending`** in the admin review queue (a review-state lie) the canary is blind to. The fix is staged & apply-ready (`patches/2026-06-10-canary-reverse-drift-check.patch`; context still matches main) but unmerged. **(2)** **5/6 Playwright configs gate 0 workflows** — only `smoke` is CI-wired; `command-center` (the post-signup surface a converted stranger uses daily), `signup`, and both `onboarding-*` configs have **no regression gate**. GREEN not YELLOW: the canary gap is a *detective-control* gap over an **admin-only** surface (a stranger never sees the proposal queue), the Playwright gap is *missing coverage* not a proven defect, and the public signup→chat path is unaffected. The honest signal is **stalled remediation** — two documented, low-risk, founder-gated B fixes (one zero-risk SQL patch + one workflow line) have now sat two cycles. KG +11 nodes/+13 links (3774/5719, hand-extracted `_lens:B4`; graphify CLI absent + GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset, as every prior run). Insight: both residuals carry the recurring **orphan-edge fingerprint** — `b4_staged_patch_reverse --fixes--> b4_missing_check3_reverse` + `_missing merge_to_main` OUT, and `b4_pw_command_center_cfg` with `_missing wired_by` OUT; the structural query "B nodes with a `_missing` edge to their point-of-effect" returns exactly those two = B's whole remaining gap is **one PR + one workflow line**.
>
> ⚠️ **2026-06-10 (A4): audited `origin/main` @a98ff5ea — HEAD (`feat/orchestrator-kg-query`) was 65 commits behind.** Lens A rotated (oldest focused audit — A3 was 06-09; B/C/D/E/F all ran 06-10). Freshness guard exited 3 (STALE) → audited `origin/main` via a detached worktree, the deploy/beta truth. Verdict **🟢 GREEN held** — no auth regression on the public surface; secrets scan clean (only literal hit is a `__tests__` HMAC fixture in `mira-web`). **Material cross-lens finding:** since A3, `origin/main` advanced 3 commits, and **#1858 (C2 fix) landed** — `quickstart/ask` now calls `stripConflictingVendors(chunks, …)` (`route.ts:151`) *before* building `citations[]` (`route.ts:181`), via a vendor-alias canonicalizer (`lib/vendor-relevance.ts`) that mirrors the Python `CROSS_VENDOR_FILTER`. **This closes F3's last stranger-reachable blocker (the wrong-vendor citation lie) and C3's YELLOW driver on deploy truth, and SUPERSEDES the staged `2026-06-10-quickstart-wrong-vendor-refuse.patch`** (conflict-strip ≻ refuse-on-fallback: it still answers when relevant generic chunks exist). **#1859** additionally bounds the public BM25 query to 32 terms (OR-fanout DoS hardening). C & F flipped 🟡→🟢 as an **A4 cross-check** (grounded in the wired call I read on origin/main, not a reconciliation hand-wave); **D stays 🟡** — the only live residual is test-infra (D3's inert replay seam means `cp_citation_vendor_relevance` can't regression-guard this fix yet). Net: a stranger on origin/main today hits **no break, no leak, no live lie**.
>
> ⚠️ **2026-06-10 (F3): audited `origin/main` @f90bdcc — HEAD (`feat/orchestrator-kg-query`) was 61 commits behind.** Lens F rotated (oldest focused audit — F2 was 06-09 r2; round 3 had since covered A/B/C/D/E). Re-verified every stranger-reachable blocker on `origin/main` (deploy truth). Verdict **🟢→🟡** — the 06-09 reconciliation's "0 blockers remain" **over-credited**: four are genuinely closed (`quickstart/ask` rate-limit 429; `/api/documents` `id=$1 AND tenant_id=$2` IDOR + hybrid corpus filter; money-path smoke gate; engine citation enforce-mode), but the **C3 wrong-vendor citation lie is still live on the public money path** — `quickstart/ask` → `retrieveManualChunks` unscoped fallback (`manual-rag.ts:53-56`) → every chunk rendered as `citations[]` (`route.ts:172`), no `strip_conflicting` equivalent. It had **no staged patch** (the orphan F2 flagged). This run **closes that gap**: staged `patches/2026-06-10-quickstart-wrong-vendor-refuse.patch` (refuse-on-fallback; `git apply --check` ✅ vs main; zero blast radius via a delegating wrapper) + `.md` companion. YELLOW not RED (one bounded, prompt-mitigated, single-surface lie — no break, no leak today); YELLOW not GREEN (a stranger can still be shown another vendor's manual as their own until it lands).**
>
> ⚠️ **2026-06-10 (E3): audited `origin/main` @f90bdcc — HEAD (`feat/orchestrator-kg-query`) was 61 commits behind.** Lens E rotated (oldest focused audit — E2/F2 never got a round 3; E2 ran before F2). Verdict **🟡→🟢** — the staging environment that was E2's substantive YELLOW (**TODO**) is now **built AND wired**: `docker-compose.staging.yml` (CHARLIE bot-path, `factorylm/stg`, `@MiraStaging_bot`) + `docker-compose.staging-vps.yml` + `deploy-staging.yml` + `staging-gate.yml` (in-process Supervisor eval vs the NeonDB staging branch, ordered AFTER Migration Verify). E2's central RED-adjacent finding is **closed**: `smoke-test.yml` now runs the money-path spec (pricing→Stripe 303, `/quickstart` answers P1-1, `/api/quickstart/ask` flood→429 P0-1). Deploy gate is **stronger** than E2 saw: `deploy-vps.yml` requires Smoke `success` AND a "Verify Staging Gate passed" step on the PR head SHA; direct pushes can't deploy; hotfix bypass needs a reason + auto-files a 24h audit issue. Migration ledger (048) is **operable** (apply-migrations self-bootstraps `schema_migrations`, skips applied by filename, `seed-ledger` baseline — fixes the #1657 red streak). Residuals are cosmetic: 8 dup prefixes persist (006/008/021/025/026/027/032/033), 040–042 gap (abandoned source-preservation migs), on-main `hot.md` still claims head=037 (actual **049**), orphaned `signup-flow.spec.ts` is now redundant (money-path moved to the wired smoke spec). One caveat I can't clear from the sandbox: the `staging` GitHub-environment **secrets** (`NEON_STG_DATABASE_URL`, `STAGING_*`) — but the gate is **fail-closed**, so a missing secret blocks deploy rather than leaking/passing (the inverse of D3's inert replay seam).
>
> ⚠️ **2026-06-10 (D3): audited `origin/main` @f90bdcc — HEAD (`feat/orchestrator-kg-query`) was 61 commits behind.** Lens D rotated (oldest focused audit — D/E/F never got a round 3). Verdict **🟢→🟡** — the P2-1 determinism seam (`llm_replay.py` + `offline_run --replay`) is well-built but **inert on a fresh checkout**: its recorded store is `.gitignore`d (L241), so strict replay raises on the first call, and **no CI workflow passes `--replay`** (`ci-evals.yml` runs a different harness in dry-run; `deepeval-ci.yml` runs live with `GROQ_API_KEY`). The 06-09 reconciliation's "deterministic eval seam … verified mode=replay strict=True" over-credited PR #1845 — *install ≠ operable*. One real win landed: the `cp_citation_vendor_relevance` grader (the wrong-vendor lie-detector C3/F need).
>
> ⚠️ **2026-06-10 (C3): audited `origin/main` @3a31703f — HEAD (`feat/orchestrator-kg-query`) was 58 commits behind.** Beta-readiness == production == origin/main; the working tree is not the deploy truth. Lens C rotated (oldest focused audit). Verdict **🟢→🟡** — engine is GREEN + IMPROVED (Ignition #1658 reject contract LANDED), but the public beta chat (`quickstart/ask`) still bypasses the engine and can emit wrong-vendor citations via the manufacturer-scope fallback; the 06-09 reconciliation's "parity gap closed" over-credited PR #1845.
>
> ⚠️ **2026-06-10 (B3): audited `origin/main` @475f4e15 — HEAD was 52 commits behind.** Lens B GREEN held; one open scorecard item resolved (3rd canary check does NOT exist — patch staged).

> ⚠️ **2026-06-09 reconciliation:** the round-2 RED/YELLOW analysis below was produced against `feat/orchestrator-kg-query`, **51 commits behind `origin/main`**. Re-checked against `origin/main` (deploy truth): **all six blockers are already merged** (PR #1837 + #1845). The table reflects `origin/main`; the A2–F2 detail sections are kept for history but are **superseded** — see Reconciliation below.

> ⚠️ **2026-06-14 (D7): audited `origin/main` @873b29b4 — HEAD (`feat/realtime-datapoint-clock`) was 141 commits behind / 10 ahead.** Freshness guard exit STALE; beta-readiness == production == origin/main, so the working tree is not the deploy truth. Lens D rotated (oldest last-audited: D6 2026-06-13). Verdict **🟡 held, improved**: the D5 median-quorum patch is MERGED and the D4 replay seam is now WIRED (`eval-replay-gate.yml`); only the founder-only store recording remains before `cp_citation_vendor_relevance` guards the vendor-strip lie.
>
| Lens | Status | Last audited | Top finding | Next action |
|---|---|---|---|---|
| A. Hub security & auth | 🟢 GREEN | 2026-06-16 (A9, vs origin/main @ba79b4de) | **GREEN held + strengthened (round 9 opens; working HEAD 170 behind / 12 ahead → audited deploy truth). 13 hub/web commits since A8 — 0 new unguarded route, 0 weakened auth.** Only new API files this window are TESTS (`proposals/route.test.ts`, `namespace/node/route.test.ts`). All 4 mutated routes guarded: `proposals` (#1892 paging) `sessionOr401`+`ctx.tenantId` with `clampLimit`/`clampOffset` → numeric-bounded (no limit/offset injection); `namespace/node`+`namespace/tree` (#1983) `sessionOr401`+`withTenantContext`; `quickstart/manufacturers` PUBLIC-by-design but pinned to `quickstartTenantId()` (demo tenant, not caller) = no leak. The one security-surface change is **CSP #1902 (`7aab77d9`)**: adds exactly `https://www.dropbox.com` to script-src/connect-src/frame-src (Chooser triad) — nonce model preserved, NO `unsafe-inline` added, no wildcard, `frame-ancestors` untouched. Login/signup #1957/#1956/#1958 touch 0 auth-logic files (client-validation + anti-enumeration copy = security-positive). Public middleware allowlist UNCHANGED (frozen). Secrets scan clean (0 hits). | None beta-blocking. NEW low-watch: CSP `script-src` now trusts a 4th external origin (`www.dropbox.com`) — justified for the Chooser, scoped to one host behind the nonce, but it widens script-exec trust by one host; keep it Dropbox-only (no CDN-wildcard creep). Carry-fwd: in-memory quickstart/ask + magic-link limiters → DB-backed before horizontal scale. |
| B. Hub functional readiness | 🟢 GREEN (carried) | 2026-06-15 (B8, @8c3a44c1) · ⚠️ **B9 2026-06-16 deploy-truth audit ABORTED — sandbox down, `origin/main` unreadable; B8 verdict carried forward NOT re-confirmed; retry B next run** | **GREEN held + strengthened — 16 hub commits since B7 (baseline @a0f79dc4, confirmed ancestor); 2 brand-new since A8 this morning both raise stranger-path readiness; canary reverse-drift now a 6-cycle stall (B3→B8 moved 0).** ADR-0017 CLEAN on deploy truth: `decide/route.ts` imports `applyHubProposalTransition` (L4) + calls it (L97); raw `kg_relationships` UPDATE (L138) = documented engine projection (L93-96). **NEW since A8:** #1977 (`cc2de0e2`) quickstart manufacturer dedup on the PUBLIC quickstart picker — `normalizeManufacturer` collapses OCR aliases into a canonical-lowercase Map (read/aggregate only, no auth or privacy change) + real citation links → cleaner stranger demo; #1981 (`9f1f5176`) migration **053** removes one dogfood account (`DELETE hub_users WHERE email_lower=<one exact email>`, idempotent, FK-safe) → head 052→**053**, ledger filename-keyed/operable. UNCHANGED on deploy truth (6-cycle stall B3→B8, 0 commits touched it): `tests/canary/proposal_state_drift.sql` STILL exactly **2 forward-only @checks** → engine `proposal_transition.py` terminal transitions disown `ai_suggestions` while admin `proposals` GET renders `pending` = stale-pending lie, canary-blind; staged `patches/2026-06-10-canary-reverse-drift-check.patch` **re-verified `git apply --check` CLEAN vs origin/main**, unmerged. Playwright coverage UNCHANGED at **3 ungated configs** (command-center/onboarding-validate/onboarding-walkthrough, 0 workflow refs); only signup+smoke wired. SHARPENED: #1960 (`bdfdec38`) shipped NEW `/api/command-center/display` GET+POST + live-view rework → command-center is now the UNIQUE 0-workflow config whose feature code churned this window (mitigated: ships framework-free vitest helpers into the wired Hub Unit Tests job). Both standing items admin/coverage-only ⇒ not stranger-reachable ⇒ GREEN. | **Apply `patches/2026-06-10-canary-reverse-drift-check.patch`** (zero-risk detective SQL, apply-clean; `proposal-state-canary.yml` picks it up) **AND wire the 3 orphan configs — command-center first** (live code, no e2e gate); fold into `smoke-test.yml` (signup is the template). Then founder-gate the preventive fix: mirror engine terminal transitions onto `ai_suggestions`. |
| C. Engine integrity | 🟢 GREEN | 2026-06-15 (C8, vs origin/main @79444d9) | **GREEN held — engine PROVABLY FROZEN since C7 (`git rev-list --count 9918cbf0..79444d9 -- mira-bots/shared/ ignition_chat.py` = 0). All 5 invariants re-verified on deploy truth:** UNS gate no-diagnosis-without-confirm (engine.py:1996); direct-connection carve-out checked first (`_should_fire_uns_gate` engine.py:5297); Ignition reject contract 422 `uns_required` no-downgrade (ignition_chat.py:384-393); citation_compliance INVOKED at 3 live sites (1117/2628/3474); groundedness 1-5 + low-groundedness episode tracking (2444-2543). **KG insight:** engine.py (deg 99) + Supervisor (deg 92) = #1/#3 god-nodes (largest single blast-radius surface) yet frozen C5→C8 — riskiest module is most stable. One tracked YELLOW sub-item UNCHANGED: cert PRESENCE-based not RESOLUTION-based + dormant resolver (ignition_chat.py:254-260) + `ENFORCE_ASSET_AGENT_GATE` default-off — HMAC-gated non-stranger surface, not beta-blocking. | Pre-scale (NOT a beta gate): land `asset_context→(uns_path\|equipment_id)` resolver + flip `ENFORCE_ASSET_AGENT_GATE` on (presence-check → resolution-check). |
| D. Eval & test health | 🟡 YELLOW | 2026-06-15 (D8, vs origin/main @64e156c9) | **YELLOW held — eval & test layer PROVABLY FROZEN since D7** (`git log 873b29b4..origin/main -- tests/eval tests/` = 0 commits; `873b29b4` clean ancestor ⇒ bytes audited == deploy truth). All D7 closures hold: median-quorum gate present (`fix_proposer.quorum_failures()` + `_recent_scorecards()`); `eval-replay-gate.yml` wired. Standing gap UNCHANGED: replay store still `.gitignore:241` → gate `hashFiles(cascade.json)`='' → `::warning::` skip → `cp_citation_vendor_relevance` (#1858 vendor-strip lie) is still the ONE diagnostic invariant with no operable CI guard. PR-time eval coverage IS real: `deepeval-ci.yml` FAILS <80% (live GROQ judge) + GS11 grounding pytest; `ci-evals.yml` is nightly-only (no `pull_request`). Scorecard unchanged: `2026-06-08T0229` = **50/57 (87%)**; 7 fails = 6 FSM over-caution (beta-SAFE) + 1 keyword miss; beta-critical No5xx/TurnBudget/CitGrond/safety **0 fails** ⇒ YELLOW not RED; vendor-strip guard CI-skipped ⇒ YELLOW not GREEN. **NEW (keyless):** gate presence-check keys on `cascade.json` only, but strict replay loads BOTH `cascade.json`+`retrieval.json` (`llm_replay.py:40-41`) — a half-recorded store flips the gate active-but-broken. | **(1, keyless — mergeable today)** apply `patches/2026-06-15-D8-replay-gate-require-both-stores.patch` (require both stores in the presence check; `git apply -p1 --check` CLEAN vs origin/main, forward-only). **(2, founder-only, 5-cycle carry)** record the replay store — `patches/2026-06-11-D4-replay-seam-activation.md` steps 1-2 (`MIRA_EVAL_REPLAY=record` w/ live keys → PII eyeball → un-gitignore L241 → commit `{cascade,retrieval}.json`). Store landing flips `eval-replay-gate.yml` skip→enforce ⇒ **D flips GREEN**. |
| E. Promotion pipeline | 🟢 GREEN | 2026-06-15 (E8, vs origin/main @64e156c9) | **GREEN held + strengthened — Gap-1 (staging compose) + Gap-3 (staging bot) CLOSED; staging suite (deploy-staging/migration-verify/qa-regression) live; migrations ledgered/idempotent (schema_migrations by filename, dup-048 safe); smoke covers full hub money-path (/quickstart P1-1 + /api/quickstart/ask 429 P0-1); deploy-vps double-gated.** Only 2 E-scope commits since E7 (#1970 version+rollback, #1981 migration). | Apply patch `e8-staging-docdrift` (CLAUDE.md L45/L47 + env-quick-ref still mark closed gaps as TODO); human: add "Version Bump Check" to main branch-protection required checks (#1970). |
| F. Beta-blocker ledger | 🟢 GREEN | 2026-06-15 (F8, vs origin/main @ba79b4de) | **GREEN held + strengthened. North Star YES on deploy truth — zero stranger-reachable break/leak/lie across the 17-commit window since F7.** Headline: **#1901 onboarding upload→ask "beta-gate close"** (10eccc7d) + **#2020 staging now usable** (stg.factorylm.com HTTPS, 85a7a1ae) + #2019 prod-guard read-only inspection. Engine PROVABLY FROZEN (0 commits to `mira-bots/shared` since F7). **Sharpest residual: the freshly-closed onboarding beta-gate ships with NO CI regression gate** — `onboarding-validate`/`onboarding-walkthrough`/`command-center` Playwright configs still gate 0 workflows (`smoke-test.yml` runs only smoke+signup). `known-issues.md` stale (2026-05-26) WORSENS (0/17 commits touched it). No code edited (audit-only). |

## Lens B — re-audit (2026-06-16, round 9 / B9 — DEPLOY-TRUTH AUDIT ABORTED: execution sandbox down)

⚠️ **This run could not perform a deploy-truth audit.** The workspace bash sandbox failed to initialize on every attempt (`ensure user: useradd failed: fork/exec /usr/sbin/useradd: input/output error`, ×4), so **git was unreachable** — no `git fetch`, no `freshness-guard.sh`, no `git show origin/main:<path>`, no graphify, no `tsc`. The working tree is branch `docs/competitive-intel-siemens-teamviewer`, which A9 (same day) measured at **170 commits behind origin/main**. Beta-readiness == production == `origin/main`, which was **unreadable this run**. Per orchestrator hard rule ("if a check can't run, note it and audit statically — don't fake results"), **B9 is recorded as a deferred/degraded run: B8's GREEN deploy-truth verdict (2026-06-15 @8c3a44c1) is carried forward but NOT re-confirmed, and Lens B is retried next run.** (B's last-audited date is intentionally left at B8 so the rotation re-selects B.)

**Why the working tree was correctly NOT audited as deploy truth — proven, not assumed (file tools only):**

- **Staleness marker 1 — ADR-0017 helper absent from hub source.** `applyHubProposalTransition` returns **0 hits across all `mira-hub` source** on this branch (Grep); it appears only in `wiki/orchestrator/` notes + the staged patch. On deploy truth (B8) it lives at `proposals/[id]/decide/route.ts:97`. The branch predates the helper.
- **Staleness marker 2 — signup gate unwired.** Only `playwright.smoke.config.ts` is referenced by any workflow here (`smoke-test.yml:64`); `playwright.signup.config.ts` is **not wired**. On deploy truth it was wired at **B7** (`44fa7d97`, smoke-test.yml:73-78). A naive working-tree audit would have falsely reported **"signup money-path has no CI gate"** — a finding **already CLOSED at B7**. This is exactly the false-RED the freshness guard exists to prevent.

**The one thing safely verifiable from the stale tree (because it is invariant across the window):**

- `tests/canary/proposal_state_drift.sql` has **exactly 2 forward-only `@check`s** (L19 `accepted_suggestion_pairs_unverified_proposal`, L36 `verified_edge_links_unverified_proposal`), no reverse check. B8 confirmed **0 commits touched this file B3→B8**, so the working-tree copy == the `origin/main` copy. The staged remediation `patches/2026-06-10-canary-reverse-drift-check.patch` (+`.md`) is **present and unmerged**. → the canary reverse-drift blind spot is now a **7-cycle stall (B3→B9)**: an engine-side terminal transition (`proposal_transition.py`) updates `relationship_proposals` but disowns `ai_suggestions`, so the admin `proposals` GET can render a stale `pending` the 2-check canary is blind to. Admin-only ⇒ not stranger-reachable ⇒ does not move B off GREEN.

**Standing B verdict (carried from B8, deploy truth @8c3a44c1):** 🟢 GREEN held + strengthened — ADR-0017 transitions clean; the only residuals are the admin-only canary reverse-drift gap (staged fix, 7-cycle stall) and **3 ungated Playwright configs** (command-center/onboarding-validate/onboarding-walkthrough). Neither is stranger-reachable.

**KG (B9):** +8 nodes / +7 edges → `kg/b9-findings.jsonl` (no-clobber on nightly graph 3888n/5853e; graphify uninstallable **and** sandbox down). **Insight:** the two staleness markers are the same false-RED class the freshness guard catches — and they made the working tree un-auditable as deploy truth, leaving `proposal_state_drift.sql` (window-invariant) as the only safely-readable Lens-B file, which corroborates the 7-cycle canary stall.

**No code edited (audit-only; no deploy-truth read possible). No new patch.** Next run: re-run Lens B once the sandbox is healthy (verify `bash wiki/orchestrator/freshness-guard.sh mira-hub mira-web` runs, then audit `origin/main`).

## Lens A — re-audit (2026-06-16, round 9 / A9 — audited origin/main @ba79b4de, HEAD 170 behind / 12 ahead)

⚠️ **Freshness guard exit 3 (STALE)** — working branch `docs/competitive-intel-siemens-teamviewer` is 170 behind origin/main; `mira-hub` + `mira-web` DIFFER. Audited `origin/main` via `git show`/`git diff`/`git grep`, NOT the working tree. Beta-readiness == production == origin/main.

**Verdict: 🟢 GREEN held + strengthened.** 13 hub/web commits since A8 baseline `80f5da18`; none added a new unguarded route or weakened an auth surface.

- **No new route surface.** `git diff --diff-filter=A 80f5da18..ba79b4de -- mira-hub/src/app/api` = only `proposals/route.test.ts` + `namespace/node/route.test.ts` (tests). The #2014 "live-screen picker" (`ba79b4de`) added 0 `.ts/.tsx` files — UI + modifications to already tenant-scoped namespace routes.
- **All 4 mutated routes guarded (origin/main):** `proposals/route.ts:126` `sessionOr401()` + `ai_suggestions` strict `s.tenant_id=$1` / `relationship_proposals` `p.tenant_id=$1 OR IS NULL` (documented global-template pattern); `namespace/node/route.ts:38` + `namespace/tree/route.ts:60` `sessionOr401()`+`withTenantContext(ctx.tenantId)`; `quickstart/manufacturers/route.ts:26` PUBLIC but `withTenantContext(quickstartTenantId())` → reads demo tenant only.
- **#1892 paging is injection-safe:** new `clampLimit`/`clampOffset` (`route.ts:97-107`) coerce `?limit`/`?offset` to bounded integers (`Number`→`Math.floor`→clamp), unit-tested in the new `route.test.ts`; values flow as numbers, not interpolated SQL.
- **CSP #1902 (`7aab77d9`) is a scoped allowlist add, not a weakening:** `+https://www.dropbox.com` in `script-src`, `connect-src`, `frame-src` only (Dropbox Chooser triad). `script-src` keeps `'self' 'nonce-…'` and gains NO `'unsafe-inline'`; no wildcard; `frame-ancestors` unchanged; 3 CSP-membership assertions added to `middleware.test.ts`.
- **Login/signup (#1957/#1956/#1958) = UX, not auth:** 0 files under `auth*`/`session.ts`/`capabilities.ts`/`api/auth` in the diff; the "if an account exists, a link is on its way" copy is anti-enumeration (security-positive).
- **Public allowlist frozen:** middleware matcher (`middleware.ts:244`) public set unchanged this window.
- **Secrets scan clean:** `git grep -iE '(api[_-]?key|secret|password|token)\s*[:=]\s*"[A-Za-z0-9_-]{16,}"' origin/main -- mira-hub/src` (excl tests) = 0 hits.

**NEW low-watch (not beta-blocking):** CSP `script-src` now trusts a 4th external script-exec origin (`www.dropbox.com`, after `accounts.google.com`/`apis.google.com`/`js.stripe.com`). Justified (standard Chooser integration), scoped to one host behind the nonce model — but third-party JS now executes in the hub origin. Keep it Dropbox-only; reject any future CDN-wildcard creep in `buildCsp`.

**KG (A9):** +9 nodes / +9 edges → `kg/a9-route-auth.jsonl` (no-clobber on nightly graph 3888n/5853e; graphify still uninstallable — no CLI/module, GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset). **Insight:** query "API route nodes mutated `80f5da18..ba79b4de` lacking a `guarded_by→sessionOr401` edge" = EXACTLY `[quickstart/manufacturers]`, which instead carries a `pinned_to_demo_tenant` edge → **0 caller-tenant-reachable routes unguarded this window**; the only NEW external-origin trust edge is `www.dropbox.com` (scoped Chooser triad, one host).

**No code edited (audit-only).** No new patch — the window is clean; CSP/paging/login changes are all hardening or readiness-raising with their own unit tests.

## Lens A — re-audit (2026-06-15, round 8 / A8 — audited origin/main @80f5da18, HEAD 154 behind / 10 ahead)

**Verdict: 🟢 GREEN held + strengthened.** Freshness guard exit STALE (`feat/realtime-datapoint-clock` 154 behind; `mira-hub` + `mira-web` DIFFER) → audited `origin/main` via `git show`. **15 commits touched `mira-hub/`+`mira-web/` since the A7 baseline `@82dce84d`; not one weakened an auth surface.**

| Surface audited (deploy truth) | A8 result | Evidence |
|---|---|---|
| `middleware.ts` public allowlist | **HARDENED** | only change since A7 is `8a8f2fce` (#1952): root→/feed redirect moved AFTER the cookie/token auth gate; redirect target hardcoded `/feed` (no user-controlled open redirect); unauth root now falls through to the login bounce preserving `/` as callback. First `middleware.test.ts` added. |
| `feat(auth)` `a485bdf8` (#1955 "discoverable password") | **UI/UX ONLY** | `login-form.tsx` +22 lines, `magic/page.tsx` 2 lines, docs+screenshots+VERSION. 44px touch targets, visible "Send link" label, password trigger restyled as a tappable button. No change to auth logic, session, or token handling. |
| NEW `api/command-center/display/route.ts` (GET list + POST register) | **SECURE** | GET+POST both `sessionOr401()` then `withTenantContext(ctx.tenantId)`; list query `WHERE d.tenant_id=$1::uuid`; POST runs `validateDisplayRegistration()` + SSRF host guard before upsert. |
| NEW `api/pm-schedules/[id]/complete/route.ts` (POST) | **SECURE (IDOR-safe)** | `sessionOr401()` → `UUID_RE.test(id)` (400 on bad id) → `withTenantContext` → `WHERE id=$1 AND tenant_id=$2`; 404 (no cross-tenant existence leak). |
| SSRF: `display/[id]` server-side probe | **MITIGATED (low-watch)** | `validateDisplayRegistration` blocks link-local/`169.254` cloud-metadata; item route is `sessionOr401()`+tenant-scoped. Prod allowlist `COMMAND_CENTER_DISPLAY_HOST_ALLOWLIST` is **env-gated** (unset ⇒ private LAN/loopback/Tailscale ranges allowed by design for on-prem HMI). Auth-required ⇒ **not stranger-reachable**. |
| Secrets in code | **CLEAN** | `git grep` for `(api_key\|secret\|password\|token) = "<16+ chars>"` on `origin/main:mira-hub/src` (excl. tests, `process.env`, placeholders) = **0 hits**. |
| Signup (stranger entry) `3e24ec4a` (#1937 GTM rework) | **AUTH UNTOUCHED** | the GTM repositioning touched 0 files under `api/auth/`; `register/route.ts` still `bcrypt.hash(password,12)` + in-memory per-IP 5/hr limit. |

**Why GREEN not YELLOW:** every stranger-reachable auth surface is byte-stable or hardened on deploy truth; the 2 new routes inherit the proven `sessionOr401()`+`withTenantContext()` gate rather than rolling their own; no new pre-auth route, no IDOR, no secret, no open redirect.

**NEW finding (low, non-blocking):** the display-host SSRF allowlist (`COMMAND_CENTER_DISPLAY_HOST_ALLOWLIST`) defaults open for private ranges. It is defense-in-depth (cloud-metadata already blocked, registration auth-gated + tenant-scoped), so it cannot be reached by a signed-out stranger — but it should be set before a multi-tenant beta that lets tenants register display hosts. → `doppler secrets set COMMAND_CENTER_DISPLAY_HOST_ALLOWLIST=<hmi-hosts> -p factorylm -c prd` (human-run; orchestrator never touches prod Doppler).

**KG:** graphify still uninstallable in-sandbox (no LLM key — `GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC` all unset, same wall as every prior run). Used the nightly graph (3888 nodes / 5853 links, built `@308e7b55`) + `kg_query.py`. **Insight:** `sessionOr401()` is the **#2 God Node** (degree 95) with **51 route-handler callers** and `withTenantContext()` is **#4** (degree 80) — the auth + tenant-isolation choke points nearly every hub route funnels through; `kg_query.py orphans` returns **0 unguarded hub HTTP-route nodes**. The 2 new A8 routes call exactly this pair. Appended **+11 nodes / +6 edges** to `kg/a8-route-auth.jsonl` + `kg/a8-findings.jsonl` (no-clobber). Rotate → B next.

## Lens B — re-audit (2026-06-15, round 8 / B8 — audited origin/main @8c3a44c1, HEAD 157 behind / 10 ahead)

**Verdict: 🟢 GREEN held + strengthened.** Freshness guard exit 3 (STALE, 157 behind, branch `feat/realtime-datapoint-clock`; mira-hub DIFFERS) → audited origin/main via `git show`/`git log`. Rotation: B oldest (B7 ran first of the 06-14 batch; A8 ran 06-15 → "Rotate→B"). Baseline B7 @a0f79dc4 confirmed ancestor of @8c3a44c1 → bytes audited == deploy truth.

**16 hub commits since B7; most cross-checked by A8** (#1955 auth UI, #1952 middleware hardening, #1937 GTM signup, #1960 command-board, #1950 PM-complete). **2 are brand-new since A8 ran this morning (after @80f5da18), both stranger-path readiness++:**
- **#1977** (`cc2de0e2`) dogfood trust/UX papercuts: `api/quickstart/manufacturers/route.ts` now dedups the PUBLIC manufacturer picker via new `lib/manufacturerNormalize.normalizeManufacturer` (collapses OCR aliases → canonical-lowercase `Map`); also friendly auth error + real citation links on `quickstart/page.tsx`. Read/aggregate only — no auth/tenant/privacy change → no leak, cleaner stranger demo.
- **#1981** (`9f1f5176`) migration **053_cleanup_dogfood_test_account.sql**: `DELETE FROM hub_users WHERE email_lower=LOWER('<one exact dogfood email>')` — idempotent, FK-safe (hub_users child of hub_tenants), single-row. Head **052→053**, ledger filename-keyed/operable. Closes the E-lens "next free 053" note (dup-048 still present; next free now 054).

**ADR-0017 CLEAN on deploy truth:** `proposals/[id]/decide/route.ts` imports `applyHubProposalTransition` (L4) + calls it (L97); the raw `kg_relationships` UPDATE (L138) is the documented engine projection mirror (comment L93-96). Single Hub chokepoint into both status tables intact.

**Standing item-1 — canary reverse-drift (now 6-cycle stall B3→B8, 0 commits touched it):** `tests/canary/proposal_state_drift.sql` is STILL exactly 2 forward-only `@check`s (`accepted_suggestion_pairs_unverified_proposal` L19 + `verified_edge_links_unverified_proposal` L36). Engine `proposal_transition.py` terminal transitions disown `ai_suggestions` (Hub-owned per docstring) while admin `proposals` GET renders `pending` = stale-pending lie the canary can't see. Staged `patches/2026-06-10-canary-reverse-drift-check.patch` **re-verified `git apply -p1 --check` CLEAN vs the origin/main blob this run**, unmerged.

**Standing item-2 — Playwright coverage UNCHANGED at 3 ungated configs:** of 7 configs, only `signup`+`smoke` carry a workflow edge (smoke-test.yml). `command-center`/`onboarding-validate`/`onboarding-walkthrough` = 0 workflow refs (grep-confirmed). **Sharpened this cycle:** #1960 (`bdfdec38`) shipped NEW `/api/command-center/display` GET+POST + live-view rework → **command-center is the UNIQUE 0-workflow config whose feature code churned this window**. Mitigated (not erased): #1960 ships framework-free `command-center-view.ts` vitest helpers into the wired Hub Unit Tests job; the gap is e2e-only.

**Why GREEN not YELLOW:** both standing items are admin-/coverage-only — the canary guards the admin-only `/proposals` queue (no stranger sees it); the 3 orphan configs are missing SUITE coverage over admin/onboarding UI, not a proven defect. Public signup→quickstart→cite money path is gated (signup+smoke) and the 2 new commits only improve it. Honest signal = stalled remediation (1 apply-clean SQL patch + 3 workflow wirings, founder-gated) around a non-stranger-reachable, slowly-shrinking gap.

**Toolchain note:** working tree 157 behind on a feature branch with package.json dep drift in-window → a detached-worktree `tsc` would run against drifted node_modules = unreliable; deploy-truth functional gates are Hub Unit Tests (vitest) + the smoke gate, both wired. No `tsc` run this cycle (consistent with B7).

**Action:** apply the canary patch (zero-risk detective SQL; `proposal-state-canary.yml` picks it up immediately) AND wire the 3 orphan configs, command-center first. Then founder-gate the preventive fix (mirror engine terminal transitions onto `ai_suggestions`). No code edited (audit-only); no new patch (canary patch already staged + re-verified; the 2 new commits already merged).

---

## Lens F — re-audit (2026-06-15, round 8 / F8 — audited origin/main @ba79b4de, HEAD 170 behind / 11 ahead)

**Verdict: 🟢 GREEN held + strengthened.** Freshness guard exit 3 (STALE) — working tree on `research/ignition-tag-client-readiness`, 170 behind / 11 ahead; audited deploy truth `origin/main @ba79b4de`. North Star answers **YES on deploy truth**: a stranger can sign up on app.factorylm.com, use MIRA's grounded chat, and nothing breaks, leaks, or lies. **17 commits** landed since the F7 baseline `@a485bdf8` (14 hub/web, **0 engine** — engine PROVABLY FROZEN since C7, re-confirmed: `git log a485bdf8..origin/main -- mira-bots/shared mira-pipeline/ignition_chat.py` = 0).

**Headline movement — two long-stalled items materially advanced:**
- **#1901 (`10eccc7d`) "onboarding upload→ask — beta-gate close."** The stranger's first real experience (upload a doc → ask MIRA about it) is now feature-complete. This is the single most beta-relevant merge in the window.
- **#2020 (`85a7a1ae`) "make staging usable (stg.factorylm.com HTTPS) + fix stale staging doctrine."** The years-long staging gap (Lens E's #1/#3) is now closed — a real staging URL exists to gate against.
- Plus #2019 prod-guard read-only prod inspection, #1983/#1988 enterprise-rooted namespace tree fix, #1892 real counts + offset pagination for `/api/proposals`, #1958 trust/data-handling signals on signup, #1977 dogfood trust/UX papercuts (friendly auth, real citation links, manufacturer dedup).

**Sharpest residual finding (new, elevated):** the freshly-closed onboarding beta-gate ships with **no CI regression gate**. `playwright.onboarding-validate.config.ts`, `onboarding-walkthrough.config.ts`, and `command-center.config.ts` still gate **0 workflows** — `smoke-test.yml` runs only `playwright.smoke.config.ts` + `playwright.signup.config.ts`; `staging-namespace-create-e2e.yml` runs the namespace spec; `enforcement-audit.yml` runs the hub-page-audit spec. So a regression in the stranger's first experience would not be caught before deploy. Staged fix: `patches/2026-06-15-F8-wire-onboarding-validate-ci.md`.

**Master plan (`origin/main`):** header still "DRAFT — planning only, no production code." Phase 0 baseline ✅ (Supervisor, FSM, `AWAITING_UNS_CONFIRMATION`, UNS resolver+gate, Gemini→Groq→Cerebras cascade, BM25+pgvector, RAG worker, 22-phrase safety classifier, Slack/Telegram). Phase 6 direct-connection UNS still presence-based, `ENFORCE_ASSET_AGENT_GATE` default-off (non-stranger HMAC surface).

### Ranked beta-blocker ledger (top-10) — 2026-06-15 (F8)

No item is a **live stranger-reachable** defect; this is the residual risk register (coverage → ops → doc-hygiene), each with an exact next step. Δ vs F7 noted.

1. **[COVERAGE-GAP · B-owned] Onboarding beta-gate has NO CI regression gate — ELEVATED.** #1901 (`10eccc7d`) closed onboarding upload→ask (stranger's first experience) but `onboarding-validate` / `onboarding-walkthrough` / `command-center` Playwright configs gate 0 workflows; `smoke-test.yml` runs only smoke+signup. *Δ worsened-in-importance:* the path is now feature-complete + beta-critical, not "future UI." → wire `onboarding-validate.config.ts` into `smoke-test.yml` (staged `patches/2026-06-15-F8-wire-onboarding-validate-ci.md`).
2. **[GUARD-GAP · D-owned] Inert deterministic replay seam** — unchanged (engine frozen; 0 eval commits since F7). `cp_citation_vendor_relevance` can't regression-guard the #1858/#1877 cross-vendor citation strip; median-quorum gate MERGED (`dc3162eb`), `eval-replay-gate.yml` exists. Not stranger-reachable.
3. **[DOC-HYGIENE] `known-issues.md` stale (2026-05-26) — WORSENS (6th cycle).** 0 of 17 commits since F7 touched it; last real edit 2026-05-29 (`91c6180c`). Now omits the entire Phase-1 hub push, #2020 staging-usable, #2019 prod-guard, #1901 beta-gate close, and every 2026-06 closed stranger blocker. → regenerate "Known Broken/Incomplete" from `gh pr list --state merged --search "merged:>=2026-05-26"`; bump date. **(30-min founder play.)**
4. **[OPS · half-open, unchanged] VPS deploy reproducibility (#736).** #1970 created `v<VERSION>` tag + `rollback/<date>` checkpoint + Release on every merge, but `deploy-vps.yml:56` still `uses: actions/checkout@v6` with no `ref:` → deploys `main` HEAD, not the tag. → add `with: ref:` resolved from `/VERSION`.
5. **[DETECTIVE-GAP · B-owned] `proposal_state_drift.sql` forward-only.** Engine terminal transitions disown `ai_suggestions` → stale-pending admin queue, canary-blind (admin-only, not stranger-reachable). → `git apply wiki/orchestrator/patches/2026-06-10-canary-reverse*`.
6. **[PRE-SCALE · A-owned] in-memory rate limiters** (quickstart/ask + magic-link) — per-instance `Map`, bypassable behind a multi-instance LB; holds on today's single instance. → DB-backed `quickstart_rate` table; `patches/2026-06-11-A5-inmem-limiter-pattern.md`.
7. **[OPS] Gemini key 403** — cascade is really 2-of-3 (Groq/Cerebras cover; smoke OK). Not verifiable from sandbox (prod Doppler forbidden). → refresh `GEMINI_API_KEY` in Doppler `factorylm/prd`.
8. **[DOC-HYGIENE · E-owned] stale `mira-hub/package-lock.json` beside `bun.lock`** — STILL PRESENT (unchanged); inert in CI but local `npm ci` re-hits the graphology/unpdf drift that broke the gate pre-#1855. → `git rm mira-hub/package-lock.json` (staged `patches/2026-06-14-E7-drop-stale-package-lock.md`).
9. **[DOC-HYGIENE] `wiki/hot.md` migration head stale** — origin/main hot.md still anchors head=037 (historical) while live hub migrations reach 045/046+. Misleads migration sessions. → bump `wiki/hot.md` migration head.
10. **[ROADMAP · not beta-blocking] Phase 6 direct-connection UNS** presence-based not resolution-based; `ENFORCE_ASSET_AGENT_GATE` default-off (HMAC-gated Ignition surface, non-stranger). → master-plan Phase 6 acceptance; `mira-pipeline/ignition_chat.py`.

**KG:** graphify still uninstallable in-sandbox (no LLM key — `GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC` all unset, `import graphify` fails — same wall as every prior run). Used the nightly graph (3888 nodes / 5853 links, built `@308e7b55`) + `tools/orchestrator/kg_query.py`. Appended **+11 nodes / +7 edges** to `kg/f8-findings.jsonl` (no-clobber). **KG insight:** the hub god-node `sessionOr401()` (degree 95, `mira-hub/src/lib/session.ts:85`) anchors the onboarding/ask stranger path — a regression in the now-ungated onboarding beta-gate is high-blast-radius, reinforcing ledger #1 as the top coverage fix.

## Lens F — re-audit (2026-06-14, round 7 / F7 — audited origin/main @a485bdf8, HEAD 153 behind / 10 ahead)

**Verdict: 🟢 GREEN held + strengthened.** North Star answers **YES on deploy truth**: a stranger can sign up on app.factorylm.com, use MIRA's grounded chat, and nothing breaks, leaks, or lies. 51 commits landed since the F6 baseline `@2337a116` — including a full **assessment-led GTM funnel rework** — and not one introduced a stranger-reachable defect. All five historically-tracked blockers re-verified closed in LIVE `origin/main` (read the wired call, not a prior session's note):

- **BREAK** — `quickstart/ask` per-IP-hash rate limit → `429` (`route.ts:109`, byte-stable).
- **LEAK** — `/api/documents` `sessionOrDemo` + canonical `(is_private = false OR tenant_id = $caller)` + single-doc `AND tenant_id = $2` (#1833) — byte-stable.
- **LIE** — `stripConflictingVendors(chunks, …)` BEFORE `citations[]` (`route.ts:152`, #1858) + cite-or-refuse system prompt + #1875 refusal→`citations: []` — byte-stable.
- **GATE** — `smoke-test.yml` P0-1 (ask flood→429) / P0-2 (`/api/documents` unauth→401) / P1-1 (`/quickstart` answer) + the **signup money-path** config wired into the deploy-blocking smoke job (`44fa7d97`); install via `bun install --frozen-lockfile` (#1855 held).
- **ENGINE** — citation enforce default-ON (`citation_compliance.py:42`, `MIRA_CITATION_ENFORCE` defaults `"1"`); engine PROVABLY FROZEN since C7.

**Headline movement — a long-stalled ops blocker materially advanced.** `38cc1cfa` (#1970) adds a repo-wide version counter: `version-gate.yml` forces `/VERSION` to bump on every code PR, and `version-tag.yml` — on every merge to `main` — creates an annotated `v<VERSION>` git tag, a paired `rollback/<date>-v<VERSION>` checkpoint, and a GitHub Release (`docs/versioning.md` documents the revert recipe). This gives every merge a unique, trackable version **and an automatic rollback target** — the thing #736 said did not exist. **But the remaining half persists:** `deploy-vps.yml` still does `actions/checkout@v6` of `main` HEAD (no `ref:`/tag pin), so the deploy itself still ships `main`, not the tag. Ledger #6 therefore NARROWS from "no reproducible deploy / rollback target" to "automatic rollback target now exists; deploy still not tag-pinned."

**GTM funnel rework (watch, not blocker).** #1937 (assessment-led GTM repositioning, public site + Hub signup), #1953/#1960 (Hub landing → maintenance command board), #1963 (pricing CTA → "Book Your Assessment" → `/buy`), #1952 (preserve `/hub` callback for unauth users), #1969/`a485bdf8` (shop-floor login usability). The public emphasis shifted toward booking an assessment, but the **self-serve signup→upload→cite money path is still wired and deploy-gated** (signup smoke config) and the public `quickstart/ask` grounded-chat demo is byte-stable with its 429 + cite-or-refuse — so the North Star path remains stranger-reachable and guarded.

**Master plan (`origin/main`):** header still "DRAFT — planning only, no production code." Phase 0 baseline is ✅ (Supervisor engine, FSM, UNS gate + resolver, Gemini→Groq→Cerebras cascade, BM25+pgvector retrieval, RAG worker, 22-phrase safety classifier, Slack/Telegram adapters). Phases 1–13 are all data-model / industrial-connect / decision-trace / tag-stream work (`tag_events` Phase 5, `flaky_input_signals` Phase 9, `direct_connection` Phase 6, citation **enforcement** Phase 7, MCP tag tools Phase 11) — **post-beta, not stranger-reachable.** Nothing in Phases 1–13 gates beta.

### Ranked beta-blocker ledger (top-10) — 2026-06-14 (F7)

No item is a **live stranger-reachable** defect; this is the residual risk register (regression-gaps → ops → doc-hygiene), each with an exact next step. Δ vs F6 noted.

1. **[GUARD-GAP · D-owned] Inert deterministic replay seam — the ONE unguarded stranger-path invariant.** `cp_citation_vendor_relevance` can't regression-guard the #1858/#1877 cross-vendor citation strip. *Δ improved:* the median-quorum gate is MERGED (`dc3162eb`) and `eval-replay-gate.yml` is now WIRED (`0388b74c`) — but INERT: it is `hashFiles`-gated on a `.gitignore`'d replay store, so it SKIPS with `::warning::`. → **founder records the replay store** per `patches/2026-06-11-D4-replay-seam-activation.md`; the gate auto-activates once the fixtures exist.
2. **[DETECTIVE-GAP · B-owned] `proposal_state_drift.sql` = 2 forward-only checks.** Engine terminal transitions disown `ai_suggestions` → stale-pending admin queue, canary-blind (admin-only, not stranger-reachable). 5-cycle stall. → `git apply wiki/orchestrator/patches/2026-06-10-canary-reverse-drift-check.patch` (apply-ready, unmerged).
3. **[COVERAGE-GAP · B-owned] 3 Playwright configs gate 0 workflows** (command-center / onboarding-validate / onboarding-walkthrough). *Δ narrowed 4→3:* the signup money-path config is now wired (`44fa7d97`). → wire the remaining 3 into `.github/workflows/` (admin/onboarding UI; not stranger-reachable).
4. **[PRE-SCALE · A-owned] in-memory rate limiters** (quickstart/ask + magic-link) — per-instance `Map`, bypassable behind a multi-instance LB; holds on today's single instance. → port to a DB-backed `quickstart_rate` table; see `patches/2026-06-11-A5-inmem-limiter-pattern.md`.
5. **[OPS · NARROWED this run] VPS deploy reproducibility (#736).** *Δ:* #1970 now auto-creates `v<VERSION>` + `rollback/<date>` checkpoint + Release on every merge (automatic rollback target now exists). Remaining half: `deploy-vps.yml` still checks out `main` HEAD, not the tag. → have `deploy-vps.yml` check out the `v<VERSION>` tag (deploy-by-tag) to close it.
6. **[OPS] Gemini key 403** — cascade is really 2-of-3 (falls through to Groq/Cerebras; smoke-tested OK). → refresh `GEMINI_API_KEY` in Doppler `factorylm/prd`.
7. **[DOC-HYGIENE] `known-issues.md` stale (2026-05-26) — WORSENS.** 0 of 51 commits since F6 touched it; it now also omits the #1970 version-tag/rollback mechanism, every 2026-06 closed stranger blocker (#1833/#1858/#1899/#1903/#1909/#1919), and the 040-042 migration gap → a new tester/contributor gets a wrong risk picture. → refresh `docs/known-issues.md`.
8. **[DOC-HYGIENE · E-owned] stale `package-lock.json` beside `bun.lock`** — inert in CI but local `npm ci` re-hits the graphology/unpdf drift that broke the gate pre-#1855; 3-cycle stall. → `git rm mira-hub/package-lock.json` (staged `patches/2026-06-14-E7-drop-stale-package-lock.md`).
9. **[DOC-HYGIENE] `wiki/hot.md` migration head=037** vs actual hub head **052** — misleads migration sessions. → bump `wiki/hot.md`.
10. **[ROADMAP · not beta-blocking] Phase 6 direct-connection UNS** presence-based not resolution-based; `ENFORCE_ASSET_AGENT_GATE` default-off (HMAC-gated Ignition surface, non-stranger). → master-plan Phase 6 acceptance; `mira-pipeline/ignition_chat.py`.

**KG:** graphify still uninstallable in-sandbox (no LLM key for extraction — `GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC` all unset, same wall as every prior run); used the nightly graph (3888 nodes / 5853 links, built `@308e7b55`) + `kg_query.py`. Appended **+13 nodes / +12 edges** to `kg/f7-findings.jsonl` (no-clobber). **Insight:** the @308e7b55 graph predates #1970, so `version-tag.yml` is ABSENT (measured drift). Modeled in deploy truth, the new mechanism is a **producer node with no consumer edge**: `version-tag.yml` *produces* the `rollback/<date>-v<VERSION>` checkpoint, but `deploy-vps.yml` *does-NOT-consume* it (still pins `main` HEAD). That "present producer / missing consumer edge" is the SAME fingerprint as D's wired-but-inert replay seam and C7's lone-producer direct-connection cert — three different lenses, one recurring shape: the artifact is built, the consuming edge is the gap. North Star's five invariants, by contrast, every one maps to a god-node guard (`sessionOr401` deg-95 → LEAK; `engine.py` deg-99 → ENGINE) that is now operably wired.


## Lens D — re-audit (2026-06-15, round 8 / D8 — audited origin/main @64e156c9, HEAD 164 behind / 11 ahead)

**Verdict: 🟡 YELLOW held — eval & test layer PROVABLY FROZEN since D7.** Freshness guard exit 3 (STALE; branch `research/ignition-tag-client-readiness` 164 behind; `tests/eval`, `tests`, `eval-replay-gate.yml` all DIFFER) → audited `origin/main` via `git show`/`git log`. `git log 873b29b4..origin/main -- tests/eval tests/` = **0 commits**; `873b29b4` (D7 baseline) confirmed clean ancestor of `64e156c9` ⇒ bytes audited == deploy truth. All four D7 finding-states re-verified byte-stable.

| D7 finding | D8 state on origin/main | Evidence |
|---|---|---|
| #3 fix_proposer phantom fix-PRs | **CLOSED (holds)** | `mira-bots/tools/fix_proposer.py:139` `quorum_failures()` + `_recent_scorecards()`; keeps only failures in ⌈window/2⌉ of last N scorecards |
| #2 `offline_run --replay` wiring | **CLOSED-wiring (holds)** | `eval-replay-gate.yml` runs `MIRA_EVAL_REPLAY=replay offline_run.py` on PRs touching `mira-bots/shared/**`+`tests/eval/**` |
| #1 replay store inert | **UNCHANGED** | `.gitignore:241` ignores `llm_replay/*.json`; gate `hashFiles(cascade.json)`='' → `::warning::` skip; founder-gated (live keys + PII eyeball) |
| #4 `test_offline_scorecard.py` writer test | **UNCHANGED** | asserts `## Failures` + `- Last response:` render only — not a diagnostic regression guard |

**CI eval reality (deploy truth):** PR-time gating = `deepeval-ci.yml` (live GROQ judge, FAILS job <80% pass, exit 1) + GS11 grounding regression pytest (unit+engine+gate) + RAGAS advisory. `eval-replay-gate.yml` (keyless, deterministic) is INERT. Nightly/dispatch only = `ci-evals.yml` (offline pytest + `synthetic_eval --mode dry-run`) — **no `pull_request` trigger**, so the deterministic diagnostic scorecard never runs PR-time. The keyless replay gate is the only PR-time deterministic guard for `cp_citation_vendor_relevance` (#1858 cross-vendor citation-strip), and it is skipped.

**Scorecard (unchanged, frozen):** newest committed `tests/eval/runs/2026-06-08T0229-offline-text.md` = **50/57 (87%)**, judge disabled. 7 fails = Cluster A (6 FSM over-qualification — `RState` PASS but expected-state miss = over-caution, beta-SAFE direction; incl. one `SAFETY_ALERT` vs `Q2`) + Cluster B (1 keyword miss, `gs3_ground_fault_14`). Beta-critical No5xx / TurnBudget / CitGrond / safety = **0 fails across 57** ⇒ YELLOW not RED; `cp_citation_vendor_relevance` CI-skipped ⇒ YELLOW not GREEN.

**NEW finding (keyless, mergeable today):** `eval-replay-gate.yml`'s store presence-check is `hashFiles('…/llm_replay/cascade.json')` — a single file — but strict replay loads BOTH `cascade.json` (LLM cascade) AND `retrieval.json` (`neon_recall`) per `llm_replay.py:40-41`, and the activation runbook's own Step 1 verify expects **both** present + non-empty. A half-recorded store (cascade captured but the DB was down during `record`, so `retrieval.json` never landed) would flip the gate ACTIVE and then raise on the first retrieval call → false-red on engine PRs. Staged keyless fix: `patches/2026-06-15-D8-replay-gate-require-both-stores.patch` (require both files; `git apply -p1 --check` CLEAN vs origin/main; reverse fails on clean = forward-only; 8 ins / 0 del; inline-documented). No code edited in place (audit-only; 1 patch staged).

**KG (D8):** sidecar `kg/d8-findings.jsonl` +12 nodes / +5 edges (no-clobber additive to nightly graph 3888/5853 @308e7b55; graphify uninstallable, all LLM keys unset). Insight: a query for "operability blockers on the replay seam" returns EXACTLY two — `d8_store_unrecorded` (founder-gated) and `d8_gate_singlefile_bug` (keyless). D8's patch removes the keyless one, leaving exactly one founder-gated blocker. Same present≠operable fingerprint E5 named and #1855 fixed for the deploy gate — except the structural "is it wired?" view misses D's SECOND blocker: the gate's activation predicate under-counts the store (1 of 2 required files).

## Lens D — re-audit (2026-06-14, round 7 / D7 — audited origin/main @873b29b4, HEAD 141 behind / 10 ahead)

**Verdict: 🟡 YELLOW held — but materially improved (2 of 4 D6 findings closed on deploy truth).** Freshness guard exit STALE (`feat/realtime-datapoint-clock` 141 behind; `tests/eval/` DIFFERS) → audited `origin/main` via `git show`. `tests/` moved by exactly **1 commit** since the D6 baseline `b2d74f7c` (confirmed ancestor) — and that commit is the remediation.

| D6 finding | D7 state on origin/main | Evidence |
|---|---|---|
| #3 fix_proposer single-scorecard → phantom fix-PRs (#1773/#1788) | **CLOSED** | `dc3162eb` (Mike, Jun 13 18:43) merged the D5 patch: `mira-bots/tools/fix_proposer.py` +77 (`quorum_failures()` median window, default 3, needs ≥2; `<window`→graceful fallback), `tests/eval/test_fix_proposer.py` +71 |
| #2 `offline_run --replay` wired into 0 workflows | **CLOSED (wiring)** | NEW `.github/workflows/eval-replay-gate.yml` → `env MIRA_EVAL_REPLAY=replay python tests/eval/offline_run.py`, triggers on `mira-bots/shared/**`+`tests/eval/**` PRs |
| #1 replay store inert (`.gitignore` + fixtures absent) | **UNCHANGED** | `.gitignore:241` still ignores `tests/eval/fixtures/llm_replay/*.json`; `cascade.json`/`retrieval.json` absent on origin/main; gate `hashFiles`-guarded → SKIPS w/ `::warning::` |
| #4 `test_offline_scorecard.py` = writer test, not diagnostic eval | **UNCHANGED** | 0 commits touched it; docstring confirms it is the #1583 `last_response` writer guard (tmp_path/monkeypatch) |

**Why YELLOW not RED:** newest committed scorecard `2026-06-08T0229` = **50/57 (87%)** LIVE; the 7 failures are FSM-over-qualification + keyword-miss clusters (diagnostic quality), and the beta-critical columns (No5xx, TurnBudget, CitGrond, safety 9/9) carry **0 fails** — no stranger-reachable break/leak/lie. The phantom-PR noise generator is now fixed.

**Why YELLOW not GREEN:** the one beta-relevant deterministic regression guard — `cp_citation_vendor_relevance` over the #1858 cross-vendor citation strip — is still **structurally skipped** in CI. The seam is now WIRED but **INERT**: the `eval-replay-gate.yml` job is `hashFiles`-gated on a store (`tests/eval/fixtures/llm_replay/cascade.json`) that doesn't exist on origin/main, so every run today emits `::warning:: INERT` and no-ops. Recording the store is a **founder-only** action (live LLM keys + PII eyeball, D4 runbook steps 1-2) an autonomous agent cannot perform.

**Remediation-state flip vs D6:** D6 said *both* remediation patches were STAGED & UNMERGED. Now the **D5 median-quorum `.patch` is MERGED** (`dc3162eb`) and the **D4 replay seam is WIRED** (`eval-replay-gate.yml`). The only remaining step is the founder store recording — and the gate auto-activates the moment it lands. There is no longer an apply-able patch for an agent here.

**KG:** graphify still uninstallable in-sandbox (no LLM key for extraction); used the nightly graph (3888 nodes / 5853 links, built @308e7b55). Appended **+8 nodes / +3 edges** to `kg/d7-findings.jsonl` (no-clobber). **Insight:** the deterministic eval seam was a *graph island* at D6 (no CI-workflow node had an inbound edge to `offline_run --replay`/`llm_replay`). On deploy truth that island now has an edge — `eval-replay-gate.yml` is the first CI node pointing into `offline_run --replay` — but the edge is (a) **absent from the graph** (built pre-`dc3162eb`) and (b) **dormant** (store-guarded). One founder store-record flips a dormant edge into a live regression guard. **No code edited (audit-only).** Rotate to **Lens E** next run (round 7).

## Lens F — re-audit (2026-06-13, round 6 / F6 — audited origin/main @2337a116, HEAD 102 behind)

**Verdict: 🟢 GREEN held + strengthened.** North Star answers **YES on deploy truth**: a stranger can sign up, use grounded chat, and nothing breaks, leaks, or lies. All five historically-tracked stranger-reachable blockers re-verified closed in LIVE `origin/main` code (read the wired call, not prior write-ups):

- **BREAK** — `quickstart/ask` per-IP-hash rate limit → `429` (route.ts:109).
- **LEAK** — `/api/documents` `sessionOrDemo` + canonical `(is_private=false OR tenant_id=$1)` filter, single-doc `WHERE id=$1 AND tenant_id=$2` (#1833, route.ts:53/66).
- **LIE** — `stripConflictingVendors(chunks, …)` BEFORE `citations[]` (route.ts:152, #1858) + **NEW #1875** blanks the citation list on a refusal answer (route.ts:182-186).
- **GATE** — `smoke-test.yml` asserts `/quickstart` answer (P1-1), `/api/quickstart/ask` flood→429 (P0-1), `/api/documents` unauth→401 (P0-2), checkout 303; install via `bun install --frozen-lockfile` (#1855 operability fix).
- **ENGINE** — citation enforce default-ON (`citation_compliance.py:42`, `MIRA_CITATION_ENFORCE` defaults `"1"`).

**6 commits since F5 (@51e3f5b0 → @2337a116) — all hardening or admin-only:**

| Commit | Surface | Verdict |
|---|---|---|
| #1935 streaming ingest-v2 | **NEW stranger-path** upload→`knowledge_entries` write | Preserves `is_private=true` literal (#1903) + `withTenantContext` + tenant-scoped `ON CONFLICT`; **ships `node-knowledge-ingest-is-private.test.ts`** (Hub Unit Tests) → leak class guarded |
| #1875 refusal-cites | quickstart/ask | Anti-lie: refusal → `citations: []` |
| #1932 admin nav + authz | admin users API | **Strengthens** auth: `requireSession()` + `requireCapability('platform.users.read')` → 401 |
| #1945 account actions | admin/users | Label rename ("Accounts"→"Platform accounts"); roles unchanged |
| #1928 honest scan UX | mira-scan-monday | Anti-lie: refuse honestly when GS10 docs missing |
| graphify auto-refresh | chore | n/a |

**Master plan:** Phase 0 ✅; Phases 4–13 are all industrial-connect (PLC/Ignition tag collector, tag-diff stream, UNS-gate gaps, troubleshooting session, decision trace, flaky-wire detector, Slack Block Kit, agent tools, eval, graph DB) — **post-beta, not stranger-reachable.** Phase 6 (`ignition_chat.py source="direct_connection"`) still presence- not resolution-based (C6 YELLOW sub-item; HMAC-gated Ignition surface, conservative-safe).

### Ranked beta-blocker ledger (top-10) — 2026-06-13

No item is a **live stranger-reachable** defect; this is the residual risk register (regression-gaps → ops → doc-hygiene), each with an exact next step.

1. **[GUARD-GAP · D-owned] Inert deterministic replay seam.** `cp_citation_vendor_relevance` can't regression-guard the #1858/#1877 vendor-strip — `offline_run --replay` is wired into 0 workflows + the replay store is `.gitignore`'d (raises on call #1). A future commit could silently re-introduce the wrong-vendor lie with no CI gate. → `git apply wiki/orchestrator/patches/2026-06-12-D5-fixproposer-median-quorum.patch` (keyless, today) then run `patches/2026-06-11-D4-replay-seam-activation.md`.
2. **[COVERAGE-GAP · B-owned] 4 Playwright configs gate 0 workflows** (command-center/signup/onboarding-validate/onboarding-walkthrough) — post-signup beta surface ungated. → wire `mira-hub/playwright.signup.config.ts` + `playwright.command-center.config.ts` into `.github/workflows/` (or fold into `smoke-test.yml`). Narrowed by #1888 route-snapshot + #1889 namespace 9/9.
3. **[DETECTIVE-GAP · B-owned] `proposal_state_drift.sql` = 2 forward-only checks** — engine terminal transitions disown `ai_suggestions` → stale-pending admin queue, canary-blind (admin-only). → `git apply patches/2026-06-10-canary-reverse-drift-check.patch` (apply-ready, unmerged 4 cycles).
4. **[PRE-SCALE · A-owned] in-memory rate limiters** (quickstart/ask + magic-link) — per-instance `Map`, bypassable behind a multi-instance LB; holds on today's single instance. → port to DB-backed `quickstart_rate` table; see `patches/2026-06-11-A5-inmem-limiter-pattern.md`.
5. **[OPS] Gemini key 403** — cascade is really 2-of-3 (falls through to Groq/Cerebras). → refresh `GEMINI_API_KEY` in Doppler `factorylm/prd`.
6. **[OPS] VPS deploy uses `main` HEAD not version tags (#736)** — no reproducible deploy / rollback target. → have `deploy-vps.yml` check out the namespaced tag.
7. **[DOC-HYGIENE · E-owned] stale `package-lock.json` beside `bun.lock`** — inert in CI but local `npm ci` re-hits the graphology/unpdf drift that broke the gate pre-#1855. → `git rm mira-hub/package-lock.json`.
8. **[DOC-HYGIENE] `known-issues.md` stale (2026-05-26)** — omits every 2026-06 closed stranger blocker (#1833/#1858/#1899/#1903/#1909/#1919) + the 040-042 migration gap + the in-mem limiter pre-scale item; a new tester/contributor gets a wrong risk picture. → refresh `docs/known-issues.md` (the canonical ledger should live here, not only in `wiki/orchestrator/`).
9. **[DOC-HYGIENE] `wiki/hot.md` migration head=037** vs actual hub head 052 — misleads migration sessions. → bump `wiki/hot.md`.
10. **[ROADMAP · not beta-blocking] Phase 6 direct-connection UNS** presence-based not resolution-based; `ENFORCE_ASSET_AGENT_GATE` default-off (HMAC-gated Ignition surface). → master-plan Phase 6 acceptance; `mira-pipeline/ignition_chat.py`.

**KG:** used the nightly graph (3888 nodes / 5853 links); appended +12 nodes / +10 edges to `kg/f6-findings.jsonl` (no-clobber). graphify still uninstallable in-sandbox (no LLM key for extraction). **Insight:** querying every stranger-path break/leak/lie invariant for "has an operable CI guard?" returns YES for all but ONE — the quickstart vendor-strip — and the NEW code this cycle (#1935) shipped *with* its own guard. The remaining beta risk is not "we don't write guards"; it is a single historical guard (`cp_citation_vendor_relevance`) built inert and never activated. **No code edited (audit-only; ledger patches staged).** Rotate to **Lens A** next run (round 7).



## Lens C — re-audit (2026-06-13, round 6 / C6 — audited origin/main @b2d74f7c, HEAD 98 behind)

**Verdict: 🟢 GREEN held.** Freshness guard exited 3 (STALE — `feat/realtime-datapoint-clock` 98 behind origin/main; `mira-bots/shared/` DIFFERS) → audited `origin/main` via `git show`, the deploy/beta truth.

**Headline: the engine layer is PROVABLY FROZEN since C5.** `git rev-list --count 4344e99a..b2d74f7c -- mira-bots/shared/` = **0**; same for `mira-pipeline/ignition_chat.py` = **0**. Of the 50 files changed C5→origin/main, 35 are `mira-hub/`, 5 `mira-scan-monday/`, the rest docs/CI — **none touch the engine or any grounded-chat surface.** C5's GREEN stands on byte-identical code; this run re-verifies the invariants and tightens the one open sub-item.

**Re-verified invariants (all clean on deploy truth):**
1. **Gate default-ON, single guarded call site.** `MIRA_UNS_GATE_ENABLED` defaults true (engine.py:336-340); the chat-gate fires from exactly one site — `engine.py:2002` `if uns_ctx.confidence > 0 and self._should_fire_uns_gate(...)`. No competing fast-path bypasses it for chat surfaces.
2. **Direct-connection carve-out is marker-gated, not blanket.** `_should_fire_uns_gate` (engine.py:5469) returns `False` ONLY when `uns_context["source"] == "direct_connection"`. Chat turns (`source` None) stay gated. Correct shape per `.claude/rules/direct-connection-uns-certified.md`.
3. **Ignition reject-on-missing-identifier contract SHIPPED** (hot.md still says "pending #1658" — stale). `ignition_chat.py:384-394`: asset-specific turn with no `asset_id`/`asset_context` → `422 {"error":"uns_required"}` (reject, NOT downgrade); general/educational fails open to plain chat; HMAC auth (401, lines 147-173) fronts the endpoint.
4. **Citation + groundedness wiring intact.** `citation_compliance` imported (19-21), enforced via rewrite (1227) + gap-admission (1238), checked (2628, 3474). Groundedness 1-5 scored (prompt L282) + low-groundedness clarify loop (2445-2543).

**The one tracked YELLOW sub-item (NOT beta-blocking):** direct-connection certification is **PRESENCE-based, not RESOLUTION-based.** `ignition_chat.py:384` stamps `direct_connection` on the mere *presence* of an `asset_id`/`asset_context`; it does not verify the token resolves to a stored UNS entity, because the `asset_context→(uns_path|equipment_id)` resolver is "NON-FUNCTIONAL until … lands" (ignition_chat.py:254-259) and `ENFORCE_ASSET_AGENT_GATE` is default-OFF (49-50). Chain: present-but-unresolvable token → marked `direct_connection` → engine skips the chat-gate (5469, trusts the marker without re-checking the path is resolved) → troubleshooting proceeds for an unverified asset.
  - **Why NOT beta-blocking:** (a) reachable ONLY via the HMAC-gated Ignition factory surface — a hub stranger-signup never sets `direct_connection` (chat path → gate fires); (b) groundedness/citations still apply (certification covers *where*, not *what*); (c) `ENFORCE_ASSET_AGENT_GATE` is the designed belt-and-suspenders, awaiting the resolver.
  - **Next action (pre-scale, master-plan Phase 6):** land the `asset_context→(uns_path|equipment_id)` resolver, then flip `ENFORCE_ASSET_AGENT_GATE` on — converts the presence-check into a resolution-check and closes the gap. Not a beta gate.

**Filed this run:** stale `process_full()` docstring (engine.py:1164-1165) claims `direct_connection` "does NOT by itself alter gate firing" — contradicts the live carve-out at 5469. Doc-only drift; staged patch `patches/c6-engine-docstring-direct-connection.patch` (apply-checked CLEAN vs origin/main) + `.md` sidecar.

**Secondary (Lens-D-owned):** the 06-08 eval cluster's false `SAFETY_ALERT` on `vfd_abb_03` is FSM over-qualification — it *over*-alerts (safe-fail: extra notification), it does NOT suppress a real safety stop. Tracked under Lens D, not a Lens C engine-integrity defect.

**KG insight (graphify graph.json @ kg/):** the top-3 god nodes by degree — `engine.py` (99), `Supervisor` (92), `process_full` (55) — ALL live in the single 5,673-line `mira-bots/shared/engine.py`. Maximal coupling concentration in one file is THE structural risk for engine integrity; the mitigant making it acceptable for beta is precisely that this file is frozen (0 commits since C5) and every safety-relevant path (gate, citations, groundedness) has a single guarded call site. KG delta: +11 nodes / +4 edges (`kg/c6-findings.jsonl`).

## Lens A — re-audit (2026-06-13, round 6 / A6 — audited origin/main @51e3f5b0, HEAD 96 behind)

**Verdict: 🟢 GREEN held + strengthened.** Freshness guard exit 3 (STALE, 96 behind) → audited `origin/main` via `git show`/`git grep`. Rotation: A oldest formal audit (A5 06-11; B5/C5/D5/E5/F5 all 06-12).

**Hub un-froze since A5** (A5 found it frozen). 18 commits touched `mira-hub/` (0 touched `mira-web/` → money-path auth GREEN by non-modification). Security-relevant, all verified on deploy truth:
- `security(hub)` **#1919** — CSV spreadsheet **formula-injection** neutralized in exports. `csv-export.ts` `neutralizeFormula()` documents DDE/HYPERLINK exfil; `api/export/route.ts` applies it. Stranger can't weaponize an export cell.
- `security(hub)` **#1903** — node-attachment chunks written **`is_private=true`**; hybrid read filter `(is_private=false OR tenant_id=$caller)` (migration 052). Cross-tenant chunk leak closed.
- **#1904** — fresh-tenant Feed no longer renders hardcoded **'Mike Harper · Admin'** to every tenant (only residue is the fix comment at `feed/page.tsx:169`). Identity-leak closed.
- **#1909** — `/magic` moved out of `(hub)` auth chrome → kills pre-auth 401 + nav leak; now public in matcher + `GATE_PATHS`.

**Controls re-verified (origin/main):**
- **Global edge auth** — `middleware.ts` decrypts next-auth JWE; non-allowlisted `/api/*` → 401 JSON, pages → `/login?callbackUrl`. CSP (per-request script nonce, strict allowlist) + HSTS preload + `X-Frame-Options: DENY` (except `/scan` marketplace) on **every** response incl. 401s.
- **Public allowlist self-auths** — `api/uploads/folder` requires `Bearer HUB_INGEST_TOKEN` + UUID `x-mira-tenant-id` (401 otherwise); `api/quickstart/ask` (anonymous North-Star chat) is per-IP-hash rate-limited (no raw IPs stored), reads only the public seeded corpus, strips cross-vendor conflicts. Rate-limit also on `auth/register`, `mira/ask`, `public/report` — the correct stranger-reachable set.
- **Secrets** — no hardcoded key/secret/password/token literals in `mira-hub/src` or `mira-web/src` (tests excluded).

**KG insight (graph.json @308e7b55, 3879n/5840e):** the two highest-degree hub nodes are both security primitives — `sessionOr401()` (deg 95) and `withTenantContext()` (deg 80); orphan scan = 6 hub degree-0 nodes, all test configs / JSX-edge blind spots, **zero unguarded API routes**.

**Non-blocker (low):** `quickstart/ask` rate limiter is in-memory/single-instance (code: 'port to shared store / quickstart_rate table' if hub scales horizontally). Fine for single-VPS beta; revisit before multi-instance.

**Next action:** none required for beta. Optional pre-scale: back `quickstart/ask` + `public/report` limiters with a shared `quickstart_rate` table. Rotate to **Lens B** next run (B5 06-12 now oldest after A6).

---

## Reconciliation — 2026-06-09 (audited origin/main, not the stale branch)

The round-2 lens sections below (A2–F2) were generated from the checked-out working tree
`feat/orchestrator-kg-query`, **51 commits behind `origin/main`** at audit time. Re-verified against
`origin/main` (what `deploy-vps.yml` ships and a beta tester hits), every blocker is closed:

- **PR #1837** (`7792c097`) — rate-limit public LLM doors (#1832), close `/api/documents` cross-tenant leak (#1833), gate the signup money path.
- **PR #1845** (`f81a7381`) — citation relevance enforce-mode (P0-3), deterministic offline-eval seam (P2-1), migration ledger (P3), ADR-0017 transition helpers (D-1).

Verified on origin/main this session: rate limiter present; `tenant_id=$2` predicate present; smoke gate runs the money-path spec; `citation_compliance.py` has `evaluate_citation_relevance()` / `strip_conflicting_citations()` + `MIRA_CITATION_ENFORCE`; `proposals/route.ts` reads `ai_suggestions` (×6); `llm_replay` initializes `mode=replay strict=True` with no keys; `048_schema_migrations_ledger.sql` is the migration head.

**Net: scorecard flips 🔴 RED → 🟢 GREEN.** Residual non-blocking items: **D** — determinism is partial (engine-internal nondeterminism remains, per #1845); **E** — 8 duplicate migration prefixes persist (low-risk now the ledger records applied files); **C** — Ignition direct-connection reject-contract (#1658) is post-beta.

**Process fix (root cause of the false RED):** the audit read a stale branch. `wiki/orchestrator/freshness-guard.sh` + `FRESHNESS_FIX.md` make every future run fetch and audit `origin/main`. Verified: the guard reports "behind origin/main by 51" and exits 3 on this branch.

> The A2–F2 sections below are retained for provenance but are **superseded by this reconciliation.**


## Lens A — re-audit (2026-06-10, round 4 / A4 — audited origin/main @a98ff5ea, HEAD 65 behind)

**Verdict: 🟢 GREEN (held).** Rotation: A was the oldest focused audit (A3 ran 06-09; B/C/D/E/F all rotated 06-10). Freshness guard exited 3 (STALE, **65 behind**) → audited `origin/main` via a detached worktree (`git worktree add --detach /tmp/main-audit origin/main`), the deploy/beta truth; the working tree was never the audit target.

**1. Middleware / auth surface — no regression.** `src/middleware.ts` (239 L) runs in the edge runtime, decodes the next-auth JWE with top-level `jose` (HKDF via Web-Crypto), sets a per-request CSP nonce + HSTS + `X-Frame-Options: DENY` (except `/scan`, the monday.com marketplace iframe, which uses `frame-ancestors`). For unauthenticated `/api/*` it returns **401 JSON** (not an HTML redirect — matches `sessionOr401()`); for pages it redirects to `/login?callbackUrl=`. The `config.matcher` excludes exactly the intended public families: `login|signup|magic|m/|quickstart|api/auth|api/public|api/quickstart|api/health|api/scanbe/healthz|api/uploads/folder|api/uploads/[a-f0-9]`. **0 new auth-orphans** vs A3 — the only route changed since A3 is `quickstart/ask`, and it was already public.

**2. Secrets scan — clean.** A value-bearing scan (`(api_key|secret|password|token|bearer)\s*[:=]\s*"<16+ chars>"`) across `mira-hub/src` + `mira-web` returns a single hit — `secret = "test_hmac_secret_value"` in `mira-web/src/routes/__tests__/inbox.test.ts` — an HMAC **test fixture**, not a prod default. No hardcoded prod secrets.

**3. Material cross-lens finding — F3/C3's last blocker is CLOSED on deploy truth.** `origin/main` advanced 3 commits since A3 (f90bdcc→a98ff5ea); the load-bearing one is **#1858 `fix(hub): cross-vendor citation conflict-strip in quickstart/ask (C2)`**. The route now imports `stripConflictingVendors` (`route.ts:12`) and calls it at **`route.ts:151`** (`chunks = stripConflictingVendors(chunks, manufacturer ?? question)`) **before** mapping chunks into `citations[]` at **`route.ts:181`**. `lib/vendor-relevance.ts` canonicalizes both the query vendor and each chunk's `manufacturer` through a `VENDOR_ALIASES` table (ports the Python `CROSS_VENDOR_FILTER` from `rag_worker.py`), drops cross-vendor chunks, keeps untagged/generic, never strips to empty. A stranger asking about a thinly-covered vendor can no longer be shown another vendor's manual as authoritative. **This closes F3's lone holdout and C3's YELLOW driver, and supersedes the staged `2026-06-10-quickstart-wrong-vendor-refuse.patch`** — conflict-strip is strictly better than refuse-on-fallback (it still answers when relevant generic chunks exist). Flagging it here (not deferring to F/C rotations) is the honest move: I read the wired call on the deploy branch.

**4. Public-path DoS hardening — #1859.** `perf(hub): bound BM25 query to 32 terms — kills the OR-fanout blowup (#1766)` caps the term-count of the unauthenticated `quickstart/ask` BM25 query, removing a query-complexity resource-exhaustion vector on the public surface.

**Residuals (NB, none stranger-facing-blocking):**
- **`uploads/folder` bearer compare is not constant-time** (`route.ts:30`, `token !== expected`) — A3 NB#2, **still open**. Token is high-entropy and network jitter dominates, so timing leakage is marginal; fix with `crypto.timingSafeEqual` (length-pad first). NB, not a blocker.
- **quickstart limiter remains in-memory/per-instance** (`route.ts:22`, comment acknowledges) — fine for the single-container hub; port to a DB-backed `quickstart_rate` table (the `public/report` pattern) before horizontal scale.
- A3 NB#3 (admin-review-queue escaping of guest-report text) not re-examined this run — carry forward.

**Why GREEN, not YELLOW:** every public (cookie-exempt) route self-authenticates (bearer), is rate-limited (IP-hash 429), or is public-tenant-pinned + now vendor-scoped; secrets are clean; the one open residual is a marginal timing side-channel on a high-entropy service token. No stranger-reachable break, leak, or live lie on origin/main.

**KG (this run, hand-extracted +7 nodes / +10 edges → 3763 / 5706, `_lens:"A4"`; graphify CLI absent + no LLM key — GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset, same as every prior run):** the two highest-degree hub nodes are the security chokepoints `sessionOr401()` (degree 93) and `withTenantContext()` (degree 80) — hub auth/tenant-isolation is centralized, which structurally explains the recurring "0 auth-orphans." New edges record the closure: `a4_quickstart_ask_r4 --closes--> f3_lie_wrongvendor`, `--supersedes--> f3_patch_refuse`, `--intentionally_bypasses--> lib_session_sessionor401` (the public-path carve-out), and `a4_bm25_termbound --hardens--> a4_quickstart_ask_r4`. **KG insight:** the entire former beta gap (one orphan patch edge in F3) is now resolved by a *merged* commit, not a staged patch — the ledger's last apply-ready orphan is retired.

---

## Lens F — re-audit (2026-06-10, round 3 / F3 — audited origin/main @f90bdcc, HEAD 61 behind)

**Verdict: 🟢 GREEN → 🟡 YELLOW (re-opened).** Rotation: F was the oldest focused audit — F2 ran 06-09 round 2 and round 3 had since covered A/B/C/D/E. Freshness guard exited 3 (STALE, **61 behind**) → audited `origin/main` via `git show`, the deploy/beta truth. F is the meta-lens: it asks the North-Star question directly — *can a stranger sign up, use grounded chat, and have nothing break, leak, or lie today?* The 06-09 reconciliation answered "yes, 0 blockers." Re-verifying each blocker in **live origin/main code** (not the prior write-up), the honest answer is **almost** — one lie persists.

**Per-blocker re-verification on origin/main (deploy truth):**

1. **[BREAK — CLOSED] `quickstart/ask` rate-limit.** `quickstartRateLimited(ipHash)` returns **429** before any work (per-IP-hash, 20/min, in-memory); `route.ts` short-circuits. Cascade-drain is further bounded by provider fallthrough + the smoke 429 assertion. ✅
2. **[LEAK — CLOSED] `/api/documents` IDOR.** `cmms_equipment ... WHERE id = $1 AND tenant_id = $2` (tenant-scoped, no cross-tenant asset resolution) + the hybrid `knowledge_entries` read filter `(is_private = false OR tenant_id = $1)` (shared OEM corpus visible, private uploads owner-only). #1833 landed. ✅
3. **[GATE — CLOSED] money-path smoke.** `smoke-test.yml` runs `playwright.smoke.config.ts` asserting pricing→Stripe 303, **`/quickstart` returns an answer (P1-1)**, **`/api/quickstart/ask` flood→429 (P0-1)**. E2's "/login-only" finding is gone. ✅
4. **[ENGINE LIE — CLOSED on the engine] citation relevance.** `citation_compliance.evaluate_citation_relevance()` (alias-aware) + `strip_conflicting_citations()` + `check_citation_compliance(..., enforce=...)` with `citation_enforce_enabled()` default-ON. Wired on both engine emit paths (C3). ✅ *but reaches only the engine path, not the hub.*
5. **[LIE — STILL OPEN] wrong-vendor citation on the public money path.** `POST /api/quickstart/ask` grounds via the TS `retrieveManualChunks`, which — when a `manufacturer` is supplied but the scoped BM25 returns 0 hits — **silently falls back to an unscoped cross-vendor query** (`manual-rag.ts:53-56`) and the route maps **every** returned chunk into `citations[]` (`route.ts:172`) with no vendor filter and no `strip_conflicting` equivalent. A stranger asking about a thinly-covered vendor can be shown another vendor's manual as authoritative. Severity: a **lie**, not a break/leak; bounded to one surface; no cross-tenant exposure; the cite-or-refuse system prompt is a weak mitigation (fallback chunks read topically relevant). **YELLOW, not RED.**

**What changed vs F2:** F2 (06-09 r2) found this lie as a blocker with **no inbound patch edge** (the only one of the trio with no staged fix — `f2_fix_citrelevance` was itself `_missing`). The other two F2 patches' targets are now **closed on main** (PR #1837/#1845). So this run the bottleneck for the *last* blocker is no longer "unknown fix" — it's "unbuilt patch," and F's job is to build it. **Staged `patches/2026-06-10-quickstart-wrong-vendor-refuse.patch`** (+ `.md`): adds `retrieveManualChunksScoped()` returning `{chunks, fellBack}`; `quickstart/ask` refuses honestly on fallback (deterministic, empty citations, no LLM call); `retrieveManualChunks` kept as a delegating wrapper so the authenticated `assets/[id]/chat` caller is unchanged. Generated via `git diff` in a throwaway `origin/main` worktree (not hand-counted hunks); `git apply --check` ✅, reverse-check fails (not already applied). Not applied in place (founder-gated — live money-path route; matches C2/D2/C3 restraint).

**Why YELLOW, not RED:** no stranger-facing **break** or **leak** is live on origin/main today — only one bounded, prompt-mitigated, single-surface **lie**. **Why YELLOW, not GREEN:** holding GREEN would repeat the reconciliation's documented over-claim; the lie is real until the patch lands.

**KG (this run, hand-extracted +14 nodes / +15 links → 3756 / 5696, `_lens:"F3"`; graphify still needs an LLM key, unavailable — GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset):** the ledger is now **one apply-ready orphan** — `f3_patch_refuse` carries a `fixes` edge **into** `f3_lie_wrongvendor` and a paired `_missing merge_to_main` edge **out** (same orphan-edge fingerprint as `uns_required`/C1, `ai_suggestions`/B2, `signup-flow.spec`/E2 — a validated artifact with no edge to where it takes effect). The four closed blockers each now carry a `closes → f3_north_star` edge. The query `patch nodes with (fix-edge IN + _missing merge OUT)` returns exactly `[f3_patch_refuse]`: **the entire remaining beta gap is a single PR.** One coupling edge worth surfacing: `f3_eval_blind --cannot_catch--> f3_lie_wrongvendor` — D3's inert replay seam means even the landed `cp_citation_vendor_relevance` grader can't regression-guard this fix until the seam is operable.

## Lens E — re-audit (2026-06-10, round 3 / E3 — audited origin/main @f90bdcc, HEAD 61 behind)

**Verdict: 🟡 YELLOW → 🟢 GREEN.** Rotation: E was the oldest focused audit — E2 ran 06-09 round 2 (before F2), and round 3 had since covered A/B/C/D; E/F still sat at round 2. Freshness guard exited 3 (STALE, **61 behind**) → audited `origin/main` via `git show`, the deploy/beta truth. The 06-09 reconciliation had already flipped the *table* to GREEN on the migration-ledger alone; E3 **earns** the GREEN with a focused audit and finds the substantive driver of E2's YELLOW — the missing staging environment — is now built and wired.

**1. The staging environment E2 booked as TODO is BUILT (the load-bearing change).** E2 (06-09, round 2) found `docker-compose.staging.yml` absent and `@MiraStagingBot` unbuilt — the canonical "staging is TODO" gap. On `origin/main` today:
- `docker-compose.staging.yml` (106 lines) — CHARLIE bot-path-only stack (`mira-pipeline-staging` :9098, `mira-bot-telegram-staging`), reads `NEON_DATABASE_URL` from `factorylm/stg`, runs `@MiraStaging_bot` via `STAGING_TELEGRAM_BOT_TOKEN`, `restart: "no"`, 256 MB–1 g caps. Spec: `docs/specs/staging-environment-spec.md`; runbook: `docs/runbooks/staging-environment.md`.
- `docker-compose.staging-vps.yml` — the full stg-* VPS stack `deploy-staging.yml` ships (separate Docker network + offset 4xxx ports, prod saas.yml untouched).
- New workflows: `staging-gate.yml`, `deploy-staging.yml`, `deploy-nginx-staging-passthrough.yml`, `staging-namespace-create-e2e.yml`.

**2. `staging-gate.yml` is a real PR-to-main gate, ordered correctly.** It instantiates Supervisor in-process against the NeonDB staging branch, runs the 10–15-question rubric eval through the Groq cascade (`tools/staging_test.py`), and posts a sticky PR comment. It has a Scope-check (#1680) that skips the eval (still exits `success`) for docs/CI/wiki/.claude-only and Dependabot PRs, so the required check stays green without a flaky LLM eval blocking unrelated PRs. It explicitly **waits for `Migration Verify`** on the head SHA before grading when migration files changed (P3-3 — never grade a pre-migration schema). It **fails closed**: `staging_test.py` exits 2 if `STAGING_TENANT_ID` is unset, so a missing secret can't silently target the prod tenant.

**3. Deploy gate is intact and STRONGER than E2 measured.** `deploy-vps.yml`:
- Triggers on `workflow_run: ["Smoke Test"]` and only proceeds when `github.event.workflow_run.conclusion == 'success'`.
- Adds a **"Verify Staging Gate passed"** step (L119): resolves the deploy SHA → its source PR → queries the most-recent `Staging Gate` run on the PR head SHA, and requires `completed:success`. **Direct pushes to main are refused** (no PR ⇒ no graded gate ⇒ no deploy).
- `skip_staging_gate=true` hotfix bypass requires a non-empty `skip_reason` and **auto-opens an audit issue** with a 24-hour follow-up checklist (the exact CLAUDE.md doctrine).

**4. E2's central RED-adjacent finding — "smoke gate verifies /login render, not the signup→chat money path" — is CLOSED.** `smoke-test.yml` runs `npx playwright test --config playwright.smoke.config.ts`; its summary table asserts: `factorylm.com/pricing` (cards + Start-Free-Trial CTA), `/api/checkout/session` (303 → checkout.stripe.com), `app.factorylm.com/ → /login`, **`/quickstart`** (public grounded chat returns an answer — money path, **P1-1**), and **`/api/quickstart/ask`** (flood → HTTP **429**, rate-limit holds, **P0-1**). E2's staged patch `2026-06-09-smoke-gate-signup-moneypath.patch` (which wired the *separate* `playwright.signup.config.ts`) is **superseded** — the money-path checks were folded directly into the CI-wired smoke config, a cleaner fix than wiring a second config.

**5. Migration ledger is operable, not just present.** `apply-migrations.yml` (`MIG_DIR=mira-hub/db/migrations`, the deploy tree): self-bootstraps `schema_migrations (migration_name UNIQUE)` so skip-logic works even before 048 runs; in `all` mode queries applied filenames and `grep -qxF`-skips them; `seed-ledger` mode marks current files applied without running (baseline for an existing DB). This is the fix for the #1657 per-file `IF NOT EXISTS` re-run-everything red streak E2 described.

**Residuals (all cosmetic / low-risk — none stranger-facing, none deploy-breaking):**
- **8 dup prefixes persist** (006/008/021/025/026/027/032/033) — exactly E2's set, within-prefix order = alpha-by-description. Harmless now the ledger keys on full filename.
- **040–042 gap** — hot.md's planned "source-preservation layer (040–042)" never landed (038, 039, then jump to 043). Abandoned/renumbered; `ls | sort` apply order tolerates gaps.
- **`docs/migrations` orphan tree** (head 008) still in the repo with no apply workflow — superseded by the ledger; root `CLAUDE.md` on main no longer inlines a `docs/migrations/004_kg_entities.sql` citation (grep clean).
- **on-main `hot.md` head claim stale** — says `head=037`; actual head is `049_grant_app_knowledge_entries_insert.sql`.
- **`signup-flow.spec.ts` is a redundant orphan** — its config (`playwright.signup.config.ts`) is wired to 0 workflows, but the money-path coverage it was written for is now duplicated in the CI-wired `smoke.spec.ts`. It's a delete candidate, not a coverage hole.

**Why GREEN, not YELLOW:** the staging pipeline is built, the deploy gate enforces both Smoke and Staging-Gate success on the PR head, the money-path is smoke-asserted, and the ledger is operable — the four things a clean promotion pipeline needs. Unlike D3's replay seam (wired into **0** workflows ⇒ provably inert), every E gate is wired and the logic is concrete shell I read, not a filename. The single thing I can't verify from the sandbox — that the `staging` environment secrets are populated — fails **closed**, so the worst case blocks a deploy rather than leaking or silently passing. That is an ops checkbox, not a beta-blocker.

**Next action:** none blocking. Founder/ops, in 30 min: (1) confirm the `staging` GitHub-environment secrets exist (`NEON_STG_DATABASE_URL`, `STAGING_GROQ_API_KEY`, `STAGING_TENANT_ID`, `STAGING_CEREBRAS/GEMINI_API_KEY`) so the gate runs rather than fail-closed-blocks; (2) one cosmetic-cleanup PR: dedupe the 8 prefixes, delete the redundant `signup-flow.spec.ts`, refresh the `hot.md` head line. F next rotation: re-verify the narrow `quickstart/ask` wrong-vendor citation slice (C3) now that the money-path is smoke-gated.

**KG insight (E3, hand-extracted +18 nodes / +17 edges → 3742/5681; graphify CLI absent + no LLM key in sandbox, same as every prior run):** the deploy gate is now a **fully-wired triangle**, the *inverse* of the recurring orphan-edge fingerprint — `wf_deploy_vps` carries **two** inbound gate edges (`wf_smoke_test --gates-->` and `wf_staging_gate --gates-->`), and `wf_staging_gate` is `orders_before`-gated by `wf_migration_verify` (migration-verify → staging-gate → deploy-vps ← smoke). The one surviving orphan is `e2_spec_signup` (`signup-flow.spec.ts`): its only callers are its own config + `smoke.spec.ts`, and its config→workflow edge is still `_missing` — BUT a new `money_path_moved_to` edge to `e2_cfg_smoke` records that the coverage relocated to the wired smoke spec, so the orphan is **defanged** (redundant test), not a coverage gap. Two E2 blocker-fingerprint nodes got `CLOSES`/`mitigates` edges this run: `e3_smoke_moneypath --CLOSES--> bf_smoke_moneypath` (#3) and `e3_ledger_skip --CLOSES--> bf_pr1657_redstreak` (#6) / `--mitigates--> bf_migration_ledger` (#5).

---

## Lens D — re-audit (2026-06-10, round 3 / D3 — audited origin/main @f90bdcc, HEAD 61 behind)

**Verdict: 🟢 GREEN → 🟡 YELLOW (re-opened).** Rotation: D was the oldest focused audit — round 3 had covered A/B/C; D/E/F still sat at their 06-09 round 2, and the round-2 order was C2→D2→E2→F2, so D is the genuinely oldest. Freshness guard exited 3 (STALE, **61 behind**) → audited `origin/main` via `git show`, the deploy/beta truth.

**The 06-09 reconciliation flipped D to GREEN on the strength of PR #1845's "deterministic eval seam … verified mode=replay strict=True with no keys." That verification proved the seam *installs* — not that it can *serve a single response*. It cannot.**

1. **The replay seam is real and well-built.** `tests/eval/llm_replay.py` monkeypatches *both* edges that make the suite nondeterministic: the LLM cascade (`shared.inference.router`) and NeonDB retrieval (`shared.neon_recall.recall_*`). Keys are content-addressed sha256 over normalized messages (ISO-8601 datetimes + UUIDs stripped), grouped per-scenario. `offline_run.py` exposes `--replay {live,record,replay}` (default `live`) and installs with `strict=True` unless `MIRA_EVAL_REPLAY_LOOSE=1`. The `cp_citation_vendor_relevance` grader (`grader.py` + `test_grader_citation.py`) **also landed** — a genuine D improvement over D2, since it finally makes the wrong-vendor lie (C3/F#2) eval-detectable. So far this all earns the GREEN.

2. **…but the recorded store is `.gitignore`d, so replay is inert on a fresh checkout.** The stores live at `tests/eval/fixtures/llm_replay/cascade.json` + `retrieval.json` — and `.gitignore:239-241` excludes them by design ("Eval record/replay stores — record locally (`MIRA_EVAL_REPLAY=record`); not a [committed artifact]"). Neither file exists anywhere on `origin/main`. With an empty store and `strict=True` (the default), `replay` mode finds `by_scenario.get(scn, []) == []`, increments `stats["misses"]`, and **raises `RuntimeError` on the first LLM call of the first scenario** (`llm_replay.py:136-138`). The "key-free deterministic eval" only runs on a developer box that has first run `--replay record` (which needs keys + DB). The recording step never lands in CI.

3. **No CI workflow passes `--replay` at all.** `ci-evals.yml` runs a *different* harness — `tests/synthetic_eval.py --mode dry-run --threshold 0.80` — not `offline_run.py`. `deepeval-ci.yml` runs **live**, passing `secrets.GROQ_API_KEY` (and documents that Dependabot PRs skip it for lack of the secret). So `offline_run --replay replay` is wired into **zero** workflows; the promotion-signal eval is still the live, nondeterministic path D1/D2 measured (band 47–51/57, spread 4, 19 flappers).

4. **The eval-fixer phantom-work harm D2 observed is unaddressed.** `fix_proposer_tasks.py` still reads the `runs/` scorecard directory (`FIX_PROPOSER_RUNS_DIR`, L100-102) with no median/multi-run gating — it consumes single snapshots, which is how adjacent noise-band samples became issues #1773 (46/57) and #1788 ("50/57 up from 46/57"). The latest committed scorecard in `tests/eval/runs/` is `2026-06-08T0229-offline-text.md` — a *live* run; no replay-mode scorecard has ever been committed, corroborating that nobody runs the deterministic path in the gate.

**Why YELLOW, not RED:** this is a **measurement-integrity** gap, not a stranger-facing break/leak/lie. The beta-critical checkpoints are bit-stable in D2's 9-run sample (`No5xx`, `TurnBudget`, `RState` 0/513; both safety fixtures 9/9), so nothing the eval measures actually breaks the stranger's product today. The promotion gate is just **blind to ±2-case regressions** and **feeds the autofix loop phantom work**. **Why YELLOW, not GREEN:** holding GREEN would repeat the reconciliation's documented over-claim — the determinism fix is shipped-but-unplugged at *both* ends (no committed data, no CI wiring).

**Next action (founder-gated, no mechanical patch staged — matching C2/D2 restraint; the real fixes touch the live inference path, the routine config, and a deliberate `.gitignore`/record decision):**
1. Run `offline_run --replay record` once (keys + DB), then either **commit** `fixtures/llm_replay/{cascade,retrieval}.json` or **un-`.gitignore`** them — the store must reach CI.
2. Add a CI step (e.g. in `deepeval-ci.yml` or a new keyless job) running `offline_run --replay replay` so the deterministic, no-key path becomes the actual promotion signal.
3. Gate the eval-fixer routine on a **3-run median**, not a single scorecard snapshot, so adjacent noise-band samples stop minting phantom issues.

The wrong-vendor lie slice (the `cp_citation_vendor_relevance` checkpoint can now catch it, but only if the seam it runs under is operable) ties D directly to C3/F — D's inert determinism is *why* the eval can't yet prove or disprove the C3 `quickstart/ask` lie.

---

## Lens C — re-audit (2026-06-10, round 3 / C3 — audited origin/main @3a31703f, HEAD 58 behind)

**Verdict: 🟡 YELLOW (was 🟢 GREEN — re-opened, narrowly).** Rotation: C was the oldest focused audit (C2 ran 06-09 round 2, before D2/E2/F2; A and B both got a round 3 after). Freshness guard exited 3 (STALE, **58 behind**) → audited `origin/main` via `git show`, the deploy/beta truth.

**The engine itself (C's literal subject, `mira-bots/shared/`) is GREEN and IMPROVED:**
- **UNS chat-gate intact.** `_should_fire_uns_gate` (engine.py:5372) checks the `direct_connection` carve-out FIRST (L5407 — returns False before the gated-intent / IDLE checks), so a certified connection can never be interrupted with a "which machine?" prompt. `_UNS_GATE_ENABLED` default true (L342); `_GATED_INTENTS = {diagnose_equipment, schedule_maintenance}` (L349).
- **Citation relevance is enforce-mode and WIRED on both engine emit paths.** `_check_citation_compliance(..., enforce=_citation_enforce_enabled())` at engine.py:2602 **and** 3436, each applying `sanitized_reply`. `citation_enforce_enabled()` defaults ON (citation_compliance.py:42, `MIRA_CITATION_ENFORCE!=0`). `evaluate_citation_relevance()` (L84) is alias-aware + fail-open; `strip_conflicting_citations()` (L126) removes wrong-vendor `[Source:]` tags + appends an honesty note.
- **Groundedness scoring intact** (1–5 dims; low-groundedness clarify episode at engine.py:2413–2511).
- **C1/C2 Ignition open item is now CLOSED on main — early (it was booked "post-beta").** `ignition_chat.py` ships the Phase-6 reject contract: an asset-specific turn carrying NO resolvable UNS identifier returns `JSONResponse(422, {"error":"uns_required"})` — "rejecting (not downgrading to chat-gate)" (L384–394); `_is_asset_specific()` (L90) fails OPEN so general/educational turns are never rejected; the surface is HMAC-gated. The long-standing "ignition_chat:208 downgrades id-less turns" finding (C1/C2) no longer holds.

**Why YELLOW: the beta money-path chat still bypasses every one of those guarantees — and the citation-relevance parity the 06-09 reconciliation declared "closed" did NOT fully land.** The stranger-reachable surface is `POST /api/quickstart/ask` (public, no-auth, IP-hash rate-limited). On origin/main it:
- grounds via the **TypeScript** `manual-rag` + `cascadeComplete` impl, NEVER `engine.process` (imports `@/lib/llm/cascade` + `@/lib/manual-rag`; no engine call anywhere in the route);
- returns **all** retrieved chunks as `citations[]` 1:1 (`chunks.map`, route.ts:172) — not the subset the model actually cited via `[n]` markers;
- has **no** `evaluate_citation_relevance` / `strip_conflicting_citations` equivalent.

The concrete lie vector is the manufacturer-scope **fallback** in `retrieveManualChunks` (manual-rag.ts:53-56): *"tries a manufacturer-scoped query first; falls back to a tenant-only query if zero hits or no manufacturer."* When a manufacturer IS supplied it's a hard filter (`AND manufacturer ILIKE`, L68-70) — safe. But if the public corpus has **zero** matching chunks for the question, the filter is **dropped** and the top-6 chunks from OTHER vendors become citation cards on an answer about the named vendor. The engine path would catch exactly this (expected vendor recognized → strip); the hub path emits it. The reconciliation's "quickstart/ask carries citation refs ×6" credited the wrong file — the ×6 is `proposals/route.ts` reading `ai_suggestions` (a B/ADR-0017 item); `quickstart/ask` imports no citation-compliance helper at all.

**Scope / severity:** a "lie" (wrong-vendor citation), not a break or a leak; low-to-moderate probability (needs a thinly-covered manufacturer + a query that still returns 6 willing-to-use chunks); the cite-or-refuse system prompt is a weak mitigation here because the fallback chunks look topically relevant (generic VFD/overcurrent text). No cross-tenant exposure — all chunks come from the single public quickstart tenant. YELLOW not RED: bounded, one surface, prompt-mitigated; but it touches **citations on the signup→chat money path**, which the ledger treats as beta-relevant. YELLOW not GREEN: holding GREEN would repeat the reconciliation's documented over-claim.

**Next action (founder-gated — edits the live money-path route, deliberately NOT hand-patched this run, matching C2/D2 restraint):** in `quickstart/ask`, when a `manufacturer` was supplied but `retrieveManualChunks` fell back to unscoped (every returned chunk's `manufacturer` ≠ requested), **refuse** ("I don't have {mfr} manuals for that in the public KB — sign up to upload your own") rather than answer off other vendors' chunks; minimally, filter `citations[]` to the requested vendor so wrong-vendor cards never render. Cheapest correct locus: return a `fellBack` flag from `retrieveManualChunks` and branch in the route. F should re-verify this narrow slice next rotation (it re-opens a thin slice of the closed F#2 lie blocker — do not double-count it as a full RED).

**KG insight (C3, hand-extracted +13 nodes / +14 edges → 3710/5649; graphify CLI absent + no LLM key in sandbox, same as every prior run):** same recurring orphan-edge fingerprint, now on the citation control. `q_citations_array --maps_all_chunks-->` exists, but its paired `--relevance_filter--> strip_conflicting_citations` edge is **`_missing`**, and `q_ask_route --shares_control--> check_citation_compliance` is **`_missing`** too — the engine emit node carries both edges, the hub beta node carries neither. Two grounding subgraphs, only one wired to the relevance control — identical topology to uns_required(C1), ai_suggestions(B2), signup-flow.spec(E2), canary(B3). **One orphan edge CLOSED this run:** `ign_chat_route --rejects_on_missing_uns--> 422` now exists — the C1 missing edge is filled (#1658 landed).

**Next action:** none RED. (1) Founder-gate the `quickstart/ask` refuse-on-fallback fix above. (2) F re-verifies the narrow citation-lie slice next rotation. (3) Engine + Ignition: no action — GREEN.

---

## Lens B — re-audit (2026-06-10, round 3 / B3 — audited origin/main @475f4e15, HEAD 52 behind)

**Verdict: 🟢 GREEN (held).** Rotation: B was the oldest focused audit (B2 on 06-09; A3 ran last). Freshness guard exited 3 (STALE, 52 behind) → audited `origin/main` via `git show`, the deploy/beta truth.

**The B2 primary drift is genuinely CLOSED on origin/main (confirmed, not just claimed):**
- `mira-hub/src/lib/proposal-transition.ts` exists — `applyHubProposalTransition()` writes `relationship_proposals` (L98) AND the paired `ai_suggestions` row (L117/L127, matched on `extracted_data->>'relationship_proposal_id'`) in one txn, so the two Hub projections can't drift via the Hub path.
- `decide/route.ts` **imports** the helper (L4) and delegates to it. The remaining raw `UPDATE kg_relationships SET approval_state='verified'` (L138) is the **engine-side** projection the helper explicitly disowns (helper docstring: "kg_*.approval_state is NOT written here") — by design, not an ADR-0017 violation.
- `proposals` GET reads both tables (`relationship_proposals` L158, `ai_suggestions` L222) — the ×6 refs the reconciliation cited are real.
- Python twin `mira-bots/shared/proposal_transition.py` + both test files + `proposal-transition.test.ts` present.

**The open scorecard item is RESOLVED: the 3rd canary check does NOT exist — and there is a live writer that can introduce the exact drift it would catch.** `tests/canary/proposal_state_drift.sql` on origin/main has exactly **two** checks, both forward-direction: (1) `accepted_suggestion → unverified_proposal`, (2) `verified_edge → unverified_proposal`. Neither fires on the **reverse** drift (terminal `relationship_proposals` + stale `pending` `ai_suggestions`). That reverse drift is not merely theoretical:
- The **engine** helper `proposal_transition.py` writes `relationship_proposals.status` (accept→verified, reject→rejected, supersede→deprecated) and **disowns `ai_suggestions`** (L7-8 docstring). Its lockstep partner is only the *Hub* TS helper — engine-triggered transitions have **no** `ai_suggestions` projection.
- `proposals` GET now **renders `ai_suggestions`**, so a stale `pending` for an already-terminal proposal is **user-visible** in the admin queue — a review-state "lie."
- **Subtlety that makes the obvious check wrong:** `flag_review`→`reviewed` and `contradict`→`contradicted` map to `aiSuggestion='pending'` *by design* (per `PROPOSAL_TRANSITIONS`). A naive `status<>'proposed' AND suggestion='pending'` check would false-positive on healthy data. The correct invariant flags only `verified`/`rejected`/`deprecated` proposals with a `pending` suggestion. → Staged exactly that as `patches/2026-06-10-canary-reverse-drift-check.patch` (legacy-safe join on the explicit link; `git apply --check` ✅ vs origin/main; yields 3 `@check` blocks).

**Why GREEN not YELLOW:** the B2 *primary* drift (Hub decide never wrote `ai_suggestions`) is closed; the residual is (a) a *detective-control* gap (the missing canary check, now patch-ready) over (b) an *admin-only* surface, reachable only when the engine triggers a terminal transition on a proposal that has a paired suggestion — not the stranger's first-five-minutes signup→chat path. The *preventive* fix (project engine terminal transitions onto `ai_suggestions`) edits the live engine write path → founder/CI-gated, deliberately not patched here; the canary should land first as its regression guard.

**Playwright coverage gap (B sub-area):** of 6 hub Playwright configs, only `playwright.smoke.config.ts` is wired into a workflow (`smoke-test.yml` L64). `command-center`, `signup`, `onboarding-validate`, `onboarding-walkthrough` configs are referenced by **0** workflows; `enforcement-audit.yml` and `staging-namespace-create-e2e.yml` run individual specs directly, not these configs. So the **post-signup beta surface** (command-center — what a stranger lands on) and the full onboarding walkthrough have **no CI regression gate**. Not "broken today" (manual screenshots exist) but unguarded during beta. Wiring `playwright.command-center.config.ts` into a staging-gated job is the highest-value follow-up.

**KG insight (B3, hand-extracted +18 nodes/+18 edges → 3697/5635; graphify still blocked — no LLM key in sandbox):** the drift is a graph fork. `ai_suggestions` now has **2 inbound write edges** — `applyHubProposalTransition --writes_lockstep-->` (good) and `proposals_get --reads-->` (the new render surface) — but the engine helper `proposal_transition.py --writes--> relationship_proposals` has a paired **`_missing` edge** `--DISOWNS--> ai_suggestions`. The canary node carries 2 `has_check` edges and a 3rd **`_missing` `MISSING_check`** edge whose target would intercept exactly that disowned-write path. Same orphan-edge fingerprint the orchestrator keeps surfacing (uns_required C1, ai_suggestions B2, signup-flow.spec E2): *a write/transition with no edge to the control that should observe it.* The staged patch adds the one missing canary→drift edge. Parallel: 3 Playwright configs are orphan nodes (config exists, no `runs` edge from any workflow).

**Next action:** none beta-blocking. (1) Apply `patches/2026-06-10-canary-reverse-drift-check.patch` (detective, zero-risk). (2) Founder-gate the preventive engine-side `ai_suggestions` projection (separate PR, after the canary). (3) Wire `playwright.command-center.config.ts` into a staging-gated workflow.


## Lens A — re-audit (2026-06-09, round 3 / A3 — audited origin/main @475f4e15, HEAD 52 behind)

**Verdict: 🟢 GREEN (held, no regression).** Rotation: A was oldest (its last *focused* audit was A2 on 06-08; the 06-09 reconciliation was a sweep). Freshness guard exited 3 (STALE, 52 behind) → audited `origin/main`, the deploy/beta truth, via `git show origin/main:<path>`.

**Both prior Lens-A blockers confirmed fixed on origin/main (no regression):**
- **#1 break** — `/api/quickstart/ask` carries the per-IP-hash rate limiter (`quickstartRateLimited()` L24; `x-forwarded-for` → sha256 ipHash L103; **429 at L107**) — PR #1832/#1837.
- **#8 leak** — `/api/documents` GET resolves `cmms_equipment` with `WHERE id = $1 AND tenant_id = $2` (L44) — the IDOR is closed; in-file comment even cites #1833.

**Full public-surface sweep (the right beta question — what can a stranger reach?).** Middleware (`middleware.ts`) returns **401 JSON for any unauth `/api/*`** (L178-196) and its `matcher` excludes exactly 7 public prefixes (L237): `api/auth`, `api/public`, `api/quickstart`, `api/health`, `api/scanbe/healthz`, `api/uploads/folder`, `api/uploads/[a-f0-9]`. Each excluded/public route was read on origin/main and **every one self-guards**:

| Stranger-reachable route | Guard (verified on origin/main) |
|---|---|
| `POST /api/quickstart/ask` | in-memory IP-hash rate limit → 429 |
| `POST /api/public/report` (NEW — guest QR intake) | type-checks + length caps (desc 2000 / contact 200) + **DB-backed** IP-hash rate limit (5/IP/hr) + parameterized queries + tenant resolved from `equipment_number`; RLS-bypass is INSERT-only and documented |
| `GET /api/quickstart/manufacturers` | `withTenantContext` scoped to the **designated public tenant**'s seeded OEM corpus — no cross-tenant leak |
| `POST /api/uploads/folder` (middleware-excluded) | **Bearer `HUB_INGEST_TOKEN`** check (401 on mismatch) + UUID `x-mira-tenant-id` required |
| `GET /api/uploads/[id]` (middleware-excluded) | dual-auth: session (`sessionOr401`) **or** bearer+tenant; all queries tenant-scoped |
| `GET /api/demo/customer` | `sessionOrDemo` + `isDemoTenant` gate + `withTenantContext` (RLS), demo-tenant only |
| `GET /api/documents` | tenant predicate (above) |

**Secrets scan:** clean across all 111 hub `route.ts` files (no hardcoded key/secret/password literal outside `process.env`).

**KG insight (A3, hand-extracted +15 nodes/+14 edges → 3679/5617; graphify still blocked — no LLM key in sandbox):** the two auth chokepoints are god-nodes (`sessionOr401()` deg 92, `withTenantContext()` deg 78). Querying the A3 slice: **7/7 stranger-reachable routes carry an outbound guard edge → 0 auth-orphans.** This is the *inverse* of the A2 fingerprint — in A2, `quickstart/ask` had no rate-limit edge and `documents` had no tenant edge (the "validated artifact with no edge to where it takes effect" orphan topology that recurred across C1/B2/E2). On origin/main both missing edges now exist; the public attack surface is fully edge-covered.

**3 non-blocking hardening items (none stranger-exploitable at single-VPS beta scale):**
1. `quickstart/ask` limiter is an in-process `Map` — resets on cold start, bypassable across instances. Fine for one VPS; **port to the DB-backed pattern `/api/public/report` already uses before any horizontal/serverless scale.**
2. `uploads/folder` compares the service token with `!==` (not `crypto.timingSafeEqual`) — negligible over TLS, cosmetic.
3. `/api/public/report` stores `description`/`contactInfo` trimmed-but-raw → confirm the admin review queue renders them escaped (React default escapes; ~5-min verify, likely already safe).

**Next action:** none blocking — beta-safe. Optionally file the in-memory→DB limiter swap as a post-beta hardening issue.


## Lens F — re-audit (2026-06-09, round 2)

**Verdict: RED (held).** Round 1 (06-08) synthesized A–E into a top-10 and found 3 stranger-reachable blockers (a break, a leak, a lie) with only #1 patch-ready. Round 2 re-verified each blocker against **live code** (not the prior write-up) and found the ledger's *contents* unchanged but its *shape* materially different: nothing has been applied since 06-08, yet the count of ready-to-ship patches rose from **1 → 3**. The blocker is no longer "we don't know the fix" — it's "the fix is staged and never reaches `main`."

**Re-verified in-tree this run (the 3 stranger-reachable blockers + the gate — all still open):**

| # | Blocker | Live-code check this run | State |
|---|---|---|---|
| 1 (break) | `/api/quickstart/ask` cascade-drain | `rg rate-limit|429` in `route.ts` → **0 hits** | OPEN |
| 8 (leak) | `/api/documents` cross-tenant IDOR | `SELECT manufacturer, model_number FROM cmms_equipment WHERE id = $1` (`route.ts:39`) — **no tenant predicate**; in-file comment even contrasts it with `/api/knowledge` | OPEN |
| 2 (lie) | wrong-vendor citation passes CitGrond | `citation_compliance.py` → **0** `uns_context`/`relevance` refs (presence-only, observation-only) | OPEN |
| 3 (gate) | broken money-path ships green | `smoke-test.yml` → **0** `signup`/money-path refs (still `/login` render only) | OPEN |

**The round-2 delta — ready-patch count 1 → 3 (all `git apply --check` clean this run):**
- `patches/2026-06-08-quickstart-rate-limit.patch` → fixes #1 (supersedes the malformed `2026-06-07` file, which still fails `apply` at the `@@` hunk — re-confirmed this run).
- `patches/2026-06-08-documents-tenant-filter.patch` → fixes #8 (`AND tenant_id=$2`).
- `patches/2026-06-09-smoke-gate-signup-moneypath.patch` → fixes #3 (wires the existing prod-safe signup spec into the smoke deploy-gate).
Applying all three on one branch closes **two** of the three stranger-facing risks (break + leak) and **gates** the third class at deploy — the single highest-leverage move available without a founder decision.

**What still has NO patch (the patchless trio — unchanged from round 1, correctly so):**
- **#2 the lie** — citation-relevance must land on **both** `citation_compliance.py` **and** the hub TS routes (C2 parity finding); spans two stacks, founder-gated.
- **#4 eval non-determinism** — pinning the FSM/keyword LLM edge edits the live inference path (D2); CI/founder-gated.
- **#5 migration ledger + dup prefixes** — `schema_migrations` ledger + dedupe is a founder design decision (E2).
These are exactly the items the remediation plan (`docs/plans/2026-06-08-beta-readiness-remediation.md`) sequences as P0-3 / P2-1 / P3-2 — design work, not mechanical patches.

**Ranked top-10 (ranking held; state column refreshed this run):**

1. **#1 break** — `/api/quickstart/ask` no rate limit. → `git apply wiki/orchestrator/patches/2026-06-08-quickstart-rate-limit.patch`
2. **#2 lie** — wrong-vendor citation reads green. → extend `check_citation_compliance` to assert cited vendor/model ⊆ `state["uns_context"]` (+ port to hub TS per C2)
3. **#3 gate** — money-path ships green. → `git apply wiki/orchestrator/patches/2026-06-09-smoke-gate-signup-moneypath.patch`
4. **#8 leak** — `/api/documents` cross-tenant IDOR. → `git apply wiki/orchestrator/patches/2026-06-08-documents-tenant-filter.patch`
5. **#4 eval non-determinism** — 47–51/57 band (D2, 9 runs). → pin temp=0+seed on FSM-routing + keyword-eval calls only
6. **#5 no migration ledger + 8 dup prefixes** — → add `schema_migrations` table; dedupe 006/008/021/025/026/027/032/033
7. **#6 PR #1657 apply-and-verify red streak** — → confirm idempotency guards green on staging Neon, route to human merge
8. **#7 migration-verify ↔ staging-gate race** — → add `needs:`/`workflow_run` ordering in `staging-gate.yml`
9. **#9 proposals contract drift** — → founder D-1: crown `ai_suggestions` + build `proposal-transition.ts` (3-part atomic, per B2)
10. **#10 ignition reject-contract** — post-beta, HMAC-gated, NOT beta-reachable; ranks last (P6 #1658)

**Why RED, not GREEN-trending:** three of the four stranger-facing risks (break, leak, lie) are *still live on the signup→chat path today*. The patches exist but are unapplied; an unapplied patch protects no one. F stays RED until the 3 clean patches land through the staging gate and the citation-relevance lie is closed.

**KG (this run, hand-extracted +19 nodes / +21 links → 3664 / 5603, `_lens:"F2"`):** the RED ledger decomposes into **two disjoint subgraphs**. First: `f2_patch_ratelimit`/`f2_patch_docs`/`f2_patch_smoke` each carry a `validated_clean_fixes` edge **into** a stranger-path blocker (`f2_b1_break`/`f2_b8_leak`/`f2_b3_gate`) — but every one also carries a `_missing` `merge_to_main → f2_missing_prpath` edge: **fix-edge in, no PR-edge out = apply-ready orphans.** Second: `f2_b2_lie`/`f2_b4_evalnoise`/`f2_b5_migledger` have **no** inbound patch edge at all (`f2_fix_citrelevance` is itself `_missing`, `spans_two_stacks`). Round 1's insight ("0 of 6 unbuilt phases anchor any blocker" — preserved as `f2_unbuilt6 --anchors_zero_blockers--> f2_ledger_r2`) sharpens to: **the bottleneck is a PR/apply step on 3 orphaned-but-clean patches, not analysis.** Same orphan topology prior lenses found for `uns_required` (C1), `ai_suggestions` (B2), and `signup-flow.spec.ts` (E2) — a validated artifact with no edge to where it takes effect.

**Graphify status:** unchanged from every prior lens — `graphify`'s Tree-sitter+LLM extraction can't run (no `GEMINI`/`GROQ`/`CEREBRAS`/`OPENAI`/`ANTHROPIC` key in the sandbox), so the F2 slice was hand-extracted (`_origin:"hand"`).


## Lens E — re-audit (2026-06-09, round 2)

**Verdict: YELLOW (held), with a ready-to-ship patch + a sharper root cause.** Round 1 (06-08) said "deploy-gate mechanics green; migrations have no ledger + dup prefixes; smoke gates `/login` not signup→chat." Round 2 confirmed every mechanic, then traced the migration source-of-truth and the smoke surface in-tree and found the picture is structurally worse on migrations and **cheaply fixable** on the smoke gate.

**What's still GREEN (verified in-tree this run):**
- `deploy-vps.yml` is correctly gated: `workflow_run: ["Smoke Test"]` + `if github.event.workflow_run.conclusion == 'success'` + a separate "Verify Staging Gate passed" step that refuses to deploy a SHA whose PR didn't pass Staging Gate (direct pushes blocked; audited `skip_staging_gate=true` hotfix path). The gate *plumbing* is sound.

**What round 2 adds (all verified in-tree):**

1. **[SOURCE-OF-TRUTH FRAGMENTED — 3 migration trees, CLAUDE.md cites the non-deploying one]** The dir `apply-migrations.yml` actually deploys is `mira-hub/db/migrations` (`MIG_DIR="mira-hub/db/migrations"`, line 119) — **47 files, numeric head 044**. But root CLAUDE.md and `.claude/CLAUDE.md` cite `docs/migrations/004_kg_entities.sql` / `005_kg_relationships.sql` as the KG-schema authority — and `docs/migrations` is a **10-file tree (head 008) with NO apply workflow at all**. A third tree, `mira-core/mira-ingest/db/migrations` (10 files, head 012), has its own `apply-ingest-migrations.yml`. The three trees **share prefix numbers for entirely different tables**: `004` is `kg_entities` (docs) vs `asset_enrichment` (hub) vs `tenant_channel_config` (ingest). Net: "what migration are we on?" has three answers, and the doc a human (or agent) reads first points at the orphan tree that never deploys.

2. **[NO LEDGER — and the workflow help text lies about it]** No `schema_migrations` table is created by any migration or consulted by `apply-migrations.yml` (the apply step selects files purely from `ls "$MIG_DIR"/*.sql | sort` or an explicit prefix list — zero `SELECT`/`INSERT` against a migrations table). Yet the workflow's `migrations` input help says `all` = "everything in mira-hub/db/migrations **not yet recorded**." There is no recording mechanism, so `all` re-applies **every** file every run and leans 100% on per-file `IF NOT EXISTS` guards — the exact guards hot.md shows failing repeatedly (033 `simulated`, `citations_present`, `event_timestamp` → PR #1657 apply-and-verify red streak). The comment `# Sort numerically for safety` sits above a plain `sort` (lexical); for zero-padded 3-digit prefixes lexical == numeric on the *prefix*, so that's benign — the real hazard is the duplicates (next).

3. **[8 DUP PREFIXES — order decided by description text]** Confirmed exactly: `006/008/021/025/026/027/032/033` each have two files. Within a colliding prefix, lexical sort orders by the **suffix description**, so `027_ai_suggestions.sql` applies before `027_namespace_direct_uploads.sql` purely because `a < n` — arbitrary with respect to dependency. Plus a **038–042 gap** (head jumps 037 → 043 → 044). hot.md still records head=037 (stale by 7 files).

4. **[NEW — ghost migration in the shipped bundle]** `mira-hub/.next/standalone/db/migrations` (the built Next.js standalone, 48 files) contains `045_knowledge_entries_private_uploads.sql` that **source `mira-hub/db/migrations` (47 files) does NOT**. So the deployed container image carries a migration that current `main` source — and therefore the apply pipeline — has no knowledge of. Either 045 was reverted from source without a rebuild, or the build outran source; either way the running artifact and the migration authority disagree by one file. (045 is present in sibling worktrees `folder-brain`/`charlie-session`, so it's real in-flight work, not corruption.)

5. **[SMOKE GATE — verifies the wrong surface, but the fix already exists]** `smoke-test.yml` runs **only** `playwright.smoke.config.ts` (`testMatch smoke.spec.ts`), whose deepest app assertion is "`/login` = Email + password + Google OAuth" — i.e. the login page *renders*. Nothing exercises pricing→trial→checkout→chat. **However**, `playwright.signup.config.ts` + `tests/e2e/signup-flow.spec.ts` already exist and are **prod-safe**: they assert the `/pricing` trial CTA is visible, that `/api/checkout/session` returns **303 → checkout.stripe.com** (no payment completed), and that the `/cmms` magic-link form validates + submits using the RFC-2606 reserved address `ci-smoke@example.com` (Resend won't deliver, no tenant created). A `rg` confirms this config is wired into **zero** workflows.

**Why YELLOW, not RED:** the gate mechanics work, the ingest + hub trees each *do* have a deploy path, and no migration hazard has shipped a broken schema to a beta tester *today*. The stranger-facing "a broken money-path ships green" risk is already booked **RED in F#3** — raising E to RED would double-count it (same handling as C/D this cycle). E stays a pipeline-governance YELLOW.

**Patch staged this run (the first E-lens patch):** `wiki/orchestrator/patches/2026-06-09-smoke-gate-signup-moneypath.patch` — inserts one step into `smoke-test.yml` running `playwright.signup.config.ts` after the existing smoke step. Generated via `git diff --no-index` (not hand-counted hunks — avoids the malformed-`@@` bug that broke the 06-07 Lens-A patch) and validated `git apply --check` ✅. Because `deploy-vps.yml` gates on the **Smoke Test** workflow's success, this immediately makes a broken trial-CTA / checkout-redirect / magic-link **block deploy**. It does **not** cover `quickstart/ask` grounded chat (no e2e spec exists for it — that's the deeper C2/F#3 gap and needs a new spec, not a wiring change).

**Apply + verify:**
```bash
cd "$(git rev-parse --show-toplevel)"
git apply wiki/orchestrator/patches/2026-06-09-smoke-gate-signup-moneypath.patch
# verify the step landed and the config it references exists:
grep -n "playwright.signup.config.ts" .github/workflows/smoke-test.yml
test -f mira-hub/playwright.signup.config.ts && echo "config present ✅"
# optional dry parse: actionlint .github/workflows/smoke-test.yml
```

**Founder calls (no patch — design/doc decisions):** (a) add a real `schema_migrations` ledger so `all` means "unapplied only" and the help text stops lying; (b) declare **one** canonical migration tree and repoint CLAUDE.md off `docs/migrations` (or delete the orphan); (c) reconcile the ghost `045` (land it in source or drop it from the build); (d) dedupe the 8 colliding prefixes or document that suffix-alphabetical order is dependency-safe.

**KG (this run, hand-extracted +17 nodes / +18 links → 3645 / 5582, `_lens:"E2"`):** the gate gap is a graph orphan. `e2_spec_signup` (the prod-safe money-path test) has exactly **1 real inbound edge** — from its own config `e2_cfg_signup` — and its only other inbound is the `_missing` `e2_wf_smoke --runs--> e2_cfg_signup` edge: a money-path test reachable by its config but by **no workflow**, the same orphan topology prior lenses found for `uns_required` (C1) and `ai_suggestions` (B2). The staged patch adds exactly that one missing workflow→config edge. The migration arm shows `e2_claudemd --cites_as_authority--> e2_dir_docs_mig` (orphan tree, `--NO_apply_workflow-->` missing) while `e2_claudemd --does_NOT_cite--> e2_dir_hub_mig` (the real deploy tree) is itself a `_missing` edge — the documentation points at the tree with no deploy path and is silent on the one with 47 live migrations.

**Graphify status:** unchanged from every prior lens — `graphify`'s Tree-sitter+LLM extraction can't run (no `GEMINI`/`GROQ`/`CEREBRAS`/`OPENAI`/`ANTHROPIC` key in the sandbox), so the E2 slice was hand-extracted (`_origin:"hand"`).

## Lens D — re-audit (2026-06-09, round 2)

**Verdict: YELLOW (held), with a sharper root cause and a much narrower fix.** Round 1 (06-08, 4 runs) called the offline scorecard non-deterministic and prescribed a record/replay seam on "the one live-LLM edge." Round 2 widened the sample to **9 back-to-back runs** (06-07T2209→06-09T0930) and the picture clarified in two ways the prior audit couldn't see: (1) the noise is *entirely* in the LLM-touched checkpoints, so the fix is far smaller than "replay the whole harness"; and (2) the non-determinism is no longer hypothetical waste — it is actively polluting the issue tracker via the eval-fixer routine.

**Measured this run (parser over all 9 run tables, 9×57 = 513 fixture-cells):**

| Metric | Round 1 (4 runs) | Round 2 (9 runs) |
|---|---|---|
| Pass-rate band | 47–50/57 (spread 3) | **47–51/57 (spread 4)** — series `[47,50,47,47,47,49,48,47,51]` |
| Flapping fixtures | ~15 | **19** |
| Hard-stable fails | 4 | **same 4** |
| Always-pass | — | 34 |

1. **[ROOT CAUSE NARROWED — noise is 100% in 2 of 6 checkpoints]** Fail-cell counts across all 513 cells: **FSM 59** (16 fixtures), **KeyKW 24** (11 fixtures), and **RState 0 / No5xx 0 / TurnBudget 0 / CitGrond 0**. The four non-LLM checkpoints are *bit-stable*. Among the 11 worst flappers, the only toggling checkpoints are FSM (9) and KeyKW (6). So the promotion gate's noise is not "the cascade is random everywhere" — it is precisely the **FSM routing decision** (LLM over-qualification) and the **keyword-eval** path. **Implication:** you don't need a record/replay seam across the whole engine; pinning the FSM-routing + keyword-eval LLM calls (temperature=0 + fixed seed, or a cassette on just those two) collapses the band, because the other four checkpoints already measure deterministically.

2. **[OPERATIONAL HARM NOW OBSERVED — the fixer files phantoms]** `offline_run.py:5` still calls the **live** Groq cascade (no seed/replay/cassette/temperature-pin present; Gemini skipped at `:652` to dodge 429s). The eval-fixer routine reads a **single** run's scorecard and files issues + names clusters off it: hot.md shows #1773 (46/57) then #1788 ("50/57 **up from** 46/57", "7 failures, Cluster A = engine.py FSM over-qualification ×6 incl. a false SAFETY_ALERT on `vfd_abb_03`"). But 46/47 and 50/51 are **adjacent samples of one 47–51 band**, not a trend — and #1788's named Cluster-A member `vfd_abb_03_acs355_cross_load` actually passes **7 of 9** runs. The "improvement" and at least one named defect are sampling artifacts. The prior audit's prediction ("a 1–2-case regression is invisible") is now realized in the opposite direction too: phantom clusters are *visible* and getting tracked.

3. **[BETA-CRITICAL CHECKPOINTS STABLE — confirmed at 9-run depth]** `No5xx` and `CitGrond` never fail (0/513); both safety fixtures (`safety_escalation_06`, `vfd_mitsu_04_fr_f800_safety`) pass 9/9. Nothing the eval measures **breaks or 5xx's or drops a citation** on the beta path. The one caveat is unchanged and already booked **RED in F#2 / C2**: `CitGrond` is presence-not-relevance, so `vfd_danfoss_04` (Siemens-for-Danfoss wrong-vendor cite) sits green at 5/6 every run — a stable *green* that is actually a lie. That fix is owed on `citation_compliance.py` **and** the hub TS routes (C2), not on the harness.

**Why YELLOW, not RED:** the eval harness does not itself break the stranger's product — it's a **measurement-integrity** problem that makes the promotion gate blind to ±2-case regressions and feeds the autofix loop phantom work. Beta-critical signals are stable green. So D stays a gate-quality YELLOW; the stranger-facing "lie" risk it surfaces is double-counted in F#2, not here.

**Sharpened next action (cheaper than round 1's):** (a) pin temperature=0 + fixed seed on the **FSM-routing and keyword-eval** LLM calls only (or cassette just those two), leaving the 4 deterministic checkpoints as-is — collapses the 47–51 band to a number; (b) **gate the eval-fixer on a 3-run median**, not a single snapshot, so it can't file `#1773/#1788`-style phantom clusters; (c) then re-baseline the 4 genuinely-stable fails (`gs3_ground_fault_14` KeyKW + 3 FSM) as the real backlog. **No patch this run** — (a) edits the live inference path (founder/CI-gated, needs `codegraph_impact` on the FSM-routing call first), (b) is a routine-config change outside the repo working tree, (c) depends on (a).

**KG (this run, hand-extracted +15 nodes / +19 links → 3628 / 5564, `_lens:"D2"`):** the noise topology is the finding. `d2_ck_fsm --noise_source_59cells-->` and `d2_ck_keykw --noise_source_24cells--> d2_noise_band`, while `d2_ck_rstate/no5xx/turnbudget/citgrond --deterministic_0fail_NOT_noise-->` the same band — the graph shows four checkpoints with zero noise-contribution edges, which is *why* `d2_seam_missing --fix_locus_narrowed_to-->` only FSM + KeyKW. The second arm renders the operational harm: `d2_fixer_routine --consumes_single_run_snapshot--> d2_noise_band --phantom_cluster_named_off_noise--> d2_issue_1788`. And `d2_fix_danfoss04 --wrong_vendor_lie_passes_presence_check--> d2_ck_citgrond` ties the one stable-green-that-lies back to F#2.

**Graphify status:** unchanged from every prior lens — `graphify`'s Tree-sitter+LLM extraction can't run (no `GEMINI`/`GROQ`/`CEREBRAS`/`OPENAI`/`ANTHROPIC` key in the sandbox), so the D2 slice was hand-extracted (`_origin:"hand"`).

## Lens C — re-audit (2026-06-09, round 2)

**Verdict: YELLOW (held), but the top finding has moved.** Round 1 (06-08) audited the engine in isolation and found it sound except for the HMAC-gated Ignition reject-contract gap → "YELLOW, not beta-reachable." Round 2 traced the **actual beta path** and found the more load-bearing fact: **the engine the prior C/D audits keep certifying is not the code a stranger runs.** The engine is integrity-intact; it just isn't on the money path, and the money path has its own, ungoverned grounding.

**What's intact (verified in-tree this run):**
- **UNS chat-gate carve-out correct.** `_should_fire_uns_gate` (`engine.py:5297`) + `AWAITING_UNS_CONFIRMATION` honor the chat-gate; the new read-only live-tag snapshot (`_maybe_attach_live_snapshot`, `engine.py:1241`) is gated on a confirmed asset and is attached *after* the gate ("live data can never bypass the gate"). The `status_summary`/`live_tag`/`action_request` fast-paths bypass only the **quality gate** (n-gram coherence), not the UNS gate — a separate, defensible concern (templated deterministic replies).
- **Citation chokepoint single + wired.** `check_citation_compliance` is called at exactly two sites (`engine.py:2529`, `:3359`), both at the `_format_reply` chokepoint. These are the **sole emitters** of `CITATION_COMPLIANCE_OK/_MISS` — the 90-day MVP ≥9/10 citation metric.
- **Groundedness scoring intact** (1–5 + low-groundedness episode tracking).

**What round 2 adds (all verified in-tree):**

1. **[STRUCTURAL — the beta path bypasses the engine] `quickstart/ask` and `mira/ask` never call `engine.process`.** `engine.process` callers are Telegram/Slack/WhatsApp/Reddit/`ask_api`/`chat/dispatcher` — **not the hub.** `mira-hub/src/app/api/quickstart/ask/route.ts:115` and `mira/ask/route.ts` each run their **own** `retrieveManualChunks` (BM25) + `cascadeComplete` (a *TypeScript* Groq→Cerebras→Gemini cascade, `lib/llm/cascade.ts`). So the stranger's grounded chat runs a **second grounding implementation** that the engine-side lenses have never measured.

2. **[LIE VECTOR on the beta path — no post-check] `quickstart/ask` returns retrieved chunks as `citations[]` mechanically** (`route.ts:137` — `chunks.map(... title: [manufacturer, modelNumber]...)`). The citations are *whatever BM25 returned*, not what the model used or what's relevant. A Danfoss question that retrieves a Siemens chunk returns a confident "Siemens" citation. There is **no `check_citation_compliance` equivalent** anywhere in the hub (`rg` for citation post-checks in the two routes: 0 relevance checks). This is D#2 / F#2 ("wrong-vendor citation reads green") living on the **beta path itself**, with even less protection than the engine path (which at least logs a MISS).

3. **[TWO GATE IMPLEMENTATIONS] `quickstart/ask` has no gate; `mira/ask` has its own.** `quickstart/ask` (anonymous landing) applies **no namespace gate** — on retrieval failure it sends `"(no manuals indexed...)"` and lets the model answer. `mira/ask` enforces a **TS-side** `gate:"namespace"` returning **428 Precondition Required** (`route.ts:136`) — functionally parallel to the Python `_should_fire_uns_gate` but a **separate codebase** that can drift. Two gate implementations, one doctrine.

4. **[ROUND-1 FINDING PERSISTS] `ignition_chat.py:208` still downgrades, doesn't reject.** `uns_source = "direct_connection" if (asset_id or req.asset_context) else None` — an id-less Ignition turn falls through to the chat-gate. `uns_required` = **0 hits** across `mira-bots`/`mira-pipeline`/`mira-mcp`. No `#1658` commit landed. Still HMAC-gated → not beta-reachable → doesn't lift C to RED on its own.

**Why YELLOW, not RED:** Lens C's literal subject — the engine in `mira-bots/shared/` — is sound. `mira/ask` *does* gate (just separately), and `quickstart/ask` is manufacturer-scoped retrieval, so the beta path is not ungrounded chaos. The stranger-reachable "lie" risk is already booked **RED in Lens F #2**; raising C to RED would double-count it. The honest round-2 move is to **re-scope C's question** from "is the engine correct?" (yes) to "do the engine's guarantees reach the beta cohort?" (**no**) — and route the fix accordingly.

**Sharpened next action:** the citation-relevance promotion (F#2) is necessary but **insufficient if applied only to `citation_compliance.py`** — it must also land on the hub TS routes, or the money path stays exposed. Founder decision: either (a) converge the hub chat onto `engine.process` (one grounding implementation, one gate, one citation chokepoint, one telemetry stream), or (b) formally spec the TS grounding contract and port gate + citation-relevance + `CITATION_COMPLIANCE` telemetry to it. **No patch this run** — the fix spans Python + TypeScript and is a founder-gated architecture call, not a mechanical diff (round-1 C also shipped no patch).

**KG (this run, hand-extracted +12 nodes / +18 links → 3613 / 5545, `_lens:"C2"`):** the two grounding paths were added as a subgraph and the topology *is* the finding. `c2_surface_hub_beta_chat --routes_to-->` both hub routes; each `--calls--> c2_fn_cascade_ts` (the TS cascade) and carries a **`_missing:true` `BYPASSES_engine`** edge to `c2_fn_engine_process` — there is **no path** from the beta surface to the node that owns `c2_fn_uns_gate` + `c2_fn_citcheck` + the `c2_metric_cite_rate` emitter. `c2_gate_ts_namespace --DUPLICATE_gate_impl_can_drift--> c2_fn_uns_gate` renders the two-gate split; `c2_rt_quickstart_ask --NO_citation_postcheck--> c2_fn_citcheck` (`_missing:true`) is the structural reason the lie vector is unguarded on the money path. The engine subgraph is fully wired and correct — it just has **zero inbound edges from the beta surface**.

**Graphify status:** unchanged from every prior lens — `graphify`'s Tree-sitter+LLM extraction can't run (no `GEMINI`/`GROQ`/`CEREBRAS`/`OPENAI`/`ANTHROPIC` key in the sandbox), so the C2 slice was hand-extracted (`_origin:"hand"`).

## Lens B — re-audit (2026-06-09, round 2)

**Verdict: YELLOW (held).** The hub's Phase-2 product surfaces (proposal queue + readiness scorecard) *work* and are tenant-scoped, so nothing breaks/leaks for the beta cohort today. YELLOW holds because the proposal state machine is built on a contradiction between two authority docs (ADR-0013 vs ADR-0017 + the CLAUDE.md glossary), and round 2 shows the drift is **wider and more structural** than round 1 found — plus the enforcement that's supposed to catch it has a blind spot exactly where the real drift lives.

**What round 1 had (06-08):** "`/api/proposals` reads `relationship_proposals` while the glossary crowns `ai_suggestions`; helper absent; prod tsc clean."

**What round 2 adds (all verified in-tree):**

1. **[CONTRACT — the crown table is operationally orphaned] `ai_suggestions` (mig 027) has ZERO read surfaces in the hub.** A full `mira-hub/src` search for `ai_suggestions` returns exactly **one** hit — a comment in `/api/proposals/route.ts:13` calling it "*a duplicate `ai_suggestions` table*." No route `SELECT`s from it. So the table the glossary says `/proposals` renders, and what "N proposals pending" counts, is **never read by any hub route**. The de-facto product surface is `relationship_proposals`.

2. **[CONTRACT — the violation is TWO surfaces, not one] `/api/readiness` + `/api/readiness/recalculate` also read `relationship_proposals` directly** (`readiness/route.ts:58,62`; `recalculate/route.ts:50,54` — `COUNT(*) … FROM relationship_proposals` for the `proposed`/`verified` readiness signals). The glossary's "`RelationshipProposal` … **Never read directly by user-facing surfaces**" is violated by the readiness scorecard as well as the proposal queue. (This also corrects the plan's "recalc can't fire": the *manual* recalc endpoint runs fine on `relationship_proposals`; what can't fire is an `ai_suggestions`-accept-driven recalc, because nothing writes `ai_suggestions`.)

3. **[STATE MACHINE — decide writes 2 of 3 projections] `/api/proposals/:id/decide`** sets `relationship_proposals.status='verified'` (`route.ts:93`) and `kg_relationships.approval_state='verified'` (`:136`) but **never** touches `ai_suggestions.status` (confirmed: no `ai_suggestions` reference in the route). ADR-0017's three projections (`ai_suggestions.status` / `relationship_proposals.status` / `kg_relationships.approval_state`) are meant to stay consistent; decide keeps 2 in sync and lets the 3rd rot.

4. **[ENFORCEMENT BLIND SPOT — the canary can't see the real drift] `tests/canary/proposal_state_drift.sql`** (nightly, fails on any non-zero row) has two checks, **both** keyed off an explicit proposal link, and both only fire on *`accepted-suggestion → unverified-proposal`* or *`linked-edge → unverified-proposal`*. The drift the decide route actually produces is the **opposite direction**: `verified-proposal + verified-edge + stale `pending` suggestion`. Neither check covers it. So the canary reads GREEN while the 3-way state machine is, by construction, inconsistent the moment any `ai_suggestions(kg_edge)` row exists.

5. **[TEST DEBT — the suite has frozen the ADR-0013 framing] The e2e proof codifies the "wrong" table, and the decide unit test locks in the raw-`UPDATE` pattern.** `tests/e2e/phase2-namespace-builder-proof.spec.ts:9` documents "`/proposals` — read-only proposals list **from relationship_proposals**" and only exercises the read shell + readiness level range (0–6) — there is **no e2e for the decide/verify mutation**. The mutation's only coverage is `decide/__tests__/route.test.ts`, which **mocks SQL and asserts the literal `UPDATE relationship_proposals` / `UPDATE kg_relationships` strings** (`:137,150,194,202,237`). So both the read test (spec comment) and the write test (string asserts) bake in the pre-helper, ADR-0013 architecture — raising the migration cost of the founder's recommended "crown `ai_suggestions`" direction.

**Why YELLOW, not RED:** today **no hub route reads `ai_suggestions`**, so the stale-suggestion drift is invisible and harmless to a beta tester — the queue and readiness surfaces are internally consistent (both read `relationship_proposals`) and tenant-scoped (`tenant_id = $1::uuid OR tenant_id IS NULL`; the `IS NULL` arm intentionally shares global/seed proposals, same pattern as the OEM-corpus hybrid). It becomes a live **"lie"** (a "pending" count that disagrees with the decided queue) only when someone builds the glossary-correct `ai_suggestions` read surface. That's the trap: the recommended fix, done naively (repoint reads at `ai_suggestions`) **without** also fixing decide + the canary, *introduces* the very drift the canary is meant to prevent.

**Sharpened next action (extends plan D-1, does not duplicate it):** the founder call is still "crown `ai_suggestions`," but it is a **3-part atomic change, not a route repoint**: (a) build `mira-hub/src/lib/proposal-transition.ts` + `mira_bots/shared/proposal_transition.py` that write all three projections in one transaction (decide routes through it; after it exists, the raw `UPDATE`s in `decide/route.ts` become bugs per the CLAUDE.md rule); (b) repoint `/api/proposals` GET **and** `/api/readiness`(+recalc) reads at `ai_suggestions`; (c) add a 3rd canary check `verified-proposal → stale `pending` suggestion`. Ship (a)+(b)+(c) together or not at all. **No patch staged this run** — (a)/(b) are founder-gated product code and (c) alone would turn the nightly canary red against current data before the decide fix lands (a half-fix that breaks CI). The whole thing is the ~½-day D-1 task already in `docs/plans/2026-06-08-beta-readiness-remediation.md`.

**KG (this run, hand-extracted +15 nodes / +18 edges → 3601 / 5527, `_lens:"B2"`):** the proposal state machine was added as a queryable subgraph. The topology *is* the finding: `tbl_ai_suggestions` has **3 inbound doctrine edges** (`adr_0017 --crowns-->`, `glossary --crowns-->`, `canary --checks-->`) and **zero route-read edges** — doctrinally crowned, operationally orphaned. Its mirror `tbl_relationship_proposals` carries **three** route-read edges, every one tagged `reads_directly_VIOLATES_GLOSSARY`, plus `adr_0013 --crowns_CONTRADICTS_ADR0017-->`. The two `crowns` edges fork to different tables — the contract drift rendered as a graph split. And `rt_proposals_decide --MISSING_WRITE_3rd_projection--> tbl_ai_suggestions` (a `_missing:true` edge) is the structural reason the canary's link-keyed checks can't see the drift: there is no edge from decide to the suggestion it should be accepting.

**Graphify status:** unchanged from every prior lens — `graphify`'s Tree-sitter+LLM extraction can't run (no `GEMINI`/`GROQ`/`CEREBRAS`/`OPENAI`/`ANTHROPIC` key in the sandbox), so the B2 slice was hand-extracted (`_origin:"hand"`).

## Lens A — re-audit (2026-06-08, round 2)

**Verdict: YELLOW (held).** Net the hub got *more* secure since the 06-07 bootstrap — three real hardening PRs landed — but the two findings that actually gate the beta money path are both still open, and the one "ready-to-ship" remediation in the whole ledger turned out to be broken. So YELLOW holds, with a sharper, now-fully-patchable remediation set.

**What landed since 06-07 (GREEN deltas, verified in-tree):**
- **#1762 — security headers.** `middleware.ts` now sets `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`, `X-Frame-Options: DENY` on every path except `/scan`, and a CSP. The `/scan` XFO carve-out (monday.com marketplace iframe) is **correctly scoped**: `frame-ancestors 'self' https://*.monday.com` for `/scan`, `frame-ancestors 'self'` everywhere else — *not* a `*` clickjacking hole. `X-Powered-By` dropped in `next.config.ts`.
- **#1764 — auth perimeter.** Unauthenticated `/api/*` now returns 401 JSON (not an HTML redirect).
- **#1761 — `/api/usage` tenant-scoped.** Moved to `withTenantContext`; the one `knowledge_entries` COUNT is explicitly documented as the universal OEM corpus (the correct call). **This establishes the precedent that fixes finding #2 below.**

**Findings (both carried over, both now cleanly patchable):**
1. **[BETA-BLOCKER — unchanged + remediation regressed] `/api/quickstart/ask` still has no rate limit.** Confirmed in source (`route.ts`, 145 lines): input length is capped (1000 chars) and retrieval is tenant-scoped via `withTenantContext(quickstartTenantId())`, but every POST fires `cascadeComplete` (700 tok / 20 s) with zero throttle → one script drains the shared free-tier cascade for the whole cohort. **The staged `2026-06-07-quickstart-rate-limit.patch` no longer applies** — its hand-written hunk headers (`@@ -1,5 +1,7 @@`) under-count the bodies, so `git apply` rejects it ("patch fragment without header"). The limiter *design* (20/IP/min, in-memory, mirrors `/api/public/report`) is sound; I regenerated it via `git diff --no-index` as **`2026-06-08-quickstart-rate-limit.patch`** — `git apply --check` ✅.
2. **[MEDIUM→now-clearly-fixable] `/api/documents` cross-tenant IDOR persists.** `route.ts:39` runs `SELECT manufacturer, model_number FROM cmms_equipment WHERE id = $1` on the raw RLS-bypass `pool` with **no tenant filter**. Auth is `sessionOrDemo` (real session OR the shared `DEMO_API_TOKEN`), so an authed tenant-A user can pass any asset UUID and read a tenant-B asset's manufacturer/model. `cmms_equipment` is per-tenant *operational* data, so #1761's precedent ("tenant-filter operational tables; leave `knowledge_entries` universal") applies exactly. Surgical fix staged as **`2026-06-08-documents-tenant-filter.patch`** (`AND tenant_id=$2`; `git apply --check` ✅) — leaves the universal `knowledge_entries` query untouched.
3. **[INFO — checked, benign] new public routes.** Public-surface census (9 no-auth routes) flagged two new since 06-07: `picker/dropbox/key` returns `DROPBOX_APP_KEY` — but Dropbox **app keys are public-by-design** (client-SDK identifier; the app *secret* stays server-side), so benign; `quickstart/manufacturers` is public but bounded (tenant-scoped, `LIMIT 50`, DB-only, no LLM) — a far cheaper surface than `quickstart/ask`. Neither is a blocker.

**New surface noted:** `lib/demo-auth.ts` (`sessionOrDemo`, used by 14 routes incl. `mira/ask` + `documents`) is a **single shared static `DEMO_API_TOKEN`** gating LLM + tenant-data routes for an expo-booth iPad. Not anonymous (so `mira/ask` is *not* an open LLM surface like `quickstart/ask`), but a shared-secret blast radius worth a rotation/scoping note post-beta.

**KG insight (this run — hand-extracted +10 nodes / +14 edges, `_lens:"A2"`):** the graph shows `lib/tenant-context` is one of the highest-degree security nodes (**78 incident edges** — most data routes flow through the RLS hub), while `api/documents/route` (13 edges) wires straight to the raw `lib/db` `pool` node with **no edge to tenant-context**. That missing edge *is* the topological fingerprint of finding #2: `/api/documents` is the raw-pool sibling sitting next to a 78-edge RLS hub it never connects to. The regenerated patch adds the tenant predicate that the graph says is missing.

**Graphify status:** unchanged — `graphify` LLM-extraction can't run (no GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC key in the sandbox), so the A2 slice was hand-extracted (`_origin:"hand"`), same constraint as every prior lens.

## Lens F — detail (2026-06-08)

**Verdict: RED.** Lens F is the meta-lens — it does not audit a new surface, it asks the North-Star question directly: *can a stranger sign up on app.factorylm.com, use grounded chat, and have nothing break, leak, or lie — today?* The answer is **no**, on all three counts, even though every component lens (A–E) is YELLOW. Each lens is "architecturally sound with one gap"; the ledger sums those gaps along the one path a beta tester actually walks, and that path has a live break (cascade-drain), a live leak-class issue (cross-tenant asset lookup), and a live lie (wrong-vendor citation reading green). The encouraging half: the top-10 is dominated by **hardening already-shipped surfaces**, not building missing ones — so RED is a fixable-this-week RED, not a re-architecture.

### Ranked top-10 beta blockers (each with exact next action)

| # | Blocker | Src | Beta-reachable? | Exact next action (command / file) |
|---|---|---|---|---|
| 1 | **`/api/quickstart/ask` — public LLM, no rate limit** (cascade-drain / cost-bomb for the whole cohort) | A#1 | **YES** — landing→chat | **Apply the staged patch** `wiki/orchestrator/patches/2026-06-07-quickstart-rate-limit.patch` → `mira-hub/src/app/api/quickstart/ask/route.ts` (IP-hash limiter, reuses `/api/public/report` pattern). *Only ready-to-ship fix in the ledger.* |
| 2 | **Wrong-vendor citation passes `CitGrond`** (Siemens SIRIUS cited for a Danfoss VLT FC360) — the "nothing lies" violation | D#2 | **YES** — every grounded reply | Add a relevance assertion to `mira-bots/shared/citation_compliance.py`: cited vendor/model must intersect `state.uns_context`; promote `CitGrond` from tag-*presence* to tag-*relevance*. Re-run `tests/eval` on `vfd_danfoss_04_vlt_fc360_edge`. |
| 3 | **Smoke gate skips the money path** — gates `/login` render, not signup→`quickstart/ask`→chat; a broken money-path can deploy green | E#2 | **YES** — deploy-time | Wire the existing signup-flow + command-center Playwright configs into `.github/workflows/smoke-test.yml` so the deploy-gate exercises the North-Star flow pre-deploy. |
| 4 | **Offline eval is non-deterministic** (47/50/47/47-of-57) — a 1–2-case regression is invisible in the ±3 noise band | D#1 | indirect (gate) | Insert a record/replay (or seed) seam on the one live edge at `tests/eval/offline_run.py:5` (`engine.process → inference_router → groq`); then re-baseline the 4 hard-stable fails. |
| 5 | **Migration pipeline has no version ledger** — lexical `sort` (not `sort -n`), 8 duplicate prefixes (006/008/021/025/026/027/032/033), 038–042 gap, head 044; re-apply rests on `IF NOT EXISTS` guards hot.md shows failing | E#1 | **YES** — stranger's signup writes tenant rows here | Add a `schema_migrations` ledger + dedupe the 8 colliding prefixes (or prove `sort -n` is dependency-safe) in `.github/workflows/apply-migrations.yml` + `mira-hub/db/migrations/`. |
| 6 | **PR #1657 `apply-and-verify` red streak** — the migration-idempotency fixes (033 `simulated`, citations_present, event_timestamp) can't land while CI is red | hot.md | indirect (blocks #5) | Confirm `455e443` (simulated-column guard) goes green on staging Neon branch → route PR #1657 to human review/merge. |
| 7 | **`migration-verify.yml` ↔ `staging-gate.yml` race** — both mutate the same `res_neon_staging` branch with NO `needs:` edge (ordering is a YAML comment) → gate can grade replies against pre-migration schema | E-KG | indirect (gate trust) | Add a `workflow_run`/`needs:` ordering edge, or merge the two into one job, in `.github/workflows/staging-gate.yml`. |
| 8 | **`/api/documents` cross-tenant leak** — raw `pool` (RLS-bypass); `knowledge_entries` + `cmms_equipment WHERE id=$1` have no tenant filter → an authed user resolves any asset's manufacturer/model by id | A#2 | **YES** — authed users | Add tenant filter to both queries (or amend the route contract if `knowledge_entries` is a deliberate universal corpus) in `mira-hub/src/app/api/documents/route.ts`. |
| 9 | **`/api/proposals` contract drift** — reads `relationship_proposals` directly while spec §357 + shipped mig 027 crown `ai_suggestions`; event-driven readiness recalc can't fire; ADR-0017 helper missing | B#1/2 | partial (proposals UI) | Founder decision: crown `ai_suggestions` (per spec) or amend spec+glossary; then build `mira-hub/src/lib/proposal-transition.ts` (the missing ADR-0017 helper). |
| 10 | **Direct-connection reject contract missing** — `ignition_chat.py:208` downgrades id-less turns to the chat-gate instead of rejecting `{"error":"uns_required"}` | C#1 | **NO** — HMAC-gated, off the hub signup path | Intent-aware reject-on-missing-identifier (P6, epic #1658). Do **not** ship a naive reject-all — it wrongly rejects educational turns ("what is MQTT?"). Ranks last *for beta* despite being a real rule violation. |

**Tier read:** #1–3 + #8 are stranger-reachable on the signup→chat path (break / leak / lie). #4–7 are gate/pipeline integrity — they don't break the demo, they let a *regression* ship unseen. #9–10 are correctness/contract debt that won't bite the first cohort. The only blocker with a ready artifact is #1; everything else is a founder decision (#9), a design-correct fix (#2, #4, #5, #7, #10), or unblocking CI (#6).

**Not in the top-10 (deliberately de-escalated):** the 6 🔲 unbuilt master-plan phases (4 PLC collector, 5 tag-event stream, 8 decision-trace storage, 9 flaky-wire detector, 11 agent-facing MCP tools, parts of 1) — the hub beta path grounds on KB retrieval + CMMS + the citation chokepoint, all ✅; live-tag grounding is a *post-beta enhancement*. Also out: Gemini-key 403 (cascade falls through, smoke OK — but it shrinks the provider pool that #1 can drain), the 06-04 `/api/health` 502 (responded OK in the Lens B/this run — re-verify in CI), and doc-drift (CLAUDE.md staging "(TODO)", hot.md head 037 vs tree 044).

**KG insight (this run's slice — `docs/specs` + `docs/plans`, hand-extracted +18 nodes / +29 links):** the ledger was added to the graph as a queryable subgraph (`lens_f_ledger --ranks_blocker--> bf_*` ×10, plus a `ranked_above` chain encoding the order). Querying it against the master plan's unbuilt-phase anchors (`tag_events`, `decision_traces`, `flaky_input_signals`, `read_tag_value`, `get_asset_context`, `demo_plc_poller`) returns **NONE** — not one unbuilt phase is an evidence anchor for any top-10 blocker. Every blocker traces to an already-shipped surface: hub API auth (`mira-hub/src/app/api/*`), the offline eval harness (`tests/eval/`), or the migration/CI pipeline (`.github/workflows/*`). Structural conclusion: **beta-readiness is bounded by hardening three shipped surfaces, not by completing the master plan's remaining 🔲 phases** — those are correctly post-beta. This is the graph-level justification for why the top-10 looks the way it does.

**Graphify status:** unchanged from Lens A–E — `graphify` is not importable in this fresh sandbox and **no LLM API key** is set (GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset), so its Tree-sitter+LLM extraction can't run. The `docs/specs`+`docs/plans` slice was hand-extracted (`_origin:"hand"`, `_lens:"F"`), same constraint noted in every prior lens.

## Lens E — detail (2026-06-08)

**Verdict: YELLOW.** The promotion pipeline is more mature than the docs claim, and the deploy *gate* is genuinely well-built — it will not ship to prod on a red smoke run, and the hotfix bypass is audited. YELLOW because the gate *under-verifies the exact thing a beta needs*: the schema layer it gates has no version ledger, and the smoke check it gates on stops at the login page, never exercising the signup→chat money path.

What's solid (GREEN):
- **Deploy gate is enforced, not advisory.** `deploy-vps.yml` runs on `workflow_run: ["Smoke Test"]` and only deploys `if github.event.workflow_run.conclusion == 'success'`; it *also* refuses unless the most-recent **Staging Gate** run on the same commit succeeded (`docs/specs/staging-environment-spec.md`). The `workflow_dispatch` hotfix bypass requires a `skip_reason` and **auto-opens an audit issue**. Concurrency `cancel-in-progress: false` — a running deploy is never cancelled.
- **Staging is real** (contradicts root CLAUDE.md, which still calls it "(TODO)"): `docker-compose.staging.yml` + `docker-compose.staging-vps.yml` exist; `staging-gate.yml` (real-data E2E: Supervisor in-process vs Neon staging branch, 10 Qs through the Groq cascade, graded on the 5-dim `mira-answer-quality-standard.md` rubric), `migration-verify.yml` (auto-apply to staging Neon branch on any PR touching the migrations dir + `verify_phase0_deploy.py` drift assert), and `deploy-staging.yml` all present.
- **Prod migrations are gated correctly.** `apply-migrations.yml` is `workflow_dispatch`, `environment=production` (required reviewers), dry-run→apply modes, single transaction per file with `ON_ERROR_STOP=1`. Prod NeonDB is never touched from CI push paths.

Findings (ranked):
1. **[BETA-RISK — migration integrity] No version ledger; lexical ordering; duplicate prefixes; sequence gap.** The apply path is `ls "$MIG_DIR"/*.sql | sort` (line 127/141 — a plain `sort`, despite the "Sort numerically for safety" comment) and consults **no `schema_migrations`-style table** (grep for `schema_migrations|INSERT|CREATE TABLE|record` in `apply-migrations.yml` → nothing but the input description). So "all = everything not yet recorded" is backed by no ledger; re-apply safety rests entirely on per-file `CREATE … IF NOT EXISTS` guards — which `wiki/hot.md` documents failing repeatedly (033 `simulated` column, `citations_present`, `event_timestamp` → the PR #1657 `apply-and-verify` red streak). On top of that, `mira-hub/db/migrations/` has **8 duplicate numeric prefixes** (006, 008, 021, 025, 026, 027, 032, 033 — two distinct migrations each) and a **gap at 038–042** with the head at **044** (`044_workflow_runs.sql`). Lexical filename order is the *only* thing keeping two same-numbered migrations in dependency order. For a beta where a stranger's signup writes tenant rows against this schema, this is the pipeline's softest spot.
2. **[BETA-RISK — gate coverage] Smoke gates the front door, not the money path.** `smoke-test.yml` (the deploy-gate trigger) probes only `factorylm.com` (`/`, `/pricing`, `/cmms`) and `app.factorylm.com` (`/`→`/login`, `/login` renders). The North Star flow — signup → `/api/quickstart/ask` / grounded chat → proposals / readiness — is **not** in the deploy gate. Signup-flow + command-center Playwright configs exist (Lens B) but aren't wired into smoke; they'd need to run pre-deploy to actually protect the beta path. Today a deploy can go green while the authenticated money path is broken.
3. **[DOC-DRIFT — head consistency] Authority docs lag the tree.** Root `CLAUDE.md` Environments table still marks `docker-compose.staging.yml` and `@MiraStagingBot` as "*(TODO)*" — the compose file + 4 staging workflows now exist (only the staging Telegram bot remains a real TODO, and it's off the hub beta path). `wiki/hot.md`'s 2026-06-04 gate-monitor line says "head=037"; the tree is at **044** (043/044 landed; the 038–042 the wiki said were "in flight / next" under PR #1710 never landed under those numbers). The migration narrative humans read does not match the tree.

KG insight (this run's slice — `.github/workflows/` + compose, previously sparse in the product graph; hand-extracted +16 nodes / +14 edges): the enforced deploy chain `push(main) → wf_smoke_test → wf_deploy_vps` (gated `conclusion==success` + staging-gate) is clean and acyclic — **but `migration-verify.yml` and `staging-gate.yml` both write/read the same `res_neon_staging` branch with NO `needs:` edge between the two workflows.** Their ordering ("migration-verify lands first so the gate sees the new schema") lives only in a YAML *comment*, not a dependency. On the graph that's two writers into one shared mutable node with no ordering edge → if they race on a PR, the Staging Gate can grade replies against **pre-migration** schema and pass a PR whose migrations would actually break prod. The deploy gate's trustworthiness has an unenforced-ordering hole exactly where schema meets the eval.

Graphify status: unchanged from Lens A–D — graphify is not importable in this fresh sandbox and **no LLM API key** is set (GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset), so its LLM extraction phase can't run. The workflow/compose slice was hand-extracted (`_origin:"hand"`, `_lens:"E"`), same constraint noted in every prior lens.

## Lens D — detail (2026-06-08)

**Verdict: YELLOW.** The suite exists, runs hourly, and the three checkpoints that map to the North Star ("nothing breaks, leaks, or lies") — safety escalation, no-5xx, and citation-presence — pass in **every** sampled run. The problem is **measurement integrity**: the headline pass-rate is noise-dominated, so the promotion gate can't reliably catch a regression. YELLOW for the gate's trustworthiness, not RED for product safety.

What's solid (stable GREEN across all 4 sampled runs 06-07T2209 → 06-08T1123):
- **Safety checkpoints pass every run.** `safety_escalation_06` and `vfd_mitsu_04_fr_f800_safety` score 6/6 each run — the SAFETY_KEYWORDS → STOP path holds.
- **No 5xx, ever.** `No5xx` ✓ for all 57 fixtures every run — the in-process engine never crashes on a fixture.
- **Citation-presence holds.** `CitGrond` ✓ for all 57 every run — every diagnostic reply carries a `[Source:]` tag or an explicit KB-gap admission (H4 enforcer intact, matches Lens C).
- Harness is well-built: `test_offline_scorecard.py` is a regression guard (#1583) forcing each failure to record its last response; `eval_watchdog.py` parses it; an active-learning loop turns 👎 feedback into draft fixtures.

Findings (ranked):
1. **[BETA-RISK — gate integrity] The offline eval is non-deterministic.** Pass rate over 4 back-to-back runs: **47 / 50 / 47 / 47** (-of-57). ~15 distinct fixtures flip pass↔fail; only **4 fail in every run**. Root cause is in the file's own docstring (`offline_run.py:5` "LLM calls go to real providers"): "offline" = *no-VPS / in-process*, NOT *no-LLM*. Response generation hits the live Groq cascade (Gemini skipped, `:652`), so binary checkpoints that parse live text flip with provider sampling. **Consequence:** a true 1–2-case regression is indistinguishable from run noise, and a clean PR can randomly "fail" the staging gate (`smoke-test.yml` + `tests/eval/`). hot.md's "50/57 (88%)" is the high-water run (06-08T0229); median is 47 (~82%).
2. **[BETA-RISK — correctness behind a green check] Wrong-vendor citation passes `CitGrond`.** `vfd_danfoss_04_vlt_fc360_edge` (a hard-stable fail) cites *"manual SIRIUS circuit breaker 3RV"* (Siemens) for a **Danfoss VLT FC360** question — yet `CitGrond` is ✓ because a `[Source:]` tag is *present*. The checkpoint validates tag *presence*, not *relevance*, so a confidently-wrong citation reads green. This is the one finding that touches "nothing lies."
3. **[MEDIUM — known clusters] 4 hard-stable failures**, two clusters. **(A) FSM over-qualification:** `pf525_f004_02` (stuck Q2≠DIAGNOSIS), `vfd_mitsu_03_a700_parameter` (Q1≠IDLE), plus danfoss_04's progression — the engine asks one more clarifier than golden expects (the *safe* direction; it never diagnoses early). **(B) keyword-match miss:** `gs3_ground_fault_14` returns a KB-gap admission on a *core AutomationDirect GS-series* ground fault, so it reads as "doesn't know its own flagship-supported gear." Matches hot.md Cluster A/B.
4. **[LOW] Numerator drift in human-facing docs.** README baseline still says "8/11" and "51 fixtures"; the suite now runs 57. hot.md's eval-fixer "50/57" is only reproducible on the best run — the scorecard humans read overstates steady-state.

KG insight (this run's slice — `tests/eval/` was previously **0 nodes** in the product graph: the truth set meant to certify beta was itself unmapped). Hand-extracted 18 nodes / 23 edges wiring the harness into real engine anchors. The topology shows **all 6 binary checkpoints sit downstream of a single `non_deterministic` edge** (`engine.process → inference_router → groq`, live). There is no deterministic seam between the fixtures and the graders — the graph-level reason the pass-rate is a 47–50 band, not a number. Fixing the gate = inserting one record/replay (or seed) node on that edge.

Graphify status: graphify imports and the sandbox has network, but **no LLM API key** (GEMINI/GROQ/CEREBRAS/OPENAI/ANTHROPIC all unset), so its LLM extraction phase can't run — the `tests/eval` slice was hand-extracted, same constraint as Lens A/B/C.

## Lens C — detail (2026-06-08)

**Verdict: YELLOW.** The engine's grounding architecture is correct and intentional. The chat-gate carve-out for direct connections is honored exactly as the rules require, citation enforcement has a single choke-point, and groundedness scoring is intact. The one real gap — the direct-connection *rejection* contract — is a known, in-comment-acknowledged P6 deferral, not a hidden bug, and it is not reachable by the beta cohort.

What's solid:
- **Chat-gate carve-out honored.** `Supervisor._should_fire_uns_gate` (`engine.py:5297-5340`) returns `False` the moment `state.context.uns_context.source == "direct_connection"` — *before* any intent/asset checks. A certified turn (Ignition/MQTT/PLC/Hub/QR) can never be interrupted with "which machine?". This is the precise behavior `.claude/rules/direct-connection-uns-certified.md` mandates.
- **Chat-gate fires correctly for chat surfaces.** Gate only fires when `uns_ctx.confidence > 0` AND intent ∈ `_GATED_INTENTS` (diagnose/schedule) AND no `asset_identified` AND state is `IDLE` (`engine.py:1905`). Truly vague turns fall through to Q1 clarifiers rather than a confident-but-wrong confirm. No diagnosis before confirmation on Slack/Telegram.
- **Citation H4 enforcement is a single choke-point.** `enforce_citation_or_gap_admission` (`engine.py:672`) is applied once, post-quality-gate, on the diagnostic reply path (`engine.py:1153`) — every diagnostic reply gets a `[Source:]` tag or an explicit KB-gap admission. `citation_compliance.check_citation_compliance` is wired as the scorer at `engine.py:2529` + `3359`.
- **Groundedness scoring intact.** 1–5 groundedness score in the LLM contract (`engine.py:280`), low-groundedness episode tracking that persists across turns and asks exactly one clarifier before accepting (`engine.py:2348-2446`), counter reset on acceptable quality.

Findings (ranked):
1. **[BETA-RISK — contract gap, P6] Direct-connection rejection NOT implemented.** `mira-pipeline/ignition_chat.py:208`: `uns_source = "direct_connection" if (asset_id or req.asset_context) else None`. When both are absent the turn is passed to `engine.process()` as a plain chat turn → it DOWNGRADES to the chat-gate ("which machine?"). The rule requires a hard REJECT — HTTP 400/422 `{"error":"uns_required"}` — *not* a downgrade. Confirmed: the string `uns_required` exists nowhere in `mira-bots/` or `mira-pipeline/` prod code. The handler comment openly defers this to Phase 6 (matches hot.md #1658 "pending"). **Severity tempered:** the Ignition endpoint is HMAC-gated (`_verify_hmac(..., MIRA_IGNITION_HMAC_KEY)` at `ignition_chat.py:170`), so it is not reachable by a stranger signing up on the hub — the beta North Star path is the hub chat-gate, which is correct.
2. **[INFO — not a regression] Only one direct-connection surface stamps the marker.** A repo-wide search shows `ignition_chat.py:208` is the *sole* producer of `source="direct_connection"` in prod; the other surfaces the rule names (MQTT/Sparkplug via `mira-bridge`/`mira-relay`, PLC bridge, Hub display, QR deep-link) do not yet stamp it. Either not engine-wired yet or post-MVP. Flag for when they land — each must populate the marker AND honor the reject contract.
3. **[LOW — verify in audit] H4 choke-point has no structural guarantee against future diagnostic early-returns.** 53 `_make_result(...)` call sites exist; greeting/help/gate-confirm/doc-gathering intentionally early-return *before* the line-1153 enforcer (acceptable — they make no grounded claims). But nothing structurally prevents a *future* diagnostic path from returning before 1153 and skipping H4. `mira-run-hallucination-audit` should assert "every diagnostic return flows through `enforce_citation_or_gap_admission`."

KG insight (this run's graph): the `direct_connection` marker is a **single-producer / single-consumer** edge — produced only at `ignition_chat.py:208`, consumed only at `engine.py:5332` — with **no validator/reject node on the edge between them**. The contract endpoint `uns_required` is a graph orphan (zero referencing symbols anywhere in the codebase). That topology *is* the structural reason a missing-identifier turn silently downgrades instead of rejecting: there's nowhere on the producer→consumer path for the reject to live. The fix is a new guard node at the producer (adapter) before `engine.process()` is called.

## Lens B — detail (2026-06-08)

**Verdict: YELLOW.** The hub ships the spec'd surfaces and production code typechecks clean; the gaps are a data-contract drift and test debt, not broken functionality.

What's solid:
- **Typecheck:** `tsc --noEmit` → ZERO errors in production `src/` code. All 9 errors confined to tests: `namespace/tree/route.test.ts` ×6 (stale `EntityFixture` missing `files_count`/`equipment_status` after schema columns were added), `rls-deny.integration.test.ts` (unused `@ts-expect-error`), `command-center-freshness.test.ts` + `workflow.test.ts` (`bun:test` types unresolved under tsc — bun/vitest toolchain mix).
- **Spec surfaces exist:** `/api/readiness` + `/readiness/recalculate` (health_scores, L0–L6), `/api/proposals` + `[id]/decide`, `/api/namespace/{tree,node,path,files}`, `/api/wizard/[step]`, quickstart routes — matching spec §"Hub UI surfaces".
- **Decide route is disciplined:** status guard (`proposed|reviewed|needs_review` only), `SELECT … FOR UPDATE` row lock, tenant-scoped, kg_relationships mirror writes `approval_state='verified'` with provenance (`relationship_proposal_id`, COALESCE keeps first link). ADR-0017 transitions are honored in practice on this route.
- 35 unit-test files in `src/`; `api-unauth-returns-401.spec.ts` guards the auth perimeter; signup-flow + smoke + command-center (local webServer) Playwright configs all present.

Findings (ranked):
1. **[BETA-RISK — data-contract drift] `/api/proposals` vs spec/glossary.** Spec §357 defines `ai_suggestions` as the queue the `/proposals` surface renders ("N proposals pending" counts it; 6 suggestion_types); migration `027_ai_suggestions.sql` shipped the table. But the route reads `relationship_proposals` directly and its comment asserts that's canonical, "NOT a duplicate ai_suggestions table." Only kg_edge-type suggestions can ever surface, and spec §658's event-driven readiness recalc (triggered on `ai_suggestions` status change) can't fire from this path. Needs an explicit decision, not a silent winner.
2. **[MEDIUM] ADR-0017 helper still missing.** `mira-hub/src/lib/proposal-transition.ts` + `mira_bots/shared/proposal_transition.py` don't exist; transitions are hand-rolled (correctly, today) in `decide/route.ts` and `namespace/node/[id]/route.ts` + `uns-backfill.ts`. Each new write-path multiplies drift risk.
3. **[MEDIUM] Playwright gaps:** no e2e covers the proposals decide flow or readiness recalculation; default `playwright.config.ts` baseURL is **production** app.factorylm.com (e2e on prod by default); ~20 one-off `proof-pr-*.spec.ts` files are historical noise that obscure living coverage.
4. **[LOW] Test-fixture rot** (the 9 tsc errors) — will bite any CI step that typechecks tests.

KG insight (graph query this run): all 106 hub API route files in the graph are connected (zero orphaned routes — no dead surface area), and the `proposals` cluster's data edges go exclusively to `relationship_proposals`; `ai_suggestions` has exactly one referencing symbol in all of `src/` (a comment). The spec's central queue table is, in code terms, write-only infrastructure.

Prod note: `app.factorylm.com/api/health` responded to a GET this run (no error) — the 06-04 CI 502 may be transient/resolved; re-verify in CI before closing.

## Lens A — detail (2026-06-07)

**Verdict: YELLOW.** The authorization architecture is correct and intentional, not accidental. The blocker is one abuse surface, not a broken model.

What's solid:
- `sessionOr401()` decodes the next-auth JWE directly from the cookie store (works around Next 16 async-headers breakage) — consistent across most routes.
- `withTenantContext()` does the right thing: `SET LOCAL ROLE factorylm_app` (no BYPASSRLS) + transaction-local `app.tenant_id`/`app.current_tenant_id`, so RLS tenant_isolation is actually enforced and scoped per-transaction.
- Hardcoded-secret scan over `src/**/*.ts` (excluding tests): **clean** — secrets read from `process.env`.
- Service-to-service routes are token-gated: `/api/internal/kg` (`INTERNAL_KG_API_KEY`, 401/503), `/api/uploads/folder` (`HUB_INGEST_TOKEN` + UUID-validated tenant header).
- `/api/public/report` (intentionally public) is rate-limited by IP hash (5/IP/hr) and bounds input length — the right template for other public routes.

Findings (ranked):
1. **[BETA-BLOCKER] `/api/quickstart/ask` — public LLM, no rate limit.** Caps question length (1000 chars) but nothing throttles request volume. Anonymous POSTs each fire a `cascadeComplete` (700 tok, 20s). A script exhausts the free-tier providers for the whole beta cohort, or runs up cost. Retrieval is correctly tenant-scoped via `withTenantContext(quickstartTenantId())`. Fix = port the `/api/public/report` IP-hash limiter. Patch attached.
2. **[MEDIUM — tenant-scope drift] `/api/documents`.** Uses raw `pool` (RLS-bypass) and the `knowledge_entries` query has **no tenant filter**, despite the route comment asserting it "stays scoped to the caller's tenant." The `cmms_equipment WHERE id = $1` lookup is also not tenant-filtered, so an authenticated user can resolve any asset's manufacturer/model by id. `knowledge_entries` may be a deliberately universal corpus (as `/api/knowledge` states), but the comment/implementation drift should be resolved explicitly — either filter by tenant or update the contract.
3. **[LOW — confirm intent] raw-pool routes without tenant refs:** `/api/knowledge/stats|growth|manufacturer` read the universal corpus (documented), acceptable. Re-confirm none expose per-tenant rows.

KG insight (from this run's graph): of 11 fully-public (no-auth) hub routes, only `/api/public/report` both bypasses RLS *and* is public — and it is rate-limited and intentionally RLS-exempt. So the public attack surface that touches tenant data directly is small; the real public exposure is the **LLM/compute** surface (`quickstart/ask`, `mira/ask`), which authz alone doesn't cover.
