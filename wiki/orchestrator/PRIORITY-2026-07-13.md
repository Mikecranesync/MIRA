# Prioritized Backlog Audit — 2026-07-13

⚠️ audited `origin/main` (working tree `fix/precommit-untracked-sigpipe` was 136 behind). Freshness guard: STALE → all liveness checks ran against origin/main + live prod endpoints.

**Raw counts:** 113 open PRs (26 dependabot) · 545 open issues. The tracker runs stale (memory `project_issue_tracker_stale`): liveness-verified before ranking. Closed as already-fixed during this sweep: #2210 (hybrid filter is on main in `neon_recall.py`), #2152 (hub up, incident over).

**Money path (unchanged):** first paying technician via self-serve Drive Commander — #2577 (wayfinder:map), #2537 (epic). Checkout is LIVE (PR #2655, $0 E2E proven 2026-07-12). Magic-link login LIVE (PR #2656 + Resend account fix 2026-07-13).

---

## TIER 1 — SHIP / money path (this week)

| # | Item | Why | Action |
|---|---|---|---|
| 1 | **#2577 first-paying-technician wayfinder** | THE north-star epic; checkout+login now both live, so the remaining gap is *acquisition + activation* | Walk the map, mark done nodes (checkout, login), pick next unblocked node |
| 2 | **Stripe webhook E2E — cancel the $0 test sub** (Mike, 2 min) | `customer.subscription.deleted` is the only untested webhook leg on the live money path | Mike cancels in dashboard; agent verifies in `mira-web` logs |
| 3 | **`SMOKE_CHECKOUT_CHECK=1` re-arm** (PR-ready, holding for Mike's go) | Checkout is now sellable — the smoke gate should protect it; #2597's disable condition is false | One-line PR + verify smoke-test.yml goes green |
| 4 | **#2428 Hub work-order save fails** (suspect mig 060 not applied to prod) | A paying customer's first workflow breaking = churn; mig 060 exists on main, prod application unverified | Run `apply-migrations.yml` dry-run → apply; retest /workorders/new |
| 5 | **#2257 Nemotron reranker 404 on every retrieval** | Silently degrades grounding quality for every customer answer; #2662 (merged today) only made the failure *loud* | Fix the key/endpoint or explicitly disable + re-tune without reranker |

## TIER 2 — trust & security (next 2 weeks)

| # | Item | Why |
|---|---|---|
| 6 | **#2437 [P0] One identity** (mira-web JWT vs Hub NextAuth vs OW accounts) | Biggest onboarding-friction debt; a stranger who pays on mira-web must land in the Hub without a second signup. Directly on money path but multi-day — schedule, don't rush |
| 7 | **#2646 + #2622 security follow-ups** (knowledge_entries allowlist TODOs justify; RBAC Phase 2 ~19 boundaries) | Labeled security; per-tenant leak class has bitten before (#1833) |
| 8 | **#2663 kiosk register-WRITE instructions in read-only answers** (filed yesterday) | Safety-adjacent; `mira-industrial-safety` must co-activate on the fix |
| 9 | **#2258 [P1] make offline-text eval suite deterministic** | Eval flakiness poisons every regression signal the eval-fixer nightly produces |
| 10 | **#2117 [P1] prod NULL-embedding backfill** | Known retrieval-recall hole on prod (staging half done in #2093) |

## TIER 3 — cohesion debt (the 2026-07-04 audit cluster; schedule deliberately)

- **#2442 [P1] consolidate the three diagnostic engines** (Supervisor is the product) — the root of most duplicated-bug-fix waste
- **#2445 [P1] wire CMMS work-order history into diagnosis** (Atlas is an island) — also a demo differentiator
- **#2446 [P1] finish mira-sidecar sunset** — last Anthropic-legacy + ChromaDB module still in repo
- **#2444 [P1] decide mira-fault-detective** — productize behind Supervisor or archive
- **#2449 [P2] mira-pipeline has zero tests** (grade F, live chat path)

## TIER 4 — KILL / CLOSE / dedupe (hygiene sweep, ~1 focused hour, big signal payoff)

1. **~35 stale P0/P1 issues predate the current product direction** and are almost certainly dead or done: the CRA-2xx Ignition series (#1145–#1159, mid-May), April ops items (#690, #855 — Resend DNS now demonstrably works, #857, #856), #603, #108, #130, #689 (superseded by beta-gate.yml), #881 (docling deployed), #913. → close with one-line evidence each.
2. **Duplicate issue clusters from web-review runs:** login-validation ×4 (#2313/#2317/#2327/#2328), SSO-expectation ×3 (#2312/#2315/#2316), placeholder-nav ×3 (#2314/#2318/#2329), CMMS-quick-links ×2 (#2197/#2299), plus #2151 vs #2266-class seed bugs. → keep one each, close rest as dup.
3. **~50 open PRs are >3 weeks old** (oldest: #1030, 2026-05-06). May-era PRs (#1030–#1591) predate 2 major architecture shifts; several are docs/wiki snapshots that will never merge. → close-with-comment or rebase-or-close ultimatum label. Drafts idle >30d (#1340, #1266, #1974…) same treatment.
4. **26 dependabot PRs** → batch: merge the patch-level ones behind green CI in one sitting; close majors that conflict with pinned constraints (e.g. redis 5→8, python-telegram-bot 21→22 need real testing — label `defer-post-revenue`).

## GATE (waiting on Mike)

- Stripe: cancel $0 test sub (Tier 1 #2)
- Doppler `IGNITION_GATEWAY_PASSWORD` stale (fails login; lockout risk on retries)
- `SMOKE_CHECKOUT_CHECK` go-ahead (Tier 1 #3)
- #2605 wiring_connections approve-path PR #2635 is `ready-for-human` review

## Liveness verdicts recorded this sweep

- #2210 CLOSED — hybrid `(is_private=false OR tenant_id=:tid)` filter verified on main in `neon_recall.py`
- #2152 CLOSED — hub serving 200; incident 3 weeks past
- #2428 OPEN-REAL — mig `060_work_orders_source_run_diff.sql` on main; prod application unverified (db-inspect next step)
- #2257 OPEN-REAL — #2662 made fallback loud; reranker itself still down
- PR #2661 (Anthropic PrintSynth) MERGED 2026-07-13 as owner-authorized print-vision-only exception → **follow-up: add the carve-out note to CLAUDE.md/PRD §4 so agents stop flagging it**

## Top-3 (the one-screen test)

1. **Ship today:** re-arm `SMOKE_CHECKOUT_CHECK` (needs Mike's word) + apply mig 060 to prod → un-break work-order creation (#2428).
2. **Kill today:** the Tier-4 sweep — ~35 dead P0/P1s + ~10 duplicate issues + May-era PRs. One hour, halves the noise floor.
3. **Biggest blocker to first paying customer:** nothing technical is blocking the *purchase* anymore — the gap is **acquisition + first-session activation** (#2577 wayfinder + #2437 one-identity so a payer lands in a working product).
