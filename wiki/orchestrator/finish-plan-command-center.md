# Finish-Plan — Command Center / agents-celery-routines stream

> Generated by the product-orchestrator scheduled run, 2026-06-02 08:16 UTC.
> Read-only `Plan` sub-agent (substituted for `feature-dev:code-architect`, unavailable this env).
> Stream: branch `feat/agents-celery-routines` == `feat/uns-node-centric-knowledge` == `feat/hub-command-center` HEAD `66445d31` (one diff, three names). 5 ahead / 2 behind `main`. 17 files, +1145/−2.
> Money-path: Hub Command Center is a customer-facing trust/onboarding surface (score 5/5).

## 1. Blockers to merge
- **No tests for any new code** (zero `command-center`/`display_endpoints` refs in test dirs). Top blocker.
- `tree/route.ts` imports `buildTree`/`NamespaceNode` from a peer App-Router route module — low risk, verify the Next build doesn't choke on `force-dynamic`.
- `display/[id]` reachability `probe()` does a server-side `fetch` to whatever `host:port/path` the DB row holds — no allow-list. Acceptable while rows are ops-seeded; note for review.
- Celery `attempt_fix`/`diagnose_failures` are report-only stubs that shell `git`/`docker`/`gh`/`launchctl` (no auto-commit/push) — safe to ship, document the stubs in the PR.
- `start-agents.sh` hardcodes `charlienode` paths + Colima socket — CHARLIE-only dev tooling; call it out.

## 2. Migration safety (030_display_endpoints_registry.sql)
Idempotent + RLS-correct: `BEGIN/COMMIT`, `CREATE … IF NOT EXISTS`, `DROP POLICY IF EXISTS` before create, conditional `GRANT`. **Reversibility is manual only** (a `-- ROLLBACK` comment, no down-script) — flag if a real down-migration is expected. Must go **dev→staging→prod via `apply-migrations.yml` (dry-run first)**; never hand-`psql`.

## 3. UNS compliance
- `tree/route.ts` keys displays on `uns_path` (LTREE) joined to `kg_entities.uns_path` — canonical/compliant. Green dot = reachability probe, explicitly NOT signal freshness (correct).
- **Direct-connection rule**: Phase-1 surface only *displays* an iframe; it does not open a MIRA engine turn, so `direct-connection-uns-certified.md` is NOT violated today. **Follow-up:** when "Ask MIRA from this panel" lands it must carry the subtree UNS path and skip the chat-gate.
- `display/[id]/route.ts`: GET-only, 405 on writes, UUID-validated, RLS-scoped, 302 to display — honors fieldbus-readonly.

## 4. Single biggest demo-readiness gap (NEW this run)
Ignition serves `X-Frame-Options: SAMEORIGIN`, which **blocks the Phase-1 direct 302 → blank iframe**. The conveyor display will NOT render until the origin-root XFO-stripping nginx proxy is in place. This — not code quality — is what stops a live demo.

## 5. Missing tests
- API route tests for `tree` (annotate/probe-cache, empty-display) and `display/[id]` (404 disabled, 400 bad-UUID, 405 non-GET, 302 target).
- A golden/eval case asserting the live-display surface won't bypass the UNS gate once engine integration arrives.
- `tests/eval` smoke for the Celery tasks (import + `_run` timeout).

## 6. Verification commands (before merge)
```
cd mira-hub && npm run build && npm run lint && npx vitest run
gh workflow run apply-migrations.yml -f env=dev -f dry_run=true   # then staging, then prod
PYTHONPATH=$PWD python -c "import tools.agents.tasks, tools.agents.celery_app"
bash install/smoke_test.sh
# Hub smoke: admin → /command-center, tree loads + dot logic; iframe via origin-root proxy (NOT raw :8088)
```

## 7. Duplicate-branch note
`feat/agents-celery-routines`, `feat/uns-node-centric-knowledge`, `feat/hub-command-center` share identical HEAD `66445d31`. **Consolidate to one branch + one PR; delete the other two refs** to avoid a triple-merge / conflicting-review mess.

---
**Bottom line:** Code has been SHIP-READY/rebase-clean for ≥4 orchestrator cycles. The lever is founder **push + PR + merge**, plus the XFO proxy for a live demo — not more analysis.
