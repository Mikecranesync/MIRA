# PrintSense framework runtime dependencies.
#
# pydantic  — the PrintSynth typed IR + JSON Schema.
# anthropic — the ISOLATED, owner-authorized print-vision interpreter
#             (print-interpretation path ONLY — NOT the general
#             Groq -> Cerebras -> Together cascade in mira-bots/shared/inference,
#             which stays No-Anthropic). MIT-licensed. Activated at runtime by
#             ANTHROPIC_API_KEY + PRINT_VISION_PROVIDER=anthropic (Doppler).
#             Imported lazily; tests mock it, so CI does not require it.
pydantic>=2.0
anthropic>=0.116.0
