"""ProveIt bottling factory demo — simulated plant + the real Conv_Simple supervised live cell.

Reuses demo/conv_simple_demo.py (the live cell's real answer card) and mqtt_uns.broker (transport).
Deterministic; no cloud/API; no PLC writes; Hub optional. Does NOT modify the Conv_Simple demo.
"""
