# Reports

Run outputs land in `<run-id>/` and are gitignored. Curated baseline reports (the numbers a PR cites) are committed by hand as `baseline-<date>.md`; ChatGPT captures (Baseline B) go in `chatgpt/<case-id>.json`.
