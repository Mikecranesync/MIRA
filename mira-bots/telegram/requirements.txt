python-telegram-bot==21.*
httpx==0.28.*
Pillow>=12.2.0
pytesseract>=0.3.13
PyYAML==6.0.3
langfuse>=4.14.0,<5.0.0
sqlalchemy>=2.0.50
psycopg2-binary>=2.9.12
deepeval>=4.0.5
praw>=7.8.1
youtube-transcript-api>=1.2.4
# PrintSense print interpreter (ISOLATED paid print-vision path only — NOT the
# Groq->Cerebras->Together cascade in shared/inference). pydantic backs the
# PrintSynth typed IR; the paid SDKs (openai Apache-2.0 = default provider,
# anthropic MIT = retained behind PRINT_VISION_PROVIDER) are lazy-imported and
# inert without their keys.
pydantic>=2.0
anthropic>=0.116.0
openai>=1.66.0
