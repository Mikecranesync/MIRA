"""MIRA Ask API — FastAPI micro-service exposing Supervisor.process() over HTTP.

Lets the Ignition garage-conveyor dashboard POST a question + live conveyor
tags and receive a MIRA answer grounded in the live machine state.
"""
