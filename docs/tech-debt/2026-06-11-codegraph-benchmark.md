# CodeGraph Benchmark — 2026-06-11

**Branch/HEAD:** `feat/orchestrator-kg-query` @ `3182a12c` · **CLI:** 0.9.5 · generated by `tools/codegraph-benchmark.sh`

## Verdict: ✅ GREEN — Tier-1 all pass, no Tier-2 regression

Tier-1 pass: **14** · Tier-1 fail: **0** · Tier-2 notes: 3 · Tier-2 regressions: **0**

## Tier-1 — call-graph regression guards (gating)
| Symbol | Expectation | Result | Verdict |
|---|---|---|---|
| `resolve_uns_path` | callers ≥ 5, incl. engine.py | 20, has engine.py | ✅ PASS |
| `_should_fire_uns_gate` | callers ≥ 1, incl. engine.py | 9, has engine.py | ✅ PASS |
| `_maybe_dispatch_via_dst` | callers ≥ 1, incl. engine.py | 2, has engine.py | ✅ PASS |
| `_make_result` | callers ≥ 1, incl. engine.py | 20, has engine.py | ✅ PASS |

**Canary:** ✅ healthy/repaired (rc=0)  
**Stale-detection:** ✅ preflight flagged STALE on backdated index (exit 1)  
**MCP/CLI parity:** ➖ MCP stdio spike not scriptable vs running daemon — structural parity (shared DB) + preflight freshness is the standing detector (not gated)

## Tier-1 — coverage (symbol resolves)
| Symbol | Location | OK |
|---|---|---|
| `Supervisor` | mira-bots/shared/engine.py:699 | ✅ |
| `classify_intent` | mira-bots/shared/guardrails.py:777 | ✅ |
| `resolve_uns_path` | mira-bots/shared/uns_resolver.py:767 | ✅ |
| `InferenceRouter` | mira-bots/shared/inference/router.py:186 | ✅ |
| `check_citation_compliance` | mira-bots/shared/citation_compliance.py:37 | ✅ |
| `withTenantContext` | mira-hub/src/lib/tenant-context.ts:22 | ✅ |
| `sessionOr401` | mira-hub/src/lib/session.ts:85 | ✅ |
| `ingest_batch` | mira-relay/tag_ingest.py:172 | ✅ |

## Tier-2 — known blind spots (reported; fail only on regression/resolution)
| Case | Expected | Now | Status |
|---|---|---|---|
| `Supervisor` | 0 (blind spot) | 0 | ✅ as-expected (0) — class instantiation not edged (#774) |
| `check_citation_compliance` | 0 (blind spot) | 0 | ✅ as-expected (0) — import-alias call not resolved |
| same-name (`resolve_uns_path` ×N defs) | ≥2 defs, can't scope | 9 defs | ✅ as-expected — callers union across defs; grep the file |

> Tier-2 cases are limitations that `grep` covers (see `.claude/rules/codegraph-usage.md` blind spots). A 🎉 RESOLVED row means the limitation was fixed upstream — update the rules + close the relevant issue. A ❌ REGRESSED row means a previously-working relationship broke — treat as a real defect.
