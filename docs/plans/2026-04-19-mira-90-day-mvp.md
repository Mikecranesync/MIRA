# MIRA 90-Day MVP — "Co-Pilot + Knowledge" to 10 Customers @ $97/mo

**Window:** 2026-04-19 → 2026-07-19 (12 weeks)
**Target:** 10 paying logos, 1 named case study with metric, ≤3 churn
**Team:** Mike (primary, 1.0 FTE), Harper (secondary, 0.5 FTE)

---

## Currently in-flight (update before starting work)

> **Protocol:** before starting a unit or major task, edit this list — claim it. Edit it again when you merge or hand off. A merge conflict on this section is the signal that two sessions tried to claim the same work; resolve by talking before editing files.

| Unit / Task | Owner | Branch | Started | Notes |
|---|---|---|---|---|
| Unit 9a (landing page rewrite) | agent-claude | feat/mvp-unit-9a-landing | 2026-04-25 20:30 UTC | Three named features above fold: Photo-to-Asset, Magic Manual Inbox, Source-Cited Answers. Last first-dollar gate (Units 2 + 6 already merged). Scope: edit `mira-web/public/index.html` only — minimal disruption to existing polish. |

(Format: `Unit N (short name)` | `mike` or `agent-claude` or `agent-multica` | `feat/mvp-unit-N-...` | `YYYY-MM-DD HH:MM UTC` | optional)

---

## Coordination — read before starting any unit

This plan is the canonical source of truth for the active 90-day MVP. Both Mike's local sessions and remote dispatch sessions read from this in-repo copy. The previous per-user copy at `~/.claude/plans/yes-giggly-floyd.md` is now a stub pointing here.

**Three commands to run before claiming a unit:**

```bash
git fetch origin main
git log origin/main --oneline -10              # what merged recently?
gh pr list --state open --json number,title,headRefName  # what's in flight?
```

If anything in the recent commits or open PRs touches the file you're about to edit → check the **Currently in-flight** section above. If still ambiguous, ping the other engineer/session in Telegram before editing.

**Why this matters (real collisions from 2026-04-19):**
- PR #422 ("kanban triage") deleted the q-trap escape system (~50 lines) without realizing 4 tests depended on it. The deletion was hidden by a separate import-error so the failures didn't surface for hours.
- PR #418 was named `feat/citation-gate` but actually contained Celery eval timeout fixes — the misleading branch name nearly tricked a parallel session into skipping real Unit 2 work.
- 6 PRs merged to main while a parallel session was working off a stale snapshot.

**The discipline that prevents these:**
1. Always start from latest `origin/main` (force-fetch before branching)
2. Branch names match unit numbers — `feat/mvp-unit-N-<short-name>` — so intent is obvious in PR lists
3. PR descriptions include `Closes Unit N` so the plan stays current
4. Multi-purpose PRs (like #422 bundling 41 files) are banned for this MVP — one logical change per PR

---

## UX Audit Baseline (2026-04-26) — Hub + Marketing

**Baseline scores** (Playwright audit, MaintainX/Linear benchmark):

| Surface | Score | Top strength | Top gap |
|---|---|---|---|
| Hub (mira-hub) | **6.6 / 10** | Feed + Alerts pages | Desktop sidebar missing 9+ pages (issue #716 — P0) |
| Marketing (factorylm.com) | **6.2 / 10** | Fast load, honest pricing | No product screenshots in hero (issue #718 — P1) |

**Target for next iteration:** Hub ≥ 7.5 / 10, Marketing ≥ 7.0 / 10

**UX issues created:** #716 (P0), #717 (P1), #718 (P1), #719 (P1), #720–#725 (P2)  
**Regression suite:** `mira-hub/tests/e2e/ux-regression.spec.ts` — 17 tests, 15 passing, 1 intentional fail (sidebar P0), 1 reserved placeholder  
**Full report:** `mira-hub/docs/ux-audit/2026-04-26/REPORT.md` · **Tag:** `mira-hub/v1.4.0`

**Hub P0 (blocks demo):** Desktop sidebar (#716) — 2–3h fix, assign to Unit 9a sprint since it blocks customer demos

---

## Execution Status (last sync: 2026-04-26 — UX audit complete)

**Units done:**
- ✅ **Unit 0** — Anchor MVP objective in `~/.claude/CLAUDE.md` (2026-04-19)
- ✅ **Unit 1** — Codebase consolidation; PR #419 merged sha `a89f6b1`. `mira-hud` + `mira-prototype` archived to `archive/*` branches; CLAUDE.md gained Deferred Modules table; relay confirmed active SaaS infra (NOT removed). Plan vs reality drift: original plan said "remove relay from root compose" — pre-flight verification showed relay was already only in saas.yml AND load-bearing for MIRA Connect. Adjusted in flight.

**Adjacent landed in this session (unblocks downstream work):**
- PR #411 Q-trap fix → Unit Tests went green on main
- PR #414 / #415 / #416 → Eval Offline + Docker Build Check unblocked
- PR #413 Velocity #3 (pre-commit + on-save eval watcher) → live for next contributor
- **PR #418 added citation GATE infrastructure** (despite the misleading "feat/citation-gate" branch name — that PR was actually about Celery eval timeout, but the merge included citation-gate machinery): `_compute_kb_status()` in `rag_worker.py:318-339`, 🟢/🟡/⚠️ banners, `test_citation_gate.py` (40 tests). **Unit 2 EXTENDS this, does not duplicate it.**

**⚠️ Main is currently RED** — last 3 commits failing CI. Cause: post-our-merge QR pipeline work (PRs #421-#427 from Mike's parallel session) including a `qr_bridge.py` Dockerfile COPY issue. NOT from this plan's PRs. **Pre-execution gate for Unit 2:** either wait for main to be green, OR ship Unit 2 on a feature branch with local-only verification and merge once main is fixed.

**Stale branch alert:** `feat/citation-gate` exists on remote — its name is misleading. Contents are pre-citation guardrail work (vision prose stripping, options filtering), NOT source-citation logic. Ignore it. Start Unit 2 fresh from main.

**Other open PRs from this session (unrelated to MVP plan, can stay open):**
- #405 Velocity #2 CI accel (xdist + GHA cache + split SAST)
- #404 Velocity #6 spec-index docs
- #402 CX ideation docs
- 4 stale Dependabot PRs (#364-366) — defer

---

## Context

MIRA today is sprawling — 11 modules, 39 containers, 0 paying customers (per `project_elegance_audit_2026_04_14.md`). Competitive research (`project_competitive_landscape_2026_04_19.md`) confirmed the SMB AI-first diagnostic-co-pilot window is open but closes in 12-18 months as MaintainX ($150M Series D) ships AI features into its 11K-customer base.

The user's strategic ask: condense the codebase to one core deliverable (chat co-pilot + knowledge ingestion), copy the named-narrow-features framing from UpKeep Nova, ship a magic inbox + UNS-style asset model + live Excel export. Use this plan to bound the work so it doesn't drift for 6 months.

**Outcome this plan targets:** by 2026-07-19, MIRA is the most focused AI-first diagnostic option for SMB plants — with 10 paying customers proving it.

---

## Three substitutions from the user's brief (engineering pushback — confirm before execution)

| User's original ask | Substitution in this plan | Why |
|---|---|---|
| "Build ours in **MySQL** using UNS coding" | NeonDB (already in stack) + `ltree` extension for hierarchical asset paths (`enterprise.site.area.line.machine`) | Adding MySQL = another DB to operate. PostgreSQL `ltree` is built for this exact use case. UNS is a naming convention, not a DB engine. |
| "Update CSV/Excel with **Claude in Chrome**" | Simple `GET /api/v1/exports/*.xlsx` endpoint — JWT-authed; customer downloads on demand | Claude-in-Chrome is beta browser-tool, not production-ready. Chrome extension is a separate product to maintain. Same UX win without the moving parts. |
| "Update **any CMMS in realtime**" | Atlas one-way push hardening only; MaintainX/Limble/Fiix deferred | Bidirectional multi-CMMS sync is enterprise middleware (~year of work). One-way push to Atlas is shipping today; harden it. Add other CMMSs after first 10 customers prove the wedge. |

If the user pushes back on any substitution, that single unit re-scopes — the rest of the plan is unaffected.

---

## In scope (must ship by 2026-07-19) — 10 units

0. ✅ **Anchor MVP objective in global `~/.claude/CLAUDE.md`** — DONE 2026-04-19
1. ✅ **Codebase consolidation** — DONE 2026-04-19 via PR #419 (relay-stays adjustment recorded above)
2. ⏭ **Source citations in RAG** — every diagnostic answer cites manual + section (NOT page; see Unit 2 detail for honesty constraint on chunk_index vs PDF page)
3. **Magic inbox** — customer forwards email with PDF attachment → ingested into KB → reply email confirms
4. **Excel/CSV live export** — `GET /api/v1/exports/{assets,work-orders}.xlsx` returns Atlas data
5. **UNS-style asset model** — install `ltree` extension; add `assets` table with `uns_path ltree`; Telegram `/asset` command
6. **Hybrid BM25 + pgvector retrieval** — `tsvector` GIN index merged into existing 4-stage retrieval via Reciprocal Rank Fusion
7. **Asset-context pre-load on QR scan** — extend `/m/:asset_tag` to inject Atlas WO history into FSM initial context
8. **Atlas one-way sync hardening** — retry queue + admin Telegram alert on persistent failure
9. **Sales artifacts** — landing page rewrite (Nova-style named features) + 1 published case study with a metric

## Out of scope (refuse mid-execution)

- Real-time bi-directional multi-CMMS sync
- MaintainX / Limble / Fiix adapters
- Claude-in-Chrome browser extension
- AR HUD / `mira-hud` work
- `mira-connect` / Modbus / PLC drivers
- Native mobile app
- Multi-tenant SSO / enterprise auth
- Pre-commit-as-CI server-side enforcement (velocity #3 follow-up)
- PDF page thumbnails in citations (text-only is enough)
- Slack/WhatsApp inbound attachments (email only for inbox)

---

## Week-by-week ship sequence

| Week | Mike (primary) | Harper (secondary) | Demoable |
|------|----------------|--------------------|----------|
| **W1** Apr 19-25 | Unit 0 (CLAUDE.md anchor, 5 min) → Unit 1 (consolidation) → Unit 2 (citations) | Unit 9a landing draft | Cited answers in Telegram |
| **W2** Apr 26-May 2 | Unit 6 (hybrid retrieval) | Unit 9a landing ship | Fault-code lookups retrieve right manual |
| **W3** May 3-9 | Unit 3 (magic inbox) | Begin pilot customer onboarding (Unit 9b) | Forward-PDF-to-KB end-to-end |
| **W4** May 10-16 | Unit 5 (UNS) + Unit 7 (QR pre-load) — parallel files | Customer interviews | `/asset` walks hierarchy; QR scan pre-loads context |
| **W5** May 17-23 | Unit 4 (Excel export) | Capture baseline metric for case study | Customer downloads `work-orders.xlsx` |
| **W6** May 24-31 | Unit 8 (Atlas hardening) + bug burndown | Verification rubric pass on all 9 | All 9 demoable; demo script rehearsed |
| **W7-W9** Jun 1-21 | On-call only; Friday hotfix release | Outbound 30 prospects/wk, 3 demos/wk | First 3 paying logos |
| **W10-W12** Jun 22-Jul 19 | Customer-driven fixes only (no new features) | Convert pipeline; case study published end W11 | 10 paying logos |

---

## Per-unit implementation detail

### Unit 0 — Anchor MVP objective in global CLAUDE.md (W1 day 1, ~5 minutes)

Why first: every other unit risks getting derailed by a future session that "has a great idea" and pulls Mike toward a refactor / new feature / new module. Anchoring the goal in the global CLAUDE.md (loaded into every Claude Code session in this project) makes the goal visible to every future agent and gives them an explicit instruction not to scope-creep.

**File to modify:** `C:\Users\hharp\.claude\CLAUDE.md` (global config — loaded into every project)

**Exact text to add at the top of the file (right after the `# Global Claude Code Config` header, before "Behavioral Instructions"):**

```markdown
---

## Active MIRA Objective (LOCKED 2026-04-19 → 2026-07-19)

**Single goal:** ship 10 implementation units to win 10 paying SMB customers at $97/mo by 2026-07-19.

**Plan of record:** `~/.claude/plans/yes-giggly-floyd.md` — read this before suggesting anything MIRA-related.

**The 10 units (and nothing else):**
0. Anchor goal in global CLAUDE.md (this section)
1. Codebase consolidation
2. Source citations in RAG
3. Magic email inbox for manuals
4. Excel/CSV live export
5. UNS-style asset model (NeonDB + ltree)
6. Hybrid BM25 + pgvector retrieval
7. QR scan pre-loads asset context
8. Atlas one-way sync hardening
9. Sales artifacts (landing page + 1 case study)

**Rules for all sessions and subagents working on MIRA until 2026-07-19:**
- Do not propose new features outside the 10 units. Defer-don't-pivot.
- Do not propose architectural rewrites, refactors, or "while we're in here" cleanup unless it is required to ship one of the 10 units.
- Do not propose adding MaintainX, Limble, Fiix, real-time sync, mobile apps, AR HUD, Modbus, Claude-in-Chrome, or any other capability listed in the plan's "Out of scope" section.
- If the user asks about an off-objective idea: capture it as a one-paragraph note in `docs/ideation/` with today's date, then return to the active plan. Do not start building it.
- The plan's "Decision points — refuse mid-execution" list is binding. Say no to those temptations even if they sound reasonable.
- If you genuinely believe a new capability must be added to hit 10 paying customers, say so explicitly and ask the user to update the plan first. Do not silently add it.

**When this lock expires (2026-07-19) or the goal is hit early:** delete this section. Replace with the next quarter's objective. Do not let it become stale background.

---
```

**Approach:** plain markdown insertion. No code, no logic. The text above goes at the top of the file so every session loads it before reading anything else.

**Reuse:** existing CLAUDE.md structure; matches the existing `## Behavioral Instructions` and `## Rules` heading style.

**Verification:**
1. Open a new Claude Code session in any project. Confirm the "Active MIRA Objective" section appears in the loaded CLAUDE.md context.
2. Ask the new session "what should I add to MIRA?" — verify it answers with reference to the 10-unit plan, not free-form ideas.
3. Ask the new session "let's add a Limble adapter to MIRA" — verify it pushes back, citing the lock.

**DoD:** the 3 verification checks pass; section visible in `~/.claude/CLAUDE.md`.

---

### Unit 1 — Codebase consolidation (W1, ~2 days)

**Files to modify:**
- `docker-compose.yml` — verify `mira-hud`, `mira-prototype`, `mira-relay` not referenced
- `docker-compose.saas.yml` — confirm `mira-relay` lives only here
- `CLAUDE.md` — add "Deferred modules" section listing `mira-connect`, `mira-sidecar`, `mira-relay` with rationale
- `Makefile` — drop archive-module targets

**Approach:** zero new code; pure git + docs. Branch names `archive/mira-hud-2026-04` and `archive/mira-prototype-2026-04` (preserves history without destroying anything).

**DoD:** `docker compose ps` lists exactly 7 services; CLAUDE.md "Deferred" section merged.

---

### Unit 2 — Source citations (W1, ~80 minutes — re-scoped from "1 day" after 2026-04-20 reality check)

**Reality check (Phase 1 exploration, 2026-04-20 02:30 UTC):**
- **Citation gate already exists.** PR #418 added `_compute_kb_status()` in `mira-bots/shared/workers/rag_worker.py:318-339`, with 🟢/🟡/⚠️ banner injection and `mira-bots/tests/test_citation_gate.py` (40 tests). Unit 2 EXTENDS this transport layer — does NOT duplicate the gate logic.
- **System prompt already says "CITE YOUR SOURCE."** `mira-bots/prompts/diagnose/active.yaml:38` (Rule 16) instructs LLM to emit `[Source: {manufacturer} {model_number}, {section}]`. Currently advisory only — compliance not verified post-LLM.
- **Per-chunk header already partially formatted.** `_build_prompt_with_chunks()` at `mira-bots/shared/workers/rag_worker.py:376` injects `[Source: {mfr} {mdl} — {section}]` per chunk — but `section` always null because `recall_knowledge()` doesn't return it.
- **The actual gap** is just the metadata transport: `neon_recall.recall_knowledge()` SELECT at `mira-bots/shared/neon_recall.py:372-395` ignores the `metadata` JSONB column and the `source_page` column. Both exist in the DB; neither flows out.
- **Honesty constraint on "page numbers":** `source_page` in the DB schema is a chunk index (sequential ID from ingest), NOT a PDF page number. Showing `"p.47"` when we mean `"chunk 47"` would mislead technicians. Use `metadata.section` (text label like "§3.4 Overcurrent") if present; fall back to bare `[Source: {mfr} {mdl}]` with no granular pointer. Backfilling real PDF page numbers is a separate ~1-week task — out of Unit 2 scope.

**4-point implementation (~80 min total):**

1. **Extend `recall_knowledge()` SELECT to return metadata** (~30 min)
   - File: `mira-bots/shared/neon_recall.py:372-395` — add `metadata, source_url, source_page` to the SELECT
   - Parse `metadata::jsonb` client-side; expose `section`, `source_url`, and `chunk_index` (renamed from misleading `source_page`) in the returned dicts
   - Backward compatible: existing callers that ignore the new fields are unaffected
   - Mirror change in `_recall_fault_code()` and `_like_search()` so all 3 streams return the same shape

2. **Inject `section` into per-chunk prompt header when present** (~20 min)
   - File: `mira-bots/shared/workers/rag_worker.py:376` — already builds `[Source: {label}]`. Update to `[Source: {mfr} {mdl} — {section}]` when `section` is non-null; keep current shape when null
   - Confirm token budget: 3 chunks × ≤40 token header overhead = <120 tokens; <5% of typical 2-3K context budget per Phase 1

3. **Post-LLM verification + auto-append footer if citation missing** (~20 min)
   - File: `mira-bots/shared/engine.py` — locate post-LLM-call site in `Supervisor.process_full()` (need to trace; reused for v1.1 prompt audit at commit `443bc42`)
   - On DIAGNOSIS or FIX_STEP states with chunks used: regex-check response for `[Source:` substring
   - If missing: append `\n\n--- Sources ---\n[1] {Manufacturer} {Model} — {Section}` per chunk from `_compute_kb_status()`'s extracted citations
   - Risk: low — append-only; doesn't modify LLM body

4. **Wire `section` (and eventually `page_num`) into existing citation gate banners** (~10 min)
   - File: `mira-bots/shared/workers/rag_worker.py:332` — already calls `metadata.get("page_num")` (currently always None). Once Step 1 surfaces `metadata`, banners inherit `section` automatically.
   - No banner code changes needed — just verification.

**Critical files (single-pane):**
- `mira-bots/shared/neon_recall.py:329-334` — `recall_knowledge()` return signature
- `mira-bots/shared/neon_recall.py:372-395` — vector query SELECT
- `mira-bots/shared/neon_recall.py:112-163` — `_recall_fault_code()` (different schema; fault codes don't have section/page; emit `[Source: Fault Code Table]` for these)
- `mira-bots/shared/workers/rag_worker.py:346-421` — `_build_prompt_with_chunks()` chunk loop + header
- `mira-bots/shared/workers/rag_worker.py:318-339` — `_compute_kb_status()` (already extracts page_num/section, but currently fed empty metadata)
- `mira-bots/shared/engine.py` — `Supervisor.process_full()` post-LLM hook (Step 3)
- `mira-bots/prompts/diagnose/active.yaml:38` — Rule 16 (no change needed; already says cite-your-source)

**Existing infrastructure to reuse (don't reinvent):**
- `_compute_kb_status()` for citation extraction (already in main)
- `[Source: ...]` header format established by PR #418
- `mira-bots/tests/test_citation_gate.py` — 40 tests; keep them green
- `tests/eval/test_grader_citation.py` — `cp_citation_groundedness` checks numeric specs cited match retrieved chunks (existing eval coverage)

**Verification (binary; per `feedback_verify_before_done.md` — fixture-based, not absence-of-error):**
1. `pytest mira-bots/tests/test_citation_gate.py -v` — all 40 tests still pass
2. `python tests/eval/offline_run.py --suite text --judge` — sample 10 random diagnostic outputs; ≥9 contain `[Source: ...]` block
3. Manual fact-check first 3 cited chunks: open the linked source manual, verify the cited section actually contains the cited fact (no hallucination)
4. Fault-code path: query "GS20 fault F23"; verify response cites `[Source: Fault Code Table]`, not a manual chunk
5. Token-overhead measurement: log average prompt size before vs after; per-chunk header overhead must stay <120 tokens for limit=3

**Definition of Done:**
- [ ] All 4 implementation points landed in a single PR
- [ ] 40 existing citation-gate tests still pass
- [ ] 10-sample eval shows ≥9 with citation footer
- [ ] First-3-chunk manual fact-check confirms cited info exists in source
- [ ] PR opened, CI green (assumes main is fixed first per pre-execution gate)
- [ ] Demoable: a Telegram diagnostic session ends with `[Source: Yaskawa GS20 — §3.4 Overcurrent]` (or similar real-customer example)

**Why "80 min" not "1 day":** Phase 1 found PR #418 already shipped the hard part (citation gate + tests). Unit 2 is now purely the metadata transport plumbing + LLM compliance check. Original 1-day estimate was based on the pessimistic assumption that no citation infrastructure existed.

**Pre-execution gate:** Main is currently RED on the 3 most recent commits (post-merge QR work, not from this plan). Either (a) wait for main to be fixed before opening Unit 2 PR, or (b) ship Unit 2 on a feature branch with local-only verification, then merge once main is green. Recommendation: (b) — don't lose the velocity.

**What's NOT in scope (refuse mid-execution):**
- Backfilling `metadata.page_num` from PDF re-extraction — separate task ~1 week
- Showing `"p.N"` for chunk indices — would mislead technicians
- Adding a JOIN against `manuals` table for friendly title — `source_url` filename is enough for v1
- Switching the LLM cascade or model — orthogonal
- Touching the system prompt Rule 16 — already correct in `active.yaml:38`

---

### Unit 6 — Hybrid BM25 + pgvector retrieval (W2, ~2 days)

**Sequenced before email/exports** because it changes a hot table; do it pre-customer.

**New migration:** `mira-core/mira-ingest/db/migrations/006_knowledge_tsvector.sql` (bumped from 004 — QR Phase 1 took 004 + 005 overnight)
```sql
ALTER TABLE knowledge_entries
  ADD COLUMN content_tsv tsvector
  GENERATED ALWAYS AS (to_tsvector('english', coalesce(content, ''))) STORED;
CREATE INDEX CONCURRENTLY idx_knowledge_entries_content_tsv
  ON knowledge_entries USING GIN (content_tsv);
```

**Files to modify:**
- `mira-bots/shared/neon_recall.py` — add `_recall_bm25(query_text, tenant_id, limit)` ranking by `ts_rank_cd(content_tsv, plainto_tsquery('english', $1))`. Add as 5th stream into `_merge_results()`. Switch `_merge_results()` from raw-score to **Reciprocal Rank Fusion** (k=60): `score(doc) = Σ 1/(k + rank_in_stream)`.

**Reuse:** existing `_merge_results()` extension model — `recall_knowledge()` signature unchanged for callers.

**Verification:** 20-query eval set in `evals/`. Compare top-3 recall before/after on fault-code + part-number queries. **Block deploy if hybrid worsens any subset.**

**DoD:** `EXPLAIN ANALYZE` confirms GIN hit; eval shows ≥+15% top-3 recall on fault-code subset; vector-only queries unchanged.

---

### Unit 3 — Magic Inbox (W3, ~5 days)

**Provider:** Postmark Inbound (signature scheme matches existing Stripe webhook in `mira-web/src/server.ts`).

**Files to modify / create:**
- `mira-web/src/routes/inbox.ts` (NEW) — Hono route `POST /api/v1/inbox/postmark`. Validates `X-Auth-Token` (Doppler-injected). Routes by `To:` address — `kb+<tenant_slug>@inbox.factorylm.com` → resolves tenant. Streams each `Attachments[]` PDF to mira-ingest.
- `mira-web/src/server.ts` — register new route
- `mira-core/mira-ingest/main.py` — reuse existing `POST /ingest/document-kb` (line 660) **unchanged**; Hono handler proxies to it with `tenant_id` derived from address slug

**Reuse:** `check_tier_limit(tenant_id)` (`db/neon.py:186`); Stripe webhook signature pattern as scaffolding; `chunk_blocks()` (`mira-crawler/ingest/chunker.py`) downstream via Open WebUI.

**Infrastructure:** one DNS MX record at `inbox.factorylm.com`; one Doppler secret `POSTMARK_INBOUND_TOKEN`.

**Verification:**
1. Postmark "Send test inbound email" — webhook fires
2. Forward 12MB OEM PDF from real Gmail → KB search finds chunks within 90s; reply email arrives within 30s
3. Forward ZIP attachment → reply says "PDFs only"; no KB rows created

**DoD:** real customer can forward `kb+acme@inbox.factorylm.com` from Gmail and search the manual in Telegram within 2 minutes.

---

### Unit 4 — Excel/CSV live export (W5, ~5 days)

**Files to create:**
- `mira-cmms/exports.py` (NEW router) — `GET /api/v1/exports/assets.xlsx` + `/work-orders.xlsx`. JWT auth via existing tenant-scoping decorator.

**Library:** `openpyxl` (Apache-2.0 ✅) — single dependency add. Stream rows in batches of 1000.

**Reuse:** existing Atlas DB queries from `mira-bots/shared/integrations/atlas_cmms.py`; existing CSV importer (`mira-web/src/lib/csv-import.ts`) for column-naming parity (round-trip compatibility).

**Verification:** tenant with 50k work orders downloads in ≤15s; opens cleanly in Excel 2021 + LibreOffice + Google Sheets; `?format=csv` opens in Excel without "data loss" warning.

**DoD:** paying customer hits URL with their JWT, downloads `.xlsx`, emails to boss without intermediate steps.

---

### Unit 5 — UNS-style asset model (W4, ~5 days)

**New migration:** `005_assets_uns.sql`
```sql
CREATE EXTENSION IF NOT EXISTS ltree;
CREATE TABLE assets (
  id uuid PRIMARY KEY,
  tenant_id uuid NOT NULL,
  atlas_asset_id text,
  asset_tag text,
  name text,
  uns_path ltree NOT NULL,
  created_at timestamptz DEFAULT now()
);
CREATE INDEX idx_assets_uns_path ON assets USING GIST (uns_path);
CREATE INDEX idx_assets_tenant ON assets (tenant_id);
```

**Files to modify:**
- `mira-bots/` Telegram dispatcher — add `/asset` command. No-arg → top 2 levels of `uns_path`. With arg `enterprise.site_a` → descend.
- `mira-core/scripts/backfill_uns.py` (NEW) — read existing `asset_qr_tags` rows, derive `uns_path` from tenant-supplied CSV; fail loudly on tenants without mapping.

**Reuse:** `asset_qr_tags` join keeps existing QR flow intact.

**Verification:** seed 1 fixture tenant with 4-level hierarchy (15 assets). `/asset enterprise.site_a` returns level-3 children. `/asset enterprise.site_a.line_2.cell_3` returns asset card.

**DoD:** live demo scrolls hierarchy from Telegram showing real customer plant paths.

---

### Unit 7 — QR scan pre-load context (W4, ~5 days)

Runs **parallel with Unit 5** — different files, no merge conflict.

**Files to modify:**
- `mira-web/src/routes/m.ts` — extend handler: after resolving `asset_tag` → cookie, call Atlas via `mira-bots/shared/integrations/atlas_cmms.py` for last 5 work orders + asset metadata. Stuff into `user_asset_sessions.context_json` (table exists, 72h TTL).
- `mira-bots/shared/session_memory.py` — surface pre-loaded context into FSM initial-context window.

**Reuse:** `user_asset_sessions` table (existing 72h TTL); existing Atlas read path.

**Verification:** scan QR for asset with prior work orders → first MIRA reply references prior WO unprompted.

**DoD:** live demo shows "I see you had WO-1234 on this pump 11 days ago — same symptom?" automatically.

---

### Unit 8 — Atlas one-way sync hardening (W6, ~3 days)

**Files to modify:**
- `mira-bots/shared/integrations/atlas_cmms.py` — wrap `create_work_order()` in Tenacity retry decorator (3 tries, exponential backoff). On final failure, enqueue to SQLite outbox in `mira-bridge`.
- `mira-bridge` Node-RED flow — add outbox-drain flow retrying every 5 min; admin Telegram alert after 3 hours of failure.

**Reuse:** existing `mira-bridge` SQLite (already used for ingestion buffering).

**Verification:** kill Atlas mid-create → outbox row appears → restart Atlas → drain succeeds within 5 min; outbox empty. Chaos test (30 min outage) → zero lost work orders + 1 admin alert.

**DoD:** documented runbook; chaos test passes.

---

### Unit 9 — Sales artifacts (W1-W2 landing; W3-W11 case study)

**9a Landing page** — `mira-web` Hono routes `/` and existing PLG funnel. Three named features above fold: **"Photo-to-Asset"**, **"Magic Manual Inbox"**, **"Source-Cited Answers"**. Pricing block: $97/mo. Reuse existing Stripe Checkout.

**9b Case study** — recruit one paid pilot from Harper's existing pipeline. Metric target: MTTR reduction OR first-call-resolution rate. Capture baseline W3, re-measure W10, publish W11 at `/customers/<slug>`.

**DoD (9a):** Lighthouse mobile ≥90; Stripe $97 test charge completes end-to-end.
**DoD (9b):** published page with one quoted metric and customer name (with permission).

---

## Dependency graph

```
Unit 0 (CLAUDE.md anchor) ──► protects everything that follows from drift
Unit 1 (consolidation)    ──► everything else
Unit 2 (citations)        ──► Unit 9a copy ("Source-Cited Answers" claim true)
Unit 6 (hybrid search)    ──► Unit 3 (forwarded manuals searchable on arrival)
Unit 5 (UNS) ─┐
              ├──► Unit 7 (QR pre-load uses asset metadata)
Unit 7 ────── ┘
Unit 4 (exports)         standalone
Unit 8 (Atlas hardening) ──► must follow Unit 7 (more Atlas traffic)
Unit 9b (case study)     ──► requires Units 2, 3, 6 demoable
```

**Critical path:** 0 → 1 → 2 → 6 → 3 → (5,7) → 8. Unit 4 floats; Unit 9 runs continuously.

---

## "First dollar" path (take money before everything ships)

By **end of W2**, three units must land:

1. **Unit 2 — Source citations** — single most differentiated thing in a demo
2. **Unit 6 — Hybrid retrieval** — guardrail preventing demo failures on fault-code queries
3. **Unit 9a — Landing page rewrite** — needed to send prospects after demos

With these, Harper can run the **first paid demo on May 4 (start of W3)** with a Stripe link in hand. Units 3 + 7 become "coming this month" upsell talk-track in early demos.

---

## Risk register

| # | Risk | Likelihood | Impact | Mitigation |
|---|------|-----------|--------|------------|
| 1 | Postmark inbound DNS/MX propagation eats 24-48h | M | H | Order DNS change W2-Friday so W3 starts with usable MX. Sendgrid Inbound Parse as swap-in. |
| 2 | Hybrid retrieval regresses top-3 on edge queries | M | H | Mandatory eval gate (Unit 6). If regression, ship as feature flag `RAG_HYBRID=on` per-tenant. |
| 3 | `ltree` migration locks `assets` table on live tenant | L | M | `CREATE INDEX CONCURRENTLY`; low-traffic window; test on staging tenant clone first. |
| 4 | OEM PDFs >35MB blow Postmark inbound limit | M | M | Reply email instructs upload at presigned URL. Build `/ingest/large-upload` (~half-day) only if 2+ customers hit. |
| 5 | 10-customer target slips because demos book slowly | H | H | Land Units 2+6+9a by end W2 so Harper has demo content for W3 outreach. |

---

## Decision points — refuse mid-execution

When tempted, **say no** to:

- "Let's also add a MaintainX adapter while we're in the integrations file" — defer; explicitly out of scope
- "Bidirectional Atlas sync would be way better than one-way" — Unit 8 is *hardening one-way*, not adding two-way
- "Render PDF page thumbnails in citations" — text citation enough for MVP
- "Inbox should also accept Slack DMs and WhatsApp attachments" — email is one path
- "Switch Postmark → custom SES handler to save $10/mo" — saves nothing, costs a week
- "While touching `knowledge_entries`, add a `manual_title` column" — only if <30 min and migration already in flight (Unit 6 W2). Otherwise defer.
- "Set up CI for pre-commit-as-server-side enforcement" — explicitly out of scope

---

## Parallelism opportunities

| Pair | Mike file scope | Harper file scope | Why no conflict |
|------|----------------|-------------------|-----------------|
| W1 | `mira-bots/shared/neon_recall.py`, `engine.py` | `mira-web/src/routes/index.ts`, `/lib/copy.ts` | Different repos in monorepo |
| W3 | `mira-web/src/routes/inbox.ts` (new), `mira-ingest/main.py` | Customer interviews + outreach | Harper not editing code |
| W4 | `mira-bots/` Telegram + migration `005_*.sql` | `mira-web/src/routes/m.ts` + `session_memory.py` | Disjoint files; only shared schema is new `assets` table |
| W5 | `mira-cmms/` exports router | Pilot customer onboarding | Disjoint repos |

W2 and W6 are single-developer weeks (delicate change in flight; pair-program rather than split).

---

## Verification rubric (per-unit "actually working in prod" checklist)

For each unit, all four boxes must be checked before declaring done. **Verify against a real fixture, not a mock.** "No error in logs" is not success (per `feedback_verify_before_done.md`).

| Unit | (a) Real fixture | (b) Cross-component check | (c) Failure-mode check | (d) Telemetry |
|------|------------------|---------------------------|------------------------|---------------|
| **0 CLAUDE.md anchor** | Section visible in `~/.claude/CLAUDE.md` | New session loads it as context | New session refuses an off-objective ask | none |
| **1 Consolidation** | `docker compose up -d` brings up 7 services | `docker compose ps` shows zero archived modules | `git checkout archive/mira-hud-2026-04` still works | none |
| **2 Citations** | 10 sampled real diagnostic queries | Output contains `per ... p.N` for ≥9/10 | Cited page actually contains fact (manual check first 3) | Langfuse no-citation rate <5% |
| **3 Inbox** | Real Gmail forward of OEM PDF | KB search finds chunk in Telegram | ZIP attachment → friendly rejection; no orphan rows | Postmark delivery rate ≥99% |
| **4 Exports** | Tenant w/ 50k WOs downloads `.xlsx` | Opens in Excel + Sheets + LibreOffice cleanly | Wrong-tenant JWT returns 403 (not other tenant data) | Endpoint p95 <15s |
| **5 UNS** | Real customer hierarchy backfilled | `/asset` walks 4 levels in Telegram | Tenant w/o backfill → clear "no hierarchy yet" | none |
| **6 Hybrid retrieval** | 20-query eval set | Top-3 recall ≥+15% on fault-code subset | Vector-only queries top-3 unchanged | Per-stream hit counts |
| **7 QR pre-load** | Real QR scan on known asset | First MIRA reply references prior WO unprompted | Asset w/ no WO history → no fabricated context | `user_asset_sessions` row written |
| **8 Atlas hardening** | Kill Atlas mid-create | Outbox drains within 5 min | 3-hour outage → admin Telegram alert | Outbox depth gauge |
| **9a Landing** | Lighthouse mobile ≥90 | Stripe test charge $97 completes | 404 page exists and looks right | GA4 events firing |
| **9b Case study** | One named publishable quote | Metric is real data, not anecdote | Customer signs off in writing | none |

---

## Critical files (single-pane reference)

- `mira-bots/shared/neon_recall.py` — citations (Unit 2) + hybrid stream (Unit 6) both extend `_merge_results()` here
- `mira-core/mira-ingest/main.py` — `/ingest/document-kb` is proxy target for Unit 3
- `mira-core/mira-ingest/db/neon.py` — `check_tier_limit()` for per-tenant gating (line 186)
- `mira-web/src/routes/m.ts` — Unit 7's QR pre-load extension lives here
- `mira-web/src/server.ts` — Stripe webhook pattern (line 15) is template for Unit 3's `inbox.ts`
- `mira-core/mira-ingest/db/migrations/` — home for `006_knowledge_tsvector.sql` (Unit 6) + next-available-number `_assets_uns.sql` (Unit 5); only one migration in flight at a time. NOTE: 004 + 005 were taken overnight by QR Phase 1 (#421) for `tenant_channel_config` + `guest_reports`.
- `mira-bots/shared/integrations/atlas_cmms.py` — Unit 8 retry+outbox; also Unit 7's read path
- `mira-bots/shared/session_memory.py` — Unit 7's FSM context surface
- `mira-crawler/ingest/chunker.py` — `chunk_blocks()` reused downstream by Unit 3
- `docker-compose.yml`, `docker-compose.saas.yml`, `CLAUDE.md`, `Makefile` — Unit 1 surface

---

## End-to-end verification (post-W6, pre-sales)

0. `~/.claude/CLAUDE.md` "Active MIRA Objective" section loaded by every new Claude Code session; off-objective asks are refused
1. `docker compose up -d` brings up exactly 7 services
2. Telegram bot answers "GS20 fault F23" with a citation matching real manual page
3. Forward Gmail attachment to `kb+acme@inbox.factorylm.com` → searchable in Telegram in 2 min
4. Scan QR sticker on real asset → MIRA's first reply references a prior WO unprompted
5. `/asset enterprise.site_a` in Telegram walks the hierarchy
6. `GET /api/v1/exports/work-orders.xlsx` (with valid JWT) downloads cleanly in Excel
7. Kill Atlas for 30 min → no lost work orders → admin alert fires
8. Lighthouse mobile ≥90 on `factorylm.com`
9. Stripe $97 test charge completes end-to-end

If all 10 pass, proceed to W7 sales push. If any fail, fix before W6 ends — do NOT enter the sales window with a known broken capability.

---

## Linked artifacts

- Original ideation: `docs/ideation/2026-04-19-mira-dev-velocity-ideation.md`
- Competitive landscape: `~/.claude/projects/.../memory/project_competitive_landscape_2026_04_19.md`
- Elegance audit: `~/.claude/projects/.../memory/project_elegance_audit_2026_04_14.md`
- Q-trap fix (precondition for green CI): PR #411
- Velocity #2 (CI accel): PR #405
- Velocity #3 (pre-commit + watcher): PR #413
- Pattern source for spec/plan structure: `docs/superpowers/specs/2026-04-19-velocity-2-impact-graph-ci-design.md`
