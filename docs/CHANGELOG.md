# MIRA Release Notes


### v3.132.0 (2026-07-11) - feat(visual): MIRA Visual Technician Phase 2 — equipment/nameplate intelligence
- **Why:** ADR-0027 Phase 2. Adds the session-integrated equipment path (photo → identity candidates → service-pack/manual resolution → cited answer) the PRD calls the strongest-readiness capability, **reusing** the Drive Commander packs + manual retrieval + the Phase 1 session. **Stacked on Phase 1 (PR #2645).**
- **What:**
  - `mira-bots/shared/visual/equipment.py` (new) — `resolve_equipment` resolves each identity signal **independently** and compares, closing the resolver's cross-signal-conflict blind spot so it **never silently selects a pack when identifiers conflict/are incomplete** (`RESOLVED`/`AMBIGUOUS`/`CONFLICTING`/`NONE`; `pack_id` non-None only for `RESOLVED`; deterministic, no LLM). `default_manual_retriever` (injectable, tenant-scoped by the session's `tenant_id` — the only guard — vendor-relevance filtered, graceful-empty). `answer_equipment` gathers deterministic pack facts + tenant manuals as `DOCUMENTED` observations → the **unchanged** Phase 1 composer (never invents; `LIKELY` for inference; energization safety short-circuit preserved even without identity).
  - `mira-bots/shared/visual/session_service.py` (extended, additive — Phase 1 print route + `ask` byte-identical) — nameplate/equipment ingest route (raw OCR kept separate from normalized) + `ask_equipment`.
  - **No new migration** — reuses the Phase 1 `observation` ledger (candidates/confidence in JSONB metadata). `answer_composer.py` + migration 063 unchanged.
  - 64 Phase 2 tests (incl. real-Postgres tenant isolation as `factorylm_app`) + 38 Phase 1 = **102 pass, zero regressions**; ruff check + format clean.
- **Scope:** additive; no deployed service / resolver / composer / Phase-1 file touched. PR-only per the PRD; stacked on PR #2645. VERSION 3.131.0 → 3.132.0.

=======
>>>>>>> origin/main

### v3.131.0 (2026-07-11) - feat(visual): MIRA Visual Technician Phase 1 — VisualSession spine + grounded answer envelope
- **Why:** ADR-0027 Phase 1 (Snippet Interpreter MVP) of the MIRA Visual Technician PRD. The north star needs a persistent, multi-image, evidence-graded visual session with claim-level uncertainty — a genuine gap today (a session held ONE photo, replaced not accumulated). This builds the spine + the structured grounded-answer contract, **reusing** the existing extraction workers rather than rebuilding them.
- **What:**
  - `mira-hub/db/migrations/063_visual_sessions.sql` — 6 tenant-scoped, RLS'd, append-only tables (visual_session / evidence_item / region_of_interest / observation / visual_question / answer_claim), faithful to the mig-038 pattern. Tenant isolation proven on a real ephemeral `postgres:16` as the `factorylm_app` role (not superuser) — `test_visual_session_migration.py` 3/3.
  - `mira-bots/shared/visual/` — `EvidenceState` (9 states, a VIEW over ADR-0026/0017 + the Print Pack field-verify tier, mirrored by the SQL CHECK); `models`; `store` (Neon, mirrors `decision_trace.py`'s connection shape; `InMemoryVisualStore` double); `quality_gate` (pure/deterministic, numpy-optional); `answer_composer` (the safety-critical deterministic core — never invents a destination/terminal/voltage/rating, labels inference `LIKELY`, short-circuits energization questions to a safety disclaimer before consulting observations); `session_service` (injectable workers, graceful degrade); `demo` CLI.
  - Tests: 38 hermetic (composer golden + no-invented-destination + safety, quality gate, session continuity/persistence, `EvidenceState`↔SQL-CHECK parity) + 3 ephemeral-Postgres tenant-isolation (skip cleanly where Docker is absent). ruff-clean.
- **Scope:** additive new package + one migration + tests. No deployed service, adapter, or existing schema touched. PR-only per the PRD directive; the Print Pack publish path (Phase 4) is untouched. VERSION 3.129.8 → 3.131.0 (3.130.0 reserved by the concurrent Print Pack PR #2642 to avoid a VERSION collision).

### v3.129.13 (2026-07-12) - chore(deps): remove unused anthropic dependency from Telegram bot
- **Why:** MIRA policy (PR #610) removed Anthropic as a provider; the `anthropic>=0.97.0` line in `mira-bots/telegram/requirements.txt` is dead weight. Verified unused via grep across the entire Telegram bot codebase.
- **What:** removed `anthropic>=0.97.0` from `mira-bots/telegram/requirements.txt`. No code change, no behavior change — pure dependency hygiene.
- **Evidence:** `grep -rn "anthropic\|import anthropic\|from anthropic" mira-bots/telegram/` returns only the requirements.txt line (no actual usage).
- **Impact:** cleanup only — no functionality affected. VERSION 3.129.1 → 3.129.2.

### v3.129.12 (2026-07-12) - test(stripe): reconcile webhook-before-signup provisioning (#2438)
- **Why:** GitHub issue #2438 flagged a race condition: if a Stripe webhook fires before Hub signup, the payment is best-effort only—no retry. A paying customer ends up without a provisioned tenant.
- **What:** integration test `mira-web/src/lib/__tests__/stripe-webhook-before-signup.integration.test.ts` (5 test cases) exercises the full queue-and-reconcile path: webhook queues payment when Hub user doesn't exist yet; later reconciliation drains the queue and provisions the tenant; re-running reconciliation is idempotent (no double-provision); Stripe redelivery doesn't double-queue (ON CONFLICT idempotency); unmatched rows stay pending with attempt count bumped; reconciliation scoped to one email finds and retries only that email.
- **Test Results:** all 5 pass; surrounding `hub-provisioning-queue.test.ts` suite remains green (9 pass).
- **Status:** Test PASSES — the queue and reconciliation code already exists (PR #2437 added the durable queue) and works as intended. Test closes issue #2438 by proving the behavior works end-to-end. No product code change.

### v3.129.11 (2026-07-12) - feat(dogfood): Drive Commander public fault-lookup funnel regression check
- **Why:** extend the daily judge to guard the public fault-lookup money path (Drive Commander SEO funnel). The freemium landing page and fault-detail views must stay live, reachable, grounded (cited), and publicly accessible — the cash-conversion gate for the PLG funnel.
- **What:** `tools/crew/dogfood/checks/fault-lookup.check` — two-persona gate verifying that `GET /drive-commander/siemens-g120` and `GET /drive-commander/siemens-g120/faults/F30001` return HTML/JSON, contain the G120 library and fault code, ground with citations, require no login. Auto-detects mira-web base URL (defaults to staging 4200 if DF_BASE is 4101). Emits `GREEN` (funnel works), `YELLOW` (works but degraded, e.g., missing citations), `RED` (blocked), or `INFRA` (route not deployed yet — never files false product-RED on pre-deployment).
- **Base URL logic:** mira-web is on separate port (4200) in staging; the check reuses the Hub base `http://100.68.120.99:4101` and auto-swaps the port. On prod/unified reverse-proxy hosts, it falls back to the same base.
- **Integration:** auto-discovered from `tools/crew/dogfood/checks/*.check`; wired into the daily judge.sh loop as a 6th check (alongside maintenance-tech, contextualization, work-order, demo-readiness, beta-gate).
- **Verdict contract:** `GREEN` (200, G120+F30001+citations, no login) | `YELLOW` (reachable but missing citations/freemium gate) | `RED` (200 but missing content/login-blocked) | `INFRA` (404/timeout, not deployed).
- **CI:** CI runs `test_judge.sh` (hermetic, passes); dry-run judge emits `INFRA` verdict (expected until route deploys).
- `VERSION 3.129.1 → 3.129.2`.

### v3.129.10 (2026-07-12) - feat(drive-commander): public G120 fault funnel + freemium gate (Pro CTA)
- **What:** Light up the Drive Commander freemium gate for Siemens G120. Landing page at `/drive-commander/siemens-g120` (lists all 13 faults). Fault pages at `/drive-commander/siemens-g120/faults/F30001` (etc.) show: free tier = fault code + name + cited parameters (id, purpose, citation + manual excerpt); Pro lock teaser = "Full troubleshooting + wiring + reset workflow + Ask MIRA + history" with CTA to pricing page.
- **Free tier display:** fault meaning (hero) + first N cited parameters from the pack (name, purpose, manual page reference + excerpt), no guesses, all grounded.
- **Pro gate:** Unlock CTA → `/pricing?product=drive-commander-pro` ($29/mo or $197/yr, individual technician license). TODO(stripe-sku): create Drive Commander Pro price in Stripe + wire Doppler `factorylm/prd STRIPE_DRIVE_COMMANDER_PRICE_ID`.
- **Routes now rendering:** G120 landing + 13 fault pages + 18 parameter detail pages (same pattern as PowerFlex 525/40).
- **Tests:** sitemap test updated to include G120 count (was 2 packs → now 3 packs); `drive-commander.test.ts` 21/21 pass.
- **Impact:** Product feature — public, indexable, no auth, freemium gate live. Users can discover G120 faults for free; Pro subscribers see full cited troubleshooting.
- **VERSION:** 3.129.2 → 3.130.0 (minor).

### v3.129.2 (2026-07-11) - feat(drive-packs): Siemens SINAMICS G120 fault/parameter pack (cited)

### v3.129.9 (2026-07-12) - feat(drive-packs): Siemens SINAMICS G120 fault/parameter pack (cited)
- **What:** new G120 pack with 13 faults (F30001–F7011) + 18 cited parameters (P0100–P2011). Schema matches powerflex_525.json reference. 100% manual-cited from G120X operational instructions (0319_en-US).
- **Routes:** `/drive-commander/siemens-g120` (landing + faults + parameters all resolve).
- **Citation coverage:** 100% — F30001 overcurrent, F30004 motor overtemp, F30006 inverter overtemp, P1200 ramp acceleration, etc. all grounded.
- **Validation:** JSON parse OK, schema compliance, all 13 faults registered, all 18 parameters resolvable. Eval fixture `vfd_siemens_01_sinamics_g120_f30001.yaml` grounds to pack.
- **Impact:** Proves schema + renderer portability across manufacturers (Siemens/Rockwell).
- **VERSION:** 3.129.1 → 3.129.2 (patch).

### v3.129.8 (2026-07-11) - feat(ops): wire ENABLE_WO_EVIDENCE (default-off) into every engine service in saas.yml (#2445 Step 1)
- **Why:** the CMMS work-order-history evidence path (`ENABLE_WO_EVIDENCE` + `MIRA_WO_EVIDENCE_TIMEOUT_S`/`MIRA_WO_EVIDENCE_LIMIT`, shipped flag-gated OFF in #2472) was settable in code but wired into **no** deployment, so enabling it meant hand-editing several services. This makes enabling a single Doppler-var flip, with **zero runtime behavior change** until then.
- **What:** added the three vars (default-off: `${ENABLE_WO_EVIDENCE:-0}`, `${MIRA_WO_EVIDENCE_TIMEOUT_S:-3.0}`, `${MIRA_WO_EVIDENCE_LIMIT:-5}`) to **every** `docker-compose.saas.yml` service that instantiates `shared.engine.Supervisor` — verified by grepping `Supervisor(` and mapping each hit to its saas build: **mira-pipeline, mira-bot-telegram, mira-bot-slack, mira-ask** (4 — the "assume three" trap: `mira-ask`/AskMira kiosk also runs the engine). Excluded (verified): `mira-mcp` proxies to mira-pipeline over HTTP (no engine); `mira-relay`/`mira-sparkplug-consumer` are ingest-only; teams/whatsapp/reddit/gchat/email adapters instantiate `Supervisor` in code but have no saas service.
- **Docs + guard:** documented all three in `docs/env-vars.md`; new `tests/test_wo_evidence_compose.py` (4 tests) asserts every engine service carries all three vars default-off, that no non-engine service receives them, and that all three are documented (keeps env-drift green: used + documented).
- **Scope:** config + docs + test only. Default OFF — nothing enabled, no Doppler/staging/prod change, `#2445` stays open. VERSION 3.129.7 → 3.129.8.

### v3.129.7 (2026-07-11) - test(engine): golden case + CI guard for CMMS work-order-history citation (#2445)
- **Why:** the CMMS work-order-history evidence path (`ENABLE_WO_EVIDENCE`, shipped flag-gated OFF in #2472) is built + unit-tested + wired into the diagnosis prompt, but #2445's Definition of Done requires a **golden case** proving a diagnosis for an asset with prior work orders cites them as evidence. That artifact was missing.
- **What:**
  - `tests/golden_factorylm.csv` — new golden case: a recurring GS10 `oC` overcurrent on conveyor CV-101 whose ideal answer cites two prior repairs (`[WO 1042]`, `[WO 0987]`) grounded in the provided work-order context, alongside the manual `[Source:]` tag. Mirrors the real `_format_wo_evidence` block shape (`[WO <num>] <date> (<status>): <title> -- <resolution>`).
  - `tests/test_wo_evidence.py` — `test_golden_set_has_wo_citation_case`: a deterministic CI guard that the truth set keeps a work-order-citation case, cites ≥2 `[WO N]`, and every cited `[WO N]` is grounded in the case's own context (no invented citations). Keeps the DoD artifact from silently rotting (the CSV isn't auto-run by the offline eval, which loads `tests/eval/fixtures/*.yaml`).
- **Scope:** test/fixture only — no engine, prompt, or flag-default change; `ENABLE_WO_EVIDENCE` stays OFF. Enablement (document the flag + wire it into the engine service `environment:` blocks + Doppler staging→gate→prod) is the deliberate follow-up. 14/14 `tests/test_wo_evidence.py` pass. VERSION 3.129.6 → 3.129.7.

### v3.129.6 (2026-07-11) - docs(discovery): prod manual-ingest broken (dead docling) + VPS memory over-subscription
- **What:** discovery record from enabling the drive-pack bridge on prod. Read-only VPS investigation surfaced two pre-existing prod issues beyond the bridge.
- **Bridge:** `MIRA_DRIVE_PACK_BRIDGE=1` enabled + verified injected into the hourly cron; candidate creation proven on the live box via a one-shot against the cached PF525 PDF (`review_only`, `promoted:false`).
- **Bug found:** `kb_growth_cron` manual ingest has failed for ~3 weeks — `mira-docling` was removed 2026-06-06 (OOM) but `full_ingest_pipeline` still calls it at `:5001` → `Connection refused` on every PDF. A real PowerFlex-525 manual is stuck failing in the queue purely because of this; fixing it makes the bridge fire automatically.
- **Memory:** the 8 GB VPS is over-subscribed (swap 3.6/4.0 GiB) — a full staging stack (`stg-*` ≈ 870 MiB+) runs alongside prod, plus two MinIO instances + JVM services (java 777 MiB swapped). That's why docling has no room. Recommend: repoint ingest off docling onto the Tika/OW path; move staging off the prod box.
- **Scope:** docs only. No code/runtime change. Full findings: `docs/discovery/2026-07-07-prod-ingest-docling-and-vps-memory.md`.

### v3.129.1 (2026-07-10) - fix(ci): install pytest-asyncio in the offline pytest jobs — stop the asyncio_mode PytestConfigWarning→exit-1 flake
- **Why:** the `Architecture Check` job (and the two other offline pytest jobs) install only `pytest pyyaml`, but every pytest run reads `asyncio_mode = "auto"` from the shared `[tool.pytest.ini_options]` in `pyproject.toml`. Without `pytest-asyncio` present that's an **unknown ini option** → `PytestConfigWarning: Unknown config option: asyncio_mode`, which some resolved pytest versions escalate to **exit 1 despite all tests passing** (`9 passed, 1 warning` + exit 1). It flaked red on 4 PRs (#2547/#2549/#2550 Drive Commander convergence) while passing on clean-env draws (#2553).
- **What:** add `pytest-asyncio` to the three offline pytest jobs in `.github/workflows/ci.yml` — `architecture-check`, the **required** `simlab-gate` (would *hard-block* merges if it flaked the same way), and `drive-pack-extract-tests`. This makes `asyncio_mode` a recognized option → no warning → the failure mode is removed at its source regardless of pytest version. No test-behavior change: these suites are synchronous, so `asyncio_mode=auto` is a no-op for them, and `pytest-asyncio`'s only dependency is `pytest` (no chromadb shadow risk).
- **Evidence:** clean-venv repro (`pip install pytest pyyaml` → `pytest tests/test_architecture.py`) emits the `PytestConfigWarning`; adding `pytest-asyncio` removes the warning and keeps `9 passed`.
- **Impact:** infra/CI only — no `mira-bots`/product code, nothing deployed. VERSION 3.129.0 → 3.129.1.

### v3.128.5 (2026-07-10) - docs(eval): Print Translator automated benchmark + Next-Steps (Claude-judge baseline + OCR A/B)
- **Why:** durable, evidence-grounded benchmark for the Print Translator, plus the "Next-Step Plan" (freeze baseline, prove the OCR path, run the OCR A/B, regression fixtures, calibration packet) — all NON-authoritative until technician calibration, no product/prompt/deploy change.
- **What:** `docs/eval/print-translator-benchmark/` — anonymized Claude (Sonnet-subagent) judging of the 10 preserved responses on a 100-pt rubric with independent 2nd-judge escalation; **Baseline A** frozen (`BASELINE_A.sha256`, immutable); **OCR-path proof** (code trace glm-ocr → theory prompt); **Baseline B** OCR A/B (image-only vs image+OCR-proxy).
- **Key finding:** OCR grounding barely moved the mean (65.4→66.6) but **doubled the hard-failure rate (20%→40%)** — it removed 2 freelance inventions but caused OCR-error propagation + confident-wrong assignments (2 clean cases flipped to independently-confirmed hard-failures). Decision (per plan): build the smallest deterministic **evidence-binding guard**; don't optimize the prompt against the judge; honest caveat that B used a cascade-vision OCR proxy (production glm-OCR unreachable from the dev box). The 4 known hallucinations (K1/K2, control-transformer, XC00, XC90) are captured as skipped-until-guard regression fixtures; a 5-case technician calibration packet (blank scorecards) is included.
- Eval artifacts only — no `mira-bots` change, nothing deployed, images kept local/out-of-git. VERSION 3.128.4 → 3.128.5.

### v3.128.4 (2026-07-10) - docs(eval): Print Translator real-world campaign package (text-only)
- **Why:** durable record of the autonomous Print Translator evaluation that surfaced (and quantified) the classifier-gate defect fixed in v3.128.3.
- **What:** `docs/eval/print-translator-campaign/` — 25-print official-OEM `corpus_manifest`, `results/` (11 real handler runs + 10 gate-bypassed real-prompt/real-cascade/real-image explanations, OCR-empty on the dev box and caveated throughout), `review_worksheet.csv` (technician-judgment columns blank), `RANKED_REPORT.md`, `GAPS.md`, `rejected.md`, `LIVE_TELEGRAM_RUNBOOK.md`, `regression_fixtures/` (xfail, not CI-collected), `images/MANIFEST.md` (hashes + source URLs). Runner: `tools/print_translator_eval/`.
- **Copyright:** the downloaded single-page OEM schematic PNGs are kept **local and git-ignored** (`images/*.png`) — only metadata (hashes + URLs) is committed. Eval artifacts only; no production/prompt/wiring change.

### v3.128.3 (2026-07-10) - fix(vision): print-classifier gate — real electrical prints stop mis-routing to EQUIPMENT_PHOTO
- **Why:** the autonomous Print Translator campaign (`docs/eval/print-translator-campaign/RANKED_REPORT.md`) measured, on real official-OEM prints with real inference, that `VisionWorker._classify_photo` mis-classified **10/11 (91%)** to `EQUIPMENT_PHOTO`/`NAMEPLATE` — the vision model correctly described each as a drawing, but two deterministic defects blocked the Print Translator (and the whole ELECTRICAL_PRINT path) from ever triggering.
- **What:** two surgical fixes to `mira-bots/shared/workers/vision_worker.py::_classify_photo`:
  - **Precedence:** new `STRONG_PRINT_SIGNALS` set ("wiring diagram", "schematic", "ladder logic", "one-line diagram", "connection diagram", "electrical drawing", …) checked **after** the nameplate checks but **before** the equipment-face keywords — a described drawing is a drawing regardless of which equipment it depicts, so "wiring diagram of a VFD **drive**" no longer loses to the word "drive".
  - **Word-boundary matching:** new `_kw_in` helper (`(?<!\w)…(?!\w)`, cached) replaces substring matching for the equipment/print/nameplate keyword sets, so "led" no longer matches inside "ti**led**", "drive" not inside "**drive**r", "run" not inside "**run**way". NAMEPLATE_OCR_FIELDS keeps substring matching on purpose (units like "5HP"/"60Hz" abut digits).
- **Impact:** the real electrical prints the campaign submitted now reach the translator; genuine equipment faceplate + nameplate photos are unchanged (guarded by tests). No prompt, wiring-DB, or architecture change.
- **Tests:** `+12` `mira-bots/tests/test_vision_classify_print_gate.py` — pins the fix against the exact real vision-model strings captured in the campaign (GS20 "wiring diagram … drive", the "titled" substring case, ABB ACS355 "connection diagram") now → `ELECTRICAL_PRINT`, plus word-boundary cases and over-correction guards (real faceplate stays `EQUIPMENT_PHOTO`, nameplate stays `NAMEPLATE`). 231 classification-adjacent tests still pass. Staging-only, no prod deploy.

### v3.128.2 (2026-07-10) - fix(bot): lazy-load the tools/ wiring writer — stop mira-bot-telegram crash-loop
- **Incident:** rebuilding the telegram bot image (deploy #2603) surfaced a latent crash: `bot.py` -> `from shared import wiring_intake` -> `shared/wiring_intake.py` did an **eager top-level** `import wiring_schematic_import` (a `tools/` writer). The bot image ships `shared/` but not `tools/` (and the path math resolves to `/tools` in the flattened container), so every start died `ModuleNotFoundError: No module named 'wiring_schematic_import'` -> `Restarting (1)` crash-loop. Pre-existing bug from the wiring PRs, surfaced by the rebuild.
- **Fix:** make the `tools/` writer import **lazy** — `_load_schematic()` imports it on first *use* (writer/intake path only), so `import wiring_intake` and the reader Q&A path always work regardless of whether `tools/` is present. No behavior change where `tools/` is available (writer path unchanged); images without `tools/` now start cleanly instead of crash-looping.
- **Tests:** `mira-bots/tests/test_wiring_intake.py` (39) + `tests/test_wiring_schematic_import.py` (10) pass; verified `import shared.wiring_intake` succeeds with `tools/` absent from `sys.path` (the container condition).

### v3.128.1 (2026-07-10) - fix(ops): ship container ops-agents into the bot image + create the 2 missing runners
- **Why:** all three container ops-agents (`morning_brief`, `safety_alert`, `pm_escalation`) failed **every** cron run — two pre-existing breakages unrelated to the alert-routing work (#2558/#2575 only added `-e TELEGRAM_ALERT_*` to already-broken commands): (1) `mira-bots/telegram/Dockerfile` never COPYed `mira-bots/agents/` → `/app/agents/*_runner.py` = "No such file"; (2) only `morning_brief_runner.py` ever existed — `pm_escalation_runner.py` and `safety_alert_runner.py` were referenced by cron but absent.
- **What:** `Dockerfile` now `COPY mira-bots/agents/ ./agents/` (ships all three runners; they import from `/app/shared`, already present). New `pm_escalation_runner.py` (due/overdue PMs from `pm_schedules` via `shared.pm_scheduler.get_due_pms`; flags `critical`; invents nothing) and `safety_alert_runner.py` (safety-intent / `SAFETY_ALERT` turns from local `mira.db` `interactions` over 24h; concise all-clear when none). Both send **only** to the ops alert bot (`TELEGRAM_ALERT_*` → staging, same precedence as `morning_brief`), never `@FactoryLM_Diagnose`; both fail-soft.
- **Tests:** `tests/test_ops_agent_runners.py` (9 cases) — PM message building, safety digest, all-clear paths, alert-channel precedence. Staging-only; `install_crons.sh` re-installs the crontab on deploy so the three agents run for real.

### v3.128.0 (2026-07-09) - feat(telegram): Print Translator — print photo → plain-English theory of operation (read-only, grounded)
- **Why:** the wiring lane (PR-1..4) writes/reads structured connections; the lighter, higher-immediacy value for a tech is *understanding* a print. Print Translator lets a technician send a photo of an electrical print with "explain this / theory of operation" and get a senior-tech plain-English explanation — grounded ONLY in the visible print, with **no wiring-DB expansion, no new tables/migrations/Hub flow, no writes of any kind**.
- **What:** new pure module `mira-bots/shared/print_translator.py` + one additive, fall-through fast-path `_try_print_translator_reply` in `mira-bots/telegram/bot.py`. Reuses the existing vision classifier (`engine.vision`) + inference cascade (`engine.router`) + the multimodal message shape from `_analyze_schematic_with_question` — **no `engine.py` edits.**
  - **Trigger:** print-explanation caption (`is_theory_request`) AND `vision_data["classification"]=="ELECTRICAL_PRINT"`. Cheap intent reject first (no vision call for non-theory captions).
  - **Grounding:** feeds `ocr_items` verbatim + the image; `THEORY_SYSTEM_PROMPT` forbids inventing wire numbers/terminals/device tags, requires flagging unreadable items, and uses hedged framing ("According to this print…", "This appears to show…") — never claims the installed machine is actually wired that way.
  - **Output:** exact 6 sections — *What this appears to be · Main visible components · Plain-English theory of operation · What must be true for it to work · What would stop it from working · Unclear or unreadable items.*
  - **Fall-through / precedence:** ordered nameplate → wiring-intake → **print-translator** → generic; returns `False` (untouched) for non-print photos, non-theory captions, and the nameplate/drive + wiring flows. Graceful `FALLBACK_REPLY` on LLM failure (empty return **or** a raised malformed-provider exception — both degrade, never crash).
- **Doctrine:** read-only generation — **no `wiring_connections` writes, no proposed rows, no control/PLC writes, no graph ingestion.** It persists nothing.
- **Tests:** `+43` hermetic `mira-bots/tests/test_print_translator.py` + `test_telegram_print_translator.py` (vision + router mocked): intent detection, 6-section prompt, OCR-verbatim grounding / no invented labels, trigger, non-print + non-theory fall-through, nameplate/wiring unchanged, no-writes assertions, and LLM-failure fallback (return-empty **and** raised-exception, the latter from an adversarial implementation-review finding). Auto-collected by `pytest mira-bots/tests/`. No migration, no Hub change; staging-only, no prod deploy.

### v3.127.0 (2026-07-09) - feat(telegram): wiring loop — photo → proposed rows, text → verified-only cited Q&A (PR-4)
- **Why:** PR-1/PR-2 gave `wiring_connections` writers, PR-3 gave it a reader/scorecard/Q&A — the missing product loop was the phone. This turns a Telegram wiring-diagram photo into `proposed` rows and lets a technician ask wiring questions answered ONLY from verified evidence, on the surface a maintenance tech actually uses.
- **What:** new `mira-bots/shared/wiring_intake.py` (pure logic + thin DB glue) + two additive, fall-through fast-paths in `mira-bots/telegram/bot.py` — reusing every merged seam, no parallel path:
  - **Photo intake** (caption "CV-101 add this wiring"): reuses `engine._extract_schematic` (PR-2, unedited) → `wiring_schematic_import.kg_payload_to_rows` → `write_rows` → `approval_state='proposed'` rows tagged `proposed_by='telegram:wiring_intake'`. Replies a preview (component/connection counts, sample wires, "PROPOSED — not trusted" warning). Never auto-verifies.
  - **Text Q&A** ("Where does W200 land?", "CV-101 what does W200 land on?"): reuses `wiring_profile.load_profile` + `answer_wiring_question` (PR-3, unedited) — answers ONLY from `verified` rows with ≥1 citation + a `read_only=true, fallback_used=false` footer; refuses proposed-only ("needs approval"), honest "no record" when absent; **no generic LLM fallback**.
  - **Asset scoping:** if no asset is named, MIRA asks for it and writes/reads nothing — never guesses an asset. (Per-chat asset memory deliberately deferred — no reusable table without a migration; asset comes from the message.)
- **mig-026 doctrine decision (recorded, not silent):** LLM/vision-derived rows **direct-INSERT as `proposed`** via the reused PR-2 seam, NOT `ai_suggestions` — because `ai_suggestions` has no wiring `suggestion_type`, `proposal_writer` is `kg_edge`-only, and Hub `/proposals` has no wiring approve path, so Option B is a large multi-surface build, not the smallest doctrine-consistent path. Human gate preserved (proposed + reader trusts verified-only). Follow-up issue tracks the ai_suggestions/Hub-review bridge.
- **Tests:** `+52` hermetic `mira-bots/tests/test_wiring_intake.py` + `test_telegram_wiring_hooks.py` (+ `fixtures/wiring_intake/`) — intent parsing, payload→proposed rows (controls dropped), fake-cursor RLS+idempotent write, preview, verified-cited / proposed-refusal / no-record answer formatting, and Telegram routing (intake, ask-if-no-asset writes nothing, fall-through for non-wiring, no generic fallback). Includes a regression test for the asset-prefixed-question fix (asset stripped so the wire isn't mis-read as a terminal). Auto-collected by `pytest mira-bots/tests/`. No `engine.py`/`tools/`/`wiring_profile/` edits, no migration, no Hub change; staging-only, no prod deploy.
### v3.126.1 (2026-07-09) - fix(ci): make main green — Architecture Check + Eval Offline root-cause fixes
- **Why:** the two checks were red on `main` for days, jamming clean-PR signal. Root-caused all three underlying failures (not papered over).
- **Architecture Check pytest** (`test_crawler_cannot_import_bots`): `mira-crawler/tasks/eval_scorer.py` (added #2576) **statically** imported `from shared.inference.router import InferenceRouter`, coupling the crawler to the bots/shared inference cascade the layering rule forbids. Fix: resolve the router **dynamically** (`importlib.import_module`) inside the existing fail-open `try` — the scorer's router need is an *optional runtime* dependency (drive-pack rows score deterministically without it), not a static coupling. Behavior unchanged; `mira-crawler/tests/test_eval_scorer.py` 10/10 still pass.
- **Architecture Check env-drift** (the same job also runs `tools/check_env_drift.py`): `HUB_BASE_PATH` (default `/hub`) and `HUB_INGEST_TOKEN` (Telegram→Hub `/api/uploads/folder` intake, #2547) were used in compose but undocumented → 2-offender drift gate. Fix: document both in `docs/env-vars.md`; drift check now reports "OK: no uncovered env-var drift."
- **Eval Offline** (`tests/lead_hunter/test_celery_tasks.py::test_discovery_{msca,ddg}_*`): the lead-hunter tests `import discover`/`celery_tasks`/`hunt` as **bare top-level names** that collide with same-named modules in the monorepo (`mira-crawler/tasks/discover.py`, `tests/eval/celery_tasks.py`). Under the full `pytest tests/ -n auto` run, another test mutates `sys.path`/`sys.modules` first, so bare `import discover` resolved to mira-crawler's copy → `ModuleNotFoundError: No module named 'celery'/'mira_crawler'`. A one-time `sys.path.insert` at conftest import time wasn't robust to that runtime mutation. Fix: `tests/lead_hunter/conftest.py` now has an **autouse fixture** that pins `tools/lead-hunter` at `sys.path[0]` and shadow-evicts colliding modules per-test, restoring the wider suite's import state after. Verified with a faithful red→green repro of the exact CI traceback.
- **Scope:** two code files (`eval_scorer.py`, `conftest.py`) + docs (`env-vars.md`) + VERSION/CHANGELOG. No production-code behavior change; all three are test-harness/layering/doc hygiene.

### v3.126.0 (2026-07-09) - feat(wiring): machine wiring profile + trust scorecard + citable Q&A + eval pack (read-only lane over wiring_connections)
- **Why:** PR-1/PR-2 gave `wiring_connections` its first *writers* (cited YAML + schematic extractor → `proposed` rows) but the table had no *reader*. This builds the product lane that turns those rows into trustworthy maintenance intelligence — strengthening the evidence loop (proof, never guess, read-only, human-approval-before-trust), never loosening it.
- **What:** new read-only package `mira-bots/shared/wiring_profile/` — four modules, all reading the ONE `wiring_connections` table (no second architecture, no new writer, no schema change; the reader scopes an asset via the existing `evidence_summary->>'asset'`):
  - `schema.py` — `WiringConnection` / `MachineWiringProfile`. **Only `approval_state='verified'` is trusted**; `trusted()`==`approved` is the only set an answer is built from; `find_by_wire` is exact-normalized (`"200"` ≠ `"W200"` — false-positive guard).
  - `reader.py` — `profile_from_rows` (pure) + `load_profile` (SELECT-only, RLS via `set_config`, `psycopg2` local-import). Never coerces: absent optional fields stay `None`, `function_class='unknown'` stays `'unknown'`, `evidence_summary` round-trips verbatim.
  - `scorecard.py` — `score_profile` → `WiringTrustScore`, mirroring the drive-pack scorecard gate-dict + trust ladder (`no_wiring`→`proposed_only`→`partial`→`trusted`). Gates: has_wiring, has_approved (not proposed-only), approved_all_sourced, approved_human_readable, approved_field_confirmed. Deliberately NOT wired into a repo-wide CI gate over live data (CV-101 is all-`proposed` by design and MUST score `proposed_only`).
  - `ask.py` — `answer_wiring_question` → `WiringAnswer`, mirroring the `DrivePackAnswer` contract. Answers "Where does W200 land?" / "What is connected to I-00?" from **approved rows only**, ≥1 citation each; REFUSES with "not enough APPROVED evidence" when a match exists only in `proposed`/`needs_review`; honest "no record" when absent. Never invents an endpoint/wire/terminal; `read_only=True`, `fallback_used=False` always.
- **Doctrine:** trusted == verified-only; a `wiring_connections`-sourced answer always carries a citation, a refusal always carries none; unknown stays unknown; no control writes; reuses the existing writers (`tools/wiring_map_import.py`, `tools/wiring_schematic_import.py`) untouched.
- **Tests:** `+74` `mira-bots/tests/test_wiring_profile_{reader,scorecard,ask}.py` (+ `fixtures/wiring_profile/`) — six hermetic suites: approval enforcement, provenance preservation, unknown-field, false-positive (W200≠200, absent→no-record), citation-or-refuse, scorecard ladder. Auto-collected by the existing `pytest mira-bots/tests/` CI step under `--cov=mira-bots/shared` (no new CI step). No migration; staging-only, no prod deploy.


### v3.125.0 (2026-07-09) - feat(wiring): wire /api/kg/schematic output into the wiring_connections seam (PR-2)
- **Why:** PR-1 (v3.124.0) proved the dormant `wiring_connections` writer with cited YAML. PR-2 wires the **live vision extractor** into the *same* seam so a real electrical drawing can propose structured connections — reuse, not a parallel path.
- **What:** `tools/wiring_schematic_import.py` — converts a `mira-mcp` `POST /api/kg/schematic` payload (`schematic_intelligence` → `to_kg_payload`) into the **same `WiringRow` shape** and writes via the **same `write_rows`** imported verbatim from `wiring_map_import`. Only `relationship_type=='electrically_connected'` relationships become conductors (derived `controls`/`protects` edges are excluded); endpoints reuse PR-1's deterministic `uuid5`; `{"ok":true,"result":{…}}` envelope and raw payload both accepted; `--dry-run`.
- **Mapping:** `rel.source_entity_id`→source device, `properties.from_terminal` (`"K1:A1"`)→`source_terminal` (`A1`); same for dest; `properties.wire_number`→`wire_number`. `drawing_ref`/`parent_equipment_id`/schematic type/symbol subtypes preserved in `evidence_summary`; `proposed_by='llm:schematic_intelligence'`; `approval_state='proposed'`.
- **Two honest gaps surfaced (not papered over):** (1) the extractor emits **no `function_class`** (a traced connection carries only from/to/wire) → rows land `function_class='unknown'` with `evidence_summary.function_class_source='unclassified_by_extractor'`, awaiting human/later classification; (2) **doctrine tension** — migration 026 reserves direct INSERT for structured imports and routes *LLM-derived* rows via `ai_suggestions`; schematic output is LLM-derived, so this reuses the PR-1 direct-INSERT seam per instruction but keeps every row `proposed` + `proposed_by='llm:*'` (auditable), flagging `ai_suggestions` routing as the doctrine-strict follow-up. No new migration; no second table; no Drive-Pack schema change; no `schema_version` bump.
- **Tests:** `+10` `tests/test_wiring_schematic_import.py` over a fixture payload — electrical-only filtering, exact mapping, envelope/raw equivalence, provenance, `proposed` default, `unknown` fclass + gap marker, dedup, reused-seam idempotency. Named CI "Unit Tests" step. Staging-only, no prod deploy.


### v3.124.0 (2026-07-09) - feat(wiring): prove the dormant wiring_connections seam — cited YAML → proposed rows (PR-1)
- **Why:** the wiring-print recovery discovery (`docs/discovery/2026-07-09-wiring-print-extraction-recovery.md`) found the `wiring_connections` table (migration 026) provisioned + schema-verified but **DORMANT** — no code writes it. This proves that single missing seam with the lowest-risk cited data (no vision, no OCR, no PDF, no Drive-Pack schema change).
- **What:** `tools/wiring_map_import.py` — a deterministic structured import that loads the hand-authored, evidence-tagged CV-101 electrical model (`plc/conv_simple_electrical/model/{wires,terminals,devices}.yaml`) and writes each conductor into `wiring_connections` as `approval_state='proposed'`. Pure core `load_wiring_rows` (8 CV-101 E-005 input conductors; `function_class` derived from the wire's own signal/type — safety/power/ground/signal, never invented; deterministic `uuid5` soft-FK endpoints) + `write_rows` (SELECT-dedup → idempotent; the table has no UNIQUE constraint). `--dry-run` writes nothing.
- **Grounding honesty:** the CV-101 wires are all model-status `field_verify` (no as-built list; only the PLC terminal↔function map is `verified`), so every row is `proposed` — grounded in verified terminal/program evidence, awaiting field confirmation. The model `status` is preserved verbatim in `evidence_summary.model_status`; nothing is upgraded or invented.
- **Doctrine:** migration 026 reserves direct INSERT for **structured imports** (routing only LLM-derived rows through `ai_suggestions`) — this deterministic cited import is exactly that sanctioned path. Linking the soft-FK endpoint ids to real `kg_entities` is deferred to PR-2.
- **Tests:** `+8` `tests/test_wiring_map_import.py` — YAML loads, expected connections, idempotent re-write (FakeCursor), evidence/provenance preserved, `approval_state='proposed'` default. Named CI "Unit Tests" step. No migration; no prod deploy.

### v3.123.1 (2026-07-09) - fix(smoke): make the checkout→Stripe gate non-blocking until a sellable product exists
- **Why:** there is no live Stripe SKU for anonymous trial entry yet, so `factorylm.com/api/checkout/session` returns `/pricing?checkout=error` instead of 303ing to `checkout.stripe.com`. The two checkout→Stripe E2E assertions were failing the **Smoke Test** gate, which silently **jams every prod deploy** (`deploy-vps.yml` only fires when smoke passes) — for a product we can't sell yet.
- **What:** gated both checkout→Stripe assertions (`money-path.spec.ts` + `signup-flow.spec.ts`) behind a new `SMOKE_CHECKOUT_CHECK` env flag, **off by default** → `test.skip` (same idiom as the existing `SMOKE_RATE_LIMIT_CHECK` post-deploy gate). Everything else in both specs (quickstart money path, tenant/IDOR gate, pricing CTA, magic-link, login options) keeps gating. Documented the re-enable rule in `smoke-test.yml` (`SMOKE_CHECKOUT_CHECK=""` with a comment).
- **Re-enable:** set `SMOKE_CHECKOUT_CHECK=1` on both smoke steps once checkout is live. Wayfinder #2577 / Drive Commander money-slice (backlog DC-H/I/J).

### v3.123.0 (2026-07-09) - feat(eval): relational distillation — grounded KG edges from real Q&A (flywheel Phase 4b)
- **Why:** Phases 3b/4a distil coverage gaps and golden cases from captured turns; the relational half was still spec'd-but-unbuilt. A *matched* drive-pack **fault** turn is grounded-by-construction (cited, `fallback_used=False`) — real evidence that a drive family has a documented failure mode. This closes the loop by turning those turns into reviewable knowledge-graph edges.
- **What:** `tools/relational_distill.py` — a matched drive-pack fault turn → one grounded `<drive family> HAS_FAILURE_MODE <fault>` proposal. Pure core `extract_relation_assertions` (deterministic, no LLM, deduped; excludes the model token so `GS10` is never mistaken for the fault); psycopg2 `main()` resolves **both** endpoints in `kg_entities` (resolve-or-**skip**, never fabricates an entity) and writes via the existing `proposal_writer.propose_relationship_cursor` → `relationship_proposals` + `relationship_evidence` (`technician_note`, cites the source turn) + bridging `ai_suggestions(kg_edge)` → Hub `/proposals`. `--dry-run`; fail-clean when `conversation_eval.meta` is absent (reuses the Phase-1 gate).
- **Never auto-verified (Iron Rule).** The only path to a verified `kg_relationships` row is a human decide on `/api/proposals/[id]/decide` (ADR-0017, `managing-the-knowledge-graph`). Unmatched turns stay Phase-3b gaps; parameter/engine turns yield nothing (no-guess). `RESOLVED_BY`-from-corrections needs a fuzzy step and is a deliberate future extension.
- **Writer:** additive optional `evidence_type` param on `propose_relationship` / `propose_relationship_cursor` (default `oem_kb` — unchanged for all existing callers) so a conversation-sourced edge cites `technician_note` truthfully.
- **Benchmark:** extended `tools/flywheel_benchmark.py` to a **6th criterion** ("Relational distillation") + rubric; the offline fixture still scores **100%** and the grader is proven to FAIL on a wrong-edge stage.
- **Tests:** `+12` `tests/test_relational_distill.py` (extraction, model-token exclusion, dedup, no-fabrication) + `+1` benchmark sabotage test + fixture-shape assertion — both named as CI "Unit Tests" steps. No migration (all tables exist); staging-only, no prod deploy.

### v3.121.1 (2026-07-09) - chore(infra): baseline staging migration ledger to zero drift + hard-gate (W3 follow-up)
- **Why:** W3 (v3.121.0) shipped the ingest apply path + drift guard but the guard was **warn-only** because staging's `schema_migrations` ledger was 22 behind. This closes the foundation: staging is baselined to **zero drift** and the staging drift check is now a **hard gate** — so migration drift can't be silently ignored before Phase 4b adds `relationship_proposals` tables.
- **Staging baseline (verify-then-record, no blind marking):** each of the 21 remaining drifting migrations was classified by probing staging's **actual** schema (the table/column/constraint it creates). **17** whose DDL was already present were recorded via `seed-ledger` (no SQL re-run); **4** were `apply`d after verifying prerequisites + idempotency — `059_namespace_filing_cabinet` (the one genuinely-missing column `namespace_direct_uploads.makes`), plus `053` (scoped no-op DELETE), `058` (DROP+CREATE POLICY), `061` (GRANT). Result: `migration_drift.py` → **82/82 recorded, 0 drift**.
- **Hard gate:** `migration-drift-check.yml` now fails loud for **staging** (zero-drift baseline established); **prod stays warn-only** (its ledger isn't baselined — needs the approved `production`-gated path; no prod psql from a code session). Also made the drift tool's summary ASCII-clean (was an em-dash that mojibaked on Windows).
- **Runbook:** `docs/runbooks/migration-apply-and-drift.md` updated with the completed baseline (exact 17+4 reconciliation), the staging hard-gate state, and the prod-baseline follow-up.
- **Prod follow-up (owner: human/DBA):** baseline the prod ledger the same way via the approved path, then drop `--warn-only` for prod to make it a hard gate too.

### v3.121.0 (2026-07-09) - feat(infra): mira-ingest migration automation + drift detection (W3)
- **Why (root cause):** the flywheel's mig 013 (`conversation_eval.meta`) reached prod via a one-off manual script but **never staging** — `apply-ingest-migrations.yml` was **prod-only and ledger-less**, so nothing could apply ingest migrations to staging and nothing detected the gap. That silent drift is why the `--live` benchmark crashed on staging. This is the durable fix so migrations can't silently diverge across dev/staging/prod again.
- **Apply path:** `apply-ingest-migrations.yml` rewritten to mirror the Hub workflow — a `target` (staging | prod) input, a `schema_migrations` **ledger** (records each applied file; `migrations=all` auto-skips already-applied), a `seed-ledger` mode to baseline an existing DB, and a generic post-apply ledger report (was hardcoded to the 006 tsvector probe). Keeps the `-- Block 1/2` `CONCURRENTLY` split.
- **Drift guard:** `tools/migration_drift.py` compares every repo migration (both `mira-hub` + `mira-ingest` dirs) to a DB's `schema_migrations` ledger and fails (or `--warn-only` reports) on anything missing. `migration-drift-check.yml` runs it warn-only against staging daily + on demand (read-only — safe against prod). Ships warn-only until the ledgers are baselined; flip to hard-gate per the runbook.
- **Proven on staging:** the drift detector found **22** missing migrations on staging (incl. `013` and all 11 ingest migrations); applying + recording `013` via the ledger path dropped drift to 21 and cleared 013 (61/82 recorded). The apply→ledger→drift loop works end-to-end.
- **Runbook:** `docs/runbooks/migration-apply-and-drift.md` — apply order, the three modes, drift detection, the one-time ledger-baseline procedure, and the Windows `channel_binding` note. Prevents this from being a one-off manual fix.
- **Tests:** `+8` in `tests/test_migration_drift.py` (pure `find_drift`/`repo_migrations`/`render`; guards that `013_conversation_eval_meta.sql` is discoverable) — named in the CI "Unit Tests" job.

### v3.120.4 (2026-07-09) - fix(eval): gap report groups under the parameter, not the model name
- **Why:** seeding the P01.24 scenario into **staging** and running the real pipeline surfaced a grouping weakness. The gap report keyed each gap on the *first* token in the question, and a model name like `GS10` also matches the parameter-token regex — so `"GS10 P01.24 meaning?"` grouped under a phantom **`GS10`** bucket instead of `P01.24`, fragmenting the count (P01.24 showed 3 asks, not 4). Real technician traffic constantly leads with the model, so this would fragment real reports.
- **Fix:** `gap_report.grouping_token(tokens)` — prefer a **dotted parameter id** (`P01.24`) as the grouping key when present, falling back to the first token otherwise (bare fault codes like `CE10` are unaffected). `aggregate_gaps` now uses it. Verified on the real staging DB: model-led and parameter-led asks consolidate into one `P01.24 = 4` bucket, no phantom `GS10`.
- **Tests:** `+3` in `tools/drive-pack-extract/tests/test_gap_report.py` (dotted-preference both orderings, fallback for bare/empty, model-led + param-led consolidate into one bucket).

### v3.120.1 (2026-07-08) - fix(eval): flywheel tools fail-clean when capture schema is absent
- **Why:** the first `flywheel_benchmark.py --live` run against **staging** surfaced two real weaknesses. (1) Migration 013 (`conversation_eval.meta`) had reached prod (manual script) but **never staging** — apply-migrations.yml is Hub-only and doesn't cover `mira-ingest` migrations. Applied 013 to staging out-of-band (additive/idempotent), so `--live` now runs there. (2) With `meta` absent, every flywheel tool crashed with a raw `psycopg2 UndefinedColumn` traceback.
- **Fix:** `gap_report.capture_schema_ready(cur)` + `META_MISSING_MSG` — the DB-glue `main()`s of `gap_report.py`, `gap_suggestion.py`, and `flywheel_benchmark.py --live` now check for the `meta` column first and exit `3` with an **actionable** message ("apply migration 013 to this environment") instead of a traceback. Also added a best-effort `sys.stdout.reconfigure("utf-8")` to the tool `main()`s so the em-dash/`…`/`×` in summaries don't mojibake on a Windows cp1252 console.
- **Tests:** `+3` in `tools/drive-pack-extract/tests/test_gap_report.py` (fake-cursor `capture_schema_ready` true/false + the message is actionable). Verified live: `--live`, `gap_report`, `gap_suggestion --dry-run`, and `harvest` all run clean against staging after the fix.
- **Known follow-up (W3):** `mira-ingest` migrations still have **no automated apply path** (only Hub migrations are covered by `apply-migrations.yml`). That's the root cause of the staging drift; a workflow fix to cover `mira-ingest` migrations is the durable remedy (not in this PR).

### v3.120.0 (2026-07-08) - feat(eval): flywheel benchmark + grading rubric (proof the loop works)
- **Why:** Each phase shipped unit tests, but "tests pass" ≠ "the loop works end-to-end." This is the proof: a grading rubric + a benchmark that runs the **real** stage cores over one ground-truth scenario and scores how faithfully the flywheel distils it. Deterministic + CI-gated — a regression in any stage drops the score and fails the build.
- **Rubric:** `docs/specs/flywheel-benchmark-rubric.md` — 5 criteria (0–100 each, PASS ≥ 90): capture→label accuracy, gap surfacing, distill precision, no-fabrication/no-guess integrity, gate safety. The scenario is the real one that motivated the flywheel (technicians repeatedly ask GS10 **P01.24**, undocumented) plus grounded turns, other packs, an unregistered pack, engine turns, and human-corrected turns.
- **Harness:** `tools/flywheel_benchmark.py` grades the real cores — `eval_scorer.label_drive_pack_row` (Phase 2), `gap_report.aggregate_gaps` (3a), `gap_suggestion.build_gap_suggestions` (3b), `harvest_golden_cases.row_to_proposal` (4a). Offline mode prints the scored table and exits non-zero on FAIL (`--json` for machine output). `--live` (read-only, ungraded) reports throughput over real staging `conversation_eval`: turns captured, matched vs gap, top gaps, packs that would get a suggestion, harvest candidates waiting. **Current offline score: 100% PASS (24/24 decisions).**
- **Falsifiable:** `tests/test_flywheel_benchmark.py` asserts the healthy fixture scores 100 AND that each grader **fails** on a deliberately broken stage (mislabel, misrank, unregistered-pack suggestion, non-review-only/pre-accepted suggestion, phantom gap) — so a green 100 is meaningful, not vacuous. Two new CI steps in "Unit Tests": the falsifiability tests (gate) + a visible scorecard.
- **Scope:** proves the distillation *plumbing* generates the right reviewable artifacts. It does **no** model training and does not replace the human `proposed → verified` / real-manual-trust gates (`feedback_extractor_real_manual_trust_gate`) — those still apply.

### v3.119.0 (2026-07-08) - feat(eval): golden-case harvester (distillation flywheel, Phase 4a)
- **Why:** Phases 1–3 capture, score, and report drive-pack gaps. Phase 4a closes the production→test loop from `docs/specs/bot-eval-loop-spec.md` (§ "Golden-case harvester"): every reply Mike confirmed **bad** with a `correction` becomes a permanent regression case so the same mistake can't ship again. `.claude/plans/use-avail-skills-to-functional-wave.md` Phase 4.
- **Tool:** new `tools/harvest_golden_cases.py` — reads `conversation_eval WHERE human_verdict='bad' AND correction IS NOT NULL AND golden_case_added=false` (uses the mig-012 `idx_..._harvest` index) and prints paste-ready `tests/bot_regression.py` `GOLDEN_CASES` dicts (`name`/`input`/`intent`) with the correction, the bad response, and the source-row UUID as **review comments**. Run: `NEON_DATABASE_URL=… python tools/harvest_golden_cases.py [--limit N]`.
- **Print-only, human-gated — never auto-promotes.** It NEVER edits `tests/bot_regression.py`; "golden cases are forever, a human eyeball is the gate" (spec § 191–210). Its only write is the gated `--mark-applied --ids <uuid…>` pass that flips `golden_case_added=true` after Mike commits the cases (so a row is harvested at most once).
- **Spec reconciliation:** the spec sketched an `expected_substring_in_response` field, but the shipped `bot_regression.py` suite is **offline/no-LLM** (checks intent + clarification, not response text). So a harvested case emits the runnable `name`/`input`/`intent` (the intent tier checks it) plus the correction as a reviewer comment — honest to what CI actually runs. Target is `bot_regression.py` (per the spec + mig-012's own comment), not the RAGAS `golden_factorylm.csv` the plan loosely named (that column set needs retrieved `contexts`, which `conversation_eval` doesn't store).
- **Tests:** `tests/test_harvest_golden_cases.py` (+11: slugify caps/fallback, field mapping + intent default, paste-ready render + review comments, quote-escaping via `repr`, correction truncation, empty-queue message, batch footer carries all ids + the exact `--mark-applied` command, name uniqueness). Named explicitly in the CI "Unit Tests" job (root `tests/` is not collected wholesale there). Pure core; no DB in tests.

### v3.118.0 (2026-07-08) - feat(eval): drive-pack gap → review suggestion (distillation flywheel, Phase 3b)
- **Why:** Phase 3a (v3.117.0) reports which parameter/fault tokens technicians keep asking that a pack can't answer. Phase 3b closes the loop by turning a *recurring* gap into a Hub review item, so the worklist reaches the review queue instead of a file. `.claude/plans/use-avail-skills-to-functional-wave.md` Phase 3.
- **Tool:** new `tools/drive-pack-extract/gap_suggestion.py` — reads the same unmatched drive-pack turns from `conversation_eval` (reuses `gap_report.aggregate_gaps`), resolves each *registered* pack's `registry_manual_id` from `registry/sources.json`, and for packs over `--min-gap-count` writes a **review-only** `drive_pack_update` `ai_suggestions` row (`status='pending'`). Run: `NEON_DATABASE_URL=… python tools/drive-pack-extract/gap_suggestion.py --tenant-id <uuid> [--min-gap-count 3] [--dry-run]`.
- **Reuses the existing gate end-to-end — no Hub change, no migration.** The row carries `registry_manual_id`, so accepting it drives the existing `suggestion-accept.ts` → `drain_build_requests.py` → `update_candidate.py` re-extraction into a *staged candidate* (guarded by `assert_not_live_packs`). If the PDF isn't cached, the drain records `build_status='failed'` with a "supply the manual" reason and a human runs `update_candidate.py` by hand. **Never auto-promoted** (ADR-0025, `.claude/rules/train-before-deploy.md`).
- **Distinct from the kb-growth bridge:** that raises a `drive_pack_update` on a manual *hash change*; this raises one on a *question-driven coverage gap*. Deduped independently by `extracted_data->>'source'='gap_report'` (one pending row per pack). Tenant-scoped via `set_config('app.current_tenant_id', …)` (mirrors `proposal_writer.py`); `--dry-run` writes nothing.
- **Tests:** `tools/drive-pack-extract/tests/test_gap_suggestion.py` (+10: registry indexing, threshold + registered-only filtering, `registry_manual_id`/`source`/`review_only` contract, title/body worklist + by-hand command, token cap, multi-pack ranking, JSON round-trip, raw-rows→`aggregate_gaps`→build end-to-end) — auto-collected by the CI offline-tests job. Pure core; no DB in tests.

### v3.117.0 (2026-07-08) - feat(eval): on-demand drive-pack gap report (distillation flywheel, Phase 3a)
- **Why:** Phases 1–2 capture (v3.115.0) and label (v3.116.0) every drive-pack turn; unmatched turns (`meta.matched=false`) are real technician questions the pack couldn't answer. Phase 3a turns those into the **worklist** — exactly the report that would have surfaced "what is P01.24?" before we added it by hand. On-demand file delivery (Mike's choice), not a Telegram digest. `.claude/plans/use-avail-skills-to-functional-wave.md` Phase 3.
- **Tool:** new `tools/drive-pack-extract/gap_report.py` — reads unmatched drive-pack turns from `conversation_eval` (uses the migration-013 partial index), groups per `pack_id` + per parameter/fault token, ranks by ask-frequency then recency, and writes `gap_report.{md,json}`. Run: `NEON_DATABASE_URL=… python tools/drive-pack-extract/gap_report.py [--limit N] [--out DIR]`.
- **Read-only.** Never writes `conversation_eval`, never touches live packs. Token regex kept in sync with `ask._PARAM_ID_RE` (copied, not imported — matches how the standalone `tools/` scripts avoid pulling the drive_packs package). Pure aggregation + thin psycopg2 `main()` glue, mirroring `registry/drain_build_requests.py`.
- **Deferred to Phase 3b (separate gated PR):** turning a recurring gap into a `drive_pack_update` `ai_suggestions` proposal (touches the Hub seam + `drive_pack_update` contract; never auto-promoted).
- **Tests:** `tools/drive-pack-extract/tests/test_gap_report.py` (+10: token extraction, per-pack/per-token grouping + ranking, no-token bucket, sample cap, missing pack_id, empty, JSON round-trip, MD rendering + pipe-escaping) — auto-collected by the CI offline-tests job. 10 green; ruff clean.

### v3.116.0 (2026-07-08) - feat(eval): auto-scorer/labeler for conversation_eval (distillation flywheel, Phase 2)
- **Why:** Phase 1 (v3.115.0) started capturing every drive-pack Q&A into `conversation_eval`; those rows had no signal yet. Phase 2 builds the never-built scorer (`docs/specs/bot-eval-loop-spec.md`, spec'd but only the logger/migration ever shipped) so the flywheel can rank gaps and verified answers. `.claude/plans/use-avail-skills-to-functional-wave.md` Phase 2.
- **Deterministic drive-pack labeler (no LLM):** `mira-crawler/tasks/eval_scorer.py::label_drive_pack_row` — a `matched` pack answer is grounded-by-construction → `auto_score=5`; an unmatched turn is a correct decline (no fabrication, no-guess intact) → `auto_score=3`, with the knowledge-gap signal carried by `meta.matched=false` for the Phase 3 gap report (not by the score). Cheaper + more reliable than judging these.
- **LLM-judge for engine/LLM turns:** new rubric `mira-bots/shared/eval_score_rubric.py` (5 criteria — answered / no-hallucination / no-redundant-Qs / cited-when-claimed / tone → `overall` 1–5 + reasoning, strict-JSON parse w/ clamp + mean-fallback) scored via `InferenceRouter.complete()` cascade **Groq→Cerebras→Together** (the spec's "Gemini" is stale; Anthropic banned, PRD §4). Writes `auto_score / auto_score_breakdown / scorer_reasoning / scorer_model / scored_at` (columns already in migration 012) off the existing `idx_conversation_eval_unscored` queue.
- **Guardrails:** fail-open end-to-end (no provider / unparseable judge / DB hiccup leaves the row unscored for a later retry, never raises into the worker); no migration (012 columns reused); no change to `engine.py`/`ask_api`/`drive_pack.py`/`ignition_chat.py`. Cross-package router import uses the established `mira-crawler`→`mira_bots.shared` path pattern.
- **Not in this PR (deploy step):** wiring the VPS Celery-beat schedule (`mira_eval.score_conversation_eval`, 03:00 UTC) — prod-touching, gated separately.
- **Tests:** `mira-bots/tests/test_eval_score_rubric.py` (+9: parse/clamp/fence-strip/mean-fallback/garbage) and `mira-crawler/tests/test_eval_scorer.py` (+10: deterministic matched/unmatched labels, mocked-router judge, fail-open on disabled/None/empty/unparseable, meta coercion) — added to the CI offline-tests job. 19 green; ruff check + format clean.

### v3.115.0 (2026-07-08) - feat(eval): capture drive-pack Q&A into conversation_eval (distillation flywheel, Phase 1)
- **Why:** MIRA's best grounded intel — every deterministic drive-pack answer, *and* the unmatched "gap" questions (e.g. "what is P01.24?" before we added it) — was invisible to the eval store: the three drive-pack paths (`_try_nameplate_drive_pack_reply`, `_try_drive_pack_followup`, `drive_command`) return early and never called `log_turn`. This is Phase 1 of the answer-distillation flywheel (`.claude/plans/use-avail-skills-to-functional-wave.md`): **capture** now, so later phases can mine gaps and distill verified answers into packs/KG/golden **through the existing proposed→verified gates** (never model weights, never auto-promoted).
- **Capture column:** migration `mira-core/mira-ingest/db/migrations/013_conversation_eval_meta.sql` adds a nullable `meta JSONB` to `conversation_eval` (additive, backfill-free) + a partial index for `meta->>'surface'='drive_pack'` (the gap-report hot path).
- **Logger:** `conversation_logger.log_turn(..., meta=)` serialises `meta` best-effort into the JSONB column (unserialisable → NULL, never drops the row); fail-open + 2 s cap unchanged; back-compat (`meta=None` when omitted).
- **Wiring:** new `bot._capture_drive_pack_turn` / `_drive_pack_meta` label each drive-pack turn with `{surface, entry, pack_id, matched, matched_kind, answer_source, resolved, (nameplate) resolution, tenant_id}` from `DrivePackAnswer.to_dict()` + `PackResolution` + `_intake_meta`. Called **after** the reply at all three sites — no added user-visible latency, no reply/behaviour change. `matched=False` is the knowledge-gap signal.
- **Guardrails:** read-only capture; PII-sanitised (existing `sanitize_text`); no LLM; no change to `engine.py`/`ask_api`/`drive_pack.py`/`ignition_chat.py`/the dispatcher gate. The engine + Slack paths already logged.
- **Tests:** `test_conversation_logger.py` (+3: meta serialises, `meta=None` back-compat, unserialisable→NULL, INSERT-columns guard incl. `meta`) and new `test_telegram_drive_capture.py` (+7: matched/unmatched/nameplate meta labels, capture calls `log_turn` with the right shape, fail-open on bad update, end-to-end follow-up capture). Drive-pack + telegram suite green (102).

### v3.114.1 (2026-07-08) - feat(crawler): safe allowlist→queue populator (Drive Commander Phase 4)
- **Why:** the `kb_growth` scheduled routine is proven end-to-end (OCR/Tika + Bravo embeddings + KB store, #2562) but `manual_queue.json` is empty — the cron has no fuel. Phase 4 makes queue population **safe and repeatable** without hand-editing the queue or scraping.
- **New `mira-crawler/cron/queue_populate.py`:** reads a curated, version-controlled allowlist YAML and appends **eligible, deduped, provenance-stamped** entries to `manual_queue.json`. Dedupe mirrors `hydrate_from_manual_cache` (skips URLs already queued or already in `knowledge_entries`, plus intra-allowlist dupes and malformed entries). Every entry gets provenance: `source`, `family`, `trust_status`, `queue_reason`, `discovered_at`. Pure DI core (`build_queue_entries`) is unit-tested offline; `main()` wires to the cron's `load_queue`/`save_queue`/`url_already_ingested`. CLI: `--allowlist <yaml> [--reason …] [--dry-run]`.
- **New `mira-crawler/cron/allowlists/drive_manuals.yaml`:** tiny curated allowlist — AutomationDirect **GS20** + Rockwell **PowerFlex 525**, both verified to serve `application/pdf` to the pipeline UA. Flags that the `sources.yaml` GS10 URL (`gs10m.pdf`) is **stale (404)** — do not re-add until the current path is verified.
- **Tests `mira-crawler/tests/test_queue_populate.py` (18):** eligibility, dedupe (queue/ingested/intra-allowlist), provenance stamping, malformed-entry + dry-run handling, allowlist parsing, and repo-allowlist validity. All green.
- **Runbook `docs/runbooks/drive-manual-queue-population.md`:** discover→queue→cron flow, how to verify+add URLs, dry-run/populate, inspect (`--status`), pause/rollback, add-a-new-vendor-allowlist safely. Never promotes drive packs; graceful failure on dead URLs.
- **Scope/safety:** additive; no change to the consumer cron or the ingest pipeline; `manual_queue.json` stays git-ignored runtime state.
- **Why:** live staging testing asked "what is P01.24?" as a follow-up. The continuity fix (v3.113.0) kept it in the GS10 conversation, but the pack documented only `P09.03` (+ 9 fault codes), so it honestly answered "not documented." This adds the real parameters so the follow-up gives a genuine, cited answer instead of a refusal.
- **What (all grounded, no fabrication):** added 7 P01 "Basic Parameters" to `packs/durapulse_gs10/pack.json` — `P01.00` (max operation frequency), `P01.01` (motor base frequency), `P01.02` (motor rated voltage), `P01.12` (accel time 1), `P01.13` (decel time 1), `P01.22` (JOG frequency), and **`P01.24` (S-curve for acceleration begin time 1)** — plus keypad view-cards for P01.12/P01.13/P01.24. Every field (name, range, default, unit, purpose) is transcribed from the **official DURApulse GS10 User Manual (1st Ed., Rev B)** chapter 4 (`cdn.automationdirect.com/static/manuals/gs10m/ch4.pdf`), each with a page citation (P01 summary p.4-8; details p.4-69/4-70; keypad procedure p.3-6).
- **Result:** "what is P01.24?" now returns `GS10 parameter P01.24 [S-curve for acceleration begin time 1]: …` with `[Source: … p.4-70]`, `matched=parameter`, `source: drive_pack`. Same for the other six. The **no-guess contract is intact** — a still-undocumented parameter (e.g. `P02.00`) still answers "not documented, here's what is covered," never a fabricated value.
- **Hub copy synced** byte-for-byte (`mira-hub/src/lib/drive-packs/gs10-pack.json`) per `test_drive_pack_hub_copy_sync`.
- **Tests:** `test_drive_packs.py` updated to assert P09.03 **and** P01.24 presence + that every parameter is `manual_cited` with a page (not an exact count, so future expansion won't break it); `test_telegram_drive_followup.py` updated — the reported P01.24 turn now asserts a real cited answer, and a new case pins the still-uncovered-parameter (P02.00) honest path. Drive-pack + telegram suite green (98).
- **Scope:** pack data + tests only. No resolver/engine/schema change; none of the protected files.

### v3.113.0 (2026-07-08) - feat(telegram): drive-conversation continuity — text follow-ups stay in the pack (un-gated)
- **Bug (staging):** after a nameplate photo correctly answered "what does CE10 mean?" for a GS10, the very next **text** turn ("what is P01.24?") returned the **invite-only enrollment wall** and had no memory of the GS10. Cause: the photo path answers *before* the enrollment gate (`shared/chat/dispatcher.py`), but a text turn goes straight through `handle_message → dispatcher.dispatch`, which gates unenrolled/non-admin users; and the fast path never remembered the resolved pack, so even past the gate the text turn didn't know it was about the GS10.
- **Fix — multi-turn continuity (the previously-deferred flow, now built):** a **bot-local** per-chat store `telegram_drive_context(chat_id, pack_id, updated_at)` — a **dedicated** SQLite table, separate from the engine's `conversation_state`, so there is **no `engine.py` change** (engine stays the reasoner, not a state bag). A nameplate/`/drive` identification writes it; a 30-min TTL keeps stale context from hijacking a new topic.
- **Text fast path:** `handle_message` now calls `_try_drive_pack_followup` **before** the engine dispatch. With a fresh drive context AND text that maps to the pack (or reads like a drive question — a parameter id `P09.03`, a fault code, or drive vocabulary), it answers from the pack: read-only, cited, **un-gated** (same public-OEM contract as the photo path — an unenrolled tester isn't dropped to the enrollment wall mid-conversation). General chat, FSM `yes`/`no` confirmations, and non-drive questions fall through **unchanged** to the normal gated dispatch.
- **No-guess preserved:** an undocumented parameter (P01.24 isn't in the GS10 pack yet — it carries P09.03 + 9 fault codes) gets the pack's honest "not documented, here's what is covered" answer, **never a fabricated value**. Expanding GS10 parameter coverage from the manual is separate, grounded content work (flagged, not done here).
- **Tests:** `mira-bots/tests/test_telegram_drive_followup.py` (+7): context round-trip + TTL; covered-parameter answered from pack; **the reported P01.24 case stays in-context + honest, not the invite wall**; non-drive text and bare `yes` fall through; no-context and stale-context fall through. Full drive/telegram suite green (35); ruff clean; Hub GS10 pack copy still synced.
- **Scope:** `bot.py` + one new test + doc/runbook updates. No change to `engine.py` / `ask_api` / `drive_pack.py` / `ignition_chat.py` / the dispatcher gate (general chat stays gated exactly as before).

### v3.112.0 (2026-07-08) - feat(drive-packs): Phase 2 — nameplate Asset Identity Packet + Manual Applicability (#2561)
- **What:** turns a nameplate scan into reusable, evidence-separated asset identity instead of using it only for one-shot answer routing (follow-on to #2560; issue #2561). Additive; the no-guess resolver is unchanged.
- **Raw OCR preserved** — `mira-bots/shared/workers/nameplate_worker.py::extract()` now returns `raw_text` (the verbatim, untruncated model response) on every path (success, JSON-parse-failure, and `None` on empty), alongside the existing 8 interpreted fields. Raw evidence is no longer discarded.
- **Asset Identity Packet** — new pure module `mira-bots/shared/drive_packs/asset_identity.py`: `AssetIdentityPacket` (frozen dataclass keeping `raw_text` **separate** from interpreted fields) + `build_asset_identity(nameplate, raw_text, resolution, evidence_image_id)`. `sku_prefix` is derived only from the leading catalog token of `model_number` (`GS11N-20P2` → `GS11N`); never fabricated; `approval_status` starts `"unreviewed"` (a packet is a proposal). Pure, never raises.
- **Telegram fast path preserves evidence** — `bot.py::_try_nameplate_drive_pack_reply` now builds the packet from the extracted fields + resolution and audit-logs a concise identity summary instead of silently dropping the fields after routing. No reply/behavior change; still read-only, no LLM fallback, no engine/FSM touch. (Persisting the packet to a Hub review record is Phase 3.)
- **Manual Applicability Artifact** — new pure module `tools/drive-pack-extract/registry/applicability.py`: `ManualApplicability` + `applicability_from_source()` reshapes a manual-registry entry (`sources.json`, key `manuals`) into structured applicability, deriving `applies_to_catalog_prefixes` (e.g. GS11N/GS13N) while excluding series names (GS10/GS-10). Never fabricates.
- **Parity guard** — `mira-bots/tests/test_manual_applicability_parity.py` proves applicability survives source → pack: every `applicable_drive_models` entry for a live pack appears in that pack's `aliases`/`match_keywords` (verified for `durapulse_gs10` **and** `powerflex_525`). Data-layer invariant; NOT a runtime resolver change.
- **Design doc** `docs/drive-commander/phase2-asset-identity-and-applicability.md` (architecture, packet schema, applicability, trust rules; the Hub asset-candidate review record is **Phase 3 / design-only**).
- **Trust rules held:** manufacturer-only / ratings-only / ambiguous still refuse; serial identifies an asset but doesn't select a pack; catalog-backed aliases only (GS12N/GS14N stay excluded — not in the official GS10 spec).
- **Tests:** +16 (asset identity) +6 (raw_text) +5 (applicability parity); full drive-pack + nameplate suite green (85 + 6); ruff clean; Hub GS10 pack copy still byte-synced. Built with Haiku + Sonnet subagents on disjoint files; integration + doc-accuracy pass by the orchestrator.
- **Scope:** no change to `engine.py` / `ask_api` / `drive_pack.py` / `ignition_chat.py`; resolver unchanged.

### v3.129.5 (2026-07-11) - docs(plan): prod ingest storage fixes — embedder (Bravo Ollama down) + KG upsert ON CONFLICT
- **Discovery (issue #2562 Phase 1 OCR proof):** the OCR path works end-to-end (scanned PDF → Tika → 224 chars) but the chunk is **never stored / not citable**. Root cause is **not** OCR — it's the storage layer.
- **Primary:** prod embeddings go to Bravo's home-lab Ollama (`OLLAMA_BASE_URL=http://100.86.236.11:11434` in `factorylm/prd`), which is **offline** (`curl … :11434 → rc=7 connection refused`) → `0 chunks stored` for every manual (OCR *and* text-layer). `knowledge_entries.embedding` is `vector(768)` (nomic-embed-text), so any replacement must be 768-dim-compatible.
- **Secondary:** `kg_writer.py` `ON CONFLICT (tenant_id, entity_type, name)` errors "no matching constraint" despite a matching non-partial unique index (`kg_entities_tenant_type_name_key`) — anomalous; needs a staging repro. Blocks the KG write, not the citable chunk.
- **Plan doc:** `docs/plans/2026-07-08-prod-ingest-storage-fixes.md` — immediate unblock (restart Bravo Ollama), durable options (prod-resident Ollama container vs cloud 768-dim provider — **owner decision**), KG constraint-promotion fix, and the re-run verification. Plan only — no prod code change or deploy.

### v3.111.2 (2026-07-08) - fix(drive-packs): GS10 pack matches real nameplate catalog prefixes (GS11N/GS13N)
- **Bug the staging smoke found:** a real GS10-series drive photographed on the bench reads **MODEL: GS11N-20P2** — field nameplates print the *catalog part number*, never the series name "GS10". The `durapulse_gs10` pack's `family.aliases` / `nameplate.match_keywords` were only `["GS10","DURApulse","GS-10"]`, so `resolve_service_pack` correctly **refused to guess** (manufacturer recognized, no model match) — right behavior, but the pack data couldn't route a genuine GS10 drive. The resolver was NOT weakened; the data feeding it was completed.
- **Fix (grounded in official evidence, no fabrication):** add the GS10 series' catalog prefixes **`GS11N`** and **`GS13N`** to both `packs/durapulse_gs10/pack.json` (`aliases` + `match_keywords`) and the manual registry `tools/drive-pack-extract/registry/sources.json` (`applicable_drive_models`). Per AutomationDirect's GS10 spec (`cdn.automationdirect.com/static/specs/gs10drives.pdf`), the GS10 line has **exactly two** prefixes — `GS11N` (1-phase 120/230V) and `GS13N` (3-phase 230/460V). **`GS12N`/`GS14N`/600V do not exist in the GS10 line and were deliberately NOT added** (the strategy doc suggested them, but adding unverified SKUs would violate the no-guess/no-fabrication rule).
- **Result:** `GS11N-20P2` (and `GS13N-*`) now resolve to `durapulse_gs10`; a CE10 question returns the grounded, cited pack answer with footer `source: drive_pack · pack: durapulse_gs10 · fallback_used: false · read_only: true`.
- **No over-match:** GS11N/GS13N are unique to the GS10 line — the GS20 family (SKUs `GS21N`/`GS23N`) still does **not** match GS10 (invariant kept green). Manufacturer-only / ambiguous inputs still refuse.
- **Tests (`mira-bots/tests/test_service_pack_resolver.py`, +5):** GS11N-20P2 resolves; GS13N-43P0 resolves; GS11N end-to-end CE10 answer carries the footer fields; GS23N (GS20) still refuses; pack metadata carries GS11N/GS13N. Full drive-pack + nameplate suite green (63 passed); ruff clean; both JSON files validated.
- **Scope:** pack/registry data + tests only. No resolver/engine code change, no schema change, none of the protected files. The broader Nameplate-OCR / Manual-Applicability strategy (Asset Identity Packet, applicability-artifact schema, sources→pack propagation, Hub asset intake) is **phased as follow-up** — this ships only the immediate, grounded GS10 fix.

### v3.111.1 (2026-07-08) - fix(compose): Tika healthcheck uses a bash TCP probe (curl not in the image)
- **Bug (cosmetic false-unhealthy):** both `mira-tika` (`docker-compose.saas.yml`, prod `mira-tika-saas`) and `stg-mira-tika` (`docker-compose.staging-vps.yml`) used `test: ["CMD","curl","-f","http://localhost:9998/tika"]`, but the `apache/tika:3.1.0.0-full` image ships **no `curl`/`wget`** — so the internal healthcheck always failed and Docker reported `(unhealthy)` even though Tika was serving fine (proven repeatedly by an **external** `curl localhost:9998/tika` → "This is Tika Server (Apache Tika 3.1.0)"). Observed during the 2026-07-08 OCR activation.
- **Fix:** switch both to a dependency-free bash `/dev/tcp` port probe: `["CMD-SHELL", "timeout 5 bash -c '</dev/tcp/127.0.0.1/9998'"]`. The image is Ubuntu-based (`/bin/bash` present), so this succeeds when Tika is listening and needs no extra binary. Same `interval/timeout/retries/start_period`.
- **Scope/safety:** compose healthcheck test string only; no image, port, network, or service topology change; identical fix mirrored in prod + staging composes. Nothing `depends_on` Tika's health today, so this only removes the misleading `(unhealthy)` label.

### v3.110.2 (2026-07-08) - fix(staging): deploy-staging rebuilds mira-bot-telegram by default (stop bot image drift)
- **Bug (staging-only, deployment drift):** the staging Telegram smoke — nameplate photo + caption → Drive Commander pack answer (#2554) — returned **stale pre-#2554 behaviour** ("invite-only" + "Photo queued for knowledge base"). Root cause: `deploy-staging.yml`'s default `TARGETS` was `mira-pipeline mira-mcp mira-hub mira-web` — it **omitted `mira-bot-telegram`**. The build + `--force-recreate` steps are scoped to `$TARGETS`, and the full-stack `up -d` never rebuilds an existing image, so `stg-mira-bot-telegram` kept running a **2026-05-25 image** (verified via `docker image inspect`) even as newer commits deployed. The 2026-06-20 wording change (`d9b564b3`) and the 2026-07-07 fast path (`_try_nameplate_drive_pack_reply`, #2554) were never in the running staging container.
- **Fix:** add `mira-bot-telegram` to the default `TARGETS` so every staging deploy rebuilds it. **`mira-bot-slack` is deliberately NOT added** — staging runs no Slack bot (`docker-compose.staging-vps.yml` omits `SLACK_BOT_TOKEN` by design), so listing it would fail `docker compose build`.
- **Guard:** `tests/test_deploy_staging_targets.py` (+2) — (1) the default `TARGETS` must include `mira-bot-telegram`; (2) every default target must be a real service in `docker-compose.staging-vps.yml` (catches a phantom target like `mira-bot-slack` before deploy time). pytest+pyyaml only.
- **Prod unaffected:** `deploy-vps.yml` already includes `mira-bot-telegram` in its default targets; prod's image is current (2026-07-08, healthy). This changes **staging deploy config only** — no prod behaviour, no app/runtime code, none of the protected files (`engine.py`/`ask_api`/`drive_pack.py`/`ignition_chat.py`).
- **Ops:** staging already redeployed out-of-band (`gh workflow run deploy-staging.yml --ref main -f services=mira-bot-telegram`); `stg-mira-bot-telegram` rebuilt from `main` and healthy. This PR makes that the default so it can't drift again.

### v3.110.1 (2026-07-08) - chore(staging): add stg-mira-tika so OCR/Tika can be proven in staging before prod
- **Why:** the OCR drain path (#2539/#2548) and its `mira-tika` service exist **only** in `docker-compose.saas.yml` (prod). Staging (`docker-compose.staging-vps.yml`) had no Tika and no crawler, so Tika activation could not be proven before prod. This adds a staging mirror so the dev→staging→prod promotion discipline applies to Tika too.
- **What:** new `mira-tika` service (container `stg-mira-tika`) using the **same pinned image** as prod (`apache/tika:3.1.0.0-full`), on `staging-net`. Staging **shares the prod host**, and prod's `mira-tika` already owns host `9998`, so this publishes on **`127.0.0.1:9999:9998`** (loopback-only; container port unchanged). `restart: unless-stopped`, `mem_limit: 1g`, healthcheck on `:9998/tika`.
- **Activation (host-safe staging value):** `TIKA_URL=http://localhost:9999` in Doppler `factorylm/stg` (distinct from prod's `http://localhost:9998`) — set only after `curl http://localhost:9999/tika` returns success. Brought up by the existing `deploy-staging.yml` (`up -d` full stack).
- **Scope/safety:** additive staging-only service; stock image, no build; loopback-bound; prod compose untouched; no drive-pack promotion. Honest caveat: staging still has no crawler, so this proves the **image/service runs and answers** — not end-to-end OCR ingest (that requires the crawler + a real scanned PDF).

### v3.110.0 (2026-07-07) - feat(telegram): nameplate-photo → Drive Commander service-pack answer
- **Gap closed:** a technician on Telegram had no bound asset, so a drive question could only be answered by typing the drive name (`/drive gs10 what is CE10`, #2525/#2526). The reliable context Telegram *does* have is a **nameplate photo**. A nameplate photo **with a question caption** (e.g. a GS10 nameplate + "what does CE10 mean?") now resolves the correct **approved** service pack and returns a cited pack answer — read-only, no LLM fallback, no guessing.
- **New surface-agnostic contract — `mira-bots/shared/drive_packs/resolver.py`:** `resolve_service_pack(*, question, drive_name, asset_make_model, nameplate, explicit_pack_id, allow_candidates)` → `PackResolution(pack_id, confidence["high"|"medium"|"none"], source, evidence, reason, ambiguous)`. Pure — no I/O, no DB, no LLM; never raises; **never returns a non-live pack id**. Resolution order `explicit_pack_id → drive_name → question → asset_make_model → nameplate`; the first signal with **exactly one** live-pack match wins, a 0-match signal falls through, and an **ambiguous** signal (>1 live pack named) refuses immediately without trying a weaker signal. Approval is structural: only packs under `mira-bots/shared/drive_packs/packs/` (`durapulse_gs10`, `powerflex_525`) are loadable; an explicit candidate id (`powerflex_40`) is refused.
- **Telegram fast path — `mira-bots/telegram/bot.py`:** factored `_format_drive_pack_reply` (now shared with `/drive`); added `_try_nameplate_drive_pack_reply`, hooked at the top of `_dispatch_single_photo`. It reuses the Supervisor's own `NameplateWorker` to **extract** fields only, resolves them via `resolve_service_pack`, and answers via `answer_question`. It only claims the turn on a confident LIVE-pack resolution (or a recognized-manufacturer refusal); every other photo falls through **unchanged** to the existing engine-dispatched flow. Never touches `engine.process`/the FSM.
- **Why no engine-level UNS backstop (carried from #2551):** `uns_context` clobbers `model` with the fault code (GS10→CE10), `product_family` is unset, and `manufacturer` alone is ambiguous — inference from weak UNS state would misroute. The connected-asset fallback stays only where a reliable make/model signal exists (Hub asset-chat, Ignition). Telegram's reliable signal is the nameplate, not inference.
- **Deferred (documented, not a gap):** the two-turn "scan nameplate then ask later" flow — the only per-chat state is the engine's private `conversation_state` (`_load_state`/`_save_state`), so stashing a `pack_id` there would need a forbidden `engine.py` change. The shipped single-message (caption-on-photo) slice is complete; a future multi-turn design must use a dedicated non-engine per-chat KV, not the FSM. Runbook: `docs/drive-commander/telegram-manual-intelligence.md`.
- **Tests:** `tests/test_service_pack_resolver.py` (+11: pack_id, candidate-reject, question, fault-code-doesn't-derail, manufacturer-only-refuse, real ambiguity, nameplate, no-signal, never-raises) and `tests/test_telegram_nameplate_ask.py` (+6: nameplate+caption cited answer, unmatched→won't-guess, manufacturer-only→ask-for-model; `NameplateWorker.extract` mocked, no vision/network). Full drive-pack regression set green (88 passed); ruff clean.
- **Scope/safety:** additive; read-only; no drive writes; no LLM fallback; tenant behavior unchanged. `engine.py`, `ask_api/drive_pack.py`, `ignition_chat.py` untouched (#2526/#2536/#2551 tests stay green).

### v3.109.1 (2026-07-07) - chore(cron): schedule the drive-pack build drain + OCR drain safety-net
- **Wiring, not features.** The two Drive Commander drains shipped in v3.109.0 (#2548/#2549) were both **opt-in workers with no scheduler** — so accepted candidate-builds and quarantined `needs_ocr` PDFs never actually drained on prod. This adds both to `scripts/install_crons.sh`, the source-of-truth crontab installer ("never edit crontab directly — add agents here"), so `bash scripts/install_crons.sh` on the VPS registers them.
- **Drive-pack build drain — `*/15 * * * *`:** `doppler run -- python3 tools/drive-pack-extract/registry/drain_build_requests.py`. Drains the Hub candidate-accept `build_requested` markers → trust-preserving extractor+grader into staged `candidates/` (guarded by `assert_not_live_packs`; **never promotes** to a served pack — human-gated per ADR-0025). No-op when the queue is empty. Run as a script path (the worker does a sibling `import update_candidate`). Issue #2544 / PR #2549.
- **OCR drain safety-net — `30 3 * * *`:** `doppler run -- python3 mira-crawler/cron/kb_growth_cron.py --drain-needs-ocr`. Re-OCRs (via Tika) any `needs_ocr` PDFs that accumulated while Tika was unreachable at ingest time; new scanned PDFs are OCR'd inline by the hourly KB Growth run (#2548), so this only sweeps residue. **INERT until `TIKA_URL=http://localhost:9998` is set in Doppler `factorylm/stg`+`prd`** — the host cron cannot reach the container-DNS default `mira-tika:9998`. Issue #2539 / PR #2548.
- **Activation (operator, human-run):** (1) `doppler secrets set TIKA_URL=http://localhost:9998` in `stg`+`prd`; (2) one-time backlog clear `ssh factorylm-prod "cd /opt/mira && doppler run -- python3 mira-crawler/cron/kb_growth_cron.py --drain-needs-ocr"`; (3) `ssh factorylm-prod "cd /opt/mira && bash scripts/install_crons.sh"` after this deploys. Both entries run under `doppler run --`, so Doppler injects `NEON_DATABASE_URL` / `TIKA_URL`.
- **Scope/safety:** additive crontab entries only; no code path changed; both drains read-only w.r.t. served packs and idempotent.

### v3.109.0 (2026-07-07) - feat(hub): honest KB-gap admission on evidence-less asset-chat (H4 parity, #2542)
- **Gap closed:** `mira-hub/src/app/api/assets/[id]/chat/route.ts` streamed a confident LLM answer even when it had **no** grounding evidence (`manualSources.length === 0` and no fresh live signals) — the trace was merely labeled `kb_gap` while the technician still saw an ungrounded answer. The Python surfaces (Telegram/Slack/Ignition/pipeline) already run the H4 enforcer `enforce_citation_or_gap_admission` (`mira-bots/shared/engine.py` + `citation_compliance.py`) so an uncited reply carries an explicit "I don't have documentation…" admission. This brings the customer-facing Hub surface to parity.
- **Soft, not a hard 412 (product-owner decision):** an evidence-less asset can **still chat** — it just gets an honest KB-gap admission. The existing hard 412 gate stays **opt-in** behind `MIRA_ENFORCE_APPROVED_ASK` / `MIRA_ENFORCE_APPROVED_RETRIEVAL` (default unchanged).
- **Two-layer defense (mirrors quickstart's cite-or-refuse):** new `mira-hub/src/lib/kb-gap.ts` exports (1) `KB_GAP_SYSTEM_INSTRUCTION`, appended to the system prompt when there's no evidence so the model admits the gap up front, and (2) a server-side safety net — after the stream assembles, if the reply has no `[Source:]` citation and no gap phrase, `KB_GAP_ADMISSION` is appended before `[DONE]` so a model that ignores the instruction still can't ship a confident ungrounded answer. Phrasing mirrors the Python `_H4_STOCK_ADMISSION` ("specific documentation indexed", "consult the asset nameplate or vendor manual", "knowledge base").
- **Mechanics:** `streamFromProvider` no longer emits the terminal `[DONE]`; the route now emits a single `[DONE]` after the safety net so the admission always lands inside the rendered response. Retrieval/tenant-scoping (`#2178` hybrid `is_private` filter) untouched; the beta-gate path (a stranger's uploaded manual → cited answer) is unaffected because that path HAS sources (`hasGroundingEvidence === true` → no admission). The **drive-pack pre-check (#2527)** returns before the cascade, so a matched pack answer is never gap-admitted.
- **Tests:** `…/chat/__tests__/route.test.ts` (+5): evidence-less confident answer gets the admission (before `[DONE]`), a model that already admits the gap is not double-admitted, a manual-sourced answer is unchanged, the opt-in 412 gate still refuses when enforcement is on with no context, and the admission phrasing check. Existing chat tests kept green.
- **Scope/safety:** additive to the default path; no branch reordered; read-only; no new hard refusal.

### v3.108.0 (2026-07-07) - feat(hub): candidate accept enqueues pack build+grade (no auto-promote) (#2544)
- **Gap closed:** accepting a `drive_pack_update` `ai_suggestions` row (mig 062) via `/api/suggestions/[id]/decide` → `mira-hub/src/lib/suggestion-accept.ts` was **status-only** — it flipped the row to `accepted` but never ran the extractor+grader. The real work is the Python CLI `tools/drive-pack-extract/registry/update_candidate.py` (classify → family generator → grading harness → staged candidate + report).
- **Shape (enqueue + drain, no synchronous shell-out):** the Hub must not shell out to Python inside an HTTP request, so the work is split. **Enqueue (`suggestion-accept.ts`):** on accept of a `drive_pack_update` suggestion, `markDrivePackBuildRequested()` writes a durable marker onto the row's own `extracted_data` (`build_requested=true`, `build_status='requested'`) — the row IS the queue, no new table/migration. A data annotation, not a status transition (so it does not route through the ADR-0017 helper). Reject enqueues nothing. **Drain (`tools/drive-pack-extract/registry/drain_build_requests.py`):** a standalone worker reads `status='accepted' AND build_requested AND build_status='requested'` rows and invokes `update_candidate.py` (`--manual <local_pdf_path> --id <registry_manual_id>`), then flips `build_status` off `requested` so a marker is drained at most once. DB I/O is injected (`drain(load_requests, save_result, runner=…)`) so the policy core is unit-tested offline; `main()` wires psycopg2 to `NEON_DATABASE_URL`/`DATABASE_URL`.
- **Trust gate preserved:** `update_candidate.py` writes only into the staged `candidates/<family>/` tree, guarded by `assert_not_live_packs()`; the drain passes no `--out` override, so it can NEVER write into the live served `mira-bots/shared/drive_packs/packs/` tree. **Auto-promotion candidate→live is forbidden** (ADR-0025, `.claude/rules/train-before-deploy.md`); promotion stays a separate human-gated step (the `candidate_report.md` reviewer checklist + recorded sign-off).
- **Tests:** Python `tools/drive-pack-extract/registry/tests/test_drain_build_requests.py` (7: runner invoked with manual+id and **no** `--out` → never targets `packs/`, rc→built/failed mapping incl. rejected-trust-status review case, missing provenance fails without running, runner-exception caught, `drain` writes a result back for every request + flips off `requested`). Hub `mira-hub/src/lib/__tests__/suggestion-accept.test.ts` (+2: accept writes the durable build-requested marker with no entity; reject enqueues nothing). All 19 registry pytest + 24 Hub vitest green. Design note: `docs/drive-commander/drive-pack-build-enqueue-and-drain.md`.
- **Scope/safety:** additive; the existing kg_entity/tag_mapping accept branches are untouched; read-only, no drive writes, no LLM. The drain is an ops/cron entry, not wired into any customer-facing request path.

### v3.106.0 (2026-07-07) - fix(telegram): route file intake through citable /api/uploads/folder (#2540)
- **Gap closed:** Telegram-uploaded PDFs/photos went to `POST /api/contextualization/import` — a verified dead-end: the route auths with `sessionOr401()` (a Bearer token 401s), its multipart branch expects a `.zip` bundle (a raw PDF/JPEG 400s), and `HUB_IMPORT_URL`/`HUB_IMPORT_TOKEN` are unset in prod so it was dark. Nothing a technician texted the bot ever became citable.
- **Fix:** `contextualization_intake.py` gains `submit_file_to_hub_folder()` (+ `hub_folder_upload_configured()`), which POSTs the raw file to the **working citable door** `POST /api/uploads/folder` with `Bearer HUB_INGEST_TOKEN` + `X-Mira-Tenant-Id` — the same golden path MiraDrop uses (`mira-hub/src/lib/local-upload.ts` → v2 Inbox → `knowledge_entries`, per-tenant `is_private=true`, citable). `mira-bots/telegram/bot.py` photo + document intake now call it; the §2 zip/envelope path is dropped for Telegram. Env-var names match `tools/mira-drop-watcher` (`HUB_URL`/`HUB_BASE_PATH`/`HUB_INGEST_TOKEN`); `HUB_IMPORT_URL` kept only as a back-compat base. Preserves the PDF-only + 20 MB limit and the "Hub intake not configured" graceful skip when env is unset. `docker-compose.saas.yml` wires the new env onto `mira-bot-telegram` (token unset → skip).
- Tests: `mira-bots/tests/test_telegram_hub_intake.py` + `test_telegram_photo_hub_wiring.py` rewritten to assert the `/api/uploads/folder` endpoint, Bearer auth, `X-Mira-Tenant-Id` header, raw-file-not-zip body, per-uploader tenant resolution, and graceful skip when unconfigured (httpx mocked).

### v3.105.0 (2026-07-07) - feat(crawler): OCR-drain needs_ocr PDFs via Tika before quarantine (#2539)
- **Gap closed:** `mira-crawler/cron/kb_growth_cron.py` detected scanned/image-only PDFs (0-char local extraction) and terminally quarantined them `status="needs_ocr"` (#2532) — but **nothing ever drained** `needs_ocr`, so scanned drive manuals could never become a pack (Drive Commander 2/8, audit §6 gap #1). The OCR-capable extractor (`mira-crawler/ingest/converter.py::extract_from_tika`, Apache Tika + Tesseract) existed but was unwired.
- **Fix (fresh 0-char):** before quarantining, the cron re-runs the ingest pipeline with a new `--ocr` flag. `full_ingest_pipeline.step_extract(..., ocr=True)` falls back to `extract_from_tika` when local pdfplumber/pypdf yields no text layer; on success the OCR text continues the **normal** ingest (chunk/embed/KG) via the single pipeline and the entry becomes `done` (+ a drive-pack candidate). Only when OCR is unavailable/empty does it land `needs_ocr`.
- **Fail-safe (safe to merge before `mira-tika` is deployed):** `converter.extract_from_tika` already swallows every error (Tika unreachable/timeout) and returns `[]`; `_attempt_ocr` additionally wraps the subprocess in try/except. An unreachable Tika falls back to the **current** behavior (quarantine `needs_ocr`) — the cron never crashes, never fails loud.
- **Drain (opt-in):** `python3 kb_growth_cron.py --drain-needs-ocr` gives each existing `needs_ocr` entry a one-time OCR retry (→ `done` on success, stays `needs_ocr` with `last_ocr_attempt_at` on failure). A normal hourly cron run is unchanged — it never touches `needs_ocr` entries.
- **No new service:** `mira-tika` already exists in `docker-compose.saas.yml` (on `mira-net`, published `127.0.0.1:9998`). **Deploy step:** set `TIKA_URL=http://localhost:9998` in Doppler `factorylm/stg` + `factorylm/prd` so the host-run cron's pipeline OCR points at the published port (the container-DNS default `http://mira-tika:9998` is only reachable in-network).
- **Tenancy unchanged:** OCR'd manuals follow the same `knowledge_entries` ingest/tenancy as text-layer manuals (`is_private` semantics untouched).
- **Tests:** `mira-crawler/tests/test_kb_growth_ocr_drain.py` (0-char+Tika-success → ingests, not quarantined; 0-char+Tika-empty → `needs_ocr`; 0-char+Tika-unreachable → `needs_ocr` fail-safe; normal + transient-download-failure unchanged; `--drain-needs-ocr` success/still-empty/unreachable/no-op) and `mira-crawler/tests/test_full_ingest_ocr_fallback.py` (pipeline `step_extract` OCR wiring). Existing `test_kb_growth_zero_char_quarantine.py` updated for the new `run_pipeline(entry, ocr=...)` signature. 76 tests pass; ruff clean.

### v3.104.0 (2026-07-07) - fix(slack): route PDF intake through citable /api/uploads/folder (#2543)
- **Gap closed:** `mira-bots/slack/pdf_handler.py::ingest_pdf` wrote Slack uploads **directly** into an Open WebUI knowledge collection (`POST /api/v1/files/` → `/api/v1/knowledge/{id}/file/add`), routed by a filename regex. Per `.claude/rules/knowledge-entries-tenant-scoping.md` the Open-WebUI-KB door is **not** citable on the Hub RAG/NodeChat path — it was a second, divergent ingestion system competing with the system-of-record, so a technician's Slack-uploaded manual never became a citable source.
- **Fix:** `ingest_pdf` now POSTs the raw file to mira-hub's **citable** `/api/uploads/folder` door (Bearer `HUB_INGEST_TOKEN` + header `X-Mira-Tenant-Id`, multipart `file`), matching MiraDrop's request shape (`tools/mira-drop-watcher/main.py`). Uploads land in per-tenant `knowledge_entries` (`is_private=true`) and are citable on the Hub NodeChat path. No new ingestion system — it reuses the existing folder door.
- **Env (consistent with the repo):** `HUB_URL` / `HUB_BASE_PATH` (default `/hub`) / `HUB_INGEST_TOKEN` / `MIRA_TENANT_ID`. The Open-WebUI collection-routing helpers (`_route_collection`, `_get_or_create_collection`) are removed.
- **Preserved:** PDF/image MIME allowlist + a 20 MB size cap (`security-boundaries.md`). When the Hub env is unset the handler logs and returns a graceful "intake isn't configured" message instead of crashing the Slack handler. `bot.py`'s call site is unchanged.
- **Tests:** new `tests/integration/test_slack_pdf_intake.py` (none existed) mocks httpx and asserts the POST hits `/api/uploads/folder` with the Bearer + tenant header and the PDF MIME part, that it gracefully skips with no HTTP call when the env is unset, and that an unsupported MIME is rejected before any call.### v3.103.0 (2026-07-07) - feat: answer the drive pack for the CONNECTED asset without naming the drive (#2527 follow-up)
- **Goal:** a surface bound to a known drive (Hub asset-chat open on a GS10, an Ignition "Ask MIRA" panel bound to a GS10) should answer "what does CE10 mean?" WITHOUT the technician typing "gs10". Two surfaces wired; the third (engine backstop) was investigated and dropped as ineffective (below).
- **Hub asset-chat** (`mira-hub/.../assets/[id]/chat/route.ts` + `mira-bots/ask_api/drive_pack.py`): the drive-pack pre-check now passes the OPEN asset's `manufacturer + model_number` as a `drive` fallback, and the `/drive-pack/ask` endpoint resolves **question-first, drive-fallback** (a drive named in the question still wins; otherwise the bound asset's drive resolves the pack). The pre-check moved to after the asset fetch (so make/model is available) but still before the approved-context gate + cascade (pack answer stays available regardless of approval, consistent with the engine surfaces); the static-vs-live prefix is now applied only when the turn carries live telemetry. +2 Python + +1 Hub tests.
- **Ignition Ask-MIRA** (`mira-pipeline/ignition_chat.py`): resolves the connected asset's manufacturer/model from `cmms_equipment` (tenant-scoped, read-only, best-effort — mirrors the Phase-4 `_agent_state_from_conn` resolver; `tenant_id::text` compare so a legacy slug tenant never hits a UUID-cast error) and folds an "Asset: <make> <model>" line into the engine message, so the existing deterministic drive-pack fast-path (#2526) resolves the pack. Folded into the message, not the retrieval query, so it never pollutes RAG recall. +8 tests.
- **Engine backstop (dropped, evidence-based):** deriving the pack from `state["context"]["uns_context"]` when the text names no drive was prototyped and **reverted** — the UNS resolver overwrites `model` with the fault code on the very turn you'd want it (`GS10` → `CE10`), `product_family` is unset, and `manufacturer` alone is ambiguous (Rockwell has two packs), so there is no reliable drive-family signal in `uns_context`. The two surface-level bindings above cover the real deployment paths; no `engine.py` change ships here.
- **Scope/safety:** read-only, no drive writes, no LLM in the pack path, additive. Pre-existing NOTE: the full `mira-pipeline` test suite has a cross-file isolation failure (`test_ignition_chat_direct_connection.py` → `test_ignition_chat_gate.py`, 7 gate tests) — verified pre-existing on clean `origin/main` by stashing this branch; local Python 3.14 only; each file passes in isolation.
### v3.102.0 (2026-07-07) - feat(hub): GS10 drive-pack answers in Hub asset-chat (read-only pre-check, #2527)
- **Gap closed:** `mira-hub/src/app/api/assets/[id]/chat/route.ts` runs its **own** Groq→Cerebras→Gemini cascade and had zero reference to the drive packs — so a GS10 CE10/P09.03 question in Hub asset-chat got a generic RAG/LLM answer, inconsistent with every **engine** surface (Telegram/Slack/Ignition/pipeline/kiosk, #2526). This wires the same deterministic, manual-cited drive-pack answer into the Hub path.
- **Architecture (smallest safe — no TS reimplementation of the Python matcher):** new read-only `POST /drive-pack/ask` route on the already-deployed `mira-ask` FastAPI service (`mira-bots/ask_api/drive_pack.py`, its **own** `APIRouter` so it constructs **no** Supervisor engine — unit-testable in isolation). It resolves the pack (`pack_id` → `drive` → question text via `resolve_pack`) and returns `answer_question(...).to_dict()`. The Hub route calls it as a **pre-check before its LLM cascade** (`route.ts`, immediately after the safety gate): on `matched && answer_source=="drive_pack"` it streams the pack answer + `[Source: … p.X]` citations as SSE and short-circuits; on any miss/timeout/error it falls straight through to the existing cascade unchanged (best-effort, 4 s timeout, `AbortSignal.timeout`).
- **Guardrails preserved:** pack-grounded only, `fallback_used=false`, `live_telemetry=false`, `read_only=true`, citations present; a `"Static pack reference — not from live telemetry."` prefix (the pre-check runs before the machine-memory packet is built, and a pack answer is always static, so the label is unconditionally accurate); `X-Drive-Pack: <pack_id>` response header for observability; optional `X-Mira-Key`/`ASK_API_KEY` gate (parity with `/ask`).
- **Wiring:** `docker-compose.saas.yml` gives `mira-hub` `MIRA_ASK_URL` (default `http://mira-ask:8011`) + `ASK_API_KEY` (both services already share `mira-net`).
- **Tests:** Python `mira-bots/tests/test_ask_api_drive_pack.py` (17: CE10/P09.03 match + cites, alias/pack_id/question resolution, optional-auth on/off, always-read-only, graceful unknown-pack); Hub `…/chat/__tests__/route.test.ts` (+3: match short-circuits with cited SSE + `X-Drive-Pack` header and the cascade never runs, no-match falls through, endpoint-error falls through). Existing chat tests kept green (fetch mock updated to distinguish the two calls the cascade path now makes).
- **Scope/safety:** additive; no existing branch reordered; read-only, no drive writes, no LLM in the pack path. **Follow-up (out of scope):** derive the pack from `state["uns_context"]`/the asset's UNS so an Ignition "Ask MIRA" panel bound to a GS10 works without naming the drive; the sibling `namespace/node/[id]/chat` route has the same split.

### v3.100.0 (2026-07-07) - fix(test): un-jam the deploy pipeline — E2E signup-flow strict-locator flake
- **Impact:** the VPS deploy gate had been jammed since v3.92.0 (#2528). `deploy-vps.yml` only runs when Smoke Test concludes `success`, but the `Smoke Test` E2E job failed on **every** commit after #2528 (v3.93.0→v3.99.0), so nothing shipped — including the merged, locally-verified drive-pack `/drive` work. The failure was **unrelated** to any of those commits (0 of them touch `mira-hub`).
- **Root cause:** `mira-hub/tests/e2e/signup-flow.spec.ts` "valid email submits and shows success (or rate-limited on repeat runs)" hits the **live** factorylm.com magic-link form. On repeat CI runs the live site returns 429, and the page renders a **hidden** `#fl-form-success` in the DOM *alongside* the visible `#fl-form-error` ("You requested a link recently. Check your inbox…"). The assertion `expect(success.or(rateLimited)).toBeVisible()` then matched **two** elements → Playwright strict-mode violation → red smoke → gate closed for everyone.
- **Fix:** the two acceptable outcomes are mutually exclusive in *visibility*, so narrow the union with `.filter({ visible: true })` (Playwright 1.59) — exactly one element matches regardless of the 200/429 outcome. Test-only change; no product code touched.
- **Note:** shipped alongside a `skip_staging_gate=true` hotfix dispatch of `mira-bot-telegram`+`mira-pipeline` (drive-pack `/drive` was stuck behind this same jam); this PR restores the normal gated deploy path so the queued crawler fixes (v3.97.0/v3.98.0) and future merges deploy without a bypass.

### v3.99.0 (2026-07-07) - chore(ops): fail2ban SSH-hardening setup script (operator-run, not auto-applied)
- **VPS fail2ban installation script** (`scripts/vps_fail2ban_setup.sh`): idempotent bash script for SSH brute-force protection on Ubuntu/Debian VPS.
- **Configuration:** [sshd] jail with maxretry=5, findtime=10m, bantime=1h; backend=systemd on supported systems.
- **Operator-run only** — does NOT execute automatically. Must be run manually as root on the VPS: `sudo bash scripts/vps_fail2ban_setup.sh`.
- **Critical:** Operator MUST edit ignoreip to include their admin IP + Tailscale subnet before running to avoid self-lockout.
- **Purpose:** respond to 126 MB of failed-login records (btmp) indicating SSH brute-force traffic on production VPS.
- Syntax validated; ready for manual deployment.

### v3.98.0 (2026-07-07) - feat(crawler): quarantine 0-char (scanned) PDFs as needs_ocr instead of retrying
- **Problem:** ingest now extracts PDF text in-process (`mira-crawler/ingest/pdf_extract.py`, pdfplumber→pypdf). For scanned/image-only manuals with no text layer, extraction returns 0 chars with no OCR available — the entry then either retried forever (`failed_retryable`) or eventually burned all `MAX_ATTEMPTS` into a hard `failed`, wasting a cron cycle every hour for a PDF that will never yield text.
- **Fix:** `mira-crawler/cron/kb_growth_cron.py::_process_entry` now detects the exact "produced 0 chars" marker `full_ingest_pipeline.step_extract` appends to `PipelineReport.errors` (rendered in the subprocess's report tail) via a new `_is_zero_char_extraction()` helper, and routes that case straight to a new terminal status `needs_ocr` on the FIRST occurrence — no retry scheduled, no attempt burned. Genuine download/network/transient failures are unaffected (they don't carry the marker) and keep going through `_classify_error()`'s existing retryable/hard path. Text-layer PDFs (chars > 0) are unaffected.
- **Observability:** `_queue_stats()` now reports a `needs_ocr` count; quarantined entries do not count toward `remaining`.
- **Tests:** `mira-crawler/tests/test_kb_growth_zero_char_quarantine.py` (marker detection, quarantine-not-retry, quarantined entries never re-selected, queue-stats count, and regressions proving normal/transient/hard cases are unaffected). Wired into the CI `drive-pack-extract-tests` job's pytest line alongside the existing crawler tests.

### v3.97.0 (2026-07-07) - fix(crawler): stop deploy from clobbering runtime ingest-queue state
- **Bug:** `mira-crawler/cron/kb_growth_cron.py` reads/writes `mira-crawler/cron/manual_queue.json` — the live ingest queue tracking which manuals are pending/done/failed. That file was **tracked in git**, and `deploy-vps.yml` runs `git checkout --force <tag>` on `/opt/mira` on every deploy — silently resetting the live queue's `status`/`attempts`/`done_at` progress back to whatever was last committed, every time.
- **Fix:** stopped tracking `mira-crawler/cron/manual_queue.json` (`git rm --cached`, file stays on disk) and added it to `.gitignore` with a comment explaining why. Runtime queue progress on the VPS now survives deploys.
- **Missing-file safety:** verified `load_queue()` would previously raise `FileNotFoundError` on a missing queue file, and both `run_batch()` and `--status` explicitly `sys.exit(1)`'d in that case — a real risk now that the file isn't guaranteed to exist (fresh box, or right after this change lands). `load_queue()` now returns `[]` when the file is absent so the cron and `--status` self-heal to an empty/sane queue instead of crashing; the redundant exists-checks in `run_batch()`/`--status` were removed since `load_queue()` now owns that case. +3 tests (missing-file `load_queue`, `run_batch`, `--status`); 46 tests green in the cron suite, ruff clean.
- **Scope:** the committed `manual_queue.json` was runtime state (34/35 entries `pending`, one-time initial backlog with mutable `status`/`done_at`/`attempts` fields) — not a curated seed a fresh box needs to hydrate from. No seed-file/hydration mechanism was added; the existing `--hydrate-from-cache` (pulls from NeonDB `manual_cache`) remains the way to (re)populate a queue.

### v3.96.0 (2026-07-07) - feat(engine): deterministic drive-pack fast-path (natural-language GS10 answers across all engine surfaces)
- **Natural conversation, no `/drive` prefix.** A text question that names a drive MIRA has a pack for (e.g. "what does CE10 mean on my gs10") is now answered deterministically from the pack via `shared.drive_packs.answer_question` BEFORE generic router/RAG — reaching **every engine surface at once** (Telegram free-text, Slack, Ignition, the VPS pipeline, the kiosk `/ask`). Builds on `answer_question` (#2523); complements the explicit Telegram `/drive` command (#2525).
- **Trigger is conservative + safe by construction:** fires only when `resolve_pack(message)` matches a drive AND `answer_question(...).matched` is True; otherwise falls through to the existing pipeline completely unchanged (a false-positive `resolve_pack` match is harmless — `matched=False` → fall through).
- **Three verified guardrails** (`mira-bots/shared/engine.py`):
  1. **Placement AFTER the safety short-circuit** — a drive-naming safety utterance ("the gs10 is arc flashing") still returns the safety STOP, never a pack answer.
  2. **Inline `[Source: doc p.X]` citations + `dispatch_kind="drive_pack"`** added to `_TRUSTED_DISPATCH_KINDS` — so the H4 citation/KB-gap enforcer doesn't bolt on a false "not enough information" line and the quality gate doesn't flag the templated text as repetitive.
  3. **Static-vs-live label** — when the turn also carries a live-tag preamble (the kiosk `[LIVE CONVEYOR STATUS]` block OR the Ignition `[LIVE TAGS …]` preamble), the reply is prefixed "Static pack reference — not from live telemetry" so a static pack answer is never conflated with a live read.
- **Tests:** +6 (`mira-bots/tests/test_engine_drive_pack_fastpath.py`), all driving the OUTER `Supervisor.process()`: CE10 cited + no false gap-admission, dispatch_kind, static-label on `[LIVE TAGS]`, safety-wins, non-drive falls through, unmatched falls through. Full engine + `bot_regression` + golden suites: 0 new failures.
- **Scope/safety:** additive fast-path; no existing branch changed; read-only, no drive writes; `answer_question` never calls an LLM. **Follow-up (out of scope):** derive the pack from `state["uns_context"]`/direct-connection asset when the text doesn't name the drive; wire the Hub asset-chat TS cascade.

### v3.95.0 (2026-07-07) - feat(telegram): read-only `/drive` command — ask a drive pack from your phone
- **First phone-reachable surface** for the GS10 drive-pack Q&A (builds on the `answer_question` core from #2523). `/drive <drive> <question>` — e.g. `/drive gs10 What does CE10 mean?` — resolves the pack via the existing `resolve_pack` alias matcher (so `gs10`/`durapulse`/`durapulse_gs10` all work) and answers **only** from `shared.drive_packs.answer_question` (deterministic pack JSON).
- **Guardrails:** NEVER calls the engine/LLM — the only answer source is the pack. An unmatched question renders the pack's honest "I won't guess" text (never a generic fallback); an unknown drive lists the available packs. Every non-usage reply carries a metadata footer: `source · pack · fallback_used · live_telemetry · read_only`, plus inline `[Source: doc p.X]` citations. Sent as **plain text** (no Markdown) so OEM text with brackets can't trip Telegram's entity parser.
- **Scope/safety:** additive handler in `mira-bots/telegram/bot.py` (+ registration + `/help` line); no engine/chat path changed; read-only, no drive writes; static manual-pack only (`live_telemetry: false`). Reaches Telegram only — Slack/Ignition/natural-language chat come via the engine fast-path (next PR); Hub asset-chat is a tracked follow-up.
- **Tests:** +5 (`mira-bots/tests/test_telegram_drive_command.py`) — CE10 grounded + all 5 metadata fields, P09.03 cited p.4-188, unknown-pack no-fallback, usage, unmatched-is-honest. Pre-existing suite unaffected.

### v3.94.0 (2026-07-07) - feat(drive-commander): read-only "ask the GS10 drive pack" text-question path
- **Gap closed:** the promoted `durapulse_gs10` pack was reachable ONLY through the live-telemetry snapshot path (`live_snapshot.build_drive_diagnostic(snapshots)`, gated on an active-fault snapshot). A plain text question ("what does CE10 mean?") with no live telemetry never resolved the pack. New `shared/drive_packs/ask.py` `answer_question(pack_id, question)` bridges a text question to the SAME pack machinery (`load_pack` + `build_cards` + the GS10 fault-intel reader) — no snapshot, no hardware.
- **Grounded + safe:** answers are composed only from the pack (fault codes, parameters, keypad cards) with the pack's own citations — **no LLM, so no generic hallucination**. `answer_source=drive_pack`, `fallback_used=false` always; an unmatched question is answered honestly ("loaded, but doesn't document that — I won't guess") and lists what the pack covers. `live_telemetry=false` (static manual-pack only), `read_only=true` (VIEW-only keypad steps; no drive writes exist).
- **CLI:** `cd mira-bots && python -m shared.drive_packs.ask --pack durapulse_gs10 --question "..."` (`--json` for structured output). Prints pack_id / schema_version / answer_source / matched / fallback_used / live_telemetry + the answer + citations.
- **Verified** against the 5 mission questions: CE10 → modbus timeout + P09.03 + first checks (cited); P09.03 → COM1 Time-out Detection (cited p.4-188); comm-timeout intent → P09.03; pack-only directive → honest coverage; unknown → no fallback.
- **Tests:** +8 home-ready smoke tests (`mira-bots/tests/test_drive_pack_ask.py`); 115 drive-pack/snapshot tests green. Runbook: `docs/drive-commander/gs10-ask-from-home.md`.
- **Scope/safety:** additive read-only module + reusable `answer_question` export. No pack promoted (GS10 already live), no runtime chat/engine path changed, no OEM manual committed, no drive-write capability.

### v3.93.0 (2026-07-07) - feat(drive-commander): chapter-section-aware citation verification (unblocks GS10-style page labels)
- **What:** closes the documented cite-integrity gap that kept the GS10 pack's citation category stuck N/A. Some OEM manuals (AutomationDirect DURApulse GS10/GS20) label pages by **chapter-section** ("4-188", "3-6") instead of a running integer page. The int-page `cite_integrity.verify_excerpt_on_page` couldn't parse those, so every such citation was unverifiable.
- **How:** `cite_integrity.is_chapter_section_label()` detects the label form, and `cite_integrity.verify_excerpt_in_document()` verifies the excerpt **whole-document** (it must appear on some page). `cite_check` now dispatches: an integer page is verified page-pinned (strong); a chapter-section label is verified whole-document (weaker but honest — still catches fabrication, tallied separately as `verified_by_label_count`). A fabricated excerpt on a chapter-section page is still unverifiable, and a dropped diagnostic-critical citation still hard-fails.
- **Effect:** a GS10-style pack graded **with a single manual PDF** now scores citation fidelity instead of N/A. (Residual: the GS10 manual ships as separate chapter PDFs, so a merged/single PDF is needed for a full GS10 re-grade; until then GS10 is still graded without a manual and stays INCOMPLETE.)
- **Perf (found + fixed in a pre-close review):** `cite_check` now reads the manual **once** — only the distinct integer pages a pack cites (a pack citing ~6 of a 156-page manual reads 6 pages), or the whole document only when a chapter-section-label citation is present. Previously every citation reopened the 34MB PDF (O(citations × pages)); the new whole-document verify was ~90 s per call. Measured: PF525 cite **~90 s → 1.6 s**, PF40 **52 s → 1.7 s** (`cite_integrity.load_normalized_pages(pdf, pages=…)`).
- **UX (found + fixed in the same review):** `grade_scientific.py` **auto-discovers** `gold/<pack_id>/gold.json` when `--gold` is omitted, so a reviewer can't accidentally grade a pack that HAS a gold set on gold-independent categories only and get a misleadingly low "no gold" / INCOMPLETE verdict (e.g. GS10 read C/75 instead of A/95.2 without `--gold`). `--gold` still overrides; a pack with no gold file is still graded gold-independent.
- **Tests:** +9 (label detection; whole-document verify real vs fabricated; cite_check dispatch verifies a `4-188` citation and still catches fabrication + dropped-critical; `load_normalized_pages` page-scoping; gold auto-discovery). 107 tests green, ruff clean. Docs updated: `GRADING_SPEC.md` §B, GS10 gold `page_numbering`, CI-gate design §2, discovery record.
- **Scope:** grading tool + gold-note + docs only. No runtime change; live packs untouched, no pack promoted, no OEM manual committed.

### v3.92.0 (2026-07-07) - chore(ops): VPS reliability hardening — stale healer entry, flaky stop-gate, ingest overlap guard, weekly docker GC
- **Self-healer stale entry (fix):** removed `mira-docling-saas` from `heartbeat_monitor.CRITICAL_CONTAINERS`. Docling was decommissioned 2026-06-06 (OOM) and is no longer in `docker-compose.saas.yml`, so the self-healer was trying to recreate a non-existent container every 15 min. Ingest now extracts in-process (`mira-crawler/ingest/pdf_extract.py`).
- **Flaky stop-gate (fix):** `tools/hooks/stop-gate.sh` Gate 3 (mira-hub build) now serializes with `flock` (where available) and treats Next's `"Another next build process is already running"` as a **benign race, not a failure** — two overlapping Stop-hook invocations were both running `next build` and racing on the Next build lock, producing spurious "build failed" blocks even though the build compiled cleanly.
- **Ingest overlap guard (automate):** `kb_growth_cron` now wraps its batch in a `singleton_lock` (advisory `fcntl` lock, OS-released on crash so it can't permanently block ingest; a second concurrent run exits cleanly). Prevents an overrunning hourly batch from being double-processed by the next tick. Fail-open if the helper isn't importable.
- **Weekly docker GC (automate):** new `scripts/vps_docker_gc.sh` (build-cache + unused-image prune, before/after log) for a weekly VPS cron — bounds the dangling-image/cache churn that re-accumulates on every deploy.
- **Scope / safety:** ops/reliability only; no product-runtime behavior change. Additive.### v3.91.0 (2026-07-07) - docs(drive-commander): scientific-grading runbook + CI promotion-gate design + discovery record
- **Runbook** (`docs/drive-commander/scientific-grading/runbook.md`): how to run scientific grading locally, read A/B/C/D/F + INCOMPLETE, author a gold set (precision-over-recall, match the pack, don't fake completeness), add a vendor's identifier conventions to the family-aware domain rules, and the five prerequisites before a pack may be promoted (grader measures, humans promote).
- **CI promotion-gate design note** (`ci-promotion-gate-design.md`): the gate blocks a promotion PR (a diff touching `mira-bots/shared/drive_packs/packs/*/pack.json`) when a pack is sub-B / INCOMPLETE / hard-gate-fail / critical-failure, scoped to trigger ONLY on promotion paths (not docs/tooling). **Deliberately NOT wired** — documents the two unresolved prerequisites (manuals aren't committed → citation can't run in CI; GS10 chapter-section page labels aren't int-verifiable) with options + a draft workflow, per the mission's "design note, don't force it".
- **Discovery record** (`docs/discovery/2026-07-07-scientific-grading-followups.md`): question, files, commands, results (the P053 float-jitter root cause; GS10 conventions; before/after grades), conclusions, the deterministic workflow, tests/fixtures, and remaining risks.
- **Scope:** docs only. No code, no runtime change, no pack promoted.

### v3.90.0 (2026-07-07) - feat(drive-commander): GS10 scientific gold set + family-aware domain rules (refs #2516)
- **What:** makes the live **durapulse_gs10** pack scientifically gradeable. Two coupled changes close the two blockers the first back-grade exposed. GS10 goes **D/60/INCOMPLETE → A/95.2/INCOMPLETE** — a large, honest improvement, still **not promotable** (citation N/A).
- **GS10 gold set** (`tools/drive-pack-extract/gold/durapulse_gs10/gold.json`): 10 fault codes with GS10 mnemonics (GFF/Lvd/oL/EF/CE1..4/**CE10**), the P09.03 [COM1 Time-out Detection] parameter, and the **CE10 → P09.03** comm-loss link (the AutomationDirect analog of PowerFlex F081→C125 / PF40 F81→A105). Values mirror the manual-cited pack; honestly flags that GS10 is largely `bench_verified` and that its citations use **chapter-section page labels** (`4-188`) the int-page cite-integrity can't verify.
- **Family-aware domain rules** (`grading/domain_rules.py`): ID conventions are now keyed off the pack's declared `family`, NOT a single hardcoded vocabulary. PowerFlex (`A105`/`F081`) and DURApulse (dotted `P09.03`, mnemonic `CE10`) each grade; an **unknown family falls back to the strict PowerFlex pattern** (deliberately NOT a broad relaxation); and the **param-id-leak guard stays absolute** for every family. So a PowerFlex id in a GS10 pack — or a GS10 id in a PowerFlex pack — is still caught as wrong-family contamination. +9 tests proving: PowerFlex still grades, GS10 IDs accepted, all four contamination directions caught, the leak guard holds under GS10 conventions, and unknown families stay strict.
- **GS10 grade (with gold, no manual):** fault + parameter coverage/precision, field accuracy, relationship accuracy, safety all **100**; provenance **50** (truthful — GS10 is mostly bench_verified, so *manual* traceability is genuinely partial); **citation fidelity N/A** (no manual + chapter-section page labels) → **INCOMPLETE, not promotable**. No critical failures. Back-grade report + README updated.
- **Scope:** gold + grading rules + docs only. **No runtime change** — the live `packs/durapulse_gs10/` pack is untouched; no pack promoted. Real OEM manuals never committed. 99 tests green, ruff clean.

### v3.89.0 (2026-07-07) - fix(drive-commander): PowerFlex 525 P053 cross-ref gap (jitter-tolerant detector) + path-free grading reports (refs #2517)
- **Root cause (the real one, not the guessed one):** the scientific back-grade docked PowerFlex 525 to A/98.3 on **fault field accuracy** — only 4 of 6 gold fault→param links resolved. The prior finding blamed "P053 was never extracted"; **that was wrong.** P053 *is* in the pack (page-66 Basic Program grid). The bug was in `extractor._find_cross_refs`: `F100 [Parameter Chksum]` and `F109 [Mismatch C-P]` both say *"Set P053 [Reset To Defaults] to 2/3"* in their action text, but pdfplumber renders the bracket `[Reset` ~0.0003 pt *above* its own id `P053`, so a raw global `(top, x0)` sort placed the bracket *before* the id and the "id immediately followed by `[`" adjacency check silently dropped the reference (`F101` survived only because its tokens tied).
- **Fix:** `_find_cross_refs` now clusters the action band into visual LINES first (the same `_LINE_TOL` tolerance the rest of the extractor already uses) and scans each line in reading order — jitter-tolerant. With the fix `P053.related_faults = [F100, F101, F109]` and all 6 links resolve. Regression test added (`test_find_cross_refs_tolerates_subpixel_top_jitter`, crafted from the real token geometry — the old global-sort returned `[]`). The gold was completed (P053 added as a graded parameter with its fault links). The PF525 F081/F082/F083→C125 and PF40 F81→A105 headline links are unchanged.
- **Proof (grader measures, humans promote — no runtime pack change):** the **live** PF525 pack is UNTOUCHED and still carries the gap — it grades **A/96.9** against the now-complete gold (the gap was slightly undercounted before). The **regenerated staged candidate** (`tools/drive-pack-extract/candidates/powerflex_525/`, trust-status `beta`) grades **A/100**. The live pack reaches 100 only on a separate, human-gated **re-promotion**.
- **Also — path-free grading reports (repeatability):** `grade.py`/`report.py` embedded the manual's ABSOLUTE local path in the committed report (`manual.path` + `extraction_command`) — a machine-specific temp path is noise and a minor local-path leak in a "reproducible" artifact. `report.py` now stores the manual FILENAME only; `grade.py` reduces absolute-path argv tokens to basenames. Both existing committed candidate reports (PF525 new + PF40 regenerated) are now path-free; guard test added.
- **Scope:** grading tool + gold + candidate + docs only. **No runtime change** — the live `packs/powerflex_525/` and `packs/durapulse_gs10/` trees are untouched; no pack promoted. Real OEM manuals never committed. 93 tests green, ruff clean.

### v3.88.0 (2026-07-07) - feat(drive-commander): scientific drive-pack grading rubric (0-100 / A-F) + back-grade of all packs
- **What:** implements `Drive_Pack_Scientific_Grading_Spec` — a deterministic, weighted, numeric (0–100 + A–F) grader that replaces subjective review, on TOP of the existing five-layer harness (which stays the measurement engine). *No drive pack is promoted on subjective review alone. Grade before promote.* **Complements** the existing `beta`/`trusted` trust-status; does not replace it.
- **Rubric (`tools/drive-pack-extract/grading/scientific.py`):** 3 hard gates (schema validity, runtime compatibility — both proven by loading through the real `drive_packs/loader.py` — and provenance presence; any failure → grade **F**, not promotable) + 8 weighted scored categories (provenance 10, fault coverage/precision 20, fault field accuracy 20, param coverage/precision 20, param field accuracy 15, relationship accuracy 10, citation fidelity 15, safety/usability 10). The weights sum to 120, so the overall is a weighted **average** that **renormalizes over gradeable categories** — a pack with no gold set (categories 2–6 N/A) or no manual (citation fidelity N/A) is flagged **INCOMPLETE** and **not promotable** rather than silently scored zero. Grade bands A(92+)/B(82+)/C(70+)/D(50+)/F. Required outputs (spec): overall score+grade, category scores, critical failures, fault/param recall+precision, citation accuracy, missing evidence, promotion recommendation — as machine-readable JSON + human-readable Markdown. Reuses `gold_score`'s `_norm`/`_norm_set` so comparisons match the trust-status path exactly; never re-implements a measurement.
- **CLI (`grade_scientific.py`):** `--gold` is optional (a no-gold pack is graded on gold-independent categories only); exits non-zero when not promotable (a promotion gate). +15 tests (91 in the tool suite, all green; ruff clean) covering band boundaries, weighted-average-over-gradeable, N/A handling, hard gates, critical failures, the relationship-leak floor, and an integration grade of the committed PF40 candidate.
- **Back-grade published (`docs/drive-commander/scientific-grading/`, per the spec's Back-Grading Requirement):** **powerflex_40** (staged candidate) = **A / 100** (full gold + manual 22B-UM001J); **powerflex_525** (live) = **A / 98.3** — the grader precisely caught a real gap (only 4/6 fault→param links present; P053 never extracted); **durapulse_gs10** (live) = **D / 60 / INCOMPLETE, not promotable** — no gold set (coverage/accuracy N/A) AND its non-PowerFlex ID conventions (dotted `P09.03`, `CE10` faults) are flagged by the PowerFlex-oriented domain rules. GS10 not clearing the bar is the intended result: *a pack promoted before this bar existed does not automatically pass it.* Recommendations recorded: author a GS10 gold set + generalize the domain rules for GS10's conventions; extract P053 for PF525.
- **Scope:** grading tool + docs only. No runtime change; the existing `grade.py` trust-status path and all shipped packs are untouched. Real OEM manuals never committed. CI-gate wiring of the scientific grader is a documented follow-up.

### v3.87.0 (2026-07-07) - fix(crawler): repoint manual ingest off the removed docling → local pdfplumber/pypdf extraction
- **What:** `kb_growth_cron`'s `full_ingest_pipeline` still POSTed every PDF to `mira-docling:5001`, which was removed 2026-06-06 (OOM — `docs/known-issues/2026-06-06-hub-upload-failures-docling-oom.md`). Result: **every manual ingest Connection-refused for ~3 weeks** (done=1/failed_retryable=5 each hour), which also silently blocked the drive-pack candidate bridge. This repoints extraction to run **in-process, no external service**.
- **New `mira-crawler/ingest/pdf_extract.py`:** `extract_pdf_text(path) → (text, method)` — pdfplumber (via the shared `ingest.converter`, tables+sections) when importable, **pypdf fallback** (the only PDF lib guaranteed on the ingest host, which runs as a `doppler run -- python3` host cron). Tika is intentionally not wired (it also needs an absent service); text-layer OEM manuals extract fine without OCR. No socket opened.
- **`full_ingest_pipeline.step_extract`** now calls it; removed `_docling_sync`/`_docling_split`/`_strip_images`, the `DOCLING_*` constants, and the now-unused `re`/`tempfile`/`time` imports. `PipelineReport.docling_*` → `extract_*`.
- **Proven on the real PF525 manual** (pulled read-only from prod): pdfplumber path 145,998 chars, **pypdf path (the prod-host path) 101,765 chars** — both with zero external services. On prod this unblocks ingest → the bridge fires automatically.
- **Tests:** `mira-crawler/tests/test_pdf_extract.py` (3) — real-PDF extraction, forced-pypdf-fallback (the host reality), bad-PDF-yields-empty-not-crash. Wired into the `drive-pack-extract-tests` CI job (+`pypdf`).
- **Known related (out of scope):** `mira-crawler/tasks/manualslib_scraper.py` still references docling — a separate scraper path, not the kb_growth ingest critical path.
- **Scope / safety:** read-only extraction change; no schema/runtime infra change; no OOM risk (in-process pypdf). Deploy re-enables 3 weeks of stalled manual ingest.

### v3.85.0 (2026-07-07) - feat(hub): surface drive-pack update candidates in the Command Center review queue
- **What:** closes the last mile of the default-off bridge (v3.82.0). A review-only drive-pack update candidate can now be filed into the Hub `ai_suggestions` queue as a new `drive_pack_update` suggestion and reviewed in **Knowledge → Suggestions** alongside every other AI proposal. **Acceptance:** *a bridge candidate appears in the Command Center as a review item; accepting it records "worth processing" and never extracts, grades, or promotes a pack.*
- **Migration `062_ai_suggestions_drive_pack_update.sql`:** widens the mig-027 `suggestion_type` CHECK to add `drive_pack_update` (idempotent DROP+ADD; no data change). Proven on ephemeral `postgres:16` (real 027→062, re-applied for idempotency): `drive_pack_update` accepted, legacy types still accepted, a bogus type still rejected by the CHECK.
- **Ingestion seam (`mira-hub/src/lib/drive-pack-suggestion.ts`):** pure `candidateToSuggestion(record)` maps a bridge candidate record (`build_candidate_record`) → the `ai_suggestions` row shape (human-readable title/body carrying the review-only doctrine + the exact `next_step`; provenance in `extracted_data`). `insertDrivePackSuggestion()` inserts at `status='pending'`, **idempotent** on `(registry_manual_id, pdf_sha256)`.
- **Endpoint (`POST /api/suggestions/drive-pack-candidate`):** accepts a candidate record for the caller's tenant. Create = any authenticated session; **decide = admin** (mirrors the PLC-import model). Invalid record → 400.
- **Decide path unchanged — status-only by design.** `decideSuggestion` already handles non-edge types generically; a `drive_pack_update` accept/reject only transitions the `ai_suggestions` row (ADR-0017 helper). It intentionally does NOT run the extractor or promote a pack — that stays the human-gated `next_step` (`.claude/rules/train-before-deploy.md`).
- **Render:** `drive_pack_update` → "Drive-pack update" label in the Suggestions queue; the list API already surfaces every non-`kg_edge` type, so no list-query change.
- **Tests:** 11 new offline tests — `drive-pack-suggestion.test.ts` (transform happy/first-extraction/invalid; insert new + idempotent) and the endpoint route test (503/401/400/200 created + idempotent). All 17 suggestion-area vitest tests green (incl. the untouched `[id]/decide` suite); tsc clean.
- **Scope / safety:** additive; read-only w.r.t. drive packs; no runtime DriveSense change. Follows the merged v3.82.0 bridge — the Python bridge stays decoupled (files land on the node; the operator/uploader POSTs them to the seam per `docs/runbooks/manual-kb-ingest-to-drive-pack-bridge.md`).

### v3.84.0 (2026-07-06) - feat(drive-commander): harden the manual→pack extractor for PowerFlex 40 + staged candidate (beta)
- **What:** the frozen PR-A/B drive-pack extractor was PowerFlex 520/525-specific — run on the real Allen-Bradley **PowerFlex 40** User Manual (22B-UM001J, 156pp) it recovered 46 param IDs but **0 faults + 0 defaults**. Six grounded hardenings (each confirmed against the real PDF) make the same reusable tool parse the PF40 family, proving the manual→pack "compiler" generalizes cross-vendor to a second AB fault-table dialect. Produces a **staged candidate** pack at `tools/drive-pack-extract/candidates/powerflex_40/` — **NOT** the live served `packs/` tree (promotion is a separate, human-gated step per train-before-deploy).
- **Extractor hardenings (`tools/drive-pack-extract/extractor.py`):** (1) **single-digit fault codes** (F2..F8) — the two FAULT-code regexes accept `\d{1,3}`; parameter-id regexes stay `\d{2,3}` (params are never single-digit); (2) **Fault-Type as a position-aware ZapfDingbats circled-digit column** (➀→1 / ➁→2, U+2780/1) rendered ~3pt above its code row, discovered per-page from the glyphs (the "Type" header renders as a garbled reversed ")1(epyT"); (3) **emit untyped faults as "—" instead of dropping** so shared-group continuations (F39/F40) and genuinely untyped faults (F48/F71/F80) still land in the fault map; (4) **label-dialect tolerance** — `(?:Values\s+)?` on both Default and Min/Max ("Values Default: 5.0 Secs" vs "Default:"/"Values Min/Max:"), numeric-only default (worded → honest null), trailing unit captured+normalized; (5) **bold-header gate** — a labeled param header is real only if its pid word is a bold heading font, rejecting a plain-Helvetica graph/curve callout (the PF40 p.71 "A034 [Minimum Freq]" typo that would otherwise pass cite-integrity); generalizes PR-B's hardcoded page-98 skip; (6) **"Options"-prefixed enum** first line + inline unit polish + **wrapped "Related Parameters:" continuation** recovery (kept out of purpose).
- **Also:** a **fault-page-identity gate** (a page is a fault table only if it carries BOTH the Description and Action headers) preserves the "no fabricated faults on a param page" guarantee that emit-not-drop would otherwise break; **span-containment** cross-ref attribution on the dingbat layout so the headline **F81 [Comm Loss] → A105 [Comm Loss Action]** link — the cross-vendor comm-loss proof (analog of GS10 CE10→P09.03 and PF525 F081→C125) — attributes to the right fault (a long numbered action list runs below the code line, where nearest-by-top misattributes it).
- **Real-manual trust doctrine:** the discovered messiness is folded into the synthetic CI fixture (`_make_pf_sample_pdf.py`, two new PF40 pages: dingbat Fault-Type column, single-digit codes, shared/untyped rows, wrapped name, PF40 label dialect, Options-prefix enum, a plain-Helvetica graph callout to reject, a wrapped Related-Parameters list) using an embedded circled-digit glyph subset. The real PF40 PDF is **never committed**. +15 extractor tests (76 total), ruff clean. An **independent** real-manual re-run + JSON inspection (trust-gate step 3) verified all 35 cites verbatim and found/fixed one blemish (the wrapped Related-Parameters continuation).
- **Candidate grade (`grading/grade.py` against the real manual):** schema PASS · cite-integrity **35 verified / 0 unverifiable** · gold overall recall **100%** / precision **100%** · diagnostic-critical recall+precision **100%** · fault recall **100%** · **0 fabrications** · domain rules clean → **TRUST STATUS = `beta`** (the automated ceiling for a manual-cited-only pack; `trusted` needs a recorded human sign-off). Artifacts: `gold/powerflex_40/gold.json`, `generate_pf40_pack.py`, `candidates/powerflex_40/{pack.json,PROVENANCE.md,grading_report.{md,json}}`.
- **Scope:** extractor tool + staged candidate + gold only. Runtime `mira-bots/shared/drive_packs/` is untouched; `resolve_pack("PowerFlex 40")` is unchanged in production (no promotion).

### v3.82.0 (2026-07-06) - feat(crawler): default-OFF bridge — successful manual KB ingest → review-only drive-pack update candidate
- **What:** wires the manual-discovery fleet to the trust-graded drive-pack path. When `kb_growth_cron` successfully ingests a manual PDF into the KB, a new **default-off** bridge optionally creates a **review-only drive-pack update candidate** — iff the PDF maps to a known drive family and its hash is new/changed. **Acceptance:** *the bridge turns successful manual KB ingest into a reviewable drive-pack update candidate, but it cannot promote or replace a trusted pack.*
- **Insertion point:** `mira-crawler/cron/kb_growth_cron.py::_process_entry` success branch (only place with mfr/model + the downloaded PDF in scope). The call site is a small `try/except` that **fails open** — a bridge error never fails the already-successful KB ingest.
- **Logic (`mira-crawler/drive_pack_bridge.py`, injectable/testable):** default-OFF behind `MIRA_DRIVE_PACK_BRIDGE=1`; honors `~/.mira/STOP_INGEST`; inherits the cron's batch throttle. Locates the PDF read-only by mirroring the pipeline's cache convention (`MANUALS_ROOT/{mfr}/{model}/{basename}.pdf`); computes the PDF SHA-256; maps `(manufacturer, model)` → a `registry/sources.json` entry (match on model, since discovery says "Allen-Bradley" while the registry says "Rockwell"); classifies via the PR #2507 registry — `unchanged` → no-op, `changed_by_hash`/`needs_initial_candidate` → candidate.
- **Candidate record (review-only):** written to `~/.mira/drive-pack-candidates/<manual_id>/candidate-<sha12>.json` (override `MIRA_DRIVE_PACK_CANDIDATE_DIR`) — NEVER under `mira-bots/shared/drive_packs/packs/` or the committed `candidates/` tree. Hard `trust_status:"candidate"`, `promoted:false`, `review_only:true`. Provenance-complete: manufacturer, model, manual_id, vendor, product_family, publication, revision, source_url, pdf_sha256, previously_registered_sha256, ingest_timestamp, local_pdf_path, and the exact `next_step` command to extract+grade. It does NOT run the extractor/grader inline.
- **Trust-preserving:** a changed manual creates a candidate, never replaces a trusted pack; extraction/grading/cite-integrity/domain + human approval remain required before any trusted-pack change; the old pack stays live. No runtime DriveSense behavior change; no KB-ingest dependency on candidate creation.
- **Tests:** 15 offline tests (`mira-crawler/tests/test_drive_pack_bridge.py`) — bridge-disabled-no-op, STOP_INGEST-prevents-work, missing-PDF-fail-open, no-mapping-fail-open-with-report, unchanged-no-candidate, changed→candidate, new-known→candidate, never-targets-trusted-packs, review-only-not-trusted, writes-only-under-candidate-dir, bridge-failure-fails-open, provenance-complete. Wired into the `drive-pack-extract-tests` CI job.
- **Docs:** `docs/runbooks/manual-kb-ingest-to-drive-pack-bridge.md`, `docs/drive-commander/workflow-create-candidate-from-discovered-manual.md`; updated `runbook-do-not-silently-trust-updated-manuals.md` + flipped `bridge-manual-discovery-to-drive-pack-grading.md` from design → implemented.
- **Scope / safety:** no new crawler, no new scheduler, additive, default-off, read-only w.r.t. packs. All 61 drive-pack+crawler offline tests green.

### v3.81.0 (2026-07-06) - feat(crawler): manual-discovery fleet-status evidence tool + deep-dive runbooks + bridge design
- **What:** answers the "manual ingest deep dive" — separate *code reality* from *runtime reality* for the OEM-manual/bulletin discovery fleet, and design (not build) the bridge from discovery to trust-graded drive-pack candidates. Additive, read-only, zero runtime change.
- **New tool `mira-crawler/fleet_status.py`** (read-only aggregator): parses the local artifacts the fleet already writes — `manual_queue.json` (status counts + newest activity), `~/.mira/ab-hunter/run-*.json`, `~/.mira/guardrails-state.json`, `~/.mira/STOP_INGEST` (auto-vs-operator via the `AUTO_PAUSED_BY_GUARDRAILS` sentinel), AB-hunter dry-run/live env — and judges each component with an **honest vocabulary** (`built_and_firing` only when a local artifact proves a recent run; else `built_but_needs_runtime_proof` / `built_but_dry_run_only` / `unknown_needs_operator_verification`). Judges queue freshness on the queue's **own** `done_at`/`started_at`, NOT file mtime (mtime lies in a checkout). Prints the exact off-box operator commands (Redis `seen_*` sizes, NeonDB freshness, Trigger bridge `:8003/health`, Trigger.dev Cloud). `--json` / `--commands`. Does **not** duplicate the existing `kb_growth_cron.py --status`.
- **Verified dataflow (the deep-dive's core finding):** all 9 discovery/ingest paths terminate in `knowledge_entries`; a repo-wide grep finds **zero** call sites from any crawler/cron/script into `tools/drive-pack-extract/`. **Manual discovery feeds the KB, not the trust-graded drive-pack path.** The live PF525/GS10 packs were hand-produced. No PDF hash or publication/revision is captured anywhere on the KB path.
- **Bridge DESIGNED, not built** (`docs/drive-commander/bridge-manual-discovery-to-drive-pack-grading.md`): insertion point = `kb_growth_cron.py::_process_entry` success branch (only place with mfr/model + local PDF path in scope), fail-open like the existing `component_template` dispatch idiom, behind a default-off flag, honoring STOP_INGEST + a per-run throttle; it computes a PDF sha256, maps `(mfr,model)→manual_id`, and calls the PR #2507 registry/`update_candidate` — **never promotes**. Staged as a follow-up because it touches a live cron and needs a new metadata mapping.
- **Tests:** 19 offline tests (`mira-crawler/tests/test_fleet_status.py`), wired into the `drive-pack-extract-tests` CI job (stdlib-only). **Docs:** `docs/runbooks/{manual-discovery-fleet-inventory,proving-crawler-last-run-evidence,ab-manual-hunter,trigger-dev-ingest-scheduler}.md` + the bridge design + `docs/drive-commander/discovery-manual-ingest-deep-dive.md`.
- **Scope / safety:** no new scheduler/crawler, no auto-promotion, no runtime DriveSense change, no manual committed. Read-only.

### v3.80.0 (2026-07-06) - feat(drive-commander): manual source registry + trust-preserving update-candidate workflow
- **What:** turns DriveSense manual→pack ingestion from one-off extraction into a **repeatable, trust-preserving update loop** — without building a second ingestion product. Answers the "keep drive manuals/packs current" brief by *reusing* the merged extractor (#2503) + grader (#2505) verbatim and adding only the thin update spine that was missing: a manual **source registry**, a read-only **change-detection** command, and a **candidate** generator. Core rule: *a changed vendor PDF creates a candidate, never an automatic trusted pack* — so an OEM manual revision can't silently rewrite MIRA's diagnostic truth.
- **Discovery first (doctrine):** a repo-wide inventory of every existing ingest/proposal/update path precedes the build — `docs/drive-commander/discovery-manual-ingest-and-update-workflow.md`. Findings: the extractor/grader/trust machinery already exists; the doc-ingest citable path is Hub node-ingest v2 (`knowledge_entries`); the proposal/approval state machine (ADR-0017 `ai_suggestions`) can host a future review item; SHA-256 hash primitives + a scheduled KB cron already exist. The only real gaps were manual-identity, hash-tied-to-pack change detection, and an update-candidate concept — all additive.
- **New (`tools/drive-pack-extract/registry/`):** `sources.json` (human-curated manual registry — vendor/model/publication/revision, approved `pdf_sha256`, generator+gold, trust status, residuals, approval record; **no PDF committed**), `registry.py` (pure load/validate + duplicate-identity detection + `classify()` state machine: `new_manual`/`unchanged`/`changed_by_hash`/`needs_initial_candidate`), `check.py` (read-only, non-gating: hash a local PDF → classify), `update_candidate.py` (reuses the existing generator + `grading/grade.py` as subprocesses → writes `candidate_report.{json,md}` with source identity + Phase-6 grading fields + pack-diff-vs-previous + reviewer checklist).
- **Trust-preserving by construction:** `update_candidate` writes **only** under `candidates/` (`assert_not_live_packs` refuses the live served `mira-bots/shared/drive_packs/packs/` tree), hard-codes `promoted: false`, and inherits the grader's automated ceiling of `beta` (`trusted` needs a recorded human sign-off). A `manual-review-only` source (`automatable:false`, e.g. GS10 today) is refused, not half-run.
- **Docs (Phase 7):** `docs/drive-commander/{workflow-register-a-manual-source,workflow-check-for-manual-updates,runbook-drive-manual-update-acceptance,runbook-do-not-silently-trust-updated-manuals,runbook-rss-product-update-adapter}.md` + the discovery record. RSS/OEM-feed automation is explicitly **staged behind the registry**, not built (the bulk crawler was disabled twice for VPS incidents #1318/#1336).
- **Tests (Phase 8, CI-safe):** 27 new offline tests (`registry/tests/`) — registry parse, automatable-requires-generator/gold, duplicate-identity fail-closed, SHA-256 determinism/change, classify state machine, no-auto-promote, no-live-packs guard, report carries provenance+trust, pack-diff. Wired into the `drive-pack-extract-tests` CI job (synthetic only; full-manual runs stay local). "Param-ids never in `related_faults`" / "unverifiable excerpts dropped" remain enforced by the reused grader + its existing tests.
- **Scope / safety:** additive, zero runtime change. Only new files under `tools/drive-pack-extract/registry/`, 5 docs + 1 discovery doc, one CI line, VERSION + CHANGELOG. No edits to the extractor/grader/schema/loader, engine, Hub, DB, or any pack. Read-only, no control writes, no manual committed. All existing 62 drive-pack tests stay green.
### v3.79.3 (2026-07-06) - fix(hooks): exempt rebase --continue/--abort/--skip/--quit from git-state-guard's mid-rebase block
- **Problem:** `tools/hooks/git-state-guard.sh` blocks git mutators while mid-rebase, but its mutator regex matched `git rebase` unconditionally — including the `--continue`/`--abort`/`--skip` invocations that are the ONLY way to resolve the wedge. A single Bash tool call doing `git rebase --continue` while mid-rebase was denied outright. The hook's own documented escape hatch (`MIRA_ALLOW_GIT_WEDGE=1`) doesn't help here either: the hook evaluates the submitted command text before any of it executes, so `export MIRA_ALLOW_GIT_WEDGE=1 && git rebase --continue` in one call is evaluated with the var still unset.
- **Fix:** new `RESOLVER` regex matches `--continue|--abort|--skip|--quit` on `rebase`/`cherry-pick`/`revert`/`am` and allows those through even while mid-rebase — they resolve the wedge, they don't create one. A plain `git commit`/`git rebase <target>` while mid-rebase is still denied as before.
- **Verified manually** (no existing test harness for this hook): built a throwaway repo with a fake `.git/rebase-merge/` marker and confirmed `git rebase --continue`, `--abort`, and `--skip` are now allowed (no deny output) while `git commit -m test` is still denied with the original message.
- **Scope:** one file, `tools/hooks/git-state-guard.sh`. No change to the detached-HEAD branch or the mutator list. shellcheck clean.

### v3.79.2 (2026-07-06) - feat(ci): migration CHECK constraint drift guardrail
- Adds `tools/check_migration_check_drift.py`, a READ-ONLY drift detector that prevents silent CHECK constraint value shrinkage (regression like migration 032's near-loss of 31 enum values → 28). Parses CHECK constraints from new/modified `.sql` migration files and diffs their declared values against the live staging database schema via `information_schema.check_constraints`.
- **Wired into `migration-verify.yml`** as a post-apply, pre-verification gate: fails (returns 1) if any migration declares a SUBSET of live constraint values (specific missing values named in error). Growth (values added) always passes. New constraint (not yet live) passes.
- **Pattern reuse:** extends the drift-check precedent established by `db-inspect.yml`'s 2026-06-24 ingest-schema probe; same READ-ONLY, staging-only architecture.
- **Tests:** 11 unit tests (parse inline/ALTER CHECK, multiline IN clauses, empty/missing, shrink/growth/unchanged detection); parser tests pure-logic (no DB). Parser accepts both inline `CREATE TABLE foo ( col CHECK (col IN ...))` and `ALTER TABLE ADD CONSTRAINT foo_check CHECK (...)` syntax.
- **Scope:** CI enforcement + awareness; does not gate the migration apply (staged after) but blocks the merge if drift is detected. Zero impact to existing migration code or schema design.

### v3.79.0 (2026-07-06) - feat(drive-commander): promote PowerFlex 525 to live + represent related_parameters (PR-B follow-up)
- **What:** completes and deploys the PR-B PowerFlex 525 work. Two things: (1) adds a `related_parameters` field to the `ParameterCard` schema so the manual-stated "Related Parameters:" chains are represented (closing a declared PR-B residual), and (2) **promotes** the graded PowerFlex 525 pack from the staged candidate location into the live served `mira-bots/shared/drive_packs/packs/` tree — so `resolve_pack("PowerFlex 525")` now returns it in production. Read-only, manual-cited, no control writes.
- **`related_parameters` (additive, backward-compatible):** `ParameterCard.related_parameters: list[str]` (default `[]`) in `schema.py` + read in `loader.py` (mirrors `related_faults`). GS10 and every existing pack load unchanged (field defaults to `[]`). The generator now recovers the field from the extractor's raw `parse_parameters()` (assembly drops it) and injects it — manual-stated metadata, not a cited excerpt. The grading harness now **scores** it (gold-floor recall, same semantics as fault→param links; a missing expected entry is a recall gap, never a fabrication; extra pack entries are fine). The pack now carries the cross-vendor diagnostic chain **F081/F082/F083 → C125 [Comm Loss Action] → P045 [Stop Mode]** (C125.related_parameters `["P045"]`, C126→`["C125"]`, t094→`["P043","P044","A426","A427"]` — all match gold).
- **Promotion (human-gated deploy, per train-before-deploy):** the pack moved candidate → live `packs/powerflex_525/` on recorded **human approval (Mike, 2026-07-06)** with a **manual-cited-only scope waiver** (no bench data → empty `live_decode`/`envelope`; deployed as a read-only manual-cited pack). Sign-off recorded in the pack's `PROVENANCE.md`. The `test_drive_packs.py::test_resolve_pack_returns_none_for_unrelated_drive` example moved from "PowerFlex 525" (now packaged) to "Yaskawa GA800" (still unpackaged).
- **Re-graded at the live location — TRUST STATUS: `beta`** (the ceiling for a manual-cited-only pack): schema PASS · cite-integrity 93 verified / 0 unverifiable · gold precision 100%, diagnostic-critical recall+precision 100%, overall recall 97%, 0 fabrications · domain clean · residuals declared (incl. the promotion waiver). Report committed at `packs/powerflex_525/grading_report.md`.
- **Scope / safety:** the ONLY runtime change is the additive `related_parameters` field (inert — no renderer consumes it yet) and the new pack file now being live-resolvable. No edits to the engine, Hub, DB, `cards.py`, `live_snapshot.py`, or the GS10 pack. Read-only, no control writes, manual never committed. 43 drive-packs runtime + 62 extractor/grading tests pass; the read-only gate covers the promoted pack.
### v3.79.0 (2026-07-06) - feat(drive-commander): manual source registry + trust-preserving update-candidate workflow
- **What:** turns DriveSense manual→pack ingestion from one-off extraction into a **repeatable, trust-preserving update loop** — without building a second ingestion product. Answers the "keep drive manuals/packs current" brief by *reusing* the merged extractor (#2503) + grader (#2505) verbatim and adding only the thin update spine that was missing: a manual **source registry**, a read-only **change-detection** command, and a **candidate** generator. Core rule: *a changed vendor PDF creates a candidate, never an automatic trusted pack* — so an OEM manual revision can't silently rewrite MIRA's diagnostic truth.
- **Discovery first (doctrine):** a repo-wide inventory of every existing ingest/proposal/update path precedes the build — `docs/drive-commander/discovery-manual-ingest-and-update-workflow.md`. Findings: the extractor/grader/trust machinery already exists; the doc-ingest citable path is Hub node-ingest v2 (`knowledge_entries`); the proposal/approval state machine (ADR-0017 `ai_suggestions`) can host a future review item; SHA-256 hash primitives + a scheduled KB cron already exist. The only real gaps were manual-identity, hash-tied-to-pack change detection, and an update-candidate concept — all additive.
- **New (`tools/drive-pack-extract/registry/`):** `sources.json` (human-curated manual registry — vendor/model/publication/revision, approved `pdf_sha256`, generator+gold, trust status, residuals, approval record; **no PDF committed**), `registry.py` (pure load/validate + duplicate-identity detection + `classify()` state machine: `new_manual`/`unchanged`/`changed_by_hash`/`needs_initial_candidate`), `check.py` (read-only, non-gating: hash a local PDF → classify), `update_candidate.py` (reuses the existing generator + `grading/grade.py` as subprocesses → writes `candidate_report.{json,md}` with source identity + Phase-6 grading fields + pack-diff-vs-previous + reviewer checklist).
- **Trust-preserving by construction:** `update_candidate` writes **only** under `candidates/` (`assert_not_live_packs` refuses the live served `mira-bots/shared/drive_packs/packs/` tree), hard-codes `promoted: false`, and inherits the grader's automated ceiling of `beta` (`trusted` needs a recorded human sign-off). A `manual-review-only` source (`automatable:false`, e.g. GS10 today) is refused, not half-run.
- **Docs (Phase 7):** `docs/drive-commander/{workflow-register-a-manual-source,workflow-check-for-manual-updates,runbook-drive-manual-update-acceptance,runbook-do-not-silently-trust-updated-manuals,runbook-rss-product-update-adapter}.md` + the discovery record. RSS/OEM-feed automation is explicitly **staged behind the registry**, not built (the bulk crawler was disabled twice for VPS incidents #1318/#1336).
- **Tests (Phase 8, CI-safe):** 27 new offline tests (`registry/tests/`) — registry parse, automatable-requires-generator/gold, duplicate-identity fail-closed, SHA-256 determinism/change, classify state machine, no-auto-promote, no-live-packs guard, report carries provenance+trust, pack-diff. Wired into the `drive-pack-extract-tests` CI job (synthetic only; full-manual runs stay local). "Param-ids never in `related_faults`" / "unverifiable excerpts dropped" remain enforced by the reused grader + its existing tests.
- **Scope / safety:** additive, zero runtime change. Only new files under `tools/drive-pack-extract/registry/`, 5 docs + 1 discovery doc, one CI line, VERSION + CHANGELOG. No edits to the extractor/grader/schema/loader, engine, Hub, DB, or any pack. Read-only, no control writes, no manual committed. All existing 59 drive-pack tests stay green.

### v3.79.1 (2026-07-06) - fix(hooks): scope Stop hook to session-touched files + add worktree file-placement guard
- **Problem:** Stop hook blocked on peer-committed files in the shared working tree (diffs against `origin/main...HEAD` in concurrent multi-session checkout; other sessions' commits got lint-gated against this session's Stop hook, blocking completion on pre-existing unrelated errors). Also, sessions/subagents wrote files into the wrong git worktree with no detection.
- **Fix 1 — Stop hook scoping:** PostToolUse Edit|Write hook now logs each file to a per-session touched-files set (`${GITDIR}/claude-sessions/${SESSION_ID}-touched-files`). stop-gate.sh intersects `CHANGED` with this log (if it exists), gating only on files THIS session actually touched, falling back to the old origin/main...HEAD behavior when log absent (backward compat for overnight/headless runs).
- **Fix 2 — worktree guard:** new PreToolUse Write|Edit hook (`tools/hooks/worktree-file-guard.sh`) warns (never blocks) when a Write/Edit targets a file in a different git working tree than the current shell, with clear "current vs. target" messaging. Skips `/tmp`, non-repo targets, and the normal case (same toplevel).
- **Scope / safety:** read-only for Fix 2; Fix 1 is append-only on the touched-files log. No changes to the git dag or commit paths. Backward compatible for sessions without the log (fall back to old behavior). Insights-report friction category 3 ("Environmental Guardrails"), two of three identified issues.

### v3.78.0 (2026-07-06) - feat(drive-commander): PowerFlex 525 pack + reproducible trust/grading workflow (PR-B)
- **What:** PR-B — the cross-vendor proof that the drive-pack loop generalizes to a drive we did NOT seed. Runs the PR-A extractor **offline** on the real PowerFlex 525 manual (`520-UM001O-EN-E`) → a **STAGED CANDIDATE** `tools/drive-pack-extract/candidates/powerflex_525/pack.json` — validated + graded there, but deliberately **NOT** added to the live served `mira-bots/shared/drive_packs/packs/` tree, so there is genuinely **no runtime resolver behavior change**: `resolve_pack("PowerFlex 525")` still returns `None` in production. Promotion of a graded candidate into the live tree is a separate, future, human-gated deploy step (`.claude/rules/train-before-deploy.md`; see "Promotion (candidate → live)" in `docs/drive-commander/runbook-pr-b-acceptance.md`). The actual deliverable of this PR is an **open, reproducible grading workflow** that decides whether generated JSON is trustworthy. Doctrine: *"The drive pack is not trusted because the extractor ran; it is trusted only when reproducible checks prove the JSON matches the source PDF within declared limits"* (`docs/drive-commander/drive-pack-trust-doctrine.md`).
- **The pack (`schema_version: 2`, manual-cited-only):** 48 fault codes (F000–F127) + 45 diagnostic parameters, all `manual_cited`. The cross-vendor parallel to GS10 CE10→P09.03 is intact and cited: **F081/F082/F083 "Comm Loss" → C125 [Comm Loss Action]** (page 162/102), with C126 [Comm Loss Time] (default 5.0s, 0.1/60.0). `live_decode.status_bits`/`cmd_word`/`registers` + `envelope` are **empty** — PowerFlex 525 has no bench data, nothing invented. Generated by the new offline `tools/drive-pack-extract/generate_pf525_pack.py` (reproducible; manual gitignored, never committed; sha256 recorded in `PROVENANCE.md`).
- **The grading harness (`tools/drive-pack-extract/grading/`, the real deliverable):** a fail-closed, 5-layer, reproducible workflow — **A** schema (reuses the runtime loader's own `_parse_pack` validation logic, run against the staged candidate directory), **B** cite-integrity (re-reads the PDF, drops unverifiable excerpts), **C** gold-set scoring vs a human-approved `gold/powerflex_525/gold.json` (precision over recall; fabrication = a value contradicting gold or a param-id leaked into `related_faults`), **D** deterministic domain rules (fault-codes-are-fault-codes, no param-id in `related_faults`, no dup ids, uncited-value guard, mandatory `view_only_warning`, provenance vocabulary), **E** a machine- + human-readable report that assigns exactly one trust status: `rejected | internal_only | beta | trusted`. `grade.py` exits non-zero on `rejected`. 25 grading self-tests + 32 extractor tests = **57 offline tests**, now wired into CI (`drive-pack-extract-tests`, synthetic 4KB fixture only — full-manual grading is a documented local command).
- **Verified result — TRUST STATUS: `beta`** (the correct automated ceiling for a manual-cited-only pack; `trusted` requires a recorded human sign-off per `runbook-pr-b-acceptance.md`). Grading report (committed as acceptance evidence next to the pack): schema PASS · cite-integrity **93 verified / 0 unverifiable** · gold-scoring **100% recall + precision, diagnostic-critical 100%, 0 fabrications** · domain PASS · 3 residuals declared honestly.
- **Anti-fabrication in action:** the orchestrator independently re-ran the extractor on the real manual and inspected the JSON (the 10-step gate). Two honest sanitizations were applied at generation (not by weakening a check): 6 cross-row-bleed `default`/`unit` fields (P045/P052/…) nulled to honest "unknown"; and the messy multi-column page-98 analog-output block (which produced a duplicate `t089`) deliberately excluded and declared a residual rather than shipping bleed. A fail-closed uniqueness guard refuses to emit a pack with duplicate parameter-ids.
- **Declared residuals (honest):** F100/F109→P053 fault→param links not recovered by the extractor (non-diagnostic-critical); page-98 analog-output block deferred (extractor-hardening = PR-A scope); `related_parameters` (e.g. C125→P045) not represented — `ParameterCard` has no such field (deferred to a runtime schema PR).
- **Docs/runbooks (required deliverable):** `docs/drive-commander/{drive-pack-trust-doctrine,workflow-generate-drive-pack,workflow-grade-drive-pack,runbook-pr-b-acceptance,runbook-adding-a-drive-family}.md`.
- **Scope / safety:** additive. Only new files under `tools/drive-pack-extract/{generate_pf525_pack.py,grading/,gold/,candidates/,.gitignore}`, the 5 docs, one CI job, VERSION + CHANGELOG. **Nothing lands under `mira-bots/shared/drive_packs/packs/`** — the staged candidate is deliberately outside the live served tree, so `resolve_pack()` behavior is unchanged and the existing `test_resolve_pack_returns_none_for_unrelated_drive` test (which asserts `resolve_pack("PowerFlex 525") is None`) stays green as-is. No edits to the extractor, `schema.py`/`loader.py`/`cards.py`, engine, Hub, DB, or any existing pack. All 22 drive-packs runtime tests stay green. Read-only, no control writes, no manual committed. **Stacked on PR-A (#2503); rebase onto main after it merges.**

### v3.77.0 (2026-07-06) - feat(drive-commander): manual→pack extraction tool + cite-integrity framework (PR-A)
- **What:** the reusable **offline** compiler that turns an OEM drive-manual PDF into structured, cited drive-pack data — the tooling that makes new drive families (PowerFlex, Yaskawa, …) cheap. New package `tools/drive-pack-extract/`; does NOT touch the runtime `mira-bots/shared/drive_packs/` package (stays pure + read-only-gated), engine, Hub, DB, or any shipped pack. This is **PR-A ("the parser works")**; the actual PowerFlex service pack is a separate, human-approved PR-B ("the pack is trusted").
- **Position-aware extraction (not `extract_tables()`):** regex-anchored parsers over pdfplumber `extract_words()` x/y coordinates recover columns that `extract_text()`/`extract_tables()` silently drop. Handles the real PowerFlex 520-UM001 messiness: footnote markers glued to fault names, shared-description multi-code fault rows (F038/F039/F040), the `Fault Type (1/2)` column, and two different parameter layouts (grid + labeled-block).
- **The correctness guarantee — `related_faults` holds only FAULT codes.** A fault's "Modify using C125" action populates the parameter's `related_faults` with the FAULT code (C125→[F081,F082,F083]); a parameter's own "Related Parameters:" line is kept separately as `related_parameters` and never conflated. Worded/model-variant ranges are honestly kept `None` rather than fabricated.
- **Cite-integrity (anti-fabrication):** every emitted entry carries a page + verbatim excerpt, and `verify_excerpt_on_page` re-reads that PDF page — the extractor DROPS any entry whose excerpt can't be verified, so a fabricated citation is impossible. Self-test with teeth (a fabricated excerpt is rejected).
- **Page-range scoping + perf:** `extract(pdf, fault_pages=…, param_pages=…)` scopes by page number on the original PDF (preserving per-page geometry, so fault-type recovery survives). Whole-doc scan (>280s timeout) → **~4s scoped**; a second perf layer (cite-integrity reopening the 33 MB PDF per entry) was found + fixed.
- **Acceptance = real-manual trust, not synthetic correctness.** Verified INDEPENDENTLY against the real PowerFlex 520-UM001: 48 faults with clean names + recovered types, 35 params with recovered numeric ranges (worded ones honestly None), `related_faults` = fault codes only across all params, cite-integrity enforced, ~4s. Documented honest residuals (README): P045/P052 conditional-default bleed; minor header-furniture bleed into one `action` field. The real manual is NOT committed; CI runs against a small synthetic fixture that reproduces the discovered messiness. 32 tests, ruff clean.

### v3.76.0 (2026-07-06) - feat(drivesense): ship GS10 pack v2 — CE10→P09.03 parameter+keypad guidance goes LIVE
- **What:** upgrades the **shipped** `durapulse_gs10` pack from schema_version 1 → 2 by moving the manual-cited P09.03 ParameterCard + view-only KeypadNavigationCard (added as a test fixture in #2498) into the real `pack.json`. The wiring shipped inert in #2499; this **flips it live** — a GOOD-quality GS10 **CE10** (Modbus comm-timeout) fault now renders the cited parameter + view-only keypad card on both surfaces (engine `render_machine_evidence` + Ignition `assess_from_paths`). This is the first *real* DriveSense behavior change (PR B/C/D were inert). Read-only, view-only, manual-cited.
- **Content (verbatim from the #2498 fixture — nothing invented):** P09.03 "COM1 Time-out Detection", range `00–1000 sec`, default `00`, `related_faults: ["CE10"]`, cited to manual page 4-188; keypad view steps (MENU→ENTER→UP 09→ENTER→UP 03→ENTER) with a mandatory non-empty `view_only_warning`, cited to page 3-6. The shipped `pack.json`, the Hub copy (`mira-hub/src/lib/drive-packs/gs10-pack.json`), and the fixture are byte-identical.
- **Scoped + safe:** the flip affects **only CE10** — P09.03's `related_faults` whole-word-token-matches the fault name, so a non-CE10 fault (GFF/oL/…) still renders only its fault card, no parameter/keypad (verified). A STALE/comm-lost CE10 fault still renders no card. Keypad text always carries `view_only_warning`; no keypad step is a write/change/save instruction. No control writes.
- **Scope:** data + tests only. Shipped `pack.json` + Hub copy JSON + the read-only gate allowlist (`_ALLOWED_TOP_LEVEL_PACK_KEYS` += `parameters`/`keypad_navigation`) + the tests that asserted "shipped = v1 / no cards" (rewritten to assert the now-live rendering — the folded-in PR E live proof). No edits to `schema.py`/`loader.py`/`cards.py`/`live_snapshot.py`/`ignition_chat.py`/Hub TS logic/engine/DB/workflows.
- **Tests:** the PR D `test_v1_*` render tests inverted to prove the shipped pack now renders the CE10→P09.03 cards on both surfaces; safety tests (view-only warning, no write imperative, STALE suppression) kept; the v1 back-compat test repointed to a synthetic v1 dict (the shipped pack is no longer the "empty v1" example); read-only allowlist + hub-copy-sync green. Reviewed (Sonnet, adversarial): CE10-scoping + safety + no-invention confirmed; one leftover v1 assertion caught + fixed. Drive-pack suite green.

### v3.75.0 (2026-07-06) - feat(drivesense): wire schema-v2 parameter + keypad cards into DriveDiagnostic (PR D)
- **What:** DriveSense PR D — wires the schema-v2 `ParameterCard` / `KeypadNavigationCard` into the diagnostic path so a v2 pack's related-parameter + view-only keypad guidance surfaces after the fault card. **Additive and byte-identical for the shipped v1 pack** (which has no v2 blocks), so **no live behavior changes** until a pack ships v2.
- **Population (`build_drive_diagnostic`, `mira-bots/shared/live_snapshot.py`):** when there is a GOOD-quality active fault, `related_parameters` = the pack's `ParameterCard`s whose `related_faults` **whole-word-token-match** the active fault name (so "CE1" never matches "CE10"), and `keypad_navigation` = the `KeypadNavigationCard` for the top related parameter. Reads the module-global `_GS10_PACK`, which is the **shipped v1 pack → empty → no-op**. STALE/bad-quality faults yield no cards (reuses the existing `_active_fault_card` gate).
- **Rendering (both surfaces):** new pure `_render_parameter_card` / `_render_keypad_card` helpers, appended in `render_machine_evidence` and `assess_from_paths` **only when the lists are non-empty**. The keypad renderer **always** emits `view_only_warning`, and suppresses the whole card if that warning is blank (belt-and-suspenders over the loader's non-empty enforcement). No keypad step is a write/change/save instruction — the prohibition lives only in the warning line.
- **Scope / safety:** touches ONLY `live_snapshot.py` + a new test file. **Shipped `durapulse_gs10` pack stays `schema_version: 1`** (not upgraded here). No edits to `schema.py`/`loader.py`/`cards.py`/shipped `pack.json`/`ignition_chat.py`/Hub/DB/workflows; public signatures unchanged; read-only, no control writes, no manual parsing. CE10→P09.03 is proven on the PR C fixture in tests only — **not live** until a separate pack-upgrade PR, and the live proof is PR E.
- **Tests (`mira-bots/tests/test_live_snapshot_v2_render.py`, 11):** v1 `render_machine_evidence` + `assess_from_paths` emit **no** parameter/keypad text (byte-identical, mutation-verified); v1 `build_drive_diagnostic` → empty slots; v2 fixture (monkeypatched pack) populates P09.03 for CE10 and renders both sections; keypad render always carries `view_only_warning`; no keypad step is a write imperative; STALE/comm-lost fault suppresses everything. Reviewed (Sonnet, adversarial): **APPROVE** — byte-identical proven empirically, view-only safety confirmed, scope clean. Net +11 tests, 0 new failures (the one local cross-file `test_drive_diagnostic` isinstance collision is pre-existing on base, CI-green-safe).
- **Deferred (non-blocking):** a punctuation-adjacent fault-token case (safe for all current data); a >1-matching-parameter keypad case (first-slice surfaces one card by design); the shipped-pack v2 upgrade + PR E live proof; the `plc/GS10_Integration_Guide.md` stale-range fix.

### v3.74.0 (2026-07-06) - feat(drivesense): first primary-manual-cited GS10 v2 pack fixture (PR C)
- **What:** DriveSense PR C — **data + tests only**. Adds the first real `schema_version: 2` GS10 pack fixture with hand-curated, **primary-manual-cited** content for the CE10 → P09.03 communication-timeout troubleshooting path. No runtime behavior change.
- **Fixture (`mira-bots/tests/fixtures/drive_packs/gs10_v2_pack.json`):** the real shipped `durapulse_gs10` v1 blocks (`family`/`nameplate`/`live_decode`/`envelope`/`knowledge`/`provenance`) copied verbatim with `schema_version` bumped 1→2, plus two authored cards: a **`ParameterCard` for P09.03** ("COM1 Time-out Detection", range `00–1000 sec`, default `00`, unit `sec`, `related_faults: ["CE10"]`, `manual_cited`) and a view-only **`KeypadNavigationCard`** (MENU→ENTER→UP to 09→ENTER→UP to 03→ENTER; mandatory non-empty `view_only_warning`; `edit_warning: null`).
- **Provenance (real primary manual — DURApulse GS10 AC Drive User Manual, 1st Ed. Rev B):** P09.03 cited to page **4-188**; keypad view navigation cited to page **3-6**. `value_meanings` is intentionally **empty** — P09.03 is a continuous 00–1000 sec value; we do **not** infer "0 = disabled".
- **Scope / safety:** the **shipped `durapulse_gs10` pack stays `schema_version: 1`** and is untouched (the v2 content lives only as a test fixture under `tests/fixtures/`, loaded via the production loader `_parse_pack`, NOT under the shipped `packs/` dir the read-only gate scans). No edits to `schema.py`/`loader.py`/`cards.py`/`live_snapshot.py`/`ignition_chat.py`/Hub/DB/network/workflows/rendering. No new surface, no runtime keypad guidance wired.
- **Tests (`mira-bots/tests/test_drive_packs_gs10_v2_fixture.py`):** 6 assertions on the real content through the production loader — v2 loads, P09.03 fields (name/range/default/unit/CE10 link/manual_cited/empty value_meanings), keypad card (parameter_id/non-empty view_only_warning/steps/menu_group/manual_cited), citations carry the real doc label + pages 4-188/3-6, and the shipped v1 pack still loads unaffected. Net: **+6 passing, zero new failures.** The one pre-existing local `test_drive_diagnostic` collection-order failure is unchanged from base (CI-green-safe; PR B merged green on the same suite).
- **Deferred (intentionally, separate follow-ups):** correcting the stale `plc/GS10_Integration_Guide.md` P09.03 range ("0.0–100.0" → manual "00–1000 sec"); wiring these cards into live diagnosis (PR D); the pre-existing test-infra module-collision debt.

### v3.73.2 (2026-07-05) - feat(drive-commander): activate the GS10 DC-bus analog card on bench hardware (scaling seed)
- **What:** the Ignition analog assessment card shipped in v3.70.0 was **deployed but inert on real hardware** — it only renders when the matched `tag_entities` row carries an explicit scaling contract in `tag_entities.scaling`, and nothing populated that column (`createTagEntity` writes 8 cols, not `scaling`/`units`). This ships the **bench activation seed** for the GS10 DC-bus tag so a live CV-101 turn now renders `DC bus: 320 V … Assessment: normal`.
- **The seed (`tools/seeds/tag_scaling_gs10.sql`):** writes the reader contract verbatim — `scaling = {"mode":"raw_register","scale":0.1}`, `units = 'V'` — keyed on the exact wire path `[default]MIRA_IOCheck/VFD/vfd_dc_bus` (the `_enrich_tag_snapshot` join key), promoted to `approval_state='verified'` (the enrichment SELECT reads only verified rows). The 300–340 V band is NOT stored — it comes from the `durapulse_gs10` pack envelope at assessment time (`expected_envelope` intentionally NULL). The ÷10 scaling is bench-verified (pinned in `mira_config_conveyor.json` + `tags.json`). Idempotent + shape-safe: UPDATE-by-`source_address` first (preserves an existing row's `uns_path`/`data_type`), INSERT only if absent — no duplicate under the `(tenant_id, uns_path)` UNIQUE.
- **Delivery (`.github/workflows/apply-tag-scaling.yml`):** sibling of `apply-approved-tags.yml` for UUID-tenant `tag_entities` scaling seeds — `target` staging/prod, `__TENANT_ID__` sed substitution, dry-run/apply, UUID validation, read-only pre-flight (existing-row inspection) + post-apply proof. Reusable by future Drive Commander per-drive scaling seeds.
- **Scope / safety:** data seed only — no product code change, no socket, no PLC/fieldbus, no prod `psql` from a session (applied via the gated workflow). Only the bench tenant's own known-good drive is promoted to `verified` (evidenced, not an auto-verify of customer/LLM data). Bench-only activation; other tenants unaffected.
- **Tests:** `mira-bots/tests/test_seed_tag_scaling_gs10.py` (4) locks the seed↔reader contract — it parses the scaling JSON / units / source_address out of the seed SQL and drives them through `wire_scaling.from_jsonb` → `live_snapshot.assess_analog_from_paths`, asserting the live DC-bus card renders (`DC bus: 320 V`, `×0.1`, `Normal band: 300–340 V`, `Assessment: normal`) and that the row lands `verified`. Adjacent scaling/analog suites 22 green; ruff clean.

### v3.73.1 (2026-07-05) - feat(drivesense): schema v2 cards for parameters and keypad navigation
- **What:** DriveSense PR B — **schema/types only**. Adds the type layer for cited configurable-parameter decode and view-only keypad navigation, so a later phase can populate them without touching live diagnosis now.
- **New pack-stored shapes (`drive_packs/schema.py`):** `ParameterCard` (cited view of one configurable parameter — the keypad/manual id like `P09.03`, NOT a Modbus register; carries `value_meanings`/`default`/`range`/`unit`/`related_faults`/citation/provenance), `KeypadNavigationCard` (ordered **view-only** button-press steps; `view_only_warning` is mandatory + non-empty as a safety contract; `keypad_steps` are display strings, never executable), and `ValueMeaning`. `Citation` relocated here (from `cards.py`, re-exported for back-compat) so the pack-stored cards can reference it without a schema→cards import cycle.
- **schema_version 2 (`drive_packs/loader.py`):** the loader now accepts `{1, 2}` (unknown version → hard error, never a silent parse) and parses two OPTIONAL top-level blocks — `parameters` + `keypad_navigation` — into the new cards, injecting `drive_family` from `pack_id`. Enforces the closed provenance vocabulary (`bench_verified`|`manual_cited`, never bare "verified") and the non-empty `view_only_warning`. A pure `_parse_pack(raw, pack_id, path)` seam was split out for dict-level testing. **v1 packs load unchanged** (the real `durapulse_gs10` stays v1; new fields default empty).
- **`DriveDiagnostic` (`live_snapshot.py`):** extended with two additive carry slots — `related_parameters: list[ParameterCard] = []` and `keypad_navigation: KeypadNavigationCard | None = None`. `build_drive_diagnostic` does **not** populate them yet, so the existing assessment + fault-card rendering is **byte-identical** (existing tests green, unchanged).
- **Scope / safety:** pure schema/types — no manual parsing, no runtime keypad guidance, no DB/network/socket, no Hub/Slack/Telegram/Ignition surface change, no runtime dependency on the (open, docs-only) PR #2494. Read-only AST gate stays green. Explicitly deferred (proposal §4/§5, not in this PR): populating the new fields in `build_drive_diagnostic`, the `evidence`/`unknowns`/`safety_warning` response fields, and the extended `Citation` (`section`/`chunk_id`/`tier`).
- **Tests:** `mira-bots/tests/test_drive_packs_schema_v2.py` (13) — v1 still loads (empty blocks), v2 parses from dict + disk, card fields + `drive_family` injection, unsupported-version / empty-warning / missing-`parameter_id` / bad-provenance rejection, `Citation` re-export identity, and `DriveDiagnostic` additive-fields-default-empty + frozen. Full drive-pack + live-snapshot surface **95 green** (incl. readonly gate + byte-identical rendering); ruff clean.

### v3.72.0 (2026-07-05) - feat(drive-commander): structured DriveDiagnostic response object
- **What:** introduces `DriveDiagnostic` (`mira-bots/shared/live_snapshot.py`) — a surface-agnostic structured diagnosis (`assessment: str|None` + `fault_card: DiagnosticCard|None`) built once by `build_drive_diagnostic(snapshots)`. Both existing surfaces (engine `render_machine_evidence`, Ignition `assess_from_paths`) now build and render THIS one object, so every DriveSense surface presents the same deterministic assessment + cited fault card. Foundation for future Hub/Slack/Telegram consumers and trace threading.
- **Behavior-preserving:** a pure refactor — the fault-card text is byte-identical to before (verified line-by-line + the three prior suites pass unchanged). No new surfaces/features. Also removed the now-dead `_render_active_fault_diagnostic` the refactor orphaned. (Rebased over main's v3.70.0 analog-scaling assessment — the two coexist: `assess_from_paths` routes the fault card through `build_drive_diagnostic`, `assess_analog_from_paths` is untouched.)
- **Scope / safety:** read-only/offline (no DB/network/socket); no edits to `template_reader.py`/`cards.py`/`drive_fault_intel.py`/`pack.json`/`ignition_chat.py`. STALE fault still yields no card (`fault_card=None`); the GOOD-quality + non-empty-enrichment gate lives in one place (`_active_fault_card`).
- **Tests:** +8 (`test_drive_diagnostic.py`: GOOD builds object, STALE/unknown/no-fault/empty → `fault_card None`, single-source-of-truth card substring in BOTH surfaces, no-socket, frozen). 59 + 12 green; 3 pre-existing suites unchanged; zero regressions. Reviewed (Sonnet): Spec OK / Quality Approved, byte-identical fault-card output confirmed.

### v3.70.1 (2026-07-05) - docs(skills): add PR/merge-state re-verification doctrine to ship-pr and ship skills
- **What:** the `ship-pr` and `ship` skills now include a **double-read-and-compare pattern** for GitHub API calls when making merge-adjacent decisions. GitHub's API cache lag (up to ~5 seconds on status changes) can cause a single `gh pr checks` or `gh pr view` read to conflict with actual state or prior user reports — particularly when a merge just landed. Both skills now mandate: if a single API read conflicts with expectation or prior information, **always re-fetch after a 2-second delay** and act only when two independent reads agree.
- **Doctrine:** the `ship-pr` skill adds §4b on "API cache lag: the insights-report pattern", citing the 2026-06-08 insights report's concrete example (merge reported ✓ → API shows OPEN → subsequent re-checks confirm the merge). The `ship` skill embeds re-verify guidance into the merge step itself (§4), instructing the operator to check `gh pr view <PR#> --json state,mergeable` before acting if any conflict arises.
- **Why:** this is a response to friction pattern #2 from the 2026-07-05 insights deployment (GitHub cache lag + concurrent-session edits = silent loss of ground truth). No code change; process-only. Allows merges + deploys to be safer under high-latency API reads.

### v3.70.2 (2026-07-05) - fix(ci): guard unguarded pipes in pre-commit hook against SIGPIPE (section 7b)
- **What:** fixes a critical hook failure when the `.git/` working tree contains a large number of untracked files (50k+). The section-7b diagnostic message `printf '%s\n' "$UNTRACKED" | head -5 | sed` would abort with exit 141 (SIGPIPE) when the buffer overflowed before `head -5` closed the read side of the pipe.
- **The fix:** guards all 7 unguarded pipes in the script with `|| true` (lines 94, 104, 105, 132, 236, 255, 266). SIGPIPE is a normal pipe-closure condition — the message is informational and the hook MUST NOT fail on it. `|| true` also guards the `echo | wc -l` on line 105 to be safe.
- **Verification:** pre-commit now exits 0 on the large-untracked scenario; reproduction test confirms exit 141 before, 0 after. All other checks unchanged. ruff clean.

### v3.70.0 (2026-07-05) - feat(drive-commander): Ignition analog assessment via an explicit per-tag scaling contract (follow-up #2)
- **What:** the Ignition direct "Ask MIRA" path can now assess **analog** live values (DC bus / current / frequency) against the GS10 pack envelope — but **only when the tag carries an explicit, verified scaling contract**. Before this, analog values were shown in the preamble but never assessed, because a wire value like `vfd_dc_bus = 3200` is ambiguous (raw register ×0.1 → 320 V, or already 320 V). Envelope assessment already shipped on the *engine* snapshot path (v3.65.0); this closes it on the *Ignition wire* path without reintroducing the ambiguous-scaling risk.
- **The contract (new pure layer `mira-bots/shared/wire_scaling.py`):** `TagScaling{mode, scale, unit, source}` with `mode ∈ {raw_register, engineering_value, unknown}`. `to_engineering()` converts a wire value ONLY when the mode is explicit (a `raw_register` tag with no `scale` inherits the pack's trusted register scaling); `unknown`/missing/un-coercible ⇒ `None` ⇒ the caller stays silent. `from_jsonb()` reads the value from `tag_entities.scaling` (contract shape `{"mode":"raw_register","scale":0.1}`; a `NULL`/mode-less value ⇒ `unknown`, never a guess).
- **Assessment (`live_snapshot.assess_analog_from_paths`):** a **separate, opt-in** function beside `assess_from_paths` — the enum/bool hard boundary is untouched (analog leaves are still NOT in the assessable sets). It reuses the existing pack envelope via a new structured `_analog_band_status` helper (the engine path's `_analog_band_observation` was refactored onto it, behavior-preserving). Renders a **self-explaining card**: `DC bus: 320 V / Source value: 3200 / Scaling: raw register ×0.1 (approved tag mapping) / Normal band: 300–340 V / Assessment: normal`. A tag with an explicit scale but no full pack band (e.g. `current`) stays silent — honest abstention (ADR-0025 §4).
- **Wire seam (`mira-pipeline/ignition_chat.py`):** the existing verified-`tag_entities` enrichment SELECT now also reads `scaling` (a read; no new write path); the endpoint builds `{path → TagScaling}` and appends the analog card(s) after the enum/bool assessment. Best-effort/never-raises; unknown/missing scaling ⇒ no card ⇒ prior display-only behavior.
- **Scope / safety:** read-only/offline (no DB write, no socket, no fieldbus); the read-only shipping gate stays green. Explicitly deferred (documented): populating `tag_entities.scaling`/`expected_envelope` for real tenants, the Hub TS mirror, component-template citation pointers on the card, per-component UNS granularity.
- **Tests:** `mira-bots/tests/test_wire_scaling.py` (12, pure contract) + `test_live_snapshot_analog_paths.py` (11: raw/eng/unknown/missing/out-of-band/pack-fallback/no-band-silent/ambiguous-3200-no-false-undervoltage) + 2 route tests proving the card reaches (and unknown scaling withholds) the Ignition prompt end-to-end. mira-bots 100 green (engine/live-snapshot/drive-pack regression unchanged = byte-identical engine path); mira-pipeline Ignition 14 green. ruff clean.

### v3.69.0 (2026-07-05) - feat(drive-commander): enrich the Ignition Ask-MIRA path with the GS10 fault card (DriveSense next phase)
- **What:** the GS10 fault diagnostic card (likely causes / first checks / cited source) now also surfaces on the **Ignition direct "Ask MIRA"** path (`live_snapshot.assess_from_paths`), not just the engine's Live Machine Evidence. Closes the follow-up-#2 deferred gap #2. A live GS10 fault reported over the Ignition wire form now gets cited, per-fault troubleshooting in the Ask-MIRA preamble.
- **No duplication:** a single shared helper `_render_active_fault_diagnostic(snapshots)` holds the GOOD-quality gate + card render; BOTH `render_machine_evidence` (engine) and `assess_from_paths` (Ignition) call it. The engine path is a behavior-preserving refactor to route through the same helper.
- **Scope / safety:** read-only/offline (no DB/network/socket); reuses #2482/#2484's `render_fault_diagnostic` + offline `drive_fault_intel.py`; no edits to `ignition_chat.py` production code (its consumer already joins evidence with newlines, so the multi-line return flows through). STALE (comms-lost) fault still suppresses the card on the Ignition path too. No new cross-surface response-object architecture (deferred until a second surface needs it).
- **Tests:** +new unit tests (Ignition known-GOOD-fault enriches, STALE suppresses, unknown keeps plain assessment, no-fault unchanged, nothing->None, single-source-of-truth identical card, socket-blocked) + 1 integration test proving the card text reaches the built Ignition prompt end-to-end. 51 + 12 green; engine/drive-pack regression 19 green; zero regressions. Reviewed (Sonnet): Spec OK / Quality Approved.

### v3.68.0 (2026-07-05) - feat(drive-commander): wire GS10 fault card enrichment into Live Machine Evidence (follow-up #2)
- **What:** the first RUNTIME caller of `build_cards`. `mira-bots/shared/live_snapshot.py` now injects an offline `FaultCodesTemplateReader` and, in `render_machine_evidence`'s active-fault branch, renders the matching GS10 diagnostic card (**likely causes / first checks / cited source**) into the engine's `## Live Machine Evidence` section — so a live GS10 fault surfaces cited, per-fault troubleshooting in Supervisor replies. Before this, the `TemplateReader` seam (#2482) had no caller.
- **Data (no DB):** a new offline adapter `mira-bots/shared/drive_fault_intel.py` holds a curated `manual_cited` GS10 fault table (9 codes) + `build_gs10_template_reader()`. Interim source until a DB-backed reader over the live `fault_codes` table lands. Provenance is honest per code — numeric faults cite P06.17 (fault records); the CE Modbus comm codes cite the RS-485 comm section, never a page the text isn't grounded in.
- **Scope / safety:** read-only/offline (no DB/network/socket); no edits to `template_reader.py`/`cards.py`/`pack.json`; the pack surface stays pure-pointer (intel lives OUTSIDE `drive_packs/`). Ignition `assess_from_paths` path, DB-backed reader, and `decision_trace` threading are deferred + documented (too broad for a small PR).
- **Tests:** +9 (`test_live_fault_card_wiring.py`: known-fault enriches, unknown keeps fallback, empty fields no bad text, no-DB/socket, drift guard). Focused set 62 green; full sweep 1061 pass / 12 pre-existing fail (verified via stash A/B — zero regressions). Reviewed (Sonnet) — a flagged overstated-citation issue was fixed before merge (honest provenance).

### v3.67.0 (2026-07-05) - feat(ci): guardrail against fabricated first-party import symbols
- Adds `tools/verify_agent_symbols.py`, which indexes every `def`/`class`/module-level name in the repo and flags newly-added `from <first-party-module> import <Name>` lines whose `Name` doesn't resolve anywhere — targets the recurring agent-fabrication failure mode called out in CLAUDE.md's "Verify Subagent Output" rule (`ConnectorService`, `approved_state`).
- Wired as a warn-only section in `.githooks/pre-commit` (avoids local Stop-hook thrash on the shared working tree) and as a PR-comment backstop in `code-review.yml`'s `static-analysis` job. Does not catch non-import fabrication (invented enum/state strings) — documented as a known gap.

### v3.66.0 (2026-07-05) - feat(drive-commander): GS10 pack enrichment — architecture doc + first TemplateReader (follow-up #1)
- **What:** starts follow-up #1 of Drive Commander (ADR-0025). Adds `FaultCodesTemplateReader` (`mira-bots/shared/drive_packs/template_reader.py`) — the first concrete implementation of the `TemplateReader` seam in `cards.py`, so diagnostic cards can carry real per-fault `likely_causes`/`first_checks`/`citations`. Backed by the `fault_codes` table (the only store keyed 1:1 to the pack's `live_decode.fault_codes`); pure/offline/injected data — no DB/network imports, passes the read-only shipping gate.
- **Also:** a reference doc `docs/drive-commander/pack-construction-and-observability.md` — how a pack is constructed, where its knowledge lives (**KB `knowledge_entries` + the `component_templates` layer + KG `kg_entities`** — the pack is a file that points at all three, never in the DB), and how a pack-driven diagnosis is traced (real trace = `decision_trace.py`; flags the phantom `agent_trace.py` and the missing per-pack/per-card trace record).
- **Scope / safety:** additive only; the reader is NOT yet wired into a live caller (no runtime/prod change). Deferred + documented: register `addr`, the `knowledge` id-pointers, engine wiring + trace threading.
- **Tests:** +9 (`test_template_reader.py`); read-only gate + drive-pack card suites green (23 in the core set; 41 related). Built subagent-driven (Sonnet impl + review, Haiku doc cross-check, Opus final whole-branch review: READY TO MERGE).

### v3.65.0 (2026-07-05) - feat(drive-commander): GS10 drive pack + pack-driven engine/Hub (ADR-0025)
- **What:** the first build of **MIRA Drive Commander** (the single sellable product, **ADR-0025**). Extracts the DURApulse GS10 drive into a **drive pack** (co-located package data `mira-bots/shared/drive_packs/packs/durapulse_gs10/pack.json`) behind a **drive-agnostic loader**, so the Python engine (`live_snapshot.py`) and the Hub (`gs10-display.ts`) both source GS10 decode from ONE pack instead of hardcoded tables. Adding the next drive becomes *authoring a pack*, not editing engine code.
- **Also:** envelope-driven analog assessment (closes the machine-memory deferred-#1 gap; honest silence on ambiguous wire scaling), a nameplate→pack resolver (reuses structured-vision), derived diagnostic cards (`TemplateReader` seam for the future `component_templates`/KG layer), and a **provable-read-only shipping gate** (AST-based, self-tested — no write FC, no fieldbus socket) per ADR-0025 §4.
- **Two bugs the review pipeline caught pre-merge:** GS10 REV+RUN cmd word was `20` (stale, falsely `bench_verified`) → corrected to bench-verified **`34`** with a `rules_core.py`-pinned ground-truth guard; and a passes-CI-breaks-image packaging bug (repo-root `packs/` not in the Hub/Python Docker images) → fixed by co-locating the pack as package data + a CI drift guard keeping the Hub copy byte-identical.
- **Scope / safety:** read-only; existing GS10 suites green and unedited; NO desktop/mobile UI in this slice (later wave).
- **Tests:** Python drive-pack + live-snapshot + engine suites green; Hub `vitest` green (`gs10-display.test.ts` unedited). Built via subagent-driven-development (7 tasks + final Opus whole-branch review).

### v3.64.0 (2026-07-05) - feat(ignition): deterministic live-tag Assessment on the HMI Ask-MIRA surface
- **What:** brings the deterministic **Assessment** (from #2476 Hub / #2478 engine) to the **Ignition direct-connection "Ask MIRA"** (`mira-pipeline/ignition_chat.py`) — the HMI deployment surface. The prompt now carries `Assessment: <one-liner>` (e.g. *"VFD looks healthy (comms OK, no fault) but the machine is stopped — most likely a command/permissive/interlock, not the drive"* or *"Active VFD fault: CE10 modbus timeout…"*) plus the live/asset-context/inference/next-checks separation instruction, alongside the existing `[LIVE TAGS]` block.
- **Why it's not a copy-paste of #2478:** the Ignition wire snapshot (`ignition/webdev/FactoryLM/api/chat/doPost.py`) is `{full_path: {"value": str(qv.value), …}}` — **string values, full-path keys, and ambiguous analog scaling** (raw register vs engineering). Re-scaling an already-engineering value would report a 10×/100×-wrong number. So the new pure helper `live_snapshot.assess_from_paths()` assesses from **only the scaling-immune enum/bool signals** (`vfd_fault_code`, `vfd_comm_ok`, `vfd_cmd_word`, `vfd_status_word`, safety bools) — coercing the string wire value per-leaf-type (so `"False"` isn't treated as truthy) — and **never re-interprets analog values** (they're shown in the preamble, not assessed). Returns `None` (honest silence) when nothing assessable is present; never fabricates.
- **Scope / safety:** text-preamble only; the direct-connection UNS certification, the reject-on-missing-identifier contract, and the trace-only `tag_evidence` are untouched. No engine evidence/citation-contract change, no write path. Defensive import — degrades to no assessment if `shared/` isn't mounted (default behavior preserved).
- **Deferred (documented):** an **analog** assessment on the Ignition path needs a trustworthy value contract (`tag_entities.expected_envelope`, or a raw-vs-engineering wire flag). See `docs/discovery/ignition_live_evidence.md`.
- **Tests:** +8 (`assess_from_paths` over the Ignition wire form incl. healthy-but-stopped / active-fault / `"False"`-not-truthy / analog-only→None / bare-scalar; 2 endpoint tests that the assessment + separation reach the prompt). Affected suites green: `test_live_snapshot` + `test_engine_live_snapshot` 32, `test_askmira_regression` 15, `test_ignition_chat_direct_connection` 11. ruff clean.

### v3.63.0 (2026-07-05) - feat(engine): Live Machine Evidence section on the Supervisor live-tag path
- **What:** the follow-up to #2476 (the Hub machine-memory intelligence bridge), now on the **Python engine** side. `mira-bots/shared/engine.py::_maybe_attach_live_snapshot` — the gated path that prepends live PLC/VFD tags after the UNS confirmation gate — now attaches a structured **`## Live Machine Evidence (observed now)`** section (decoded values + a deterministic **Assessment** + the live/asset-context/inference/next-checks separation instruction), the Python mirror of the Hub's `renderMachineEvidenceSection`. New pure functions in `mira-bots/shared/live_snapshot.py`: `assess_snapshots()` (the deterministic "VFD-healthy-but-stopped / active-fault / comms-down / running" assessment, mirroring the Hub `deriveContextIntelligence`) and `render_machine_evidence()`.
- **No regression to marker-keyed behavior:** the section **embeds the canonical `[LIVE CONVEYOR STATUS]` block verbatim**, so the engine's kiosk quality-gate bypass and direct live-tag fast-path (both keyed on `_LIVE_STATUS_HEADER`) keep working unchanged. `render_status_block` is retained (still used by `ask_api`).
- **Scope / safety:** text-preamble only, gated + best-effort exactly as before — it cannot make MIRA *act*, only frame live evidence better. Does NOT touch the engine's structured-evidence/citation/groundedness contract, add a fieldbus/write path, or change the direct-connection UNS certification.
- **Deferred (documented, next PR):** wiring the assessment into `mira-pipeline/ignition_chat.py` (the Ignition HMI surface) — its snapshot is **pre-scaled + arbitrary-keyed** (`{"Motor_Current_A": {"value": 11.2}}`), so it can't reuse `live_snapshot`'s raw-register decode without a `tag_entities`-driven mapping. Also deferred: a genuine structured/citable live-evidence type in the Supervisor. See `docs/discovery/engine_live_evidence.md`.
- **Tests:** +9 (pure `assess_snapshots` incl. VFD-healthy-but-stopped, `render_machine_evidence`, updated engine-attach assertions); affected suites green (`test_live_snapshot` + `test_engine_live_snapshot` 26, `test_askmira_regression` 15 — confirms the marker path intact, `test_ignition_chat_direct_connection` 9). ruff clean.

### v3.62.0 (2026-07-04) - feat(hub): machine memory → intelligence bridge (Ask MIRA sees live tag values)
- **What:** the live Machine Memory state (current decoded tag values, freshness-aware machine state, persisted anomalies) is now a first-class **intelligence input** to the asset-page "Ask MIRA". Adds a deterministic, read-only **machine context packet** (`mira-hub/src/lib/machine-context-packet.ts` + pure `machine-context-intelligence.ts`) that composes the existing `buildMachineMemoryResponse` output into `{machine_state, summary, live_tags, active_conditions, changed_recently, freshness, evidence}`, and bridges it into `/api/assets/[id]/chat` as a citable `## Live Machine Evidence (observed now)` prompt section (via `renderMachineEvidenceSection`). MIRA is instructed to separate live evidence / asset+doc context / inference / next checks.
- **Why it matters:** before this, the asset chat saw runs/state-windows/anomaly rows but **not the current live tag values** (`approvedLiveSignalCount` was hardcoded `0`). Now it can answer "why is this machine stopped?" from the actual decoded VFD signals.
- **Build-on-what-exists (no reinvention):** reuses `buildMachineMemoryResponse`, `deriveCurrentState`, `classifyTagFreshness`, `formatTagValue` (VFD decode), and the persisted `run_diff` anomalies (the A0–A12 `rules_core` output). Does **not** re-implement the rule engine (no 5th copy), add a `machine_events` table, or introduce any tag write path. Intelligence is computed **once** in `buildMachineMemoryResponse` and shared by the card, the SSE stream, and the chat packet.
- **UI:** `MachineMemoryCard` now shows a one-line "Assessment" (the same deterministic summary MIRA receives).
- **Discovery:** `docs/discovery/machine_memory_intelligence_bridge.md`.
- **Persistence (deferred, documented):** state windows + typed anomalies already persist via the `run_engine` worker; persisting *Ask-MIRA diagnostic snapshots* is a follow-up, as are the Python-engine structured-live-evidence path, `tag_entities`-driven meaning, and per-component UNS.
- **Tests:** 32 new/updated; full `mira-hub` lib+component suite green (the only local reds are the pre-existing/environmental `sitemap-drift` + `cmms-deploy-env`).

### v3.61.7 (2026-07-04) - fix(web): durable Stripe->Hub provisioning with retry + idempotency
- The webhook's `hub_users` bridge was best-effort: if a customer paid before registering on the Hub (`activateHubUserByEmail` UPDATE matched 0 rows) or the Hub DB call threw, the paying customer stayed unprovisioned with no retry. Adds `plg_pending_hub_provisioning` (keyed on the Stripe event id — `ON CONFLICT DO NOTHING` makes event redelivery a no-op, and the underlying `hub_users` UPDATE is itself idempotent, so replays can never double-provision). The webhook (`checkout.session.completed` / `customer.subscription.deleted`) queues a pending record on `matched=0` or error, and closes queued records once a delivery lands. `reconcilePendingHubProvisioning()` retries pending rows; hooked fire-and-forget into `/api/magic/login` and the `/api/register` returning-active path, and exposed as a `POST` endpoint.
- Verified: 9/9 new tests (`hub-provisioning-queue.test.ts`) pass; `bun test` shows 299 pass / 16 fail / 7 errors — the 16 fail + 7 errors are pre-existing on `main` (identical count, an unrelated `@neondatabase/serverless` dep issue), confirmed by running the same suite against a clean `origin/main` checkout. `bunx tsc --noEmit` shows 35 errors on both this branch and clean `main` (identical pre-existing `"user"` key overload class) — no new type errors introduced.

### v3.61.6 (2026-07-04) - feat(engine): CMMS work-order evidence context (flag-gated OFF)
- Adds `mira-bots/shared/wo_evidence.py::recall_work_orders` + `Supervisor._build_wo_evidence_context`/`_format_wo_evidence` — a citable `[WO ####]` evidence block sourced only from real `work_orders`/`cmms_equipment` rows, injected into `_call_with_correction`'s context alongside the existing interlock/live/KG context. Gated behind `ENABLE_WO_EVIDENCE` (default off); every guard (no tenant, no asset, DB error, timeout) returns `""` rather than raising, so the diagnosis path is unaffected while off. 13 new tests (`tests/test_wo_evidence.py`); 141 existing engine tests unaffected.

### v3.61.5 (2026-07-04) - feat(engine): tenant plan/quota check at Supervisor entry (flag-gated, fail-open) (#2436)
- Billing/quota was enforced only on the web path — Telegram, Slack, and Ignition chat accepted unlimited queries with no tenant plan check. Adds one quota/plan check (`mira-bots/shared/quota.py`) at the Supervisor's engine entry point so every chat surface inherits it, instead of each adapter reimplementing enforcement. `ENFORCE_PLAN_QUOTA` defaults `"0"` (off, zero DB calls/behavior change); fail-open on every error path (flag off, no tenant_id, no `NEON_DATABASE_URL`, DB exception/timeout, unknown tenant, no limits configured). Runs before `process_full()`, ahead of the UNS confirmation gate / direct-connection certification. 133 regression tests clean.

### v3.61.4 (2026-07-04) - feat(tools): env-var drift checker (docker-compose.saas.yml vs Doppler docs) + CI step (#2452)
- Adds `tools/check_env_drift.py` — a stdlib-only static checker comparing env vars referenced in `docker-compose.saas.yml` / `docker-compose.staging-vps.yml` against `docs/env-vars.md` + `.env.template`, flagging used-but-undocumented and documented-but-unused vars. Seeded with an allowlist (`tools/env_drift_allowlist.txt`) of ~171 pre-existing offenders to burn down over time. Wired into the `architecture-check` CI job.
- Documents the 14 `MQTT_INGEST_*` + `MIRA_MACHINE_MEMORY_UNS_PATHS` vars the checker caught (landed on main after the allowlist was seeded, genuinely undocumented) in `docs/env-vars.md` rather than adding them to the allowlist — per the checker's own "new drift must not be added here" policy.

### v3.61.3 (2026-07-04) - chore(repo): clean root — archive handoffs/reports/screenshots, delete mira_copy dead weight (#2453)
- Deletes `mira_copy/{outputs,prompts,templates}/` (generated marketing-copy artifacts) and archives root-level handoff docs / competitor reports / promo screenshots into `docs/archive/` + `docs/promo-screenshots/`. Keeps `mira_copy`'s 5 core importable module files (`__init__.py`, `__main__.py`, `cli.py`, `client.py`, `generate.py`) — still imported live by `mira-crawler/tasks/{social,blog,content}.py`. Referenced nginx confs left in place.

### v3.61.2 (2026-07-04) - fix(tools): route demo_plc_poller through the one-pipeline ingest contract (#2450)
- `tools/demo_plc_poller.py` shipped its own rival `live_signal_cache`/`live_signal_events` DDL and wrote directly to NeonDB, bypassing `.claude/rules/one-pipeline-ingest.md`. Removes the rival schema/DB-write code and routes tag values through `build_tag_entry`/`build_ingest_batch` (`mira-relay/ingest_contract.py`) → `ingest_batch` (`mira-relay/tag_ingest.py`), mirroring `simlab/publishers.py::RelayIngestPublisher`.

### v3.61.1 (2026-07-04) - test(pipeline): bootstrap offline unit suite for the live VPS chat path (#2449)
- Adds 17 offline unit tests in `tests/pipeline/` covering mira-pipeline's OpenAI-compat `/v1/chat/completions` contract, the Ignition direct-connection UNS-certification contract (`.claude/rules/direct-connection-uns-certified.md`), and the Groq→Cerebras→Together inference cascade (incl. PII sanitization). Alongside the pre-existing `tests/test_mira_pipeline.py` (9 tests), not a replacement.
- Fixes a `sys.modules['main']` cache collision: `tests/flywheel/test_interlock_extract.py` / `tests/regime2_rag/test_content_chunking.py` insert `mira-crawler/` (which also has a `main.py`) at `sys.path[0]` during collection, in xdist-worker order this suite doesn't control — whichever `main.py` wins the race gets cached for the rest of that worker. `tests/pipeline/conftest.py` now re-wins the path race and evicts the stale cache right before `import main`, same pattern as `tests/regime6_sidecar/conftest.py`.

### v3.61.0 (2026-07-04) - feat(repo): MODULES.md lifecycle manifest + CI guard (#2443)
- Adds `MODULES.md` classifying all 26 top-level modules as DEPLOYED / BENCH / CI / DEFERRED / LEGACY / ORPHAN (matching `docs/audits/2026-07-04-cohesion-audit.md` §1), plus `tests/test_modules_manifest.py` as a CI guard so a new top-level module can't go unclassified and a removed one can't leave a stale entry.

### v3.60.4 (2026-07-04) - fix(ci): include mira-web in default deploy TARGETS
- `deploy-vps.yml`'s default `TARGETS` list was missing `mira-web` — marketing-site PRs merged to main but never auto-deployed (no stated exclusion rationale in `docs/known-issues.md`). Adds it to the default rebuild list alongside the other services.

### v3.60.3 (2026-07-04) - perf(relay): one Neon connection per ingest_batch — cut the ~2s live-tag floor
- **Bottleneck:** the Hub live-tag lag bottomed out at a ~2 s floor set by the relay ingest path, not the browser/collector. `NeonTagStore` opened **3 fresh Neon connections + 3 pre-pings per push** — `_engine()` rebuilt the SQLAlchemy engine on every call with `pool_pre_ping=True`, and `ingest_batch` called `load_allowlist` + `current_state_simulated` + `persist_batch` as three separate connections.
- **Fix:** cache the engine at module level keyed by `neon_url` (`relay_server` builds a fresh `NeonTagStore` per request, so instance caching wouldn't persist); drop `pool_pre_ping` (pure overhead with `NullPool` + Neon's PgBouncer — every `connect()` is already a fresh physical connection); and add `NeonTagStore.session(tenant_id)`, a context manager that opens ONE transaction, runs `SET LOCAL` once, and binds all three pipeline ops to it via `_BoundNeonTagStore`. `ingest_batch` uses the session when the store provides one, else falls back to `nullcontext(store)` so the in-memory test double is unchanged.
- Preserves `persist_batch`'s one-transaction guarantee (strengthened — reads + writes now share the same txn, so any failure rolls the whole push back), keeps the canonical `live_signal_cache` upsert SQL and RLS tenant bind, and stays inside the Contract-5-allowlisted file (one-pipeline law intact).
- Expected push ~2.47 s → ~0.6–0.8 s; with the live SSE push (#2463), 750 ms poll (#2460), and 500 ms Fixed-Delay timer (#2462), sub-second end-to-end. Deploy via `deploy-vps.yml services="mira-relay"`; re-measure with the cadence probe (#2473).
- Verified: 191 relay tests pass (+2 new one-session-per-push regressions), `tests/test_architecture.py` Contract 5 green, `ruff` clean.

### v3.60.2 (2026-07-04) - fix(hub): primary buttons (and controls) invisible in light mode — Tailwind v4 CSS-var syntax
- **Bug:** in **light mode**, the WO-wizard "Next" button (and other primary buttons/controls) were invisible — white text on a white background. They rendered fine in dark mode. Root cause: mira-hub is on **Tailwind v4**, but the shadcn UI components still used the **v3** CSS-variable shorthand `bg-[--brand-blue]`. v4 removed that shorthand (it now requires `bg-[var(--brand-blue)]` or `bg-(--brand-blue)`), so those utilities emitted **no `background-color`** — leaving `text-white` elements that only show against a dark page. The repo was already mid-migration (31 call sites correctly used `bg-[var(--…)]`); the shared components were missed.
- **Fix:** convert the broken `X-[--token]` arbitrary values to the v4-valid `X-[var(--token)]` across the shared UI components (`button`, `select`, `tabs`, `input`, `badge`, `card`) and the demo conveyor page — 55 occurrences, 7 files. Pure syntax migration; no token values or hardcoded hex changed (`.claude/rules/ui-style.md`). Fixes every primary button, dropdown, tab, input, badge, and card in light mode across the hub.
- Verified: transformation is mechanical and structurally clean (no double-wrapped `var(var(`); the correct form matches the 31 pre-existing `[var(--…)]` usages and Tailwind's documented v3→v4 CSS-var change. Live light-mode render validated by CI build + post-deploy.

### v3.60.1 (2026-07-04) - perf(hub): SSE push for the Machine Memory live-signal card (Tier 2)
- Closes stage 5 of `docs/perf/live-latency-budget.md` (the browser poll) — the Machine Memory card now holds an `EventSource` connection to a new `GET /api/assets/[id]/machine-memory/stream` route instead of polling every 750 ms. The route re-assembles the same response every 350 ms and pushes the full payload the instant `live_tags`/`current_state` changes (or every 3 s regardless, so runs/windows/anomalies still refresh). Each tick opens+releases its own pooled `withTenantContext` connection — never held across ticks — and a transient DB error just skips that tick instead of killing the stream. 15 s heartbeat comments keep proxies from dropping the connection; a 10-minute lifetime cap forces a clean reconnect. The card falls back to its existing polling `setInterval` if the SSE connection errors (e.g. a proxy blocking `text/event-stream`), so a blocked stream never breaks the card.
- Extracts the GET route's response assembly (uns_path resolve → `fetchMachineMemory`/`fetchLiveSignals` → freshness classification → `deriveCurrentState` → `formatTagValue`) into `mira-hub/src/lib/machine-memory-response.ts` (`buildMachineMemoryResponse`), reused byte-for-byte by both the polling GET route and the new SSE route. `MachineMemoryCard.tsx`'s `MachineMemoryResponse` type now re-exports from that single source; its success-branch handling (change-flash detection + `changeCounts`) is extracted into a shared `applyData()` used by both the initial GET and every SSE message.

### v3.60.0 (2026-07-04) - feat(crew): seed VFD-07 live signals + a citable customer doc — dogfood 3 YELLOWs → real GREEN
- The dogfood judge held **3 empty-tenant YELLOWs**: `maintenance-tech` and `demo-readiness` ("NO live signal moving on screen") and `contextualization` ("no customer documents attached"). Root cause: the synthetic seeder created assets/WOs/PMs/KG but **never** seeded `installed_component_instances`, `live_signal_events`, or `knowledge_entries` — so VFD-07 (the Allen-Bradley/PowerFlex asset every check picks) had an empty live panel and no customer evidence. A one-off DB poke wouldn't survive the routine's 4h self-healing reseed, so the fix lives in `mira-hub/scripts/seed-synthetic-users.ts`.
- **Seeds, for VFD-07 on the synthetic tenant only** (`…099`, isolation-probe-safe): 3 installed components (Output Current / DC Bus Voltage / Drive Temperature) + a **fresh** simulated signal history each run (delete-then-insert scoped to `simulated=true` so the "latest" reading is seconds old, not frozen, and the table never grows) + a 2-chunk **customer commissioning record** (`is_private=true` per the knowledge_entries hybrid law; BM25-citable — `content_tsv` is generated, so a NULL embedding is fine).
- **Verified live (seed→mint→judge on staging), all 5 paths GREEN (0 red · 0 yellow):** the seeded signal flips maintenance-tech + demo-readiness; the customer doc flips contextualization. **Honesty-checked** per the advisor: the customer doc is genuinely **cited by MIRA on the asset-chat surface** — asked "what is the P042 accel ramp for VFD-07?", the answer returns *"8.0 seconds, as per the site commissioning record [2]"* and paraphrases the site note, citing `VFD-07-site-commissioning-record.pdf` — not the generic OEM manual. (`/api/quickstart/ask` still cites the public OEM corpus **by design** — it queries the founder tenant, not the caller's; per-tenant customer docs are cited on the authenticated asset-chat surface.)
- Adds `mira-hub/src/lib/seed-synthetic-signal-doc.test.ts` (5 guards — vitest only runs `src/**`, so it reads the script from `../../scripts/`) protecting the fixtures, the `is_private=true` write, the delete-then-insert freshness, the distinctive P042 citation detail, and synthetic-tenant-only writes. Updates the judge's methodology footnote so a GREEN reads as "works given a seeded fixture," **not** "a cold stranger gets live signals + grounded docs automatically" (only the beta-gate path proves the stranger case).
### v3.59.5 (2026-07-04) - perf(ignition): collector timer 2000ms Fixed-Rate -> 500ms Fixed-Delay
- The FactoryLM collector's gateway timer (`MiraTagStream/resource.json`) pushed one tag batch every 2000 ms (Fixed Rate) — the single biggest controllable stage in the tag->Hub latency budget (`docs/perf/live-latency-budget.md`). Now `delay: 500, fixedDelay: true` (Fixed Delay): waits 500 ms after each push completes, so a slow cloud POST can never overlap/pile up, and cache freshness improves ~2 s -> ~0.7 s. **Applies only after an Ignition service restart** (config-as-files is not hot-reloaded) — a bench deploy step, not automatic.

### v3.59.4 (2026-07-04) - perf(hub): faster live-tag display poll + latency budget doc
- Live-tag latency was ~3.5 s typical / ~7 s worst because the pipeline stacks **three independent polls** (gateway scan ~1 s + collector push 2 s + browser poll 2 s), each adding up to its full interval. Documents the full per-layer budget in `docs/perf/live-latency-budget.md` and drops the card display poll (`MachineMemoryCard.tsx POLL_INTERVAL_MS`) from 2000 → 750 ms so the browser stays ahead of a faster collector. The dominant levers (collector timer 2000→500 ms, a 250 ms gateway scan class) are gateway settings; sub-second "instant" is the Tier-2 SSE-push follow-up (the relay already has a WebSocket).

### v3.59.3 (2026-07-04) - fix(historian): cross-window cooldown for info-severity anomalies (#2431)
- A persistent MEDIUM/LOW/INFO condition (e.g. A9 out-of-range DC bus) re-fired one identical `run_diff` row per state-transition window. New `RunStore.recent_anomaly_pair_ts()` + `historize_machine_memory(anomaly_cooldown_seconds=1800)` suppress an info-severity anomaly when the same `(diff_type, tag_path)` was written within the cooldown, regardless of window; CRITICAL/HIGH keep their per-window row. Worker knob `MIRA_ANOMALY_COOLDOWN_SECONDS` (0 disables); summary gains `anomalies_cooldown_suppressed`. 4 new tests.

### v3.59.2 (2026-07-04) - chore(db-inspect): work_orders schema + migration-ledger probe (#2428)
- Extends the read-only `db-inspect.yml` scoreboard with a `work_orders` column/`schema_migrations` ledger probe (the check that diagnosed #2428: migration 060 never applied to prod) and a latest-`machine_state_window` probe (bench tenant State-bubble truth, #2432 follow-through).

### v3.59.1 (2026-07-04) - fix(hub): WO wizard forward buttons say Next (#2427)
- Adds the missing `common.next` i18n key ("Next" / "Siguiente") and relabels both `/workorders/new` step-forward buttons (previously "Description" / "Review" — destination nouns, not actions). Static-markup test pins every `<ArrowRight/>` forward button to `common.next` and the key's presence in both locales.

### v3.59.0 (2026-07-04) - feat(wiring-diagram): grounded IEC-60617 electrical-print generator
- Adds `mira-bots/shared/wiring_diagram/` — a spec-driven electrical wiring-diagram generator (schema → symbols → layout → renderer). Consumes a validated `DiagramSpec` (components + terminals + connections + buses) and renders **SVG** (dependency-free), **PNG/PDF** (via PyMuPDF/`fitz`, imported lazily). No LLM call lives in the renderer — provider-agnostic (PRD §4). **Provenance:** lifted from `openclaw/openclaw/diagram/` (MIT, FactoryLM) per `docs/discovery/electrical_print_reuse_audit.md`; the PNG path was switched off cairosvg (LGPL) to `fitz` and a vector `render_pdf` added. openclaw shipped the engine with **no tests**; this adds `mira-bots/tests/test_wiring_diagram.py` (7 tests: IR round-trip, SVG carries components/terminals/wires, PNG/PDF magic-byte checks — `pytest.importorskip("fitz")` so the bot-test CI env skips the render cases while the SVG core always runs — a cairosvg-absence license guard, markdown summary).
- Adds the **grounded** CV-101 worked example under `docs/onboarding/cv-101-evidence/`: `build_cv101_print.py` constructs the `DiagramSpec` **strictly from cited bench evidence** (`wiring_evidence.md`) — confirmed devices + connections only; UNKNOWN loads/terminals/wiring are recorded as explicit gap NOTES rather than invented — and emits `cv101_print.{svg,pdf}` + `cv101_diagram_spec.json`.
- **Scope note:** self-contained library + grounded demo; not yet wired into a production bot/MCP/HMI surface. `fitz` (PyMuPDF) is AGPL-3.0 — already present on `main` (email adapter, `plc/conv_simple_electrical/render_sheet.py`), so this introduces no new licensing posture, but flag it for any customer-shipped path.

### v3.58.12 (2026-07-04) - feat(crew): dogfood autonomous filing + RED escalation (durability Phase 4)
- Turns the dogfood judge from "tests + reports" into an autonomous QA routine. **4a**: `DOGFOOD_FILE_ISSUES=1` in `com.factorylm.dogfood-judge.plist` — the 4h run now auto-files confirmed, **two-persona-verified**, deduped REDs as issues. Safe by construction: a RED is filed only after a *second* persona reproduces it, deduped first, and on a dedupe match in non-interactive mode `create_issue.sh` **declines silently** (no re-file, no repeat comment), so a persistent RED files exactly once. Verified live on the real scheduled path: run logged "filing ENABLED", 0 REDs → filed nothing (correct).
- **4b**: `dogfood-judge-heartbeat.yml` now **fails on RED** (not just staleness) — a customer-blocking verdict pages a human via the GH workflow-failure notification. YELLOW (degraded) does not fail. Verified: parses the newest #2417 verdict (YELLOW → no escalation; simulated RED → fails).
- **4c**: adds `dogfood-weekly-digest.yml` — a Monday rollup that reads the last 7 days of #2417 verdicts and posts a green/yellow/red trend so "is the product getting better or worse?" is answerable at a glance. Verified: tallies the live 5 verdicts. actionlint + plutil clean. Phase 4 of `docs/plans/2026-07-03-dogfood-routine-durability.md`.

### v3.58.5 (2026-07-03) - feat(crew): dogfood routine observability + heartbeat (durability Phase 2)
- Makes the 4h dogfood judge **observable and self-monitoring** so it can't die silently. `scheduled_run.sh` now posts each run's verdict (GREEN/YELLOW/RED + counts + top blockers + evidence pointer) to a durable tracking issue (**#2417**) with a `<!-- dogfood-heartbeat -->` marker — best-effort, never fails the run, works in report-only mode. You see every verdict in GitHub (phone/email), no SSH to Bravo.
- Adds `.github/workflows/dogfood-judge-heartbeat.yml` — a **dead-man's-switch** that runs on GitHub every 6h, reads only #2417 (no staging access, so a Bravo reboot can't kill it), and **fails/notifies if the newest verdict is >9h old** (2+ missed 4h runs = the routine stopped). Verified live: a real run posted a YELLOW verdict to #2417; the freshness check reads it (fresh → OK, simulated 12h → fires). actionlint + `shellcheck -S warning` clean. Phase 2 of `docs/plans/2026-07-03-dogfood-routine-durability.md`.

### v3.58.4 (2026-07-03) - test(hub): guard the synthetic-seeder tenants mirror (durability Phase 3)
- Adds `mira-hub/src/lib/seed-synthetic-tenants-mirror.test.ts` — a static guard that fails if `seed-synthetic-users.ts` stops mirroring **both** synthetic tenants into the data-side `tenants` table (the FK target of `knowledge_entries.tenant_id`, #1899b). Dropping the mirror silently re-breaks every QA-persona document upload (`knowledge_entries_tenant_id_fkey` 23503 → "Server storage error") and the beta-gate journey. Lives under `src/` so the existing Hub Unit vitest picks it up (vitest `include` is `src/**` only — a test under `scripts/` would never run). Phase 3 of `docs/plans/2026-07-03-dogfood-routine-durability.md`.

### v3.58.3 (2026-07-03) - fix(ignition): guard `__file__` in tag allowlist/collector for Jython script-resource scope
- On a real Ignition gateway, `allowlist.py` and `collector.py` run as **project script-library resources**, where Jython does **not** define `__file__`. Both modules dereferenced `os.path.abspath(__file__)` at **import time** (allowlist for its in-repo fallback path; collector for the `api/chat` `sys.path` insert), so importing either as a script resource raised `NameError: name '__file__' is not defined` — breaking tag-allowlist resolution on the exact deployment surface the Maintenance-Intelligence panel uses (WebDev module not installed → Perspective project-library scripts). Both now guard the `__file__` use in `try/except NameError`, falling back to the absolute Gateway install paths (allowlist) / flat-sibling import (collector). Also adds the non-`projects/` `data/factorylm/approved_tags.json` install-path variants. Verified: main raises `NameError` on a no-`__file__` import; the guarded version imports and resolves cleanly. 51 ignition tests green (`tests/ignition/test_allowlist.py`, `test_tag_collector.py`, `tests/test_conveyor_allowlist_parity.py`).

### v3.57.1 (2026-07-03) - ci(crew): guard the synthetic-worker + dogfood safety tests
- Adds `.github/workflows/crew-tests.yml` — Phase 1 of the dogfood-routine durability plan (`docs/plans/2026-07-03-dogfood-routine-durability.md`). Runs `shellcheck -S warning` on the crew scripts + the three hermetic suites (runner 19 / judge 10 / gate 7 = **36 tests**, `gh` shimmed, no network) on any PR touching `tools/crew/**` or `tools/qa/create_issue.sh`. These suites are the only thing that keeps the **verify-before-file gate** honest (independent reproduction + distinct verifier + dedupe); before this they ran in NO CI, so the gate could silently regress into filing false bugs. The plan doc lays out Phases 2–5 (heartbeat/observability, seeder-mirror guard, auto-filing + escalation, and moving execution off Bravo when a public staging URL exists).

### v3.56.1 (2026-07-03) - feat(crew): dogfood judge on a 4-hour schedule (Bravo launchd)
- Adds `tools/crew/dogfood/scheduled_run.sh` + `com.factorylm.dogfood-judge.plist` — runs the dogfood judge every 4h on Bravo (the only host that can reach the Tailscale staging Hub; same rationale + stg-token/env pattern as the RBAC weekly-inspect job). Each run: idempotent reseed (also applies the tenants mirror so the beta-gate upload path works) → re-mint the persona sessions (NextAuth sessions expire, so a cadence must re-mint) → run the judge across all product paths → report to `qa/dogfood/latest-report.md` + a timestamped run dir, and append a one-line verdict to `qa/dogfood/history.log`. **Report-only by default** — auto-opening GitHub issues unattended is outward-facing; set `DOGFOOD_FILE_ISSUES=1` to enable the judge's gated, two-persona-verified, deduped filing. Installed + verified end-to-end on Bravo (`launchctl start` → seed ok → sessions minted → judge verdict).

### v3.56.0 (2026-07-03) - feat: Litmus bench proof + difference-engine proofs + CV-101 electrical prints & MIRA grounding (#2401)
- Bundles the **Litmus bench proof** (MIRA reads the Micro820 through Litmus Edge), the **signal-difference-engine** proof (SimLab → event → supervisor + baseline learner + drift/never-seen), and auditable **ProveIt/SimLab contextualized-diagnosis proof packets** onto current `main`.
- Adds the **CV-101 electrical print set** (`plc/conv_simple_electrical/`) — model-first: structured YAML is the source of truth, `render_sheet.py` emits `sheets/<id>.{svg,pdf,png}`. **E-005 PLC Inputs** grounded in the real Micro820 program; **E-007 RS-485/Modbus** rebuilt in the recovered `CommsToVFD` style, reconciled to bench-verified values (Channel 2, SGND=pin 3, 8N1).
- Adds the **conveyor grounding + anomaly-log** harness: `plc/litmus/demo_context_model.py` answers "Why is CV-101 stopped?" from the approved context model with an *Evidence used* table + explicit refusals for unmapped signals; `plc/conv_simple_anomaly/anomaly_log.py` drives the Ask MIRA A0–A12 brain across a 14-scenario fault battery.
- Merged current `main` into the branch (113 commits); the one content conflict (`mira-bots/shared/neon_recall.py` approval-gate block) resolved identical to `main`. Verification: 27 anomaly-rule tests pass; net delta vs `main` is additive (+3,146 / 0 deletions).

### v3.55.4 (2026-07-03) - feat(crew): beta-gate dogfood journey + fix(hub): QA seeder tenants mirror
- Adds the **beta-gate** human journey to the dogfood judge (`tools/crew/dogfood/checks/beta-gate.check`): as an owner persona, create a namespace node → **upload a manual through the real folder=brain door** (`/api/namespace/node/<id>/files/` → real ingest) → wait for ingestion → ask the **node-scoped** chat a question the manual answers. Because node-chat reads only that node's brain, a cited answer PROVES retrieval of the upload — it can't be faked by the shared OEM corpus. This is the daily-deploy-truth companion to the CI release gate (`tests/beta/beta_ready_upload_retrieval_citation.py`). Verified live against staging: **GREEN** end-to-end (upload HTTP 201 → ingested → answer states the manual fact with an inline citation).
- **fix(hub): synthetic QA seeder now mirrors both synthetic tenants into the data-side `tenants` table** (`mira-hub/scripts/seed-synthetic-users.ts`). Real signup (`lib/users.ts`, #1899b) mirrors `hub_tenants` → `tenants` (the FK target of `knowledge_entries.tenant_id`); the seeder omitted it, so **every QA-persona document upload 23503'd on `knowledge_entries_tenant_id_fkey`** and surfaced as "Server storage error." Found by the new beta-gate check (upload 500 on the synthetic tenant); root-caused to the missing mirror; the beta-gate journey went GREEN once the mirror was present. This also explains the dogfood judge's contextualization YELLOW ("0 customer documents") — the QA tenant physically couldn't hold an upload.

### v3.55.3 (2026-07-03) - fix(hub): GET /api/assets/[id]/context 404'd for real assets (wrong id-space)
- The UNS confirmation-gate context endpoint looked the `[id]` path param up in **`kg_entities`**, but that param is a **`cmms_equipment.id`** — the canonical asset id-space every other asset sub-route (`detail`/`documents`/`chat`/`validation-qa`) keys on. A CMMS-registered asset that hasn't been promoted to a `kg_entities` row (the common case) therefore returned **404 "Asset not found"** for an asset that plainly exists, breaking the "is this the right asset?" confirmation card. The route now resolves identity from `cmms_equipment` (404 only if truly absent there) and enriches `uns_path` from `kg_entities` via the same `(id::text=$ OR entity_id=$)` bridge the asset-chat route uses — `null` when there's no kg row. (Separate query, not a join: `cmms_equipment.tenant_id` is TEXT but `kg_entities.tenant_id` is UUID, so a column compare errors.) Found by the dogfood judge's contextualization path (`/context` 404 despite the asset existing); verified live against staging (query returns the asset) + 3 regression tests (`context/__tests__/route.test.ts`). Hub release `mira-hub/v2.24.2`.

### v3.53.4 (2026-07-01) - feat(crew): synthetic-worker runner + dogfood judge + retries/--until-find
> Note: `v3.53.3` was independently taken by #2387 on main; this crew PR ships as `v3.53.4`, so the runner + judge (originally staged as 3.53.3) is released here together with the retries / `--until-find` work.
- Adds `tools/crew/dogfood/judge.sh` + four `.check` packs (maintenance-tech, contextualization, work-order, demo-readiness) that walk MIRA's core product paths against the live staging Hub as real QA personas and classify each GREEN/YELLOW/RED in **business language**, writing a two-minute founder report to `qa/dogfood/latest-report.md`. Every RED is cross-verified under a **second** persona session before it can be filed (finder≠verifier), deduped first, refused on INFRA/ambiguous; deterministic corpus-aware asset selection avoids flaky verdicts.
- Adds the gated filer `tools/qa/create_issue.sh` + `tools/crew/run_synthetic_workers.sh`: no finding is filed without an independent reproduction, a distinct verifier, `dogfood`/`crew` labels, and (for P0) `--allow-p0`. Dry-run by default; no app/runtime code.
- The synthetic-worker runner now persists across transient blips and keeps probing until it finds a real bug — **without lowering the verify-before-file gate**. `--retries N` (default 3, `--retry-delay`) treats a scenario as reproduced if the bug signal appears on ANY attempt (defeats network/deploy-not-landed flakiness, catches intermittent bugs); "reproduced" still means the deterministic bug signal was observed.
- `--until-find` (+ `--max-rounds`/`--max-seconds` budget) re-runs ALL scenarios in rounds until one reproduces a real, gate-passing bug OR the budget is hit. A healthy system stops honestly at the budget with `found=0` ("nothing to fix") — it never invents a finding to satisfy the flag. Per-round artifacts land in `<out>/round-N/`.
- Adds two false-positive-resistant probe scenarios so `--until-find` has real invariants to test: `wo-roundtrip` (create-time work-order priority/title must survive a re-read — #2375-class) and `asset-detail-parity` (a listed asset must be fetchable by id, read-only consistency). Both emit a bug signal ONLY on an unambiguous HTTP-200-but-wrong-data violation; any non-200 is treated as transient and refused, so HTTP flakiness is never filed as a bug. Hermetic tests — judge 10/10, runner **19/19** (12 original + 7 new), gate 7/7; shellcheck clean. Verified live against staging: retries visible, honest budget-exhaust, no manufactured finding.

### v3.53.2 (2026-06-30) - fix(hub): GET /api/work-orders/[id] returns resolution + closed_at (#2375)
- A completed work order read back with `resolution=null` and `closed_at=null` even though PATCH persisted them — the GET detail `SELECT` and `rowToWO` serializer omitted the closure columns, so the next technician saw a blank closure. The GET now selects and returns `resolution`, `fault_description`, and `closed_at`.
- Found by the dogfood judge (`tools/crew/dogfood`) and reproduced field-by-field against staging (PATCH returns the values; GET dropped the keys entirely). Regression test `mira-hub/src/app/api/work-orders/[id]/route.test.ts`. Hub release `mira-hub/v2.24.1`.

### v3.44.1 (2026-06-26) - fix(hub): wire Atlas SSO deploy secrets
- Passes the shared Hub-to-Atlas SSO signing configuration into the production `mira-hub` container so the merged `/api/cmms/sso` route can sign live Atlas handoff assertions.
- Makes synthetic Hub user seeding consume Doppler/env-backed per-persona passwords for live proof runs without logging credential values.

### v3.43.1 (2026-06-26) - fix(hub): add Atlas CMMS SSO handoff
- Adds a signed Hub-to-Atlas SSO bridge so authenticated Hub users open FactoryLM Works routes through `/api/cmms/sso` without typing separate Atlas credentials.
- Routes the Hub CMMS setup page CTA and quick links through the SSO handoff, with regression coverage for token exchange, safe redirect fallback, and missing-secret handling.

### v3.42.6 (2026-06-26) - fix(hub): route CMMS links into FactoryLM Works app
- Fixes the Hub CMMS setup quick links and Atlas record-level deep links to use the live FactoryLM Works app route family (`/app/work-orders`, `/app/assets`, `/app/preventive-maintenance`, `/app/reports`) instead of marketing paths that return the provider's 404 content.
- Adds focused regression coverage for the setup-page quick links and Atlas provider deep-link templates.

### v3.42.5 (2026-06-25) - fix(web): canonical trailing-slash redirects
- Fixes FactoryLM trailing-slash redirects so HTTPS proxy requests like `/pricing/` trim to the canonical no-slash HTTPS public URL. Redirect authority is allowlisted and normalized so hostile `X-Forwarded-Host` values, loopback hosts, and attacker-supplied public-host ports cannot steer `Location` headers.
- Adds a CMMS health regression proving the Hub browser-facing CMMS URL remains `https://cmms.factorylm.com` and never exposes the internal Docker hostname `cmms-backend`.

### v3.42.4 (2026-06-25) - feat(hub): one-board command center status view
- Adds a Command Center one-board status panel backed by `/api/hub/status`, showing conveyor cell and Stardust block-zone running, blocked, faulted, and stale states in a compact responsive grid.

### v3.42.3 (2026-06-25) - feat(hub): tenant-scoped Hub status API
- Adds `/api/hub/status` for tenant-scoped one-board Hub status cards, backed by `live_signal_cache` and a deterministic Stardust/conveyor signal summarizer with regression tests for tenant query scoping and demo-session fallback.

### v3.42.2 (2026-06-25) - fix(hub): React hook lint cleanup
- Cleans the reported Hub React hook lint violations in admin users, alerts, and asset detail documents by preserving hook order and moving initial data fetch state updates out of effect-body function calls.

### v3.42.1 (2026-06-25) - security(rag): tenant isolation and prompt-boundary hardening
- Keeps `/api/knowledge/search` pinned to shared OEM rows only (`is_private = false`) and adds a behavioral regression test proving private tenant snippets are not serialized while shared snippets still return.
- Moves retrieved RAG reference blocks out of system-role authority for Hub asset/node chat and bot RAG prompts. Retrieved docs are now framed as untrusted reference data in the final user turn, with forged source headers neutralized and citation labels sanitized.

### v3.42.0 (2026-06-25) - feat(qa): synthetic dogfood agent loop
- Adds a Celery-backed synthetic dogfood loop for the Hub production QA tenant. The worker runs the existing persona Playwright suite, writes raw/summarized artifacts, redacts evidence, classifies findings, and dry-run/files deduplicated GitHub issues for P0-P2 problems while leaving P3 noise in reports.
- Adds a dedicated Playwright worker image plus SaaS `mira-redis`, `mira-synthetic-dogfood-worker`, and `mira-synthetic-dogfood-beat` services. The loop is default-off (`SYNTHETIC_DOGFOOD_ENABLED=0`) and issue writes require `DOGFOOD_ISSUE_MODE=write`.
- Dry-run proof against `https://app.factorylm.com`: Playwright exit code 0, 0 findings, 0 GitHub writes. Runbook: `docs/runbooks/synthetic-dogfood-agents.md`.

### v3.41.0 (2026-06-24) - feat(retrieval): approval-gated garage conveyor retrieval
- Adds `MIRA_ENFORCE_APPROVED_RETRIEVAL` for approved-only KB retrieval, keeps it default-off, and includes the trusted-corpus backfill SQL required before enabling the gate. Hub `HAS_DOCUMENT` approval now marks the approved upload's chunks `verified=true`, source payloads expose `verified` plus `approved_source_count`, and the garage conveyor golden path proves unreviewed context is hidden until approved.

Extracted from CLAUDE.md to keep the build-state file within the ~200 line compliance budget.
For current build state, see `CLAUDE.md` in project root.

### v3.39.17 (2026-06-22) — test(eval): skip non-deterministic FSM check on vfd_danfoss_04 out-of-KB fixture
- **Problem:** `vfd_danfoss_04_vlt_fc360_edge` (user asks about a nonexistent VLT FC 360) is a stochastic out-of-KB edge case. The engine's `Q→DIAGNOSIS` gate transition is LLM-non-deterministic, so the terminal FSM state flips `Q1`/`Q2`/`DIAGNOSIS` run-to-run; the grader's `cp_reached_state` then fails whenever it lands at `Q1` — producing **11/11 spurious FSM-only failures** in the #1948 eval-flakiness triage even though keyword + citation checks pass.
- **Fix:** added `skip_fsm_check: true` (the grader's purpose-built flag for exactly this class — `tests/eval/grader.py:110`), so the fixture validates honest out-of-KB behavior via `expected_keywords` + citation groundedness instead of FSM depth. Mirrors the existing `04_yaskawa_out_of_kb.yaml` precedent. Fixture-only; no engine/grader code change.
- **Context:** follow-up **(c)** from the #1948 triage (PR #2222). Siblings: #2258 (eval-suite determinism via record/replay), #2257 (Nemotron-404 reranker outage). PR #2256.

### v3.39.14 (2026-06-22) — docs+tools(kb): full-PDF docling ingest tooling + don't-promote findings
- **Outcome:** bulk-loading full OEM manuals into the shared corpus is NOT the lever for answer quality (retrieval ranking is — #2237). `docling` extraction works (772 Micro820 tables → Markdown; quality gate drops `�`/`CCChhh` noise), but staging retrieval showed full-manual chunks don't surface better than the curated chunks already covering GS10/Micro820. Loaded chunks deleted from staging; **nothing to prod.**
- **Delivered (docs + tools only, no engine/service code):** `ingest_local_pdf.py` (`--use-docling` + quality gate), `verify_seed.py` / `retrieval_check.py` / `coverage_check.py`, the shared-corpus load runbook (`docs/runbooks/load-oem-manuals-into-kb.md`), and the plan + verdict (`docs/plans/2026-06-22-full-pdf-docling-ingest-plan.md`). Ready to re-run if/when the page-picking work lands. PR #2238.

### v3.39.13 (2026-06-22) — feat(retrieval): equipment-aware reranking in production (behind flag)
- **Problem (verified on the prod path, not the harness):** `recall_knowledge` RRF is vendor-blind — "GS10 overcurrent" ranked a Yaskawa **V1000** chunk #1 over GS10. The equipment-scope rerank that fixes it lived **only** in `tests/mira_bench.py`, so every live surface (Telegram/Slack/pipeline/Hub) shipped vendor-confused retrieval.
- **Fix:** ported an equipment rerank into `mira-bots/shared/neon_recall.py`, **behind `MIRA_EQUIPMENT_RERANK` (default OFF → zero prod impact until Phase 4)**. Overfetch → RRF → float chunks matching the query's extracted equipment → truncate. Positive-boost only (no harness `v1000/powerflex` denylist, so a real V1000/PowerFlex question still returns that vendor); no equipment in query ⇒ no-op.
- **Evidence:** unit `tests/regime2_rag/test_equipment_rerank.py` 5/5; raw prod path on staging — vendor confusion eliminated; retrieval A/B (`tools/seeds/oem-manuals/phase3_retrieval_ab.py`) avg relevance **0.62 → 0.96**, coverage 0.75 → 1.00, **0/10 regressions**. Plan: `docs/plans/2026-06-22-retrieval-page-picking-equipment-rerank.md`. PR #2237. (Flag flip + LLM-dim bench + deploy = Phase 4.)

### v3.39.11 (2026-06-22) — fix(test): re-green pricing-CTA smoke after live copy change
- The `factorylm.com/pricing` featured CTA copy changed to **"Book Assessment — $500 →"** (price added); `mira-hub/tests/e2e/signup-flow.spec.ts` still asserted `/book your assessment/i` → E2E-smoke red on every PR. **The money path is healthy** — verified live: CTA → `/buy` (HTTP 200), and the checkout session API 303-redirects to checkout.stripe.com. Loosened the assertion to price-agnostic `/book assessment/i` + fixed the stale test title/comment. No app/behavior change.

### v3.39.10 (2026-06-22) — fix(ci): Hub E2E required check no longer blocks docs/non-hub PRs
- **Bug:** `hub-e2e.yml` (the required status check "Hub E2E (command-center + onboarding)") used an `on:`-level `paths-ignore` (`docs/**`, `wiki/**`, `**/*.md`, `.claude/**`). A required check suppressed by a path filter **never reports its context**, so any all-ignored PR (e.g. docs-only #2199) sat `Expected` → permanently `BLOCKED` and could only land via admin-merge.
- **Fix:** removed the `on:`-level `paths-ignore`. The workflow now always runs on PRs to main and reports the required context; cost stays controlled **inside the job** by the existing `dorny/paths-filter` `changes` step + `if: steps.changes.outputs.hub == 'true'` gates, which short-circuit non-hub PRs to trivially-green in seconds (the workflow's own documented design). No change to what e2e actually runs on hub PRs.

### v3.38.0 (2026-06-21) — feat(observe): observability + eval layer + preformatted agent registry
- **Observability/eval core (phases 0–3):** every real MIRA answer can emit a local `AnswerTrace` JSONL (`MIRA_LOCAL_TRACE=1`) at the one production seam (`_schedule_decision_trace` → `build_answer_trace`), covering Telegram/Slack/pipeline/Ignition. Governance + incident checks run observationally under `MIRA_TRACE_CHECKS=1`. JSON-first, read-only, **off by default = zero prod behavior change**. SimLab eval-pack runner + CLI viewer.
- **Preformatted industrial agent template registry (Cognite-style wedge):** `shared/observe/agent_registry.py` — `AgentManifest` + JSON loader + `route_agent`. An "agent" is a contract+label over the one engine (`Supervisor.process`), not a second execution path. **Read-only enforced at LOAD time** — a manifest allowing any write/control tool is rejected. 3 starter agents wired (maintenance_troubleshooter default, root_cause_analysis on causal Qs, manual_qa on doc lookup); `agent_id/version/risk/allowed_tools` land on every trace. Observational output-contract check.
- `rca_starter.yaml` eval pack (5 blueprint RCA cases, 5/5 PASS mock). 73 observe + 12 simlab tests green; ruff clean. End-to-end verified (`tools/verify_phase1_trace.py`).
- **Deferred (named, not redefined):** Langfuse cloud bridge (audit-blocked: PII leak + Telegram no-op + region misconfig) and Hub UI. The local `AnswerTrace` is a *local* trace, NOT a Langfuse trace. Docs: `docs/observability-and-evaluation.md`, `docs/observability-and-preformatted-agents.md`.

### v3.36.0 (2026-06-21) — feat(pipeline): asset-agent deployment gate resolver (Northstar Phase 4)
- Makes the train-before-deploy gate **functional**. `mira-pipeline/ignition_chat.py` `_lookup_agent_state()` was a self-described **NON-FUNCTIONAL** stub — it matched the incoming Ignition token *literally* against `asset_agent_status.uns_path::text`/`equipment_id::text`, but the token is a tag-path (`[default]Conv/State`) or display name that never equals a stored ltree/UUID → refuse-all when `ENFORCE_ASSET_AGENT_GATE` is on.
- **Real resolution** (new `_agent_state_from_conn` seam + local `_slug`): JOINs the bridge tables so a structured `asset_context`/display-name token resolves via `cmms_equipment.equipment_number` (slug-normalized, e.g. `GS10-VFD`→`gs10_vfd`) — the robust path, independent of the nullable `cmms_equipment.uns_path` backfill — plus the canonical `uns_path`/`equipment_id` forms and best-effort Ignition tag-path via `installed_component_instances.plc_tag`. Tenant-isolated by the driving `asset_agent_status.tenant_id` predicate (JOINs key on the tenant-owned `equipment_id` UUID); no migration.
- **Residual gap (documented, default-OFF preserved):** a flat Ignition tag-path only resolves when `plc_tag` is populated (e.g. by the tag-CSV import); absent that, such turns refuse. Keep `ENFORCE_ASSET_AGENT_GATE` OFF until proven per tenant.
- Doctrine: `mira-uns-architecture` (local `_slug` mirrors `uns.slug()`; mira-pipeline can't import mira-crawler), `train-before-deploy`, `direct-connection-uns-certified`. Read-only; gate decision/answer branch + 422 `uns_required` untouched; hallucination-audit clean for the diff.
- Tests: `mira-pipeline/tests/test_ignition_chat_resolver.py` (+8: slug grammar, param binding, query covers all forms, miss→None, state passthrough). Gate + direct-connection suites unchanged-green (20). NOTE: a pre-existing `direct_connection`→`gate` test-ordering pollution (7 fails) reproduces on `main` with this change stashed — not introduced here.

### v3.35.0 (2026-06-21) — feat(hub): KG navigator Phase 2 — ingest→suggest loop (grounded)
- **The other half of the loop.** When a document attached to a `/namespace` node finishes parsing, MIRA auto-proposes a grounded **`HAS_DOCUMENT`** edge node→manual on the graph — evidence is the document's own chunks (`evidence_type='document_page'`, excerpt + page). New `mira-hub/src/lib/node-document-proposals.ts`; fired fire-and-forget from `node-knowledge-ingest.ts` after `updateUploadStatus(...,'parsed')`, decoupled + never-throws. `NODE_DOC_PROPOSALS=0` kill switch.
- **Iron Rule (ADR-0017):** `relationship_proposals(status='proposed', created_by='rule')` with evidence — never straight to `kg_relationships`; promotion is a human action via `POST /api/proposals/[id]/decide`.
- **Real-DB-verified:** the dev run caught + fixed a live `ON CONFLICT` schema-drift (real `kg_entities` unique key is `(tenant_id, entity_type, name)`, not `entity_id`). 4 vitest units + an unmocked dev run. No migration. (Builds on Phase 1, v3.32.0.)

### v3.34.0 (2026-06-21) — feat(hub): PLC Import wizard (Phase 8 upload UI)
- Adds the Hub upload surface for the offline **PLC Parser → tag-import** chain (Northstar plan §Phase 8 exit). New page `/plc-import` (`mira-hub/src/app/(hub)/plc-import/page.tsx`): upload a Rockwell **.L5X** / vendor tag **.CSV** export → preview the read-only parser report (controller, vendor, counts, per-tag proposed ISA-95 UNS paths with high/medium/low confidence, and a safety **review** banner) → "Create N proposals" writes `tag_mapping`/`kg_entity` rows to the `/knowledge/suggestions` review queue (`commit=true`). Optional enterprise/site/area/line UNS-prefix inputs (the export only knows the lower levels). Nothing auto-verified; nothing written to a PLC.
- **`mira-hub/src/lib/plc-import-view.ts`** (new) — pure, deterministic view-model mapping the `POST /api/connectors/plc/import` response (`report@1` + `uns_candidates`) into render states: `parsed` / `export_needed` (closed-project export guidance, 422) / `unsupported` (413/503/error). Keeps the page thin + unit-testable. Tests: `plc-import-view.test.ts` 9/9.
- Nav: "PLC Import" added to the secondary group (`access-control.ts`, admin roles) + `Upload` icon registered in the sidebar icon map. Route snapshot regenerated (`docs/SITEMAP.md` + `sitemap.snapshot.json`).
- Screenshots (desktop 1440×900 + mobile 412×915) in `docs/promo-screenshots/2026-06-20_plc-import-wizard_*.png` — faithful static render of the component (the authed Hub can't boot in CI/local without NEON + a session + the ingest sidecar; the page is covered by the view-model unit tests, with live e2e on staging).

### v3.33.3 (2026-06-21) — fix(plc-parser): bundle tag_csv.py into mira-ingest image (CSV uploads 500'd)
- **Follow-up to v3.33.2.** Wiring `mira_plc_parser` into the mira-ingest image (v3.33.2) exposed a latent gap: the CSV parser (`mira_plc_parser/parsers/csv_tags.py`) reuses the pure `tag_csv.py` from the Ignition tree by *path* (`parents[3]/ignition/webdev/FactoryLM/api/diagnose/tag_csv.py`). The image COPYs only the package, so that path is absent in-container → CSV uploads to `/ingest/plc-parse` returned **500 (`FileNotFoundError`)**. Verified live on prod after the v3.33.2 deploy (L5X 200 ✓, CSV 500 ✗). Not a regression — CSV was unreachable (503/stuck) before; now it parses.
- **`csv_tags.py`** — `_load_tag_csv()` now honors `MIRA_TAG_CSV_PATH` (explicit override for packaged deployments) before the repo-layout default, and raises an actionable error if neither resolves. Backward-compatible: dev / CLI / the parser's own tests (where the repo tree exists) are unchanged.
- **`mira-core/mira-ingest/Dockerfile`** — bundles `ignition/…/tag_csv.py` → `/app/vendor/tag_csv.py` and sets `ENV MIRA_TAG_CSV_PATH`. The file is pure/self-contained (no further sideloads), so no other change is needed.
- Why CI missed it: the parser's tests run in the repo layout (where the path resolves) and aren't in CI; the Docker Build Check builds but doesn't exercise `/ingest/plc-parse`. New `tests/test_csv_tags_path.py` (+5) guards the env-override resolution + the missing-path error. Golden CSV test unchanged (4).

### v3.33.2 (2026-06-21) — fix(contextualization): replace dead Python parse worker with inline mira-ingest HTTP call
- **P0: in-Hub PLC upload worker was dead on prod.** The contextualization sources route spawned `python ctx_parse_worker.py <id>`, but the Hub ships as a Node-alpine image (build context `./mira-hub`) with no Python, no `psycopg2`, and no `mira_plc_parser` on it. `spawn()` emits an async `'error'` rather than throwing, so the route returned `workerStarted:true` while the child died — every `ctx_sources` row stuck at `status='pending'`.
- **`mira-hub/src/lib/contextualization/parse-source.ts`** (new) — `parsePlcViaIngest()` forwards the upload to mira-ingest `/ingest/plc-parse` (reusing the `lib/plc-import.ts` pattern); `reportToExtractions()` ports the worker's tag-dictionary → `ctx_extractions` mapping. **`…/[id]/sources/route.ts`** — removed the spawn; parse inline over HTTP, write extractions, flip to `done`. **Every** parse failure (422/413/503/502/timeout/DB-write) flips the source to `error` + `error_message` — never stuck `pending`. UI consumes the terminal status. Dead worker + test deleted.
- **mira-ingest image** — wired the stdlib-only `mira_plc_parser` package in (repo-root build context + COPY) so `/ingest/plc-parse` returns 200 instead of 503. Updated **all** ingest build sites to repo-root context: `docker-compose.saas.yml`, `mira-core/docker-compose.yml` (dev/oracle), `.github/workflows/ci.yml` (Docker Build Check). Added a repo-root `.dockerignore` so root-context builds stop shipping node_modules/.git/.next. Also added `relevance.py` to the ingest COPY allow-list (imported by `main.py`, previously omitted; harmless only because `RELEVANCE_GATE_ENABLED` defaults off).
- Scope: restores **PLC-export (L5X / vendor tag CSV) parity** — the inputs the dead worker handled. PDF/manual content extraction is unchanged (still 422s → `error`); a separate gap.
- Tests: 83/83 contextualization+plc unit tests (5 new mapper tests). Local `docker build` of mira-ingest from repo root verified green.

### v3.33.0 (2026-06-20) — feat(hub): materialize tag_entities on PLC tag_mapping accept (Phase 8)
- Closes the last functional gap in the offline **PLC Parser → Hub tag-import** chain (Northstar plan Phase 8): approving a parser-proposed `tag_mapping` in `/proposals` now creates a **verified `tag_entities` row** (mig 025), so an approved PLC tag becomes a typed, queryable signal the relay / engine / MCP read — not just a flipped status. Previously `tag_mapping` accept was status-only (only `kg_entity` materialized an entity).
- **`mira-hub/src/lib/suggestion-accept.ts`** — `decideSuggestion` verify path now branches `tag_mapping` → `createTagEntity()`. New exported `mapTagDataType()` maps the parser's *declared* PLC/IEC/Rockwell type (BOOL/DINT/WORD/REAL/…) to the `tag_entities.data_type` CHECK enum; an export with no declarable type (CCW name-only case) is transitioned but **not** materialized — the typed table never holds an invented type. Insert is `approval_state='verified'`, `proposed_by='import:plc_parser'`, idempotent via `ON CONFLICT (tenant_id, uns_path)`.
- Verified vs proposed honored: materialization happens only on the explicit human accept (plc-tag-mapper rule — never auto-verify). Read-only; no PLC writes.
- Tests: `suggestion-accept.test.ts` +6 cases (typed materialization, name-only/unmappable skip, reject, `mapTagDataType`). Full file green (12); adjacent `plc-proposals` suite unchanged (7).

### v3.32.0 (2026-06-20) — feat(hub): KG navigator Phase 1 — graph↔namespace cross-link UX
- **Closed-loop graph builder, slice 1.** On `/knowledge/map`, a node's detail panel now has a **"📁 Add documents"** link that deep-links to that node in the namespace (`/namespace?node=<kg_entities.id>`) to attach manuals — the first half of the click-node → add-doc → suggest-connections loop.
- **Any edge is now inspectable.** Edge click opens a panel for *verified* edges too (was proposed-only): friendly type, source→target, confidence, and the `evidence_summary` snapshot. Proposed-edge confirm/reject unchanged (ADR-0017 — never auto-verify).
- **Payload plumbing (additive, no migration):** `/api/kg/graph` selects `entity_id` (→ `GraphNode.entityId`) and `evidence_summary` (→ `GraphLink.evidenceSummary`). Columns pre-exist (mig 001/029).
- Proof: 158/158 KG unit tests, e2e `tests/e2e/kg-navigator-phase1.spec.ts`, desktop+mobile + verified-edge screenshots. Route SQL run unmocked on dev (200 + valid shape).

### v3.29.0 (2026-06-20) — feat(hubv3): Hub-centered contextualization intake (P0–P8)
- **Hub = system of record for contextualization.** Offline Contextualizer + Telegram are now ingest clients submitting one shared **Contextualization Intake Contract** (`contracts/contextualization/intake_contract.py` + `mira-hub/src/lib/contextualization/intake-contract.ts` + JSON Schema; ADR-0023). All ingest enters as `proposed`/pending; Hub does final merge, approval, and publish to the project model / UNS / i3X / MIRA KB.
- **Migration `056`** — staging schema: `ctx_import_batches` (review_status proposed→approved|rejected|needs_review, ingest_route), `ctx_extraction_asset_matches` (match_strength strong|probable|none, candidate_asset_id), source/bundle `sha256` dedup keys, RLS + grants. Reversible (rollback block in-file). Applied + verified on staging 2026-06-20.
- **P2** import endpoint accepts the contract + sha256 dedup. **P3** asset-matching engine (strong/probable/none vs `cmms_equipment`). **P4** batch Review Queue + approval-aware promote (no overwrite of verified). **P5** offline bundle: `evidence.json` + entity UUIDs + sanitized/derived-context export mode (`mira-contextualizer` v2.3.0). **P6** Telegram thin evidence client → Hub intake. **P7** Hub↔offline UI label parity + Import Review nav link. **P8** garage-conveyor demo + §6 acceptance test matrix + `docs/runbooks/garage-conveyor-demo.md`.
- Shipped independent of the `mira-plc-parser` desktop feature (`feat/plc-mapper-gui` #2068) to avoid that branch's parser-merge conflict with main's #2091; uses main's parser as the compose dependency. PRD: `docs/plans/2026-06-20-hubv3-contextualization-intake-prd.md`. Rollback: `docs/runbooks/hubv3-rollback.md`.

### v3.28.4 (2026-06-20) — fix(deploy): pin prod VPS to the release tag, not main HEAD
- **`.github/workflows/deploy-vps.yml`** — the prod deploy now checks out the `vX.Y.Z` tag that `version-tag.yml` lays on the merge commit (`git checkout <tag>`) instead of `git reset --hard origin/main`. Closes the versioning-discipline gap in `docs/versioning.md`: a rollback target is only real if prod references a tag, not a moving `main` HEAD. Same tree deployed (tag == main HEAD); prod's `git describe` now shows a clean release ref, so `docs/runbooks/hubv3-rollback.md` ROLLBACK-A has a real anchor. Graceful fallback to `origin/main` if the tag hasn't landed yet.

### v3.28.3 (2026-06-20) — security(hub): filter private knowledge_entries from /api/knowledge/search (#2044)
- **Cross-tenant content leak closed.** `/api/knowledge/search` (BM25 + ILIKE fallback paths) queried `knowledge_entries` on the owner pool with no `is_private` filter. Since mig 052, per-tenant uploaded manuals live in this table with `is_private = true`; without the filter any authenticated tenant's search returned another tenant's private manual snippets. Added `AND is_private = false` to both SQL paths.
- Regression test: `mira-hub/src/app/api/knowledge/search/__tests__/route.is-private.test.ts` asserts the filter is present in every DB-bound query.
- **chore:** removed 2 stale `@ts-expect-error` directives (TS2578) in `rls-deny.integration.test.ts` + `sitemap-drift.test.ts` — full `tsc --noEmit` is now clean.
- **docs:** `docs/known-issues.md` refreshed — beta gate PASSING stanza, round 7-9 closures (#1833/#1901/#2020/#2077/#2082), #736 mitigation note, #1858 watch item.

### v3.28.2 (2026-06-17) — feat(plc-parser): offline UNS/i3X proposal layer (L5X → ISA-95 → i3X)
- Closes the missing middle link in the **offline PLC Program Teacher** chain identified by `docs/research/plc-program-teacher-status.md`: the merged #2065 parser extracted maintenance context but emitted no UNS/i3X and was wired to nothing. Ports the proven, deterministic, stdlib-only transform from PR #2068 onto the merged parser core and exposes it from the terminal.
- **`mira-plc-parser/mira_plc_parser/uns.py`** (new) — propose one ISA-95 UNS path per tag (`enterprise/site/area/line/asset/signal`). Upper levels from a controller-seeded, user-overridable prefix; lower levels from VFD-signal roles + asset candidates; `high`/`medium`/`low` confidence; `source: proposed` (never auto-verified).
- **`mira-plc-parser/mira_plc_parser/i3x.py`** (new) — transform the proposals into a CESMII i3X-shaped payload (ObjectTypes + ObjectInstances, container hierarchy via `parentId`, `elementId` = UNS path). Offline, read-only — no push to a live i3X server.
- **`mira_plc_parser/pipeline.py`** — `render_json()` now embeds `uns_candidates` + `uns_prefix` (and `counts.uns_candidates`), so `analyze --format json` emits the full chain.
- **`mira_plc_parser/cli.py`** — new offline `i3x-export` subcommand writes `<stem>.i3x.json`.
- **`mira-core/mira-ingest/main.py`** — new `POST /ingest/plc-parse` endpoint (PR-B): the Hub→Python bridge for the offline chain. Accepts an L5X/CSV upload, runs the parser, returns `{report, i3x?}` with proposed UNS candidates; closed/binary project files → 422 with export guidance (mirrors the CLI exit codes). The parser is imported lazily → HTTP 503 if not yet wired into the image (build-context wiring is a flagged Dockerfile follow-up). Tests: `tests/test_plc_parse.py` 5/5 (TestClient).
- **`mira-hub/src/app/api/connectors/plc/import/route.ts`** + **`mira-hub/src/lib/plc-import.ts`** (new, PR-B Hub side) — `POST /api/connectors/plc/import`: auth-gated (`sessionOr401`) multipart upload that forwards an L5X/CSV to the `/ingest/plc-parse` sidecar and passes the `{report, i3x?}` + status straight through (200 parsed · 422 closed-project export guidance / unsupported · 413 too large · 503 parser-not-wired · 502 sidecar-unreachable). Closed/binary project files are NOT blocked at the Hub so the parser's export guidance reaches the user. Parse + return only — no DB writes (the `ai_suggestions` write is PR-C, where `ctx.tenantId` gets threaded through). Tests: `__tests__/route.test.ts` 6/6 (vitest, session + fetch mocked); eslint + tsc clean. `mira-hub/package.json` 2.11.0 → 2.12.0. The live Hub→ingest→parser round-trip verifies on staging.
- **`mira-hub/src/lib/plc-proposals.ts`** (new, PR-C) — write the parser's proposals into `ai_suggestions` so they reach the `/proposals` approval queue. `plcReportToSuggestions(report)` is a pure, deterministic map: one `tag_mapping` per parsed tag (UNS path + signal role) + one `kg_entity` per distinct asset; confidence bands → calibrated floats; `proposed_by='import:plc_parser'`. `insertPlcSuggestions(tenantId, rows)` does one atomic multi-row INSERT at **`status='pending'`** under the tenant's RLS context (mig 027) — **never auto-verified** (approval is a separate human action; `kg_edge`/relationship_proposals untouched). Wired behind a `commit=true` form flag on the import route (default = preview/no-write); the response then carries `committed` + `proposalsCreated` + `suggestionIds`. Tests: `__tests__/plc-proposals.test.ts` 7/7 + 3 route commit-path tests (16 hub tests total); eslint + tsc clean. The live `commit → ai_suggestions → /proposals` round-trip verifies on staging (needs NeonDB).
- **`mira-hub/src/lib/suggestion-accept.ts` + `src/app/api/suggestions/[id]/decide/route.ts`** (new, accept hop) — close the offline chain's last link: a non-edge `ai_suggestions` accept path (the existing `/api/proposals/[id]/decide` is `relationship_proposals`/`kg_edge`-only). On **verify** of a `kg_entity` proposal it (1) transitions `ai_suggestions.status`→`accepted` via the ADR-0017 helper (`aiSuggestionId`, never a raw UPDATE) and (2) creates a **`approval_state='verified'`** `kg_entities` row from the payload — so the merged i3X server (`approval_state='verified'` filter) serves the accepted asset immediately. Converts the parser's slash UNS path → ltree dot path for `kg_entities.uns_path` (the bug-class catch). `tag_mapping` and other non-edge types transition status only (no entity) in this slice. On **reject**, status→`rejected`. Guards: `pending`-only (409 otherwise), tenant-scoped (404 cross-tenant), human-triggered (never auto-verify). Tests: `suggestion-accept.test.ts` 7/7 + route 6/6 (hub suite 749 total); eslint + tsc clean.
- **`mira-hub/src/app/(hub)/knowledge/suggestions/page.tsx`** — wire **Accept/Reject buttons** onto the `/proposals` *suggestions* list (previously read-only). `decideSuggestion()` posts to `/api/suggestions/[id]/decide`, with optimistic removal + toast, mirroring the existing relationship-proposal `decide()`. Buttons show only on the Pending tab. eslint + tsc clean; full hub suite 749 green. Screenshot owed on a running Hub (can't render offline). With this, the offline chain is operable end-to-end from the UI; the live round-trips (Hub→ingest→parser, commit→/proposals, accept→kg_entities→i3X) + the ingest deploy-wiring remain the staging-verification items.
- No network, no LLM, no PLC writes (honors `.claude/rules/fieldbus-readonly.md`). The networked i3x reconcile client + desktop GUI from #2068, and the `ai_suggestions` write path (PR-C), are deferred follow-ups.
- Tests: `mira-plc-parser/tests/` 52 passed (15 new: 7 `test_uns` + 8 `test_i3x` incl. an offline CLI test); ruff clean. Demo (`conveyor.L5X`): 11 tags → 11 UNS paths (3 high / 2 medium / 6 low) → i3X payload (6 types, 7 containers, 11 instances).

### v3.28.1 (2026-06-19) — feat(seeds): seed-path guarantee — embedding-coverage gate + discipline (#2094)
- Closes the remaining **seed-path guarantee** ask in #2094 (PR #2085 shipped the backfill tool + canary). SQL seeds insert text-only `knowledge_entries` rows (no `embedding` column) → `embedding = NULL` → invisible to the vector + product-name retrieval streams (`neon_recall._product_search` filters `embedding IS NOT NULL`); nothing guaranteed a seed got embedded, silently degrading retrieval (root cause of the 2026-06-17 Q01/Q10 miss; same class as #1385).
- **`.github/workflows/apply-seeds.yml`** — adds a read-only **embedding-coverage gate** after the apply step (mode=apply): a corpus-wide `count(*) WHERE embedding IS NULL AND source_type IN ('field-guide','integration_guide','component_template')`, mirroring the canary's definition of "dark". Goes RED with the off-CI backfill command when any curated chunk is unembedded. Count-only — no Ollama needed.
- **`tools/seeds/README.md`** — documents **seed → backfill → verify**, the off-CI backfill command (Tailnet node only — CI runners can't reach the `nomic-embed-text` Ollama, same constraint as `seed-oem-manuals.yml`), and ingest-on-write (Option B) as the deferred long-term fix.
- Gate is corpus-wide (not tenant-scoped): `knowledge_entries.tenant_id` is UUID and the curated corpus is system-tenant-owned. **Expected:** stays RED against prod until the prod backfill (#2093) runs — surfacing the open debt, not hiding it. Verified read-only vs dev (canary asserts; gate scalar = 19 → FAIL fires).

### v3.28.0 (2026-06-17) — fix(hub): embed-on-write for node-attachment chunks (#2099)
- Closes #2099. `writePdfChunksForNode` (`mira-hub/src/lib/node-knowledge-ingest.ts`) inserted node-attachment chunks with `embedding = NULL` by design — but `asset-intelligence.searchKB` runs a tenant-scoped vector query filtering `embedding IS NOT NULL`, so a tenant's own uploaded-manual chunks were **silently excluded from the KB vector ranker** (surfaced only via the lower-quality text fallback). Confirmed in prod during #2093: `node_attachment` NULL-embedding rows grew 1 → 60.
- **`embedPendingNodeChunks`** (new, exported) — best-effort trailing pass: after chunks are inserted (BM25-live, hot insert path untouched), it SELECTs NULL-embedding `node_attachment` rows in bounded batches, embeds them with `nomic-embed-text` (768-dim asserted) **outside** the `withTenantContext` transaction (never holds a pooled connection across embed HTTP), and UPDATEs the valid vectors. Fired and-forget from `writePdfChunksForNode` so the upload never blocks on it; covers both callers (node-attach door + `local-upload.ts` blind door).
- **Resilient by construction (#1385):** embedder down/unset/wrong-dim → chunks stay BM25-live, upload succeeds, nothing throws. `NODE_EMBED_ON_WRITE=0` kill switch. The #2098 canary + `tools/backfill_knowledge_embeddings.py` remain the net.
- Tests: 4 new cases (768 happy-path, throw-resilience, wrong-dim refusal, kill switch); existing batching test unchanged + green.

### v3.27.9 (2026-06-19) — fix(hub): unbreak repo-wide Hub E2E — stale onboarding specs (#2108)
- The `Hub E2E (command-center + onboarding)` check had been failing on every hub PR since the onboarding-flow change. Two stale assumptions in the e2e specs: #1993 inserted an upload step between Review and Try (so `step-try` no longer appears right after `onboarding-finish`), and #1976 routed client fetches through `${API_BASE}/api/assets/` (trailing slash, which the bare `**/api/assets` route mock no longer matched).
- Walk the new upload step (`step-upload` → `onboarding-upload-continue`) and mock the trailing-slash assets path in `onboarding-validate.spec.ts` + `onboarding-walkthrough.spec.ts`. Test-only; no product behavior change. Unblocks Hub E2E for all open hub PRs.

### v3.27.8 (2026-06-17) — chore(qa): land Hermes outside-in QA tooling + durable login helper on main (#2013)
- Cherry-picks the validated Hermes QA scaffolding out of the mixed `chore/hermes-qa-durable-account` branch onto `main`, so any checkout (and the Hermes dogfood agent) actually has it — the tooling existed only on an unmerged branch, which is why #2013 read as "still blocked" even though the durable test account + login path were live.
- **`dogfood-output/qa-login-save-state.mjs`** — durable-account Playwright login helper: signs into `app.factorylm.com` with the password flow and saves `dogfood-output/.auth/app-state.json` for reuse. Re-verified against prod from a clean checkout (`ok:true`, lands authenticated; `/hub`→`/onboarding/`, `/command-center/` + `/namespace/` authenticated).
- **`tools/qa/`** — `README.md` runbook (durable `hermes-qa-maint@example.com` account + Doppler `factorylm/dev` creds + mint command), `lib.mjs` (Playwright resolver), browser-smoke, console/network capture, manual-upload smoke, and the dedupe-first issue creator.
- No engine/prod/runtime code; additive QA tooling only.

### v3.27.0 (2026-06-17) — feat(mira): "Why MIRA Thinks This" decision-trace panel (Phase 2)
- **Northstar alignment Phase 2** (`docs/specs/why-mira-thinks-this-spec.md`, PRD §11). Every Hub `AssetChat` answer can now expand into an evidence + confidence + missing-context + feedback panel, turning MIRA into a *challengeable* agent.
- **`mira-hub/src/app/api/assets/[id]/chat/route.ts`** — the Hub chat route now writes its own `decision_traces` row (`platform='hub'`) at stream end (best-effort, never blocks the stream) and emits the `trace_id` up-front over SSE. (The Hub chat previously wrote no trace at all — only the engine surfaces did.)
- **`mira-hub/src/app/api/decision-trace/[id]/route.ts`** (new) — tenant-scoped read API (`withTenantContext`; cross-tenant → 404).
- **`mira-hub/src/app/api/decision-trace/[id]/feedback/route.ts`** (new) — `POST` records trace-linked feedback (`good`/`bad`/`missing_context`/`needs_review`); the trace must belong to the caller's tenant.
- **`mira-hub/src/components/WhyMiraThinksThis.tsx`** (new) + **`AssetChat.tsx`** — the expandable panel, in house style. Renders **only real data** (manual/tag/KG evidence, citations badge, heuristic confidence, `outcome='kb_gap'`→missing-context); PRD §11 `decision_path`/`context_ignored`/`next_check` are **deferred (Phase 2.1)** and intentionally not faked.
- **`mira-hub/db/migrations/055_decision_trace_confidence_and_feedback.sql`** (new) — adds `decision_traces.confidence` + the `decision_trace_feedback` table (UUID tenant family; RLS + grants; idempotent). Seeds Phase 10 (feedback consolidation).
- Tests: 7 new vitest route tests (tenant-scoping 404s, verdict validation, ownership checks). Full Hub unit suite green (719 passing; 1 pre-existing unrelated `sitemap-drift` failure).

### v3.26.1 (2026-06-17) — security(ignition): customer-shipped Ignition bundle is read-only (no plant writes)
- **`ignition/project/com.inductiveautomation.perspective/`** — removed all `system.tag.writeBlocking` plant-write paths from the customer-shipped Perspective bundle, per `docs/mira-ignition-secure-architecture.md` ("read-only by default") and `.claude/rules/fieldbus-readonly.md`. VFD control (speed setpoint, FWD/STOP/REV, fault reset) is **bench-only** — it lives in `plc/live_monitor.py` / the ConvSimpleLive bench project, never in the shipped bundle.
  - **SpeedControl** view (pure VFD control: speed slider/numeric + FWD/STOP/REV writing `VFD_CmdWord`) **removed** from the bundle — view deleted + dropped from `page-config/config.json` and the `NavBar`. It was never in the intended read-only bundle (`ConveyorStatus`/`Chat`/`FaultLog`/`NavBar`).
  - **FaultLog** view kept but made **read-only**: removed the `ClearFaultsButton` (`writeBlocking VFD_CmdWord 2`); the accompanying note now directs operators to clear faults at the VFD keypad (MIRA is read-only). No faked/inert STOP/CLEAR controls remain.
- **`tests/regime7_ignition/test_no_customer_write_paths.py`** (new) — CI guard: fails if any shipped Perspective view re-introduces `system.tag.write*`, or if `SpeedControl` reappears in the bundle. Prevents the read-only HMI from silently becoming a control surface again.
- Note: gateway-script writes to `[default]Mira_Alerts/*` **memory** tags (`tag-change-fsm-monitor.py`, `timer-stuck-state.py`) are internal anomaly alerts, not plant writes, and are intentionally out of scope of this guard.

### v3.25.0 (2026-06-16) — feat(staging): digital-twin review layer + migration-collision doctrine
- **`tools/staging/staging-smoke.sh`** — deterministic curl-and-assert health gate for the staging twin (Cluster Law 2: a binary check is a script, not an LLM). Asserts the externally-reachable review surfaces (Hub 4101, Web 4200) return 200; probes pipeline/atlas best-effort (the deploy workflow is authoritative for those on `127.0.0.1`). shellcheck-clean; verified live (Hub+Web PASS).
- **`tools/staging/hermes-staging-review.sh`** — async qualitative review: runs the smoke gate first, and only on PASS asks Hermes (on CHARLIE) to browse the staging Hub and post a terse verdict to Telegram. Advisory, never a CI gate.
- **`docs/runbooks/staging-twin-review.md`** — the operating model: prod auto-deploys on push-to-main, so the twin reviews the **candidate ref before merge** (`deploy-staging.yml --ref <branch>` → review → merge → prod). Documents the two-layer gate + the deferred hard-gate follow-up. Pairs with the `--ref` fix (v3.24.9 / #2063).
- **`.claude/rules/mira-hub-migrations.md` §7** — new doctrine: duplicate numeric migration prefixes are COSMETIC (the runner keys `schema_migrations` by full basename, not prefix); renumbering an already-applied migration makes the runner re-run it → drift. Do not renumber applied migrations.

### v3.24.9 (2026-06-16) — fix(ci): deploy-staging honors dispatched `--ref`
- **`.github/workflows/deploy-staging.yml`** — the VPS deploy step runs over an ssh heredoc quoted with `<< 'ENDSSH'`, so `GITHUB_REF_NAME` was evaluated *on the VPS* (where it's unset) and the deploy ref always fell back to `staging`. Every `workflow_dispatch --ref <X>` silently deployed the `staging` branch regardless of `X`. Fix threads the runner's `github.ref_name` through the ssh command line as `DEPLOY_REF`, and the heredoc now reads `REF="${DEPLOY_REF:-${GITHUB_REF_NAME:-staging}}"`. Unblocks `--ref main` staging deploys. Cherry-picked from `test/staging-1901-deploy` (`a5cd0fcc`) to land on main independently of the #1901 onboarding stack.

### ops/kiosk-runbook (2026-06-06) — AskMira / mira-ask deploy + prod verify runbook + close `services` gap
- **`docs/runbooks/kiosk-askmira-deploy-and-verify.md`** — new dedicated runbook covering when this routine applies (`mira-bots/ask_api/`, kiosk engine fast-paths, AskMira view), the required deploy dispatch (`services=mira-ask`), verification sequence (Mode A bake + Mode B browser drive), guardrails (no scorer/golden weakening, no `mira-pipeline /v1/chat/completions`, no Anthropic, kiosk-scoped), and the known Q1 length caveat as a separate-PR follow-up. Created because the 2026-06-06 PR #1754 / #1755 / MIRA_PLC#25 cycle surfaced no existing doctrine for this path.
- **`.github/workflows/deploy-vps.yml` line 199** — added `mira-ask` to default `TARGETS`. Auto-deploys triggered by Smoke Test no longer skip the kiosk path. Engine changes shipping to Telegram / Slack will also rebuild `mira-ask-saas`. Closes the gap recorded in the 2026-06-06 fix cycle (auto-deploy after PR #1754 left `mira-ask` on its 47-hour-old image; manual `services=mira-ask` dispatch was required as a workaround).
- **`CLAUDE.md` Pointers** — added a link to the runbook.

### mira-hub/v1.5.3 (2026-05-09) — Allow /pricing in robots.txt + nginx HTTP/2 (closes #1104, #1106)
- **`mira-hub/src/app/robots.ts`** — Added `/pricing` to allow list so crawlers can index the pricing/conversion page.
- **`nginx-phase2-live.conf`** — Added `http2` to both `listen 443 ssl` directives (app.factorylm.com + chat.factorylm.com). Estimated 1,440ms savings on login page LCP.

### v2.7.0 (2026-04-14) — Active learning loop: production 👎 → fixtures → draft PR (closes #219)
- **`mira-bots/tools/active_learner.py`** — `ActiveLearner` class: scans `feedback_log` for `/bad` entries, reconstructs conversations from `interactions`, anonymizes via Claude (PII stripped, vendor/model preserved), infers eval pass criteria with confidence gate (default: 0.6), generates YAML fixtures matching existing eval schema.
- **`tests/eval/active_learning_tasks.py`** — Celery `shared_task` `mira_active_learning.run_nightly` at 04:00 UTC. File-based lock (30-min stale timeout). Honors `ACTIVE_LEARNING_DISABLED=1` env var.
- **`tests/eval/fixtures/auto-generated/`** — Fixture staging area. Draft PR opened to this path; reviewed fixtures promoted to `tests/eval/fixtures/` with sequential numbering.
- **Draft PR format**: one commit per run, PR title `auto: active-learning fixtures from YYYY-MM-DD feedback (N new)`, body includes fixture table + review checklist + hashed source chat_ids.
- **`ACTIVE_LEARNING_GH_TOKEN`** — New Doppler secret required: GitHub PAT with `contents:write` + `pull_requests:write`.
- **Eval still 10/10** — Active learner runs independently; does not affect existing eval harness.

### v2.6.0 (2026-04-14) — LLM-as-judge eval layer — closes #217 (Karpathy alignment P0)
- **`tests/eval/judge.py`** — Cross-model LLM judge. Four Likert dimensions (1–5): `groundedness`, `helpfulness`, `tone`, `instruction_following`. Routes judge calls away from the response generator: Claude-generated → Groq judge; Groq/Cerebras-generated → Claude judge; unknown → Claude. Implements ADR-0010 gap #1.
- **`tests/eval/run_eval.py`** — Judge integration: `run_scenario()` calls `judge.grade()` once per scenario (last response, last user question). Scorecard gains four judge columns + aggregate summary section with trend arrows vs. previous run. Raw judge JSON written to `{scorecard_stem}-judge.jsonl`. `EVAL_DISABLE_JUDGE=1` skips all judge calls.
- **`tests/eval/celery_tasks.py`** — New `mira_eval.run_batch_with_judge` task (03:00 UTC nightly). Hourly `mira_eval.run_batch` now explicitly sets `EVAL_DISABLE_JUDGE=1` (fast/cheap). Separate lock files — nightly judge run does not block hourly run.
- **`tests/eval/test_judge.py`** — 12 offline unit tests (mocked APIs). Covers: disabled mode, provider routing (5 cases), JSON parsing + validation, red-team keyword-stuffed gibberish (scores ≤2 on groundedness/helpfulness), pass case (scores ≥4 all dimensions), Pilz manual-miss (instruction_following ≤2).
- **`tests/eval/fixtures/11_pilz_manual_miss.yaml`** — Regression guard for `GET_DOCUMENTATION` intent. Reconstructed from chat `b500953b` (2026-04-14 Pilz forensic). Verifies "find a manual" returns vendor URL, not a diagnostic question. Judge target for `instruction_following` validation.
- **Deploy:** No container restart needed. `judge.py` calls external APIs (Groq/Anthropic) directly from the eval subprocess. VPS Celery worker needs restart to register `mira_eval.run_batch_with_judge` task + add Beat schedule entry.

### v2.5.2 (2026-04-14) — Hotfix: Apify crawlerType enum fix (closes #230)
- **`crawlerType: playwright:chrome`** — Fixed invalid enum value `"playwright"` in `crawl_routes.yaml` and `route_fallback.py`. Apify rejects bare `"playwright"`; valid values are `playwright:chrome`, `playwright:firefox`, `playwright:adaptive`. Confirmed from e2e Pilz job `2f78ae8b`.
- **`route_fallback.py` reads from YAML** — `crawlerType` sourced from `params.get("crawlerType", "playwright:chrome")` instead of hardcoded.

### v2.5.1 (2026-04-14) — Phase 2 Route Fallback Registry — ADR-0009 (closes #211)
- **`config/crawl_routes.yaml`** — Vendor-specific strategy priority lists. Pilz, Yaskawa, Siemens, ABB skip `apify_cheerio` and start with `apify_playwright` (JS-rendered SPAs).
- **`mira-core/mira-ingest/route_fallback.py`** — Fallback orchestrator: `LOW_QUALITY`/`SHELL_ONLY`/`EMPTY` automatically retries through `apify_playwright → duckduckgo_site_search → llm_discover_url`.
- **Three fallback strategies**: Apify Playwright (Chromium headless), DuckDuckGo site-scoped PDF search, LLM URL discovery (Gemini→Groq→Claude-Haiku + HTTP HEAD validation).
- **Budget controls**: max 3 strategies, $0.20 hard stop. `SKIP_ON_RETRY` prevents re-running the failed primary.
- **Eval**: 10/10. Scorecard: `tests/eval/runs/2026-04-14-v2.5.1-pre.md`.

### v2.5.0 (2026-04-14) — Phase 1 Crawl Verification Layer (closes #210)
- **`crawl_verifier.py`** — New post-crawl QA module. Every Apify run now produces a verified outcome code: `SUCCESS`, `LOW_QUALITY`, `SHELL_ONLY`, `EMPTY`, `FAILED`. Zero silent greens.
- **Key metric: `avg_content_length`** — Discriminates real manual content (10K–50K chars/page) from download listing pages (700–3,500 chars/page). 4,000 char threshold. This is what catches the Yaskawa/Pilz patterns.
- **Honest technician notifications** — `LOW_QUALITY`/`SHELL_ONLY` sends "found listing pages, not manual content — send a PDF directly" instead of "New knowledge added ✅".
- **`GET /ingest/crawl-verifications`** — Last 50 verification records as JSON. Used by eval loop and future dashboard.
- **`POST /ingest/crawl-classify-historical`** — Retroactive run classification for past crawls.
- **KB write correlation** — `_ingest_scraped_text` now returns bool and logs `kb_ok` with `run_id`, so auth failures (like the v2.4.0 OPENWEBUI_API_KEY incident) are visible.
- **Proof-of-concept**: Yaskawa V1000 run `Brgo1xN4QLjhr0Pgc` (previously "SUCCEEDED") classified as `LOW_QUALITY` — 103 listing pages, avg 1,736 chars, content_density=0.12.
- **Sample scorecard**: `tests/eval/crawl-verifications/2026-04-14-sample.md`
- **Eval**: 10/10 (no regression).

### v2.4.1 (2026-04-14) — Hotfix: safety keywords, doc phrase gap, OPENWEBUI_API_KEY
- **Safety keywords expanded** — `SAFETY_KEYWORDS` now includes electrical isolation / live-work phrases: `"isolate the power"`, `"isolating power"`, `"de-energize"`, `"de-energizing"`, `"pull the fuse"`, `"pull the breaker"`, `"live wire"`, `"live panel"`, `"working on live"`, and more. Forensic: Mike's Turn 2 description of pulling cables from a live distribution block was routed to diagnostic FSM instead of STOP escalation.
- **`_DOCUMENTATION_PHRASES` expanded** — Added 12 article-agnostic variants: `"find a manual"`, `"get a manual"`, `"need a manual"`, `"looking for a manual"`, `"looking for the manual"`, `"find a datasheet"`, etc. Forensic: Mike's exact phrase `"Can you find a manual for this kind of distribution block"` returned Q3 FSM instead of vendor URL + scrape-trigger.
- **System prompt Rule 21** — MIRA must never confirm unverified user actions as fact. Reflect user's exact words, do not add specificity. Forensic: `"I pulled the big one"` → MIRA said `"You've removed the main power cable"` (false attribution).
- **OPENWEBUI_API_KEY rotated** — Stale key `sk-c416...` replaced with current valid key from Open WebUI DB. All Apify crawl results were silently failing to write to KB (0/N ingested on every job). Rotated in Doppler + redeployed `mira-ingest`. KB writes now return 200.
- **Eval:** 10/10. Scorecard: `tests/eval/runs/2026-04-14-v2.4.1-pre.md`.

### v2.4.0 (2026-04-14) — GET_DOCUMENTATION intent + cross-vendor guard
- **`GET_DOCUMENTATION` intent** — `classify_intent()` now returns `"documentation"` for explicit manual/datasheet/pinout requests (checked before `industrial` so "manual" doesn't swallow doc queries). Responds immediately with vendor support URL, no LLM call (~17ms). Closes #203.
- **Cross-vendor contamination guard** — `rag_worker` clears chunks when query vendor ≠ chunk manufacturer → honesty directive fires. Fixes GS20 queries returning Allen-Bradley content. Closes #204.
- **Async scrape-trigger** — On `documentation` intent, `engine._fire_scrape_trigger()` fires as `asyncio.create_task()` → `POST mira-ingest:/ingest/scrape-trigger` → Apify actor starts crawling vendor docs. End-to-end confirmed: Pilz query → Apify run `9ELqsnRqp384TeoxJ` completed.
- **None guard** — `vendor_support_url(None)` / `vendor_name_from_text(None)` no longer crash when `asset_identified` state is `None`.
- **Eval runner fix** — `run_date` UnboundLocalError when `--output` is a `.md` path (nightly Celery runner).
- **APIFY_API_KEY** wired into `mira-ingest` container via `docker-compose.saas.yml`.
- **Eval baseline:** 10/10 (was 8/10). Scorecard: `tests/eval/runs/2026-04-14-v2.4.0-pre.md`.
- **Follow-up:** #207 (per-call tenant_id, model extraction, model-level KB check, logger propagation).

### v0.5.4 (2026-04-14) — P0 Open WebUI UX fixes
- **P0-1**: Continue button (`### Task: Continue generating...`) now echoes last assistant turn instead of returning blank
- **P0-2**: Regenerate button dedup — FSM rolls back to prior state when identical user message detected in consecutive turns
- **P0-3**: PDF uploads in OW now route to `mira-ingest /ingest/document-kb` instead of silently falling through to OW native embedding
- **Rollback anchor:** `v0.5.3-pre-p0-ux` tag on commit before merge; to roll back: `git checkout v0.5.3-pre-p0-ux` → rebuild `mira-pipeline`
