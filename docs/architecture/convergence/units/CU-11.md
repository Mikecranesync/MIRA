# Convergence Unit — CU-11: Wire the §Gate 7 independent adversarial-review lane

**Status:** implemented — awaiting Gate 9 human GO
**Doctrine:** `docs/architecture/FACTORYLM_MIRA_ARCHITECTURE_CONVERGENCE.md` §Gate 7 (amended by this unit) + §9 (Adversarial Reviewer) + §19 ("independent adversarial review passes" is part of Definition of Done).

## Current behavior (pre-change)

§Gate 7 named **"GPT-5.6 Sol / Codex, fresh context"** as the independent adversarial
reviewer. That lane was never wired: no configuration, no credential, no vendor identity
anywhere in the repo — invoking it would have failed at first use. Every unit so far
(CU-P1, CU-02, CU-06) substituted fresh-context reviewer agents and recorded the
deviation; three documents claimed the gap was "tracked in the backlog" before any entry
existed (the reason CU-11 exists — BACKLOG.md CU-11 § "Why it is its own unit").

## Target architecture

An **invocable, evidence-producing** Gate 7 lane that any unit can run and any unit
record can cite:

- `tools/gate7_review.py` — disprove-brief review of a PR/branch diff on the free-tier
  **Groq → Cerebras → Together** cascade (owner decision in merged PR #3261: **no
  OpenAI**; PRD §4-compliant, keys already exist as Doppler/repo secrets —
  `TOGETHERAI_API_KEY` is live in `unseen-generalization-lane.yml`).
- `/gate7-review` (`.claude/commands/gate7-review.md`) — the procedure: finalize diff →
  cascade lane → (xhigh) fresh-context agent reviewers in isolated worktrees → paste the
  evidence block into the unit record → BLOCK is stop-the-line → re-run per round.
- §Gate 7 doctrine text amended to stop naming a rejected provider and to state the
  honesty requirement (below).

## Why this change exists

CU-03 (knowledge_entries write path) is auto-xhigh (tenancy-adjacent) and **must not walk
Gate 7 on a substitute**. Wiring the lane first stops substitute-review deviations
accumulating and unblocks the critical path (BACKLOG ordering deviation, recorded under
CU-02).

## Canonical implementation

- **`tools/gate7_review.py`** (new, ~270 lines): gathers the diff (`gh pr diff N` or
  `git diff BASE...HEAD`), builds a disprove brief (the §Gate 7 refutation axes verbatim +
  optional unit record + capped diff), calls the cascade with `httpx`, parses an explicit
  `Verdict: PASS|BLOCK`, and emits a structured evidence block naming every provider that
  ran. Effort semantics: `high` = first available provider decides; `xhigh` = **every**
  available provider reviews (min 2) and all must PASS. Exit codes: 0 PASS / 1 BLOCK /
  2 indeterminate / 3 lane unavailable. **Fail-loud by design** — unlike the advisory
  `code-review.yml` comment bot, provider failure is never a pass.
- **`.claude/commands/gate7-review.md`** (new): the invocable step + the evidence shape.
- **Doctrine amendments** (4 sites): §Gate 7 default (lane + honesty note), §Gate 7
  xhigh semantics, §9 Adversarial Reviewer, §15 pilot chain line, §20 reviewer line.

**Honesty requirement (stated, not implied away):** without a dedicated second frontier
vendor, "independent" means **fresh context** (stateless call; the reviewer sees only the
brief + diff), **an isolated worktree** when the reviewer is an agent, and **a brief to
disprove** — *not vendor independence*. The evidence block prints this basis so a unit
record cannot imply a cross-vendor check that did not happen.

## Old implementation

The doctrine prose naming GPT-5.6 Sol / Codex (amended in place, history in git). No code
removed — there was never any code.

## Affected modules

`tools/gate7_review.py` (new), `.claude/commands/gate7-review.md` (new),
`docs/architecture/FACTORYLM_MIRA_ARCHITECTURE_CONVERGENCE.md` (amended),
`docs/architecture/convergence/BACKLOG.md` (status), this record. **No runtime/product
code, no schema, no deploy surface** — the script is dev/CI tooling.

## Contracts/invariants

- The lane never soft-skips: every non-PASS state has a distinct non-zero exit code.
- The evidence block always names the providers that actually ran and the independence
  basis.
- Cascade order and providers are PRD §4 (Groq → Cerebras → Together); no Anthropic, no
  OpenAI-the-vendor (gpt-oss-120b is an open-weights model served by the free-tier
  providers, not an OpenAI API dependency).
- Known limitation (recorded, accepted): `DIFF_CHAR_CAP = 24_000` chars — large diffs are
  truncated and the brief tells the reviewer to flag load-bearing cuts. Free-tier TPM is
  the binding constraint; big units should review per-commit or per-file-group.

## Risk classification

Low (docs + dev tooling; can only block merges via humans choosing to honor its verdict —
it is not yet a required CI check). No §Gate 7 xhigh trigger. Reviewer effort: High
(default), and this unit's own review additionally dogfoods the new lane.

## Behavior-lock tests (Gate 3)

The lane is exercised directly (evidence below): `--dry-run` proves brief construction on
the real diff; a no-keys invocation proves exit 3 (fail-loud); the dogfood run proves the
end-to-end cascade path and verdict parsing. No pytest contract added — the script is not
imported by anything (a CI contract on a dev tool would be speculative; revisit if the
lane becomes a required check).

## R0 SHA/checkpoint

`cd22ed994` (= main at branch time; tag lineage v3.274.1+). Docs + a new uninvoked script:
**Recovery = `git revert` of the squash merge.** No data/schema state.

## Data/schema recovery procedure

N/A — no data or schema touched.

## Implementation plan (executed)

1. Recon: BACKLOG CU-11 entry + #3261 owner decisions + doctrine §Gate 7/§9/§15/§20 +
   `code-review.yml` cascade pattern + `pr_self_fix.sh` + secrets inventory
   (`TOGETHERAI_API_KEY` confirmed live in CI).
2. `tools/gate7_review.py` + `/gate7-review` + doctrine amendments.
3. Verify: ruff clean; dry-run on the real diff; exit-code checks; dogfood run of the
   lane on this unit's own diff.
4. Adversarial review (substitute panel + lane dogfood — see below), fixes, PR.

## Shadow-validation plan

N/A — there is no old implementation to shadow (the old lane never existed).

## Adversarial reviewer effort

High. **Deviation (final allowed instance):** CU-11 is itself reviewed by (a) a substitute
panel of independent fresh-context reviewer agents (per the BACKLOG allowance for CI-only
units, deviation recorded here), **plus** (b) a dogfood run of the new lane on this unit's
own diff — the first real-lane execution. After CU-11 merges, the substitute-only pattern
is retired; units use `/gate7-review`.

_Review results recorded below before merge._

## Human approval requirement

Gate 9: Mike's GO required before merge (docs+tooling, low risk, but it amends doctrine).

## Promotion plan

Single PR off `main` (not stacked). Docs + tools + `.claude/` — `ci.yml` runs on the
non-docs paths; no deploy needed after merge (no runtime service touched;
`deploy-vps.yml` not dispatched).

## R1 SHA/checkpoint

_To be recorded at merge (merge SHA, auto-tag, rollback checkpoint)._

## Observation window

First real consumer is **CU-03's Gate 7 xhigh walk** — that run is the acceptance test of
the lane under its intended load. Watch for: free-tier rate limits forcing provider
fallthrough (evidence block will show it), and verdict-parse indeterminates (exit 2) if
gpt-oss drifts from the output format.

## Deletion criteria

N/A — nothing deleted. (The stale Gemini third slot in `code-review.yml`'s advisory
cascade is out of scope here — surgical-change rule; noted for a future touch of that
workflow.)

## Evidence required for GO

- ruff clean on `tools/gate7_review.py`; dry-run brief built from the real diff.
- Exit-code proof: no-keys → 3; dogfood run → real verdict with named providers.
- Substitute panel verdicts + dispositions recorded above.
- Full CI green on the PR.
