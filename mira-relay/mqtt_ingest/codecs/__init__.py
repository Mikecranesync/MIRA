"""Codecs that lower a transport payload into canonical tag entries.

``sparkplug_b`` — Sparkplug B protobuf (Phase 3b). A future ``plain_json`` codec
(Phase 3a) terminates identically: a list of ``build_tag_entry`` dicts.
"""
