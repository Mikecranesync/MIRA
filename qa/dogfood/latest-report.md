# FactoryLM Dogfood Report — 2026-06-30 02:58 UTC

## Overall: RED
_1 red · 3 yellow · 0 green · 0 infra — across 4 product paths, run as real QA personas against the staging Hub._

> **What this means:** RED = a customer/demo is blocked right now. YELLOW = it works but degraded. GREEN = a real user can do this today.

## Top blockers (fix these first)
1. **[RED] Work order** — a completed work order reads back with resolution=null and closed_at=null — the closure notes the next tech needs are gone _(already tracked — #2375 (commented/declined — no new issue))_
2. **[YELLOW] Contextualization** — VFD-07 is named but has ZERO customer documents and an EMPTY UNS map — MIRA answers from generic OEM data, not this customer's reality (approval wall untested — no proposals present)
3. **[YELLOW] Demo readiness** — the story (asset→fault→evidence→action) lands, but NO live signal is moving on screen — the demo looks static; wire the demo tenant (DEMO_API_TOKEN) for the live beat

## Path verdicts (what a customer experiences)
| Path | Verdict | What a real user hits |
|---|---|---|
| Contextualization | YELLOW | VFD-07 is named but has ZERO customer documents and an EMPTY UNS map — MIRA answers from generic OEM data, not this customer's reality (approval wall untested — no proposals present) |
| Demo readiness | YELLOW | the story (asset→fault→evidence→action) lands, but NO live signal is moving on screen — the demo looks static; wire the demo tenant (DEMO_API_TOKEN) for the live beat |
| Maintenance tech | YELLOW | the tech gets a cited answer, but the asset shows NO live signal data — the live status panel is empty, so a prospect watching a demo sees nothing moving |
| Work order | RED | a completed work order reads back with resolution=null and closed_at=null — the closure notes the next tech needs are gone |

## Filed / would-file (every one has repro + business impact, deduped)
- **Work order** → DUPLICATE of #2375 (commented/declined — no new issue) _(found by carlos, verified by dana)_

## Duplicate issues found (NOT re-filed)
- Work order: DUPLICATE of #2375 (commented/declined — no new issue)

## Suspected flaky / infrastructure (not product bugs — not filed)
- _None._
- Demo-readiness note: RBAC personas live on a synthetic test tenant (assets seeded, live signals/docs sparse); the demo tenant needs DEMO_API_TOKEN (not provisioned), so demo-readiness is reported, not auto-driven.

## Suggested next prompt (paste to Codex / Claude)
> Fix the **Work order** path: a completed work order reads back with resolution=null and closed_at=null — the closure notes the next tech needs are gone. Find the route/handler behind it in mira-hub, write a failing test that reproduces the dogfood transcript in `dogfood-output/qa-runs/dogfood-2026-06-30T02-57-50Z`, then make it pass. Re-run `tools/crew/dogfood/judge.sh` to confirm the path turns GREEN.

---
_Raw transcripts (not required reading): `dogfood-output/qa-runs/dogfood-2026-06-30T02-57-50Z`. Generated by `tools/crew/dogfood/judge.sh`. Methodology: RBAC personas live on a synthetic test tenant (assets seeded, live signals/docs sparse); the demo tenant needs DEMO_API_TOKEN (not provisioned), so demo-readiness is reported, not auto-driven._
