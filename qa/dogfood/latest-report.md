# FactoryLM Dogfood Report — 2026-06-30 08:22 UTC

## Overall: YELLOW
_0 red · 3 yellow · 1 green · 0 infra — across 4 product paths, run as real QA personas against the staging Hub._

> **What this means:** RED = a customer/demo is blocked right now. YELLOW = it works but degraded. GREEN = a real user can do this today.

## Top blockers (fix these first)
1. **[YELLOW] Contextualization** — VFD-07 is named but has ZERO customer documents and an EMPTY UNS map — MIRA answers from generic OEM data, not this customer's reality (approval wall untested — no proposals present)
2. **[YELLOW] Demo readiness** — the story (asset→fault→evidence→action) lands, but NO live signal is moving on screen — the demo looks static; wire the demo tenant (DEMO_API_TOKEN) for the live beat
3. **[YELLOW] Maintenance tech** — the tech gets a cited answer, but the asset shows NO live signal data — the live status panel is empty, so a prospect watching a demo sees nothing moving

## Path verdicts (what a customer experiences)
| Path | Verdict | What a real user hits |
|---|---|---|
| Contextualization | YELLOW | VFD-07 is named but has ZERO customer documents and an EMPTY UNS map — MIRA answers from generic OEM data, not this customer's reality (approval wall untested — no proposals present) |
| Demo readiness | YELLOW | the story (asset→fault→evidence→action) lands, but NO live signal is moving on screen — the demo looks static; wire the demo tenant (DEMO_API_TOKEN) for the live beat |
| Maintenance tech | YELLOW | the tech gets a cited answer, but the asset shows NO live signal data — the live status panel is empty, so a prospect watching a demo sees nothing moving |
| Work order | GREEN | a completed work order keeps its resolution and close time on read-back |

## Filed / would-file (every one has repro + business impact, deduped)
- _Nothing eligible to file this run._

## Duplicate issues found (NOT re-filed)
- _None._

## Suspected flaky / infrastructure (not product bugs — not filed)
- _None._
- Demo-readiness note: RBAC personas live on a synthetic test tenant (assets seeded, live signals/docs sparse); the demo tenant needs DEMO_API_TOKEN (not provisioned), so demo-readiness is reported, not auto-driven.

## Suggested next prompt (paste to Codex / Claude)
> Fix the **Contextualization** path: VFD-07 is named but has ZERO customer documents and an EMPTY UNS map — MIRA answers from generic OEM data, not this customer's reality (approval wall untested — no proposals present). Find the route/handler behind it in mira-hub, write a failing test that reproduces the dogfood transcript in `dogfood-output/qa-runs/dogfood-2026-06-30T08-21-47Z`, then make it pass. Re-run `tools/crew/dogfood/judge.sh` to confirm the path turns GREEN.

---
_Raw transcripts (not required reading): `dogfood-output/qa-runs/dogfood-2026-06-30T08-21-47Z`. Generated by `tools/crew/dogfood/judge.sh`. Methodology: RBAC personas live on a synthetic test tenant (assets seeded, live signals/docs sparse); the demo tenant needs DEMO_API_TOKEN (not provisioned), so demo-readiness is reported, not auto-driven._
