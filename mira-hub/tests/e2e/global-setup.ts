/**
 * Superseded by the audit-setup project (see playwright.config.ts +
 * tests/e2e/audit-setup.ts). Kept as a thin shim because removing the file
 * required a Bash perm we don't have. Safe to delete on the next janitorial pass.
 */
export {};
