# Equipment Notebook technician benchmark — 2026-08-12

Run against a fresh notebook loaded with the **official PowerFlex 525 quick-start
(520-QS001)** — which verifiably contains the answers (P042, F004, terminal 07,
etc.). Deterministic marker + citation scoring, no LLM judge. Oracle verified
against Rockwell 520-QS001 / 520-UM001 (`oracle.json`).

## Result: 10/10, stable across 3 consecutive runs

| case | class | before (main) | after |
|---|---|---|---|
| decel-ramp ("slow down ramp") | synonym+exact-spec | ✗ "could not find" + 4 irrelevant pages cited | ✓ **P042 [Decel Time 1]**, cited p.21 |
| param-lookup-p042 | exact-param-lookup | ✗ "could not find" | ✓ **P042 [Decel Time 1]** decel max→0 Hz, cited p.21 |
| second-speed-terminal | ambiguity+terminal | ✗ "could not find" + 6 pages | ✓ **Terminal 07** = Speed Reference 2, cited p.15 |
| motor-frequency | config-vs-monitoring | ✗ wandered into b015/b016 monitoring | ✓ config params + **"b001/b002 are display only, cannot be set"** |
| machine-overview | identity+coverage | ✗ summarized the start-up checklist | ✓ identity + loaded-source coverage + "full user manual required" |
| fault-f004 | fault-code | ✓ UnderVoltage | ✓ **F004 UnderVoltage**, cited |
| fault-clear | procedure | — | ✓ **A551 [Fault Clear]** / Stop key, cited |
| absent-cable-length | answer-not-present | (would fabricate/cite pages) | ✓ honest "not specified here, see full manual", no bogus spec |
| absent-fault-f999 | answer-not-present | (would cite pages as proof) | ✓ refuses, **0 citations** |
| wrong-domain (hydraulic) | off-scope | — | ✓ insufficient_evidence, **0 citations** |

## Gates met
- P042 retrieved from the uploaded quick-start source. ✓
- Terminal 07 vs Preset-Frequency ambiguity handled with the terminal number. ✓
- "Motor frequency" no longer conflates configuration with monitoring (b001/b002 flagged display-only). ✓
- Every grounded benchmark answer carries a citation; every refusal ships **zero** citations. ✓
- Zero unsupported claims / no "not found while showing pages as proof". ✓

## Latency / cost
- ~1.0–2.0 s per question (Groq `openai/gpt-oss-120b`, free tier, ~$0). Retrieval
  adds 3–4 doc-scoped SQL passes (~tens of ms each) on the small per-notebook set.
- No whole-manual re-read (contrast the bakeoff's Gemini native lane: ~117k
  tokens/question, daily-quota fragility). This is the Verdict-B parse-then-QA
  path: retrieve grounded chunks → cite-or-refuse over them.

## Reproduce
```
node tests/equipment/benchmark/runner.mjs <notebookId> [baseUrl] [stateFile]
```
Needs a running server + a notebook loaded with the PF525 quick-start; auth via a
Playwright storageState file (default `tests/equipment/.state/notebook.json`).
Exit code = failures. Oracle: `oracle.json`.
