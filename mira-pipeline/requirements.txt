# OpenAI-compatible API server
fastapi==0.115.*
pydantic>=2.0,<2.12  # <2.12: fastapi pulls pydantic unbounded; 2.12+ breaks the RAG worker structured-output build (unable to infer type for attribute "description"; engine.py:5686). mira-pipeline-saas is the live VPS chat path and copies mira-bots/shared/.
uvicorn[standard]==0.34.*

# Supervisor dependencies (match mira-bots/telegram/requirements.txt)
httpx==0.27.*
Pillow>=12.0
pytesseract>=0.3
PyYAML==6.0.3
langfuse>=2.50.0,<3.0.0
sqlalchemy>=2.0
psycopg2-binary>=2.9

# SSE streaming
sse-starlette>=2.0
